# GVC refinements, 2026-09-21

| Folder | Status | Tests |
|---|---|---|
| weighting/ | Exact variant-vs-group decomposition; `means_differ` vs `sign_reversal`; exact per-group contributions (primary) and row-mass bins (visualisation only). Results describe REGISTRY STRINGS until identities are repaired. | 8 |
| consequence/ | Census of ClinVar MC annotations recovering the SO accessions and non-first consequences that `patch_clinvar_alleles.py` discarded. Recovery from the same source bytes, not reannotation. | 5 |
| identity/ | (census parser now takes the GeneID after the LAST colon; symbols such as HHC2:066588 are real and reported) Census of ClinVar GENEINFO recovering every (symbol, source GeneID) association per VariationID -- the variant-gene relation components must be built from. Refuses malformed entries and duplicate VariationIDs. | 5 |
| identity/probe_release_provenance.py | Read-only probe: VCF ##fileDate vs variant_summary release evidence, cohort VariationID overlap across both files, the GENEINFO 10-gene spike, and raw malformed entries. Exercised end to end on fixtures; NO unit tests. | - |
| identity/authenticate_same_release.py | Authenticates cohort clinical_sig and review status against the SAME-RELEASE VCF keyed by VariationID (never coordinates), using the project's canonical normaliser. Measures cohort rows sharing a VariationID. Exercised end to end on fixtures; NO unit tests. | - |
| identity/verify_residuals.py | Over ALL rows: classifies every clinical-significance disagreement as delimiter-only or not, printing the remainder; classifies every shared-VariationID pair as a PAR1 or PAR2 X/Y pair (region-aware offsets derived from supplied GRCh38 intervals) or flags it. Exercised end to end on fixtures; NO unit tests. | - |
| identity/acquire_identity_bundle.py | Downloads human NCBI gene_info, gene_history and the HGNC complete set into a timestamped directory with per-file provenance (URL, retrieval time, HTTP status, Last-Modified, ETag, declared vs received bytes, SHA-256). Refuses truncated transfers, removes partial files on any failure, exits non-zero if anything failed. URLs verified against NCBI and HGNC documentation. Tested against a local server; NO unit tests. | - |
| identity/resolve_gene_ids.py | Resolves every source GeneID in the relation against the frozen bundle: current / migrated (path kept) / discontinued / chain problem / unknown; checks each source symbol through gate_a.GeneResolver; audits HGNC cross-references both ways. Tested on fixtures; NO unit tests. | - |
| identity/build_components.py | Gene components from the RESOLVED relation. canonical_components is the reviewed builder with only its ID pattern parameterised (verified identical to the reference on 300 random HGNC relations). Built twice: all resolved records, and excluding declared non-gene types (default biological-region). Audits the old symbol-keyed registry's leakage, optionally restricted to the evaluated rows with spans taken from FULL membership. | 3 (+6 census) |
| identity/stratify_by_leakage.py | Re-evaluates EXISTING validation predictions split into rows whose gene component was unseen in training vs leaked (spans from FULL membership). No refitting. Brier, AUROC (matches scikit-learn to 1e-16 with ties) and prevalence per stratum; paired contrasts WITHIN a stratum with component-resampled intervals; below --min-components (default 30, a declared policy) no zero-exclusion claim is made. --models selects columns explicitly; auto-detection warns by name on constant columns. Every contrast is attributed exactly to components, with leave-top-k-out POINT estimates (no intervals: the components are chosen after seeing the data). Concentration is also reported as a share of total ABSOLUTE contribution, which stays in [0, 1]. | 6 |
| report/generate_gate_a_report.py | The ONE generator of the Gate A measurement document. Every figure and verdict is computed from the artifacts; a missing key stops it with the exact path; it REFUSES to generate unless six cross-output consistency checks pass (artifacts must describe the same rows); an appendix lists every key path read with its artifact's SHA-256; it refuses to overwrite. Contributors are labelled as registry gene_symbol strings, not resolved genes. Tested against report/fixtures, whose three hand-aligned values are documented in report/fixtures/README.md. | 5 |
| report/amend_measurement_docs.py | Inserts dated status blocks into the two 2026-09-20 measurement documents after their titles, preserving every original byte. The newline is taken from the title line's own terminator, so working copies that MIX conventions (LF lines plus a trailing CRLF) are handled; each file's CRLF and LF counts are printed. Refuses if the Gate A document is absent, a title differs, or already amended, and writes nothing unless every check passes. | 4 |
| gate_a/ | The reviewer's Gate A package, unchanged; byte-identical to its MANIFEST.json. | 12 |
| ontology/ | Pinned Sequence Ontology, data-version 2026-08-07, with provenance. | - |
| dependence/ | NOT PROMOTED. Pending: unequal-size and heteroskedastic fixtures against D = 1 + rho*(sum n^2/N - 1), informative sizes, few clusters, repeated-simulation coverage, and a quantified 500-vs-2000 replicate contribution. | 5 |

Run each folder's tests from inside that folder: `python -W error -m unittest -v`.

## Consolidation note
weighting/weighting_decomposition.py and the reviewer's gate_a.weighting_attribution agree to 1.0e-17
across 200 random populations, with identical sign_reversal flags. They are one method in two places.
Integrate ONE into the project package -- the reviewer's version, which also validates group identities --
and keep row-mass bins only as an optional view on its per-group contributions.
