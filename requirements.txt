#MANUAL EDIT 2026-04-29 (Monzia): pandas pinned to 2.3.3 to avoid pandas 3.0 string-dtype break.
# 2026-06-29: pandas 3.0.4 upgrade ATTEMPTED + rolled back. Data-prep proven byte-equivalent on
# 3.0.4 (string-dtype break does NOT occur), but pandas 3.0.4's Windows cp312 wheel segfaults
# pd.date_range (0xC0000005) across all numpy 2.0.2-2.3.3. BLOCKED; retry when pandas > 3.0.4.
# fastapi 0.136.1 verified current per PyPI 2026-04-23. starlette 1.0.0 coherent with fastapi 0.135.2/0.136.1.
# DO NOT regenerate this file with pip-compile under Python 3.14.
# Future: re-run pip-compile under Python 3.12 once .venv312 is bootstrapped (PHASE_2_FEATURES.pipcompile_python312_migration).
# This file is autogenerated by pip-compile with Python 3.14
# by the following command:
#
#    pip-compile --output-file=requirements.txt --strip-extras requirements.in
#
annotated-doc==0.0.4
    # via fastapi
annotated-types==0.7.0
    # via pydantic
anyio==4.13.0
    # via
    #   starlette
    #   watchfiles
biopython==1.87
    # via -r requirements.in
catboost==1.2.10
    # via -r requirements.in
certifi==2026.2.25
    # via requests
charset-normalizer==3.4.6
    # via requests
click==8.3.1
    # via uvicorn
colorama==0.4.6
    # via
    #   click
    #   uvicorn
contourpy==1.3.3
    # via matplotlib
cycler==0.12.1
    # via matplotlib
fastapi==0.135.2
    # via -r requirements.in
fonttools==4.62.1
    # via matplotlib
graphviz==0.21
    # via catboost
h11==0.16.0
    # via uvicorn
httptools==0.7.1
    # via uvicorn
idna==3.11
    # via
    #   anyio
    #   requests
joblib==1.5.3
    # via
    #   -r requirements.in
    #   scikit-learn
kiwisolver==1.5.0
    # via matplotlib
lightgbm==4.6.0
    # via -r requirements.in
matplotlib==3.10.8
    # via catboost
narwhals==2.18.1
    # via plotly
numpy==2.4.4
    # via
    #   -r requirements.in
    #   biopython
    #   catboost
    #   contourpy
    #   lightgbm
    #   matplotlib
    #   pandas
    #   scikit-learn
    #   scipy
    #   xgboost
packaging==26.0
    # via
    #   matplotlib
    #   plotly
pandas==2.3.3
    # via
    #   -r requirements.in
    #   catboost
pillow==12.1.1
    # via matplotlib
plotly==6.6.0
    # via catboost
pyarrow==23.0.1
    # via -r requirements.in
pydantic==2.12.5
    # via
    #   -r requirements.in
    #   fastapi
pydantic-core==2.41.5
    # via pydantic
pyparsing==3.3.2
    # via matplotlib
python-dateutil==2.9.0.post0
    # via
    #   matplotlib
    #   pandas
python-dotenv==1.2.2
    # via uvicorn
pyyaml==6.0.3
    # via uvicorn
requests==2.33.0
    # via -r requirements.in
scikit-learn==1.8.0
    # via -r requirements.in
scipy==1.17.1
    # via
    #   -r requirements.in
    #   catboost
    #   lightgbm
    #   scikit-learn
    #   xgboost
six==1.17.0
    # via
    #   catboost
    #   python-dateutil
starlette==1.0.0
    # via fastapi
threadpoolctl==3.6.0
    # via scikit-learn
typing-extensions==4.15.0
    # via
    #   fastapi
    #   pydantic
    #   pydantic-core
    #   typing-inspection
typing-inspection==0.4.2
    # via
    #   fastapi
    #   pydantic
tzdata==2025.3
    # via pandas
urllib3==2.6.3
    # via requests
uvicorn==0.42.0
    # via -r requirements.in
watchfiles==1.1.1
    # via uvicorn
# websockets==16.0  # disabled 2026-06-08: hard pin conflicts with the langgraph-sdk transitive websockets requirement (pip ResolutionImpossible on a clean install). websockets is transitive-only; the resolver selects a compatible version.
    # via uvicorn
xgboost==3.2.0
    # via -r requirements.in

jinja2>=3.1,<4.0

# Run 11 Standard-scope dependencies (Standing Rule 31)
optuna>=4.0
polars>=1.0
duckdb>=1.0
pykan>=0.2.0
# PINNED EXACTLY (2026-07-13). Was `imodelsx>=1.0.13`.
#
# 1.0.13 is the LATEST release (there is no 1.0.14), so `>=` bought nothing but risk. This
# project's stated first-class goal is to MEASURE AND COMPARE machine-learning algorithms;
# a floating model library makes those measurements non-reproducible on principle -- Run 15's
# numbers are only meaningful against a known KAN implementation.
#
# 1.0.13 additionally ships a hard bug: KANClassifier.fit() references `test_size`,
# `random_state` and `shuffle` as BARE NAMES, so it raises NameError and cannot run at all.
# It is repaired IN-PROCESS by _repair_imodelsx_kan_bare_names() in
# src/genomic_variant_classifier/models/kan.py -- NOT by the sed-into-site-packages that
# used to live in the launch scripts and that Continuous Integration, Docker and the Run 17
# bootstrap never executed. If imodelsx ever ships a fixed release, RE-MEASURE before moving
# this pin: tests/unit/test_kan_actually_fits.py will tell you whether the repair is still
# needed.
imodelsx==1.0.13

gudhi  # SR31 PASS 2026-06-03 (v3.12.0, SimplexTree API verified)
transformers==4.46.3

# =============================================================================
# THE TORCH / GRAPH-NEURAL-NETWORK / DRIFT STACK -- ADDED BY HAND 2026-07-13.
# =============================================================================
# These packages were MISSING from this file entirely, and their absence had been
# silently disabling a headline model.
#
# HOW THEY WENT MISSING
#   This file is a pip-compile artifact of requirements.in (see the header), and it was
#   compiled UNDER PYTHON 3.14 -- where `torch` has no wheels. pip-compile therefore
#   resolved the dependency graph WITHOUT the torch stack and emitted a requirements.txt
#   with torch, torch-geometric, networkx, numba, pandera, pyspark and river simply absent.
#   requirements.in declares every one of them (lines 38-51). requirements.lock, compiled
#   under Python 3.12, HAS every one of them. Only this file -- the ONLY file that anything
#   actually installs -- was missing them.
#
# WHAT THAT COST (measured 2026-07-13)
#   * Continuous Integration installs requirements-api.lock + requirements.txt +
#     requirements-dev.txt. It NEVER installs requirements.lock. So `torch_geometric` was
#     absent from every Continuous Integration run, and ELEVEN test entries silently
#     skipped: the STRING-DB graph neural network, the hetero-graph neural network,
#     GraphGPS, the graph-neural-network ablation, and the inductive path. `gnn_score` is a
#     REAL, non-degenerate feature in the ensemble. **The entire graph-neural-network branch
#     was untested by the gate.**
#   * The Docker TRAINING image (Dockerfile:159) also installs this file -- so it cannot run
#     the graph neural network either.
#   * `torch` itself reached every environment only as a TRANSITIVE dependency of imodelsx:
#     undeclared and unpinned, in a deep-learning project.
#   * `torch_geometric` reached the rented instance only via an ad-hoc, unpinned
#     `pip install -q networkx torch_geometric` in vm_bootstrap_run.sh:147.
#   * `mapie` was declared NOWHERE, so tests/conformal/test_mapie_crosscheck.py -- the ONLY
#     independent check of this project's from-scratch conformal prediction against a mature
#     reference implementation -- had NEVER EXECUTED, on any machine, ever. It was installed
#     and run on 2026-07-13: all 3 tests PASS. Our LAC sets match MAPIE element-wise exactly
#     and our APS is a valid, tighter variant. A correct test that never runs is not a test.
#
# WHY PINNED BY HAND RATHER THAN REGENERATED
#   The header says: "re-run pip-compile under Python 3.12 once .venv312 is bootstrapped".
#   .venv312 IS bootstrapped -- but requirements.in lacks the UPPER BOUNDS that this file's
#   exact pins encode, both of them bought with real incidents:
#       transformers  .in says >=4.40 (no ceiling) -> pip-compile resolves 5.8.0, which is
#                     the family that KILLED a Run 17 smoke test (Nucleotide Transformer
#                     imports a symbol transformers 5.x removed). This file pins 4.46.3.
#       pandas        .in says >=2.0 (no ceiling). This file pins 2.3.3 after the 3.0.4
#                     Windows cp312 date_range segfault (see line 1-4).
#   Regenerating today would therefore re-introduce a break the project already paid to
#   escape. Fixing requirements.in's bounds and regenerating properly is roadmap 6.18 -- a
#   deliberate, separately-verified change, NOT a side effect of closing this gap.
#
# EVERY VERSION BELOW IS THE VERSION CURRENTLY INSTALLED IN .venv312, AGAINST WHICH THE FULL
# SUITE IS GREEN (1860 passed, 0 failed, 0 warnings), AND EVERY ONE MATCHES requirements.lock
# EXACTLY. They are measured, not chosen.
torch==2.11.0                # undeclared until today; arrived transitively via imodelsx
torch-geometric==2.7.0       # the GNN branch: gnn.py, gnn_optim.py, hetero_gnn*.py
networkx==3.6.1              # STRING-DB graph construction
numba==0.65.1
pandera==0.24.0              # schema-drift detection (5 tests, never ran in CI)
pyspark==3.5.8               # 3 tests in test_core (never ran in CI)
river==0.23.0                # online drift detection (1 test, never ran in CI)
mapie==1.4.1                 # conformal cross-check oracle -- NEVER RAN ANYWHERE until today
pybigtools>=0.3.0  # PhyloP BigWig reader (added Run-17; cp39-cp313 wheels)
pyBigWig>=0.3.25; sys_platform != "win32"  # BigWig conformance oracle; no Windows wheel exists
