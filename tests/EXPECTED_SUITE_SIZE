# THE SUITE-SIZE RATCHET -- the single source of truth for how many tests exist.
# Created 2026-07-13 (roadmap 6.14).
#
# WHY THIS FILE EXISTS
# -------------------
# The G1 pre-flight gate carried a hard-coded pytest floor. It rotted FIVE TIMES IN TWO DAYS:
#
#     1485  ->  1805  ->  1842  ->  1850  ->  1853
#
# Every single time, the number sat directly beneath an emphatic, all-capitals comment
# ordering the next person to raise it -- written by the person who then failed to raise it.
# At 1485 against a suite of 1,815, THREE HUNDRED AND THIRTY tests could have silently
# vanished -- deleted, mis-named, or lost to a collection error -- and the gate would still
# have reported PASS. A floor that drifts below reality is not a floor; it is a rubber stamp.
#
# THE COMMENT DOES NOT ENFORCE ITSELF, AND NO VOLUME OF EMPHASIS WILL MAKE IT.
# That is roadmap section 7, root pattern (a): a number written down once and never
# re-derived becomes a lie on a schedule. The fix is never a louder comment. The fix is to
# make FORGETTING FAIL.
#
# HOW IT WORKS
# -----------
# `tests/conftest.py` reads this file. Under the explicit `--assert-suite-size` flag it
# compares the number below against the number of tests pytest actually COLLECTED, and
# ABORTS THE RUN on any disagreement -- in either direction:
#
#     collected < expected   ->  tests have VANISHED. Something is silently not running.
#     collected > expected   ->  tests were ADDED and this file was not updated.
#
# Both are failures. The second is the ratchet: you cannot add a test without bumping this
# number, because the gate goes red until you do. Forgetting is no longer possible.
#
# G1 (`scripts/Run_Preflight_Local.ps1`) and Continuous Integration (`.github/workflows/
# ci.yml`) BOTH pass `--assert-suite-size`, and BOTH read this file. The number therefore
# lives in exactly ONE place. It previously lived in four, and disagreed with itself in
# three of them.
#
# Running a subset locally (`pytest tests/unit/test_foo.py`) is UNAFFECTED: without the
# explicit flag, the check does not run at all.
#
# WHY *COLLECTED* AND NOT *PASSED*
# --------------------------------
# The collected count is ENVIRONMENT-INDEPENDENT. The passed/skipped split is not:
#
#     2026-07-13, Windows + full data:   1863 passed +  7 skipped              = 1870
#     2026-07-13, Linux CI runner:       1856 passed + 13 skipped + 1 xfailed  = 1870
#
# Same collection, different outcomes -- Continuous Integration legitimately skips the
# Windows-targeted post-flight tests, the ESM-2 tests (HuggingFace Hub is offline by design),
# and the tests needing gitignored cohort data. Asserting `passed` would force two numbers
# and re-create the divergence this file exists to kill. Asserting `collected` gives ONE
# number that is true everywhere.
#
# NOTE: this only works because the environments now COLLECT the same tests. Before
# 2026-07-13 they did not -- `torch_geometric`, `pandera`, `pyspark`, `river` and `mapie`
# were missing from Continuous Integration, and a module-level `importorskip` collapses N
# tests into ONE skip entry, so CI collected 1825 against a local 1868. Closing that gap
# (roadmap 6.17) is what made this ratchet possible at all.
#
# WHEN YOU ADD OR REMOVE TESTS
# ----------------------------
# Run the full suite, read the collected count (passed + skipped + xfailed + xpassed), and
# update the number below IN THE SAME COMMIT. The suite will tell you the exact figure and
# refuse to go green until you do.
#
# Format: exactly one line below that is a bare positive integer. Comments and blank lines
# are ignored. Anything else is a hard error -- a malformed ratchet is a dead ratchet.
#
# HISTORY -- every change to this number, with the reason. Keep it.
#
#   1870  2026-07-13  first value. Windows (Python 3.12): 1863 passed + 7 skipped.
#                     Linux runner (3.11 and 3.12): 1856 passed + 13 skipped + 1 xfailed.
#                     Same collection, different outcomes -- hence `collected`, not `passed`.
#
#   1882  2026-07-13  +12: tests/unit/test_suite_size_ratchet.py -- the ratchet's own
#                     self-test (roadmap 6.9: a guard with no self-test is a rumour).
#
#                     THE RATCHET'S FIRST ACT WAS TO CATCH ITS OWN AUTHOR. Adding those 12
#                     tests without touching this number turned the suite RED immediately:
#
#                         SUITE-SIZE RATCHET FAILED (roadmap 6.14)
#                           expected (tests/EXPECTED_SUITE_SIZE): 1870
#                           actually collected:                   1882
#                           12 MORE test(s) than expected.
#                           Set the value to 1882 IN THE SAME COMMIT as the tests.
#
#                     pytest exit code 4. That is the whole design working: five times in two
#                     days a hard-coded floor went stale beneath an all-capitals comment
#                     ordering someone to raise it, and five times the comment did nothing.
#                     This cannot be walked past.
#
#   1891  2026-07-13  +9: tests/unit/test_requirements_files_agree.py (roadmap 6.19).
#
#                     THE RATCHET CAUGHT ITS AUTHOR A SECOND TIME, and again named the exact
#                     number (expected 1882, collected 1891, exit code 4). It is not a
#                     formality: I would have written "1891" from arithmetic and been wrong,
#                     because the file contains a 6-way parametrised test that expands to 6
#                     items, not 1.
#
#                     Those 9 tests guard a REAL defect found today: requirements-api.txt and
#                     requirements.txt disagreed on NINE of the THIRTEEN packages they share
#                     (pyarrow >=15,<20 vs ==23.0.1, and xgboost >=2,<3 vs ==3.2.0, were
#                     outright ResolutionImpossible). Continuous Integration installs BOTH into
#                     ONE environment, and pip reported the conflict in an ERROR line on every
#                     one of 508 runs -- then exited 0. Pinning them duplicates nine numbers
#                     across two files, which is root pattern (a); the test RE-DERIVES the
#                     agreement on every run so the duplication is guarded, not trusted.
#
#   1924  2026-07-13  +33: tests/unit/test_drift_reference_profile.py (21, roadmap 6.20) and
#                     tests/unit/test_zero_variance_guard.py (12, roadmap 6.21).
#
#                     THE RATCHET CAUGHT ITS AUTHOR A THIRD TIME, and again named the exact
#                     figure (expected 1891, collected 1924, exit code 4). The number below is
#                     COPIED FROM THE RATCHET'S OWN OUTPUT. It was not computed by hand -- the
#                     one previous attempt to do that arithmetic was wrong, and the history
#                     entry above says so.
#
#                     WHAT THE 33 GUARD:
#
#                     (a) 21 tests -- the aggregate-only drift reference profile (6.20). The
#                     scheduled drift monitor could never run in hosted Continuous Integration
#                     because its reference was a 23.8 MB cohort matrix behind a Google Drive
#                     placeholder that was never wired. It is now a 1,450 KB committed
#                     histogram. The central test asserts the Population Stability Index is
#                     BIT-IDENTICAL between the profile and the raw matrix -- `==`, not
#                     pytest.approx -- and it is verified on the REAL 1,038,974-variant Run-15
#                     matrix, not a fixture. Measured worst delta across all 78 features:
#                     0.000e+00. The rest assert that what the profile CANNOT compute (the
#                     multivariate Maximum Mean Discrepancy and Szekely-Rizzo energy tests) is
#                     reported as NOT COMPUTED and never as a benign default -- because a
#                     silently-passing `mmd_pvalue = 1.0` would have permanently disarmed the
#                     urgent-retrain escalation while looking perfectly healthy.
#
#                     (b) 12 tests -- the zero-variance / silent-zero guard (6.21). Run 15
#                     trained, evaluated and PUBLISHED AN AUROC OF 0.998 with THIRTY-SIX of its
#                     78 features CONSTANT ZERO -- 46% of the feature space, across 1,038,974
#                     variants. GTEx, 1000 Genomes, FinnGen, AlphaFold/protein structure,
#                     MaxEntScan, UniProt, ESM-2, EVE, dbSNP, PhyloP, OMIM, ClinGen: all
#                     silently stubbed to 0.0 by connectors that return zeros instead of
#                     raising when their source file is absent (omim.py:105 is the canonical
#                     example). Every abort gate in the launcher checks that a FILE EXISTS,
#                     which is a PROXY. These tests assert the FEATURE VARIES, which is the
#                     thing itself. Root pattern (c), the most expensive one this project has.
#
#   1932  2026-07-13  +8: the documentation/contract gates (roadmap 6.22).
#                     tests/unit/test_schema_baseline_matches_contract.py (5) and
#                     tests/unit/test_methods_feature_count.py (3).
#
#                     THE RATCHET CAUGHT ITS AUTHOR A FOURTH TIME -- and this time the lesson
#                     was not "you forgot to bump it" but "your arithmetic was wrong AGAIN".
#                     I predicted +9 and wrote that in the plan. The ratchet said +8. The
#                     difference: I counted the `baseline` pytest FIXTURE in the schema test
#                     file as a test. It is not one. The file has 5 tests, not 6.
#
#                     That is a one-line error of exactly the kind this file exists to catch,
#                     and it is the FOURTH consecutive time I have miscounted (1891 was wrong
#                     from arithmetic; 1882 was wrong from arithmetic; 1924 I did not attempt).
#                     The number below is COPIED FROM THE RATCHET'S OUTPUT. It is never, under
#                     any circumstances, to be computed by hand -- the evidence that a human
#                     cannot reliably do this is now four entries long.
#
#                     WHAT THE 8 GUARD -- two documents that had quietly become false:
#
#                     (a) 5 tests -- data/reference/schema/schema_baseline.json had drifted
#                     TEN COLUMNS behind TABULAR_FEATURES (cosmic_recurrence, cosmic_sig_tier,
#                     3x finngen_r13_*, 2x genomiclm_*, 2x kegg_*, omim_n_diseases_molecular).
#                     Its own `captured_from` field confessed why: "derived: run16b-smoke
#                     baseline + hetero_gnn_score + 5 rnaseq_* SURGICALLY ADDED". It was
#                     hand-maintained, so it rotted. The schema-drift gate -- whose entire job
#                     is to catch a column set that has silently changed -- WAS ITSELF a
#                     silently-changed column set, and would have fired on Run 17 for its own
#                     staleness. A gate that cries wolf about itself gets switched off, and
#                     then the next real schema change goes through unseen.
#                     Fixed by DERIVATION: build_schema_baseline.py --from-contract now runs
#                     the real feature builder (engineer_features) over the correctness
#                     harness fixture and asserts the column set AND ORDER equal
#                     TABULAR_FEATURES. Regenerated 2026-07-13: 95 columns, hash a759084188...
#
#                     (b) 3 tests -- METHODS.md, the scientific description of this model,
#                     claimed "a total of 64 tabular features" against a contract of 95, with
#                     a group table that summed to 62. Three numbers, no two agreeing. It also
#                     listed HGMD Professional as data source 12 ("Disease mutation flag,
#                     report count") -- a source whose licence was never obtained and whose
#                     two columns were constant zero throughout. A wrong number in a methods
#                     document is the one that ends up in a paper.
#
#   1933  2026-07-13  +1: tests/unit/test_schema_baseline_matches_contract.py::
#                     test_every_baseline_dtype_is_float64 (roadmap 6.22).
#
#                     Added after the FIRST version of `build_schema_baseline.py
#                     --from-contract` -- written the same day -- captured the RAW dtypes that
#                     engineer_features() emits, which are int64 for roughly forty columns
#                     (the binary indicators af_is_absent, is_snv, cadd_high, ... and the
#                     integer counts ref_len, ..., several of them explicitly `.astype(int)`).
#
#                     But the schema gate does NOT validate raw builder output. It validates
#                     the PERSISTED matrix, outputs/<run>/full/splits/X_train.parquet, which is
#                     STANDARDISED before it is written -- and scaling makes every numeric
#                     column float64. Measured on the real Run-15 artifact: 78 of 78 columns
#                     are float64, INCLUDING af_is_absent, ref_len, is_snv and cadd_high.
#
#                     SchemaDriftAgent._dtype_family is IDENTITY for numeric dtypes (it
#                     collapses only the pandas 2.x/3.x string spellings), so that baseline
#                     would have reported ~40 DTYPE CHANGES against a matrix with nothing
#                     wrong with it, and the gate would have exited 2 = SCHEMA DRIFT DETECTED
#                     on Run 17. A gate that fires on nothing teaches everyone to ignore it,
#                     and then the next REAL schema change goes through unremarked. It would
#                     have replaced a stale-columns bug with a wrong-dtypes bug, in a gate
#                     whose only value is being trustworthy.
#
#                     THE TELL WAS ON SCREEN THE WHOLE TIME AND WAS READ PAST: the OLD baseline
#                     recorded hgmd_is_disease_mutation as float64, even though
#                     engineer_features cast it with `.astype(int)`. That one line said the
#                     baseline had been captured from a PROCESSED matrix. It was quoted twice
#                     in the session before anyone noticed what it meant.
#
#                     Fixed, and PROVEN rather than asserted: --from-contract now casts to
#                     float64, and the new --verify-against <matrix> flag REFUSES to write the
#                     baseline unless every dtype it can check matches a real persisted matrix.
#                     Run 2026-07-13 against the Run-15 X_train: 76 columns in common, 0 dtype
#                     mismatches, 19 columns (post-Run-15 features) reported as UNVERIFIED
#                     rather than silently accepted. Baseline hash a759084188... (contaminated)
#                     -> bbaba75d1f... (correct).
#
#   1940  2026-07-14  +7: tests/unit/test_readme_claims.py (roadmap 6.23).
#                     Full diagnosis: docs/audits/README_AUDIT_2026-07-14.md
#
#                     THE README WAS THE LARGEST SURVIVING INSTANCE OF ROOT PATTERN (a).
#                     It stated the FEATURE COUNT in NINE places with FOUR different values
#                     (80 x6, 78, 79) against a true contract of 95; the TEST COUNT in THREE
#                     places with THREE values (862 in a badge, 501/501 twice) against a true
#                     1,926 passing; and that the message-bus suite passed on Python 3.14.3 in
#                     one paragraph and 3.12.10 in another. It listed HGMD Professional as an
#                     integrated data source in three places -- a source never licensed, never
#                     wired, constant zero for the life of the project. Its training quickstart
#                     used `--parquet`, A FLAG THAT HAS NEVER EXISTED: anyone who copied the
#                     documented way to train this model got an argparse error.
#
#                     It also claimed "connector silent-zero hardening -- regression tests
#                     assert that connector fallbacks fail loud, not silently return 0.0."
#                     That is the EXACT OPPOSITE of the truth, and the safeguard whose absence
#                     produced roadmap 6.21 (36 of 78 features constant zero in Run 15).
#
#                     All performance figures were WITHDRAWN (owner's instruction, 2026-07-14):
#                     every one came from a run whose feature space was 46% non-existent, and
#                     the per-model comparison table ranked twelve algorithms against each other
#                     on it. A cross-algorithm comparison is precisely the artefact a half-empty
#                     feature space invalidates -- model families degrade differently under
#                     missing signal. The figures are not even restated in the withdrawal
#                     notice: a number in a warning banner is still a number people quote.
#
#                     BOTH NEW TESTS FAILED ON THEIR FIRST RUN, AND BOTH WERE THE TEST'S FAULT,
#                     NOT THE README'S -- recorded because it is the lesson:
#
#                     (a) The feature-count sweep matched "36 of its 78 features", "the 38
#                         features that were real" and "GTEx (6 features)" -- HISTORICAL
#                         statements, all true and all necessary. A sweep that cannot tell "the
#                         model HAS n features" from "36 of its 78 features WERE zero" is not a
#                         feature-count test; it is a ban on writing history down, in a project
#                         whose entire method is writing history down. Replaced with an
#                         enumerated list of claim sites, each of which MUST be present (so the
#                         check cannot go vacuous) and each of which must say 95.
#
#                     (b) The quickstart flag-check filtered at CODE-BLOCK level and then
#                         harvested every flag in the block. The quickstart is ONE block holding
#                         FOUR commands, so it attributed `--port` (uvicorn) and five
#                         run_drift_monitor.py flags to run_phase2_eval.py and reported six
#                         false failures. Every one of those flags was valid -- for the script
#                         it actually belonged to. The test was measuring the wrong thing while
#                         looking authoritative. Now parsed PER COMMAND, following backslash
#                         continuations, and validated against EACH script's own flags.
#
#   1943  2026-07-14  +3: tests/unit/test_console_strings_are_ascii.py (2, roadmap 6.24) and
#                     tests/unit/test_readme_claims.py::
#                     test_readme_base_model_roster_matches_the_ensemble (1, roadmap 6.23).
#
#                     (a) THE BASE-MODEL ROSTER. The README said the ensemble had TWELVE base
#                     classifiers. It has THIRTEEN. The old list made two compounding errors
#                     that nearly cancelled: it OMITTED `svm` and `svm_bagged_rbf` (-2) and
#                     COUNTED the Graph Attention Network as a base classifier (+1) -- the GAT
#                     produces `gnn_score`, a FEATURE, and contributes no out-of-fold column.
#                     13 - 2 + 1 = 12.
#
#                     THIS IS NOT A COSMETIC COUNT. Roadmap 6.6a is the defect in which a
#                     13-model ensemble SILENTLY BECAME a 12-model ensemble: the
#                     Kolmogorov-Arnold Network's out-of-fold step raised, a bare
#                     `except Exception` swallowed it, and the model vanished from
#                     trained_models_, from the blend, and from every cross-algorithm
#                     comparison artifact -- while the run reported normal metrics. ANYONE
#                     CHECKING A RUN'S TWELVE MODELS AGAINST A README THAT SAID TWELVE WOULD
#                     HAVE CONCLUDED THE ENSEMBLE WAS COMPLETE. The document would have
#                     concealed the exact defect it took weeks to find. A roster count that
#                     disagrees with the code is not a typo in a doc; it is a disabled alarm.
#                     The test reads `base_estimators` from a LIVE VariantEnsemble instance --
#                     not a regex, because a regex is exactly what missed `kan` (added via
#                     dict-unpacking behind `_KAN_AVAILABLE and not cfg.skip_kan`) and produced
#                     the wrong count in the first place.
#
#                     (b) CONSOLE STRINGS MUST BE ASCII. 181 string literals across 41 files in
#                     src/ passed non-ASCII to logger.* / print() / warnings.warn() / raise:
#                     73 em-dashes, 31 arrows, 13 box-drawing, plus emoji, Greek letters and
#                     ellipses. On a Windows cp1252 console these render as MOJIBAKE.
#
#                     OBSERVED AND RECORDED IN RUN 16 -- docs/runs/RUN_16_results.md:84:
#                     "Cosmetic: Reactome warning string uses an em-dash that mojibakes in
#                     PowerShell -- switch to ASCII `--` in reactome.py." NOTHING HAPPENED.
#                     reactome.py still carried the em-dash EIGHT WEEKS LATER, on 2026-07-14.
#                     A finding in a document is a comment. A finding that fails a test is a
#                     gate.
#
#                     Reconfiguring stdout to UTF-8 does NOT fix it -- scripts/train.py:61
#                     already does exactly that. Python then writes correct UTF-8 BYTES and a
#                     cp1252 console DECODES them as cp1252, producing the mojibake. Fixing it
#                     that way would require `chcp 65001` on every machine, in every terminal,
#                     forever. A fix whose correctness depends on the terminal's code page is
#                     green on one box and broken on another: root pattern (d), the same shape
#                     as the sed-patched imodelsx that silently dropped KAN from every
#                     Continuous Integration run for two months.
#
#                     The project had ALREADY made this decision -- tests/unit/
#                     test_variant_ensemble_ascii.py asserts it -- and enforced it on exactly
#                     ONE file out of the 41 that violated it.
#
#                     THREE DEFECTS WERE FOUND IN THE REPAIR TOOL ITSELF, EACH RECORDED BECAUSE
#                     EACH IS THE LESSON:
#
#                     (i)  BYTE-OFFSET SLICING. `ast` col_offset/end_col_offset are UTF-8 BYTE
#                          offsets, not character offsets. The first rewriter sliced a Python
#                          str with them. An em-dash is ONE character but THREE bytes, so every
#                          offset after a non-ASCII character on the same line was shifted +2
#                          per character. Measured: evaluator.py:574's three box-drawing
#                          literals sliced to "'*7} " and "8} {'" -- garbage. The bug is
#                          INVISIBLE on pure-ASCII lines, i.e. on exactly the lines that do not
#                          need fixing. It had already written to 36 files before it was caught.
#                          Fixed: encode -> slice by byte -> decode.
#
#                     (ii) NO STRUCTURAL PROOF. The rewriter checked only that the result still
#                          PARSED. A mis-spliced edit can be perfectly valid Python. Added
#                          _structural_shape(): compares both ASTs with every string value
#                          blanked, proving the ONLY thing that changed is string content.
#
#                     (iii) SPLIT BRAIN. The gate was widened to catch INDIRECT console strings
#                          (`_DIVIDER = "="*60` -> `logger.info(_DIVIDER)`) and the FIXER was
#                          not -- so the gate flagged 21 strings in 5 files that the repair tool
#                          was structurally incapable of repairing. A repair tool that cannot
#                          fix what its own gate reports turns a red test into a manual chore,
#                          and manual chores are what rot. ONE detector, shared by both.
#
#                     VERIFIED: two passes, 181 strings, 41 files. git diff 174 insertions /
#                     174 deletions -- EXACTLY BALANCED, so no line was added or lost and only
#                     string contents changed. 17 of the 21 indirect strings sat on distinct
#                     lines (four lines carried two each), and 157 + 17 = 174 closes the
#                     arithmetic. Gate green.
#
#   1944  2026-07-14  +1: tests/unit/test_readme_claims.py::
#                     test_readme_does_not_quote_an_environment_dependent_passing_count
#                     (roadmap 6.25).
#
#                     THE README QUOTED A *PASSING* COUNT, AND THE TEST GUARDING IT HAD A
#                     TOLERANCE THAT HID THE DRIFT.
#
#                     The 2026-07-14 README correction wrote "1,926 tests passing". The suite
#                     then grew to 1,943 collected. The guard asserted:
#
#                         assert collected - n <= 50       # 1943 - 1926 = 17. Passed. Silently.
#
#                     A tolerance on a number that CAN be exact is not engineering judgement;
#                     it is a place for rot to live.
#
#                     AND THE ANSWER WAS ALREADY IN THIS FILE'S OWN HEADER, ABOVE. Scroll up:
#                     "WHY *COLLECTED* AND NOT *PASSED* -- The collected count is
#                     ENVIRONMENT-INDEPENDENT. The passed/skipped split is not ... Asserting
#                     `passed` would force two numbers and re-create the divergence this file
#                     exists to kill." That paragraph was read, entries were written beneath
#                     it, and then the README was made to quote a passing count anyway.
#
#                     FIXED: the README states COLLECTED; one test asserts EQUALITY against
#                     this file with no tolerance; a second BANS passing counts outright.
#
#                     BOTH NEW TESTS FAILED ON FIRST RUN, AND BOTH FAILURES WERE THE TEST'S
#                     FAULT -- for the SECOND TIME IN TWO TURNS, in the SAME FILE, for the SAME
#                     REASON. The blanket ban on the string "1,926" fired on the README's own
#                     CORRECTION NOTE: the blockquote explaining that "1,926 tests passing" was
#                     stale. The test forbade the document from recording its own repair --
#                     exactly as the feature-count sweep had matched "36 of its 78 features"
#                     one turn earlier, and had been fixed there by enumerating claim sites.
#
#                     Now: TEST_COUNT_CLAIM_PATTERNS enumerates the three claim sites, and the
#                     passing-count ban skips blockquote lines. Blockquotes are COMMENTARY --
#                     the document explaining its own history. Body text is CLAIMS. A gate that
#                     cannot tell "the suite HAS n tests" from "this README used to SAY n" is
#                     not a gate; it is a ban on writing history down, in a project whose whole
#                     method is writing history down.
#
#                     Also corrected while here: the README stated the message-bus suite's size
#                     as 35/35 in one place and 34/34 eighty lines later -- because the original
#                     stated it TWICE and the 2026-07-14 audit corrected one occurrence and
#                     missed the other. The second also pointed at
#                     `agent_layer/test_message_bus.py`, WHICH DOES NOT EXIST (measured: the
#                     file is tests/unit/test_message_bus.py, 35 test functions). The duplicate
#                     is DELETED, not synchronised. And "docs/CHANGELOG.md (1,500+ lines)",
#                     stated twice against an actual 4,133, is now stated ZERO times: a line
#                     count conveys nothing and goes stale by construction.
#
#   1946  2026-07-15  +2: tests/unit/test_readme_claims.py::
#                     test_readme_agent_roster_matches_the_orchestrator_registry and
#                     ::test_readme_never_states_a_wrong_agent_count (roadmap 6.26).
#
#                     THE README SAID THE SYSTEM HAS 13 AGENTS. IT HAS 22.
#
#                     A 41% undercount of the supervisory layer the document calls the system's
#                     defining feature -- stated in SIX places (badge, opening paragraph,
#                     architecture diagram, key-properties bullet, section heading, repository
#                     -structure listing), wrong in all six. EIGHT of the thirteen names it did
#                     give were wrong: it listed SchemaDriftAgent, ConceptDriftAgent,
#                     LabelShiftAgent, CalibrationDriftAgent, InfrastructureDriftAgent,
#                     FairnessSubgroupAgent, AdversarialSubmissionAgent and
#                     AnnotationPolicyAgent; every actual class is a ...MonitorAgent. And NINE
#                     agents were entirely undocumented: AdaptationAgent, AgentOpsMonitorAgent,
#                     DataReadinessAgent, DatabaseFreshnessMonitorAgent,
#                     FeatureCoverageSentinelMonitorAgent, FinOpsAdvisorAgent,
#                     ModelInsightsAgent, ProvisioningAgent,
#                     ReclassificationSentinelMonitorAgent.
#
#                     `SchemaDriftAgent` was the most dangerous of the wrong names, because a
#                     class by that name EXISTS -- it is the schema-drift DETECTOR used by
#                     scripts/run_schema_drift_check.py. It does not descend from BaseAgent and
#                     is not in the registry. Anyone auditing the agent layer against that table
#                     would have gone looking for the wrong object and found one.
#
#                     THE 2026-07-14 README AUDIT FOUND THIS AND FILED IT "UNRESOLVED."
#                     Section 4.2: "Thirteen agents are named ... Whether all thirteen are
#                     scheduled and running is UNRESOLVED." Meanwhile
#                     scripts/check_agents_active.py -- NAMED IN A COMMENT INSIDE THE REGISTRY
#                     BEING READ (orchestrator.py:160) -- answers it in one command:
#                     "22 agents (registered=22, scheduled=22) ... 0 dormant/problem agent(s)".
#                     A finding in a document is a comment. The auditor wrote that sentence into
#                     the roadmap four times on 2026-07-13 and then did exactly this.
#
#                     THREE FAILURES IN BUILDING THE FIX, each recorded because each is the
#                     lesson:
#
#                     (i)   THE FIRST AST SCAN RETURNED 13 -- exactly the README's wrong number.
#                           It filtered bases on `"Agent" in base_name`, which MISSES every agent
#                           inheriting from `DriftMonitorBase` (a base class whose name contains
#                           no "Agent"). A malformed search returned the wrong answer AND made it
#                           look confirmed. It was caught only because `DriftMonitorBase` -- an
#                           abstract base -- appeared in a list that was supposed to be agents.
#
#                     (ii)  THE SECOND SCAN, of the registry, returned 18 -- by regexing quoted
#                           keys at module level and sweeping up `full`, `drift`, `ts`, `status`,
#                           `duration_ms` and `error`, none of which are agents. The registry is
#                           an INSTANCE attribute built inside a method.
#
#                     (iii) THE TESTS THEMSELVES FAILED ON `ImportError: cannot import name
#                           'AgentOrchestrator'`. THE CLASS IS `Orchestrator`. The name was
#                           assumed, never checked, and then propagated into NINE places --
#                           INCLUDING TWO LINES OF THE README, which briefly documented a class
#                           that does not exist. Twenty other files in the repository import
#                           `Orchestrator` correctly; not one of them was read. This is the same
#                           defect as the README's `agent_layer/test_message_bus.py` -- a
#                           confidently-stated path to nothing -- committed inside its own fix.
#
#                     Now: both tests read the roster from a LIVE Orchestrator instance
#                     (`__new__` + `_register_agents()`, no __init__ side effects), and
#                     AGENT_COUNT_CLAIM_PATTERNS enumerates all six claim sites.
#
#   1963  2026-07-15  +17, closing TWO silent-zero defects that were both feeding the
#                     sequence branch, and both of which would have reached Run 17.
#                     MEASURED, not predicted: `pytest tests/ --collect-only -q` ->
#                     "1963 tests collected in 39.70s", exit 0.
#
#                     +12  tests/unit/test_genomiclm_llr_is_computed.py  (NEW FILE)
#                     +5   tests/unit/test_seq_window_join.py  (5 -> 10)
#                     +0   tests/unit/test_train_cnn_activation.py  (2 rewritten, no delta)
#
#                     (A) genomiclm_llr HAD BEEN IDENTICALLY 0.0 FOR ALL 4,420,180 COHORT
#                         ROWS SINCE THE CONNECTOR WAS WRITTEN. `_masked_centre_logratio`
#                         located the variant's centre token with
#                         `tok(win, return_offsets_mapping=True)`. Nucleotide Transformer's
#                         tokeniser is `EsmTokenizer`; `is_fast` is False; HuggingFace
#                         raises NotImplementedError for that argument on every SLOW
#                         tokeniser. It raised on EVERY window. A bare `except Exception`
#                         swallowed it into a `logger.debug` -- below the default level, so
#                         it printed NOTHING -- and carried `# pragma: no cover`, so the
#                         coverage tool was told not to look either. Measured 2026-07-15:
#                         "is_fast: False | EsmTokenizer | OFFSET RAISES ->
#                         NotImplementedError".
#
#                         THREE BLIND SPOTS KEPT IT GREEN. (1) `genomiclm_delta_norm` never
#                         touches offset mapping, so the sibling feature stayed ALIVE and the
#                         connector looked healthy. (2) `build_reference_slice` FEEDS
#                         `genomiclm_llr` a synthetic `rng.uniform(-12, 4)`, and
#                         engineer_features reads it as a plain `df.get` passthrough -- so the
#                         stage-5 zero-audit graded the FIXTURE and never invoked the
#                         connector (7c: a gate that checks a PROXY is not a gate). (3) The
#                         harness comment asserted all six new columns were "live connectors
#                         -- Run-17 real-data smoke shows them populated"; the smoke audit it
#                         CITED records `genomiclm_llr` dead in all splits. The comment
#                         contradicted its own evidence, and the 2026-07-11 handoff had warned
#                         in bold: "if any SHOULD populate on the fixture and doesn't, that
#                         specific one is a real regression, not an allowlist gap."
#
#                         Left alone this would have been caught -- by hard-failing Run 17 at
#                         `_assert_no_dead_features`, after full data preparation, on the
#                         rented graphics-processing-unit, on paid compute. A true gate firing
#                         in the most expensive place available.
#
#                     (B) 21,814 COHORT ROWS (0.494%) CARRY FABRICATED SEQUENCE AND FOUR
#                         SEPARATE DETECTORS COULD NOT SEE THEM. The live artifact's manifest
#                         says n_ok 4,398,366 / n_poly 21,814 of 4,420,180: rows where
#                         delta_window_builder could not build a window, wrote POLY = "N"*101,
#                         and flagged `ok=False`. Every consumer instead compared window
#                         CONTENT against "A"*101 -- the JOIN's fill, a different failure path
#                         entirely: train.py:485, genomic_lm._poly, seq_window_join's own
#                         fallback, and rekey_seq_windows_v2:146. Four detectors, one blind
#                         spot, 21,814 rows through it. They reached cnn_1d as an ALL-ZERO
#                         tensor with ref == alt (delta channels identically zero) and reached
#                         Nucleotide Transformer as ||alt_emb - ref_emb|| == exactly 0.0 --
#                         the value genomic_lm's own docstring defines as "window unavailable
#                         / model unavailable". The EXIT_NOT_CHECKED bug again: "I could not
#                         look" rendered identically to "I looked and found zero".
#
#                         WIDENING THE CHECKS TO ALSO MATCH "N"*101 WOULD HAVE BEEN PATCHWORK
#                         ON AN ERROR OF PRINCIPLE. A window reading "A"*101 MAY BE REAL --
#                         poly-A tracts are real biology. Content can never separate "the
#                         reference genuinely says A" from "we gave up and typed A". Only
#                         provenance can, and `build_seq_windows.py:154` had been writing an
#                         `ok` column into the parquet the entire time. The ground truth was
#                         in the artifact; the join simply never read it. attach_delta_windows
#                         now returns a WindowAttachment with an explicit `usable` mask.
#
#                     FOUR OF THE SEVEN TESTS THAT WENT RED WERE ASSERTING THE DEFECTS.
#                     Not stale -- WRONG, and green the whole time. test_seq_window_join
#                     pinned the poly-A filler as the contract; test_train_cnn_activation
#                     asserted `n_unmapped == 0` on a frame containing a None (tier 1 ended in
#                     a hardcoded `return out, 0`, reporting zero unmapped while `.fillna`
#                     fabricated a window for that row) and its own comment named the
#                     fabrication approvingly: "# NaN -> poly-A fill".
#
#                     AND THE CHANGE SILENTLY DISARMED A LIVE GATE. rekey_seq_windows_v2's
#                     verify_against_cohort (main() returns 6 and REFUSES TO WRITE on
#                     key_mismatch > 0) detected unmapped rows by matching "A"*101. Moving
#                     PLACEHOLDER_BASE to "N" made that comparison match nothing: del_unmapped
#                     -> 0, key_mismatch -> 0, gate passes unconditionally. A working gate
#                     became a rubber stamp, and ONLY ITS OWN TESTS CAUGHT IT.
#
#                     ONE OF THE SEVEN WAS THIS AUTHOR'S NEW TEST FIRING ON THIS AUTHOR'S NEW
#                     COMMENT. test_no_pragma_no_cover_hides_the_llr_path banned the substring
#                     "pragma: no cover", then matched the comment QUOTING the deleted handler
#                     to explain why it was deleted. That is the THIRD time in this project a
#                     blanket string ban has fired on the prose written to describe the rule
#                     (see 1944: a ban on "1,926" matching its own correction note; and the
#                     feature-count sweep matching "36 of its 78 features"). Its sibling in the
#                     same file, written in the same sitting, parses with `ast` and got it
#                     right. Fixed structurally -- a real pragma follows CODE, a quotation is a
#                     whole-line comment -- and negative-tested by
#                     test_this_files_pragma_guard_can_actually_fail.
#
#                     STILL OPEN, and this is the one that matters for Run 17: scripts/
#                     train.py (441/458/480) and scripts/run_phase2_eval.py (425-427) still
#                     unpack the deprecated lossy 2-tuple and therefore still read
#                     usable=False rows as real. train.py feeds the CNN's X_seq. The migration
#                     list lives in WindowAttachment.__iter__'s docstring and deletes itself
#                     when empty.
1963
