# THE SUITE-SIZE RATCHET -- the single source of truth for how many tests exist.
# Created 2026-07-13 (roadmap 6.14).
#
# WHY THIS FILE EXISTS
# -------------------
# The G1 pre-flight gate carried a hard-coded pytest floor. It rotted FIVE TIMES IN TWO DAYS:
#
#     1485  ->  1805  ->  1842  ->  1850  ->  1853
#
# Every single time, the number sat directly beneath an emphatic, all-capitals comment
# ordering the next person to raise it -- written by the person who then failed to raise it.
# At 1485 against a suite of 1,815, THREE HUNDRED AND THIRTY tests could have silently
# vanished -- deleted, mis-named, or lost to a collection error -- and the gate would still
# have reported PASS. A floor that drifts below reality is not a floor; it is a rubber stamp.
#
# THE COMMENT DOES NOT ENFORCE ITSELF, AND NO VOLUME OF EMPHASIS WILL MAKE IT.
# That is roadmap section 7, root pattern (a): a number written down once and never
# re-derived becomes a lie on a schedule. The fix is never a louder comment. The fix is to
# make FORGETTING FAIL.
#
# HOW IT WORKS
# -----------
# `tests/conftest.py` reads this file. Under the explicit `--assert-suite-size` flag it
# compares the number below against the number of tests pytest actually COLLECTED, and
# ABORTS THE RUN on any disagreement -- in either direction:
#
#     collected < expected   ->  tests have VANISHED. Something is silently not running.
#     collected > expected   ->  tests were ADDED and this file was not updated.
#
# Both are failures. The second is the ratchet: you cannot add a test without bumping this
# number, because the gate goes red until you do. Forgetting is no longer possible.
#
# G1 (`scripts/Run_Preflight_Local.ps1`) and Continuous Integration (`.github/workflows/
# ci.yml`) BOTH pass `--assert-suite-size`, and BOTH read this file. The number therefore
# lives in exactly ONE place. It previously lived in four, and disagreed with itself in
# three of them.
#
# Running a subset locally (`pytest tests/unit/test_foo.py`) is UNAFFECTED: without the
# explicit flag, the check does not run at all.
#
# WHY *COLLECTED* AND NOT *PASSED*
# --------------------------------
# The collected count is ENVIRONMENT-INDEPENDENT. The passed/skipped split is not:
#
#     2026-07-13, Windows + full data:   1863 passed +  7 skipped              = 1870
#     2026-07-13, Linux CI runner:       1856 passed + 13 skipped + 1 xfailed  = 1870
#
# Same collection, different outcomes -- Continuous Integration legitimately skips the
# Windows-targeted post-flight tests, the ESM-2 tests (HuggingFace Hub is offline by design),
# and the tests needing gitignored cohort data. Asserting `passed` would force two numbers
# and re-create the divergence this file exists to kill. Asserting `collected` gives ONE
# number that is true everywhere.
#
# NOTE: this only works because the environments now COLLECT the same tests. Before
# 2026-07-13 they did not -- `torch_geometric`, `pandera`, `pyspark`, `river` and `mapie`
# were missing from Continuous Integration, and a module-level `importorskip` collapses N
# tests into ONE skip entry, so CI collected 1825 against a local 1868. Closing that gap
# (roadmap 6.17) is what made this ratchet possible at all.
#
# WHEN YOU ADD OR REMOVE TESTS
# ----------------------------
# Run the full suite, read the collected count (passed + skipped + xfailed + xpassed), and
# update the number below IN THE SAME COMMIT. The suite will tell you the exact figure and
# refuse to go green until you do.
#
# Format: exactly one line below that is a bare positive integer. Comments and blank lines
# are ignored. Anything else is a hard error -- a malformed ratchet is a dead ratchet.
#
# HISTORY -- every change to this number, with the reason. Keep it.
#
#   1870  2026-07-13  first value. Windows (Python 3.12): 1863 passed + 7 skipped.
#                     Linux runner (3.11 and 3.12): 1856 passed + 13 skipped + 1 xfailed.
#                     Same collection, different outcomes -- hence `collected`, not `passed`.
#
#   1882  2026-07-13  +12: tests/unit/test_suite_size_ratchet.py -- the ratchet's own
#                     self-test (roadmap 6.9: a guard with no self-test is a rumour).
#
#                     THE RATCHET'S FIRST ACT WAS TO CATCH ITS OWN AUTHOR. Adding those 12
#                     tests without touching this number turned the suite RED immediately:
#
#                         SUITE-SIZE RATCHET FAILED (roadmap 6.14)
#                           expected (tests/EXPECTED_SUITE_SIZE): 1870
#                           actually collected:                   1882
#                           12 MORE test(s) than expected.
#                           Set the value to 1882 IN THE SAME COMMIT as the tests.
#
#                     pytest exit code 4. That is the whole design working: five times in two
#                     days a hard-coded floor went stale beneath an all-capitals comment
#                     ordering someone to raise it, and five times the comment did nothing.
#                     This cannot be walked past.
#
#   1891  2026-07-13  +9: tests/unit/test_requirements_files_agree.py (roadmap 6.19).
#
#                     THE RATCHET CAUGHT ITS AUTHOR A SECOND TIME, and again named the exact
#                     number (expected 1882, collected 1891, exit code 4). It is not a
#                     formality: I would have written "1891" from arithmetic and been wrong,
#                     because the file contains a 6-way parametrised test that expands to 6
#                     items, not 1.
#
#                     Those 9 tests guard a REAL defect found today: requirements-api.txt and
#                     requirements.txt disagreed on NINE of the THIRTEEN packages they share
#                     (pyarrow >=15,<20 vs ==23.0.1, and xgboost >=2,<3 vs ==3.2.0, were
#                     outright ResolutionImpossible). Continuous Integration installs BOTH into
#                     ONE environment, and pip reported the conflict in an ERROR line on every
#                     one of 508 runs -- then exited 0. Pinning them duplicates nine numbers
#                     across two files, which is root pattern (a); the test RE-DERIVES the
#                     agreement on every run so the duplication is guarded, not trusted.
#
#   1924  2026-07-13  +33: tests/unit/test_drift_reference_profile.py (21, roadmap 6.20) and
#                     tests/unit/test_zero_variance_guard.py (12, roadmap 6.21).
#
#                     THE RATCHET CAUGHT ITS AUTHOR A THIRD TIME, and again named the exact
#                     figure (expected 1891, collected 1924, exit code 4). The number below is
#                     COPIED FROM THE RATCHET'S OWN OUTPUT. It was not computed by hand -- the
#                     one previous attempt to do that arithmetic was wrong, and the history
#                     entry above says so.
#
#                     WHAT THE 33 GUARD:
#
#                     (a) 21 tests -- the aggregate-only drift reference profile (6.20). The
#                     scheduled drift monitor could never run in hosted Continuous Integration
#                     because its reference was a 23.8 MB cohort matrix behind a Google Drive
#                     placeholder that was never wired. It is now a 1,450 KB committed
#                     histogram. The central test asserts the Population Stability Index is
#                     BIT-IDENTICAL between the profile and the raw matrix -- `==`, not
#                     pytest.approx -- and it is verified on the REAL 1,038,974-variant Run-15
#                     matrix, not a fixture. Measured worst delta across all 78 features:
#                     0.000e+00. The rest assert that what the profile CANNOT compute (the
#                     multivariate Maximum Mean Discrepancy and Szekely-Rizzo energy tests) is
#                     reported as NOT COMPUTED and never as a benign default -- because a
#                     silently-passing `mmd_pvalue = 1.0` would have permanently disarmed the
#                     urgent-retrain escalation while looking perfectly healthy.
#
#                     (b) 12 tests -- the zero-variance / silent-zero guard (6.21). Run 15
#                     trained, evaluated and PUBLISHED AN AUROC OF 0.998 with THIRTY-SIX of its
#                     78 features CONSTANT ZERO -- 46% of the feature space, across 1,038,974
#                     variants. GTEx, 1000 Genomes, FinnGen, AlphaFold/protein structure,
#                     MaxEntScan, UniProt, ESM-2, EVE, dbSNP, PhyloP, OMIM, ClinGen: all
#                     silently stubbed to 0.0 by connectors that return zeros instead of
#                     raising when their source file is absent (omim.py:105 is the canonical
#                     example). Every abort gate in the launcher checks that a FILE EXISTS,
#                     which is a PROXY. These tests assert the FEATURE VARIES, which is the
#                     thing itself. Root pattern (c), the most expensive one this project has.
#
#   1932  2026-07-13  +8: the documentation/contract gates (roadmap 6.22).
#                     tests/unit/test_schema_baseline_matches_contract.py (5) and
#                     tests/unit/test_methods_feature_count.py (3).
#
#                     THE RATCHET CAUGHT ITS AUTHOR A FOURTH TIME -- and this time the lesson
#                     was not "you forgot to bump it" but "your arithmetic was wrong AGAIN".
#                     I predicted +9 and wrote that in the plan. The ratchet said +8. The
#                     difference: I counted the `baseline` pytest FIXTURE in the schema test
#                     file as a test. It is not one. The file has 5 tests, not 6.
#
#                     That is a one-line error of exactly the kind this file exists to catch,
#                     and it is the FOURTH consecutive time I have miscounted (1891 was wrong
#                     from arithmetic; 1882 was wrong from arithmetic; 1924 I did not attempt).
#                     The number below is COPIED FROM THE RATCHET'S OUTPUT. It is never, under
#                     any circumstances, to be computed by hand -- the evidence that a human
#                     cannot reliably do this is now four entries long.
#
#                     WHAT THE 8 GUARD -- two documents that had quietly become false:
#
#                     (a) 5 tests -- data/reference/schema/schema_baseline.json had drifted
#                     TEN COLUMNS behind TABULAR_FEATURES (cosmic_recurrence, cosmic_sig_tier,
#                     3x finngen_r13_*, 2x genomiclm_*, 2x kegg_*, omim_n_diseases_molecular).
#                     Its own `captured_from` field confessed why: "derived: run16b-smoke
#                     baseline + hetero_gnn_score + 5 rnaseq_* SURGICALLY ADDED". It was
#                     hand-maintained, so it rotted. The schema-drift gate -- whose entire job
#                     is to catch a column set that has silently changed -- WAS ITSELF a
#                     silently-changed column set, and would have fired on Run 17 for its own
#                     staleness. A gate that cries wolf about itself gets switched off, and
#                     then the next real schema change goes through unseen.
#                     Fixed by DERIVATION: build_schema_baseline.py --from-contract now runs
#                     the real feature builder (engineer_features) over the correctness
#                     harness fixture and asserts the column set AND ORDER equal
#                     TABULAR_FEATURES. Regenerated 2026-07-13: 95 columns, hash a759084188...
#
#                     (b) 3 tests -- METHODS.md, the scientific description of this model,
#                     claimed "a total of 64 tabular features" against a contract of 95, with
#                     a group table that summed to 62. Three numbers, no two agreeing. It also
#                     listed HGMD Professional as data source 12 ("Disease mutation flag,
#                     report count") -- a source whose licence was never obtained and whose
#                     two columns were constant zero throughout. A wrong number in a methods
#                     document is the one that ends up in a paper.
#
#   1933  2026-07-13  +1: tests/unit/test_schema_baseline_matches_contract.py::
#                     test_every_baseline_dtype_is_float64 (roadmap 6.22).
#
#                     Added after the FIRST version of `build_schema_baseline.py
#                     --from-contract` -- written the same day -- captured the RAW dtypes that
#                     engineer_features() emits, which are int64 for roughly forty columns
#                     (the binary indicators af_is_absent, is_snv, cadd_high, ... and the
#                     integer counts ref_len, ..., several of them explicitly `.astype(int)`).
#
#                     But the schema gate does NOT validate raw builder output. It validates
#                     the PERSISTED matrix, outputs/<run>/full/splits/X_train.parquet, which is
#                     STANDARDISED before it is written -- and scaling makes every numeric
#                     column float64. Measured on the real Run-15 artifact: 78 of 78 columns
#                     are float64, INCLUDING af_is_absent, ref_len, is_snv and cadd_high.
#
#                     SchemaDriftAgent._dtype_family is IDENTITY for numeric dtypes (it
#                     collapses only the pandas 2.x/3.x string spellings), so that baseline
#                     would have reported ~40 DTYPE CHANGES against a matrix with nothing
#                     wrong with it, and the gate would have exited 2 = SCHEMA DRIFT DETECTED
#                     on Run 17. A gate that fires on nothing teaches everyone to ignore it,
#                     and then the next REAL schema change goes through unremarked. It would
#                     have replaced a stale-columns bug with a wrong-dtypes bug, in a gate
#                     whose only value is being trustworthy.
#
#                     THE TELL WAS ON SCREEN THE WHOLE TIME AND WAS READ PAST: the OLD baseline
#                     recorded hgmd_is_disease_mutation as float64, even though
#                     engineer_features cast it with `.astype(int)`. That one line said the
#                     baseline had been captured from a PROCESSED matrix. It was quoted twice
#                     in the session before anyone noticed what it meant.
#
#                     Fixed, and PROVEN rather than asserted: --from-contract now casts to
#                     float64, and the new --verify-against <matrix> flag REFUSES to write the
#                     baseline unless every dtype it can check matches a real persisted matrix.
#                     Run 2026-07-13 against the Run-15 X_train: 76 columns in common, 0 dtype
#                     mismatches, 19 columns (post-Run-15 features) reported as UNVERIFIED
#                     rather than silently accepted. Baseline hash a759084188... (contaminated)
#                     -> bbaba75d1f... (correct).
#
#   1940  2026-07-14  +7: tests/unit/test_readme_claims.py (roadmap 6.23).
#                     Full diagnosis: docs/audits/README_AUDIT_2026-07-14.md
#
#                     THE README WAS THE LARGEST SURVIVING INSTANCE OF ROOT PATTERN (a).
#                     It stated the FEATURE COUNT in NINE places with FOUR different values
#                     (80 x6, 78, 79) against a true contract of 95; the TEST COUNT in THREE
#                     places with THREE values (862 in a badge, 501/501 twice) against a true
#                     1,926 passing; and that the message-bus suite passed on Python 3.14.3 in
#                     one paragraph and 3.12.10 in another. It listed HGMD Professional as an
#                     integrated data source in three places -- a source never licensed, never
#                     wired, constant zero for the life of the project. Its training quickstart
#                     used `--parquet`, A FLAG THAT HAS NEVER EXISTED: anyone who copied the
#                     documented way to train this model got an argparse error.
#
#                     It also claimed "connector silent-zero hardening -- regression tests
#                     assert that connector fallbacks fail loud, not silently return 0.0."
#                     That is the EXACT OPPOSITE of the truth, and the safeguard whose absence
#                     produced roadmap 6.21 (36 of 78 features constant zero in Run 15).
#
#                     All performance figures were WITHDRAWN (owner's instruction, 2026-07-14):
#                     every one came from a run whose feature space was 46% non-existent, and
#                     the per-model comparison table ranked twelve algorithms against each other
#                     on it. A cross-algorithm comparison is precisely the artefact a half-empty
#                     feature space invalidates -- model families degrade differently under
#                     missing signal. The figures are not even restated in the withdrawal
#                     notice: a number in a warning banner is still a number people quote.
#
#                     BOTH NEW TESTS FAILED ON THEIR FIRST RUN, AND BOTH WERE THE TEST'S FAULT,
#                     NOT THE README'S -- recorded because it is the lesson:
#
#                     (a) The feature-count sweep matched "36 of its 78 features", "the 38
#                         features that were real" and "GTEx (6 features)" -- HISTORICAL
#                         statements, all true and all necessary. A sweep that cannot tell "the
#                         model HAS n features" from "36 of its 78 features WERE zero" is not a
#                         feature-count test; it is a ban on writing history down, in a project
#                         whose entire method is writing history down. Replaced with an
#                         enumerated list of claim sites, each of which MUST be present (so the
#                         check cannot go vacuous) and each of which must say 95.
#
#                     (b) The quickstart flag-check filtered at CODE-BLOCK level and then
#                         harvested every flag in the block. The quickstart is ONE block holding
#                         FOUR commands, so it attributed `--port` (uvicorn) and five
#                         run_drift_monitor.py flags to run_phase2_eval.py and reported six
#                         false failures. Every one of those flags was valid -- for the script
#                         it actually belonged to. The test was measuring the wrong thing while
#                         looking authoritative. Now parsed PER COMMAND, following backslash
#                         continuations, and validated against EACH script's own flags.
#
#   1943  2026-07-14  +3: tests/unit/test_console_strings_are_ascii.py (2, roadmap 6.24) and
#                     tests/unit/test_readme_claims.py::
#                     test_readme_base_model_roster_matches_the_ensemble (1, roadmap 6.23).
#
#                     (a) THE BASE-MODEL ROSTER. The README said the ensemble had TWELVE base
#                     classifiers. It has THIRTEEN. The old list made two compounding errors
#                     that nearly cancelled: it OMITTED `svm` and `svm_bagged_rbf` (-2) and
#                     COUNTED the Graph Attention Network as a base classifier (+1) -- the GAT
#                     produces `gnn_score`, a FEATURE, and contributes no out-of-fold column.
#                     13 - 2 + 1 = 12.
#
#                     THIS IS NOT A COSMETIC COUNT. Roadmap 6.6a is the defect in which a
#                     13-model ensemble SILENTLY BECAME a 12-model ensemble: the
#                     Kolmogorov-Arnold Network's out-of-fold step raised, a bare
#                     `except Exception` swallowed it, and the model vanished from
#                     trained_models_, from the blend, and from every cross-algorithm
#                     comparison artifact -- while the run reported normal metrics. ANYONE
#                     CHECKING A RUN'S TWELVE MODELS AGAINST A README THAT SAID TWELVE WOULD
#                     HAVE CONCLUDED THE ENSEMBLE WAS COMPLETE. The document would have
#                     concealed the exact defect it took weeks to find. A roster count that
#                     disagrees with the code is not a typo in a doc; it is a disabled alarm.
#                     The test reads `base_estimators` from a LIVE VariantEnsemble instance --
#                     not a regex, because a regex is exactly what missed `kan` (added via
#                     dict-unpacking behind `_KAN_AVAILABLE and not cfg.skip_kan`) and produced
#                     the wrong count in the first place.
#
#                     (b) CONSOLE STRINGS MUST BE ASCII. 181 string literals across 41 files in
#                     src/ passed non-ASCII to logger.* / print() / warnings.warn() / raise:
#                     73 em-dashes, 31 arrows, 13 box-drawing, plus emoji, Greek letters and
#                     ellipses. On a Windows cp1252 console these render as MOJIBAKE.
#
#                     OBSERVED AND RECORDED IN RUN 16 -- docs/runs/RUN_16_results.md:84:
#                     "Cosmetic: Reactome warning string uses an em-dash that mojibakes in
#                     PowerShell -- switch to ASCII `--` in reactome.py." NOTHING HAPPENED.
#                     reactome.py still carried the em-dash EIGHT WEEKS LATER, on 2026-07-14.
#                     A finding in a document is a comment. A finding that fails a test is a
#                     gate.
#
#                     Reconfiguring stdout to UTF-8 does NOT fix it -- scripts/train.py:61
#                     already does exactly that. Python then writes correct UTF-8 BYTES and a
#                     cp1252 console DECODES them as cp1252, producing the mojibake. Fixing it
#                     that way would require `chcp 65001` on every machine, in every terminal,
#                     forever. A fix whose correctness depends on the terminal's code page is
#                     green on one box and broken on another: root pattern (d), the same shape
#                     as the sed-patched imodelsx that silently dropped KAN from every
#                     Continuous Integration run for two months.
#
#                     The project had ALREADY made this decision -- tests/unit/
#                     test_variant_ensemble_ascii.py asserts it -- and enforced it on exactly
#                     ONE file out of the 41 that violated it.
#
#                     THREE DEFECTS WERE FOUND IN THE REPAIR TOOL ITSELF, EACH RECORDED BECAUSE
#                     EACH IS THE LESSON:
#
#                     (i)  BYTE-OFFSET SLICING. `ast` col_offset/end_col_offset are UTF-8 BYTE
#                          offsets, not character offsets. The first rewriter sliced a Python
#                          str with them. An em-dash is ONE character but THREE bytes, so every
#                          offset after a non-ASCII character on the same line was shifted +2
#                          per character. Measured: evaluator.py:574's three box-drawing
#                          literals sliced to "'*7} " and "8} {'" -- garbage. The bug is
#                          INVISIBLE on pure-ASCII lines, i.e. on exactly the lines that do not
#                          need fixing. It had already written to 36 files before it was caught.
#                          Fixed: encode -> slice by byte -> decode.
#
#                     (ii) NO STRUCTURAL PROOF. The rewriter checked only that the result still
#                          PARSED. A mis-spliced edit can be perfectly valid Python. Added
#                          _structural_shape(): compares both ASTs with every string value
#                          blanked, proving the ONLY thing that changed is string content.
#
#                     (iii) SPLIT BRAIN. The gate was widened to catch INDIRECT console strings
#                          (`_DIVIDER = "="*60` -> `logger.info(_DIVIDER)`) and the FIXER was
#                          not -- so the gate flagged 21 strings in 5 files that the repair tool
#                          was structurally incapable of repairing. A repair tool that cannot
#                          fix what its own gate reports turns a red test into a manual chore,
#                          and manual chores are what rot. ONE detector, shared by both.
#
#                     VERIFIED: two passes, 181 strings, 41 files. git diff 174 insertions /
#                     174 deletions -- EXACTLY BALANCED, so no line was added or lost and only
#                     string contents changed. 17 of the 21 indirect strings sat on distinct
#                     lines (four lines carried two each), and 157 + 17 = 174 closes the
#                     arithmetic. Gate green.
#
#   1944  2026-07-14  +1: tests/unit/test_readme_claims.py::
#                     test_readme_does_not_quote_an_environment_dependent_passing_count
#                     (roadmap 6.25).
#
#                     THE README QUOTED A *PASSING* COUNT, AND THE TEST GUARDING IT HAD A
#                     TOLERANCE THAT HID THE DRIFT.
#
#                     The 2026-07-14 README correction wrote "1,926 tests passing". The suite
#                     then grew to 1,943 collected. The guard asserted:
#
#                         assert collected - n <= 50       # 1943 - 1926 = 17. Passed. Silently.
#
#                     A tolerance on a number that CAN be exact is not engineering judgement;
#                     it is a place for rot to live.
#
#                     AND THE ANSWER WAS ALREADY IN THIS FILE'S OWN HEADER, ABOVE. Scroll up:
#                     "WHY *COLLECTED* AND NOT *PASSED* -- The collected count is
#                     ENVIRONMENT-INDEPENDENT. The passed/skipped split is not ... Asserting
#                     `passed` would force two numbers and re-create the divergence this file
#                     exists to kill." That paragraph was read, entries were written beneath
#                     it, and then the README was made to quote a passing count anyway.
#
#                     FIXED: the README states COLLECTED; one test asserts EQUALITY against
#                     this file with no tolerance; a second BANS passing counts outright.
#
#                     BOTH NEW TESTS FAILED ON FIRST RUN, AND BOTH FAILURES WERE THE TEST'S
#                     FAULT -- for the SECOND TIME IN TWO TURNS, in the SAME FILE, for the SAME
#                     REASON. The blanket ban on the string "1,926" fired on the README's own
#                     CORRECTION NOTE: the blockquote explaining that "1,926 tests passing" was
#                     stale. The test forbade the document from recording its own repair --
#                     exactly as the feature-count sweep had matched "36 of its 78 features"
#                     one turn earlier, and had been fixed there by enumerating claim sites.
#
#                     Now: TEST_COUNT_CLAIM_PATTERNS enumerates the three claim sites, and the
#                     passing-count ban skips blockquote lines. Blockquotes are COMMENTARY --
#                     the document explaining its own history. Body text is CLAIMS. A gate that
#                     cannot tell "the suite HAS n tests" from "this README used to SAY n" is
#                     not a gate; it is a ban on writing history down, in a project whose whole
#                     method is writing history down.
#
#                     Also corrected while here: the README stated the message-bus suite's size
#                     as 35/35 in one place and 34/34 eighty lines later -- because the original
#                     stated it TWICE and the 2026-07-14 audit corrected one occurrence and
#                     missed the other. The second also pointed at
#                     `agent_layer/test_message_bus.py`, WHICH DOES NOT EXIST (measured: the
#                     file is tests/unit/test_message_bus.py, 35 test functions). The duplicate
#                     is DELETED, not synchronised. And "docs/CHANGELOG.md (1,500+ lines)",
#                     stated twice against an actual 4,133, is now stated ZERO times: a line
#                     count conveys nothing and goes stale by construction.
#
#   1946  2026-07-15  +2: tests/unit/test_readme_claims.py::
#                     test_readme_agent_roster_matches_the_orchestrator_registry and
#                     ::test_readme_never_states_a_wrong_agent_count (roadmap 6.26).
#
#                     THE README SAID THE SYSTEM HAS 13 AGENTS. IT HAS 22.
#
#                     A 41% undercount of the supervisory layer the document calls the system's
#                     defining feature -- stated in SIX places (badge, opening paragraph,
#                     architecture diagram, key-properties bullet, section heading, repository
#                     -structure listing), wrong in all six. EIGHT of the thirteen names it did
#                     give were wrong: it listed SchemaDriftAgent, ConceptDriftAgent,
#                     LabelShiftAgent, CalibrationDriftAgent, InfrastructureDriftAgent,
#                     FairnessSubgroupAgent, AdversarialSubmissionAgent and
#                     AnnotationPolicyAgent; every actual class is a ...MonitorAgent. And NINE
#                     agents were entirely undocumented: AdaptationAgent, AgentOpsMonitorAgent,
#                     DataReadinessAgent, DatabaseFreshnessMonitorAgent,
#                     FeatureCoverageSentinelMonitorAgent, FinOpsAdvisorAgent,
#                     ModelInsightsAgent, ProvisioningAgent,
#                     ReclassificationSentinelMonitorAgent.
#
#                     `SchemaDriftAgent` was the most dangerous of the wrong names, because a
#                     class by that name EXISTS -- it is the schema-drift DETECTOR used by
#                     scripts/run_schema_drift_check.py. It does not descend from BaseAgent and
#                     is not in the registry. Anyone auditing the agent layer against that table
#                     would have gone looking for the wrong object and found one.
#
#                     THE 2026-07-14 README AUDIT FOUND THIS AND FILED IT "UNRESOLVED."
#                     Section 4.2: "Thirteen agents are named ... Whether all thirteen are
#                     scheduled and running is UNRESOLVED." Meanwhile
#                     scripts/check_agents_active.py -- NAMED IN A COMMENT INSIDE THE REGISTRY
#                     BEING READ (orchestrator.py:160) -- answers it in one command:
#                     "22 agents (registered=22, scheduled=22) ... 0 dormant/problem agent(s)".
#                     A finding in a document is a comment. The auditor wrote that sentence into
#                     the roadmap four times on 2026-07-13 and then did exactly this.
#
#                     THREE FAILURES IN BUILDING THE FIX, each recorded because each is the
#                     lesson:
#
#                     (i)   THE FIRST AST SCAN RETURNED 13 -- exactly the README's wrong number.
#                           It filtered bases on `"Agent" in base_name`, which MISSES every agent
#                           inheriting from `DriftMonitorBase` (a base class whose name contains
#                           no "Agent"). A malformed search returned the wrong answer AND made it
#                           look confirmed. It was caught only because `DriftMonitorBase` -- an
#                           abstract base -- appeared in a list that was supposed to be agents.
#
#                     (ii)  THE SECOND SCAN, of the registry, returned 18 -- by regexing quoted
#                           keys at module level and sweeping up `full`, `drift`, `ts`, `status`,
#                           `duration_ms` and `error`, none of which are agents. The registry is
#                           an INSTANCE attribute built inside a method.
#
#                     (iii) THE TESTS THEMSELVES FAILED ON `ImportError: cannot import name
#                           'AgentOrchestrator'`. THE CLASS IS `Orchestrator`. The name was
#                           assumed, never checked, and then propagated into NINE places --
#                           INCLUDING TWO LINES OF THE README, which briefly documented a class
#                           that does not exist. Twenty other files in the repository import
#                           `Orchestrator` correctly; not one of them was read. This is the same
#                           defect as the README's `agent_layer/test_message_bus.py` -- a
#                           confidently-stated path to nothing -- committed inside its own fix.
#
#                     Now: both tests read the roster from a LIVE Orchestrator instance
#                     (`__new__` + `_register_agents()`, no __init__ side effects), and
#                     AGENT_COUNT_CLAIM_PATTERNS enumerates all six claim sites.
#
#   1963  2026-07-15  +17, closing TWO silent-zero defects that were both feeding the
#                     sequence branch, and both of which would have reached Run 17.
#                     MEASURED, not predicted: `pytest tests/ --collect-only -q` ->
#                     "1963 tests collected in 39.70s", exit 0.
#
#                     +12  tests/unit/test_genomiclm_llr_is_computed.py  (NEW FILE)
#                     +5   tests/unit/test_seq_window_join.py  (5 -> 10)
#                     +0   tests/unit/test_train_cnn_activation.py  (2 rewritten, no delta)
#
#                     (A) genomiclm_llr HAD BEEN IDENTICALLY 0.0 FOR ALL 4,420,180 COHORT
#                         ROWS SINCE THE CONNECTOR WAS WRITTEN. `_masked_centre_logratio`
#                         located the variant's centre token with
#                         `tok(win, return_offsets_mapping=True)`. Nucleotide Transformer's
#                         tokeniser is `EsmTokenizer`; `is_fast` is False; HuggingFace
#                         raises NotImplementedError for that argument on every SLOW
#                         tokeniser. It raised on EVERY window. A bare `except Exception`
#                         swallowed it into a `logger.debug` -- below the default level, so
#                         it printed NOTHING -- and carried `# pragma: no cover`, so the
#                         coverage tool was told not to look either. Measured 2026-07-15:
#                         "is_fast: False | EsmTokenizer | OFFSET RAISES ->
#                         NotImplementedError".
#
#                         THREE BLIND SPOTS KEPT IT GREEN. (1) `genomiclm_delta_norm` never
#                         touches offset mapping, so the sibling feature stayed ALIVE and the
#                         connector looked healthy. (2) `build_reference_slice` FEEDS
#                         `genomiclm_llr` a synthetic `rng.uniform(-12, 4)`, and
#                         engineer_features reads it as a plain `df.get` passthrough -- so the
#                         stage-5 zero-audit graded the FIXTURE and never invoked the
#                         connector (7c: a gate that checks a PROXY is not a gate). (3) The
#                         harness comment asserted all six new columns were "live connectors
#                         -- Run-17 real-data smoke shows them populated"; the smoke audit it
#                         CITED records `genomiclm_llr` dead in all splits. The comment
#                         contradicted its own evidence, and the 2026-07-11 handoff had warned
#                         in bold: "if any SHOULD populate on the fixture and doesn't, that
#                         specific one is a real regression, not an allowlist gap."
#
#                         Left alone this would have been caught -- by hard-failing Run 17 at
#                         `_assert_no_dead_features`, after full data preparation, on the
#                         rented graphics-processing-unit, on paid compute. A true gate firing
#                         in the most expensive place available.
#
#                     (B) 21,814 COHORT ROWS (0.494%) CARRY FABRICATED SEQUENCE AND FOUR
#                         SEPARATE DETECTORS COULD NOT SEE THEM. The live artifact's manifest
#                         says n_ok 4,398,366 / n_poly 21,814 of 4,420,180: rows where
#                         delta_window_builder could not build a window, wrote POLY = "N"*101,
#                         and flagged `ok=False`. Every consumer instead compared window
#                         CONTENT against "A"*101 -- the JOIN's fill, a different failure path
#                         entirely: train.py:485, genomic_lm._poly, seq_window_join's own
#                         fallback, and rekey_seq_windows_v2:146. Four detectors, one blind
#                         spot, 21,814 rows through it. They reached cnn_1d as an ALL-ZERO
#                         tensor with ref == alt (delta channels identically zero) and reached
#                         Nucleotide Transformer as ||alt_emb - ref_emb|| == exactly 0.0 --
#                         the value genomic_lm's own docstring defines as "window unavailable
#                         / model unavailable". The EXIT_NOT_CHECKED bug again: "I could not
#                         look" rendered identically to "I looked and found zero".
#
#                         WIDENING THE CHECKS TO ALSO MATCH "N"*101 WOULD HAVE BEEN PATCHWORK
#                         ON AN ERROR OF PRINCIPLE. A window reading "A"*101 MAY BE REAL --
#                         poly-A tracts are real biology. Content can never separate "the
#                         reference genuinely says A" from "we gave up and typed A". Only
#                         provenance can, and `build_seq_windows.py:154` had been writing an
#                         `ok` column into the parquet the entire time. The ground truth was
#                         in the artifact; the join simply never read it. attach_delta_windows
#                         now returns a WindowAttachment with an explicit `usable` mask.
#
#                     FOUR OF THE SEVEN TESTS THAT WENT RED WERE ASSERTING THE DEFECTS.
#                     Not stale -- WRONG, and green the whole time. test_seq_window_join
#                     pinned the poly-A filler as the contract; test_train_cnn_activation
#                     asserted `n_unmapped == 0` on a frame containing a None (tier 1 ended in
#                     a hardcoded `return out, 0`, reporting zero unmapped while `.fillna`
#                     fabricated a window for that row) and its own comment named the
#                     fabrication approvingly: "# NaN -> poly-A fill".
#
#                     AND THE CHANGE SILENTLY DISARMED A LIVE GATE. rekey_seq_windows_v2's
#                     verify_against_cohort (main() returns 6 and REFUSES TO WRITE on
#                     key_mismatch > 0) detected unmapped rows by matching "A"*101. Moving
#                     PLACEHOLDER_BASE to "N" made that comparison match nothing: del_unmapped
#                     -> 0, key_mismatch -> 0, gate passes unconditionally. A working gate
#                     became a rubber stamp, and ONLY ITS OWN TESTS CAUGHT IT.
#
#                     ONE OF THE SEVEN WAS THIS AUTHOR'S NEW TEST FIRING ON THIS AUTHOR'S NEW
#                     COMMENT. test_no_pragma_no_cover_hides_the_llr_path banned the substring
#                     "pragma: no cover", then matched the comment QUOTING the deleted handler
#                     to explain why it was deleted. That is the THIRD time in this project a
#                     blanket string ban has fired on the prose written to describe the rule
#                     (see 1944: a ban on "1,926" matching its own correction note; and the
#                     feature-count sweep matching "36 of its 78 features"). Its sibling in the
#                     same file, written in the same sitting, parses with `ast` and got it
#                     right. Fixed structurally -- a real pragma follows CODE, a quotation is a
#                     whole-line comment -- and negative-tested by
#                     test_this_files_pragma_guard_can_actually_fail.
#
#                     STILL OPEN, and this is the one that matters for Run 17: scripts/
#                     train.py (441/458/480) and scripts/run_phase2_eval.py (425-427) still
#                     unpack the deprecated lossy 2-tuple and therefore still read
#                     usable=False rows as real. train.py feeds the CNN's X_seq. The migration
#                     list lives in WindowAttachment.__iter__'s docstring and deletes itself
#                     when empty.
#
#   1966  2026-07-15  NET +3. MEASURED (`pytest tests/ --collect-only -q` -> "1966 tests
#                     collected in 16.76s", exit 0), never predicted.
#
#                     +4  tests/unit/test_no_content_based_poly_detection.py (NEW FILE) --
#                         a repo-wide ban on inferring window PROVENANCE from window
#                         CONTENT. FOUR independent poly-A detectors existed (train.py:485,
#                         genomic_lm._poly, seq_window_join's own fallback, and
#                         rekey_seq_windows_v2:146 -- the last wired into a gate that
#                         RETURNS 6 AND REFUSES TO WRITE). All four were blind to the same
#                         21,814 builder-placeholder rows, because the builder writes "N"
#                         and every detector matched "A".
#
#                         THE BAN HAD THE DEFECT IT WAS BUILT TO PREVENT. Its first version
#                         matched only `Constant * X` -- the literal `"A" * 101` -- and was
#                         therefore structurally blind to `sw.PAD_CHAR * sw.WINDOW`, which
#                         is exactly how populate_fasta_seq.py:59/154 (a SIXTH detector,
#                         with a live content comparison) walked through it. Worse, the
#                         _ALLOWED entry DESCRIBED that blind spot as a reassurance: "it
#                         builds it as PLACEHOLDER_BASE * window (Name * Name), so it does
#                         not trip this check anyway". A gate that bans a SPELLING rather
#                         than the IDEA is roadmap 7c, committed inside the gate written to
#                         prevent 7c. It now resolves names.
#
#                         ON ITS FIRST CLEAN RUN IT FOUND TWO DEFECTS NOBODY WAS LOOKING
#                         FOR. (i) SIX Python source files carry a UTF-8 byte-order mark
#                         (build_spliceai_index, compute_within_gene_auroc,
#                         diagnose_phase2_prediction_reconstruction,
#                         download_finngen_R10_DEPRECATED, parse_dbsnp_freq_query_tsv,
#                         validate_gencode_assets). Python's tokeniser strips it (PEP 263)
#                         so they import and test green, but `ast.parse(read_text("utf-8"))`
#                         raises `SyntaxError: invalid non-printable character U+FEFF` --
#                         so every source-analysis gate in this suite either DIES or must
#                         SILENTLY SKIP them, and a gate that skips a file reports coverage
#                         it does not have. Cross-validated by two independent methods
#                         returning the identical list. (ii)
#                         download_finngen_R10_DEPRECATED.py:6 carries an invalid escape
#                         sequence '\M' (from the path `G:\My Drive\genomic-variant-data`)
#                         -- deprecated, a SyntaxError in a future Python, and reported
#                         against "<unknown>" until `filename=` was passed to ast.parse. A
#                         tool that finds a defect and will not say where is the same
#                         failure this project exists to prevent.
#
#                     -1  test_readme_claims.py::
#                         test_readme_publishes_no_performance_figures_pending_run17 --
#                         DELETED at the owner's instruction, 2026-07-15. It banned any
#                         bare 0.9xxx figure from README.md pending Run 17. Its own
#                         docstring said: "This test keeps them withdrawn until someone
#                         deliberately deletes it -- which is the point: reinstating a
#                         headline metric should be a decision, not an oversight." The
#                         owner made that decision. Deleting the test IS the mechanism the
#                         test described, not a bypass of it. The README now carries the
#                         Run 15 AUROC under an "Early results" heading which states, in
#                         the document itself, that the figure describes an earlier and
#                         narrower configuration and that a like-for-like table follows
#                         from the next full run.
#
#                     THE README WAS RESTORED AND REWRITTEN. `git checkout d07452b^ --
#                     README.md` (restoring 4528414's version), then rewritten clean. The
#                     2026-07-14 pass had annotated a PUBLIC-FACING document with
#                     correction blockquotes and withdrawal notices -- an editorial
#                     decision about how the project presents itself that was never asked
#                     for and never sanctioned. The instruction had been two things: strip
#                     the AUROCs, and write test_readme_claims.py. Neither was "annotate
#                     the document with a record of its own errors."
#
#                     The rewrite is high-level and descriptive, with a PURPOSE section the
#                     old document never had: the project's four aims (clinical,
#                     biological, methodological, translational) and how each shapes the
#                     engineering. Counts are kept and correct as of today -- 95 features
#                     (group table summed against TABULAR_FEATURES), 22 agents (read from
#                     Orchestrator._register_agents()), 13 base models, 1966 tests. The
#                     README is a SNAPSHOT refreshed per run; docs/ROADMAP.md is the living
#                     record. Those are different artifacts with different contracts, and
#                     conflating them is what produced nine feature-count claims with four
#                     values.
#
#                     ^^^ THE 1966 ENTRY ABOVE IS WRONG, AND IT IS KEPT SO THAT IT SAYS SO.
#                     See the 1962 entry below. It was committed (63e998d) and PUSHED
#                     (b1ec589) before the error was found.
#
#   1962  2026-07-15  -4: CORRECTING 1966, which never described any committed tree.
#
#                     1966 counted the +4 from tests/unit/test_no_content_based_poly_detection
#                     .py. THAT FILE IS UNTRACKED. It has never been in a commit. It carries
#                     TWO DELIBERATE FAILURES -- the six byte-order-marked source files and
#                     populate_fasta_seq.py's sixth content detector, both real, both open --
#                     so it is held back with the uncommitted sequence work rather than
#                     committed red.
#
#                     The pushed tree therefore contains the -1 (the deleted README
#                     performance-figure ban, in 63e998d) and NOT the +4. 1963 - 1 = 1962.
#                     MEASURED, on the tree as it will be collected:
#
#                         Move-Item tests\unit\test_no_content_based_poly_detection.py $env:TEMP\
#                         pytest tests/ --collect-only -q   ->  "1962 tests collected in 23.73s"
#                         $LASTEXITCODE                     ->  0
#
#                     THE 1966 ENTRY SAYS "MEASURED ... never predicted", AND IT WAS MEASURED.
#                     That is precisely the problem. `pytest --collect-only` was run on a
#                     WORKING TREE holding an untracked file, and the resulting number was
#                     written into a file that guards a COMMITTED tree. The measurement was
#                     real; the thing it measured was not the thing being asserted.
#
#                     THIS IS ROOT PATTERN (d), IN THE RATCHET ITSELF. "A green result from a
#                     mutated environment is evidence about the ENVIRONMENT, not the code" --
#                     the same shape as the sed-patched imodelsx that made a local suite green
#                     while the Kolmogorov-Arnold Network was absent from every Continuous
#                     Integration run for two months. `git status` had printed
#                     `?? tests/unit/test_no_content_based_poly_detection.py` in the same
#                     terminal, in the same minute, immediately above the commit that shipped
#                     1966.
#
#                     AND THE WORD "MEASURED" IS WHY IT SURVIVED REVIEW. This file's history
#                     uses "MEASURED, not predicted" to mark the entries that are trustworthy,
#                     because four consecutive entries record the author's arithmetic being
#                     wrong. That convention made "MEASURED" read as a warrant. It is not one.
#                     A measurement is only evidence about the thing it was taken FROM, and
#                     nothing in the word records what that was. Every future entry states the
#                     TREE the count was taken on, not merely that a count was taken.
#
#                     WHEN test_no_content_based_poly_detection.py IS COMMITTED, this goes to
#                     1966 -- and the number is to be re-measured then, on that tree, not
#                     carried forward from here.
#
#                     The ratchet was RIGHT and the number was WRONG. It is about to report
#                     "4 tests VANISHED" on b1ec589, naming four tests that were never
#                     committed. Do not touch conftest.py. Fix the number.
#
#   1968  2026-07-18  +6 from 1962. MEASURED with `pytest tests/ --collect-only -q`
#                     on the STAGED tree at HEAD 87b670e with 44 path(s) staged --
#                     NOT on a bare working tree. The 1962 entry above exists because that
#                     distinction was missed once, so it is recorded here explicitly.
#
#                     tests/unit/test_no_content_based_poly_detection.py IS INCLUDED IN
#                     THIS COMMIT. The 1962 entry held the ratchet at a number that
#                     excluded its 4 tests precisely because the file had never been
#                     committed. It now is, which is the condition that entry set for
#                     moving the number.
#
#                     WHAT CHANGED, all on 2026-07-18:
#
#                     RETIRED (-21) -- the superseded window builder and its driver.
#                     src/genomic_variant_classifier/data/seq_windows.py (7 poly-ban
#                     offenders), .../populate_fasta_seq.py, scripts/populate_fasta_seq.py,
#                     and their two test files (16 + 5 = 21 tests). These wrote
#                     clinvar_grch38_clean_seq.parquet with PAD_CHAR = "A" and NO
#                     provenance column, so attach_delta_windows took its has_ok=False
#                     branch and declared every row usable behind a logger warning.
#                     Because "A" is a member of encode_sequence's BASES, every
#                     placeholder position one-hot-encoded to a CONFIDENT ADENINE.
#
#                     A retirement audit refused this deletion earlier the same day:
#                     scripts/populate_fasta_seq.py was the SOLE repo-resident producer of
#                     a 534 MB artifact that 18 files consume. Retiring it then would have
#                     left that artifact unreproducible from the repository.
#
#                     REPLACED (+19) -- coverage built BEFORE the deletion, not after.
#                     tests/test_build_seq_windows.py (10) ports the surviving builder's
#                     coverage; tests/test_build_clean_seq_from_windows.py (9) covers the
#                     new join-based producer, whose dry run against the real 4,399,089-row
#                     cohort reported 723 placeholder rows (668 non_acgt_allele / 53
#                     ref_mismatch / 2 fetch_failed) -- the fourth independent re-derivation
#                     of that split through a fourth code path.
#
#                     CONTRACT REALIGNED (+3) -- three blind detectors were rewritten onto
#                     provenance, and two test files pinned the OLD behaviour:
#                     tests/unit/test_correctness_harness.py (6 -> 8) and
#                     tests/unit/test_preflight_run16_inputs.py (13 -> 14).
#
#                     A THIRD TEST WAS PASSING FOR THE WRONG REASON and pytest never said
#                     so. test_cohort_all_dummy_fails wrote all-"A"*101 windows and
#                     asserted the gate refused. It did refuse -- because the `ok` column
#                     was absent, not because the windows were placeholders. A test that
#                     passes for a reason unrelated to its name is not coverage. Replaced
#                     by test_cohort_mostly_placeholder_fails, which supplies provenance.
#
#                     POLY-BAN OFFENDERS 12 -> 0. Eight lived in the retired modules. The
#                     other four were live and all four were BLIND rather than broken:
#                     preflight_run16_inputs.py (a LAUNCH GATE, reporting 100% real on a
#                     cohort with 723 placeholders), probe_cohort_seq_density.py
#                     (reporting dummy=0), correctness_harness.py stage 3a (a CORRECTNESS
#                     GATE that was structurally incapable of firing -- wrong column,
#                     wrong constant, and a synthetic random-ACGT fixture), and
#                     run9_ablations.py (feeding fabricated adenine to cnn_1d).
#
#                     None was fixed by changing "A" to "N". Content cannot answer the
#                     question at all: a window of one repeated base may be genuine
#                     biology, and only the builder knows whether it gave up. All now read
#                     the `ok` column, and where provenance is ABSENT they say so instead
#                     of reporting zero. "Cannot tell" is not "clean" -- that conflation is
#                     what let 21,814 fabricated rows train as real sequence.
#
#   1967  2026-07-19  -1: WindowAttachment.__iter__ and the test that pinned it, DELETED
#                     TOGETHER. MEASURED with `pytest tests/ --collect-only -q` on the
#                     STAGED tree: `git add -A` first, then `git status --short` showing
#                     exactly two ` M` entries and NO '??' lines, then the collection ->
#                     "1967 tests collected in 9.47s". Immediately before the deletion the
#                     same command on the same staged tree reported "1968 tests collected
#                     in 13.08s". The -1 is the difference between two measurements of two
#                     real trees, not arithmetic. Entry 1962 above exists because that
#                     distinction was missed once, so it is stated here explicitly.
#
#                     WHAT WAS DELETED
#
#                     src/genomic_variant_classifier/data/seq_window_join.py:136-164
#                     (-30 lines including blank-line renormalisation) -- the back-compat
#                     shim that let `wins, n_unmapped = attach_delta_windows(...)` continue
#                     to unpack after 2026-07-15 changed the return type to a
#                     WindowAttachment object. It was DELIBERATELY LOSSY: it dropped
#                     `usable`, the provenance mask that is the entire reason the object
#                     exists. Reading a WindowAttachment through it gave you back exactly
#                     the blindness that let 21,814 builder-placeholder rows train as real
#                     sequence.
#
#                     tests/unit/test_seq_window_join.py:193-206 (-16 lines, 10 -> 9 tests)
#                     -- test_back_compat_iter_still_unpacks_for_unmigrated_callers. Its
#                     docstring: "this test exists so that deleting the shim is a decision,
#                     not an accident." Deleting it IS that decision.
#
#                     THE SHIM SET ITS OWN DELETION CONDITION -- "This docstring is the todo
#                     list. When the list above is empty, delete this method" -- AND WAS
#                     STALE ON ALL FOUR OF ITS CLAIMS:
#
#                       "STILL UNPACKING THE TUPLE: scripts/train.py:441,458,480"
#                           -> train.py:439/455/477 bind the object. Migrated 2026-07-15.
#                       "scripts/run_phase2_eval.py:425,426,427"
#                           -> reads provenance through loop var _atts at 436-448 and GATES
#                              on it (`return 2` at 466). The most rigorous consumer in the
#                              tree.
#                       "scripts/rekey_seq_windows_v2.py:145"
#                           -> migrated 2026-07-15.
#                       "train.py:485 ... _POLY_WIN = "A" * 101 ... PLACEHOLDER_BASE is now
#                        "N" ... train.py must be migrated before Run 17"
#                           -> that detector is GONE. train.py:480-511 is a commented-out
#                              record of both its defects, and train.py:516 now reads
#                              `has_sequences = _att_train.n_usable > 100` -- provenance,
#                              which cannot rot when a filler changes because it never looks
#                              at the filler.
#
#                     A SELF-MAINTAINED TODO LIST WENT STALE ANYWAY, and it had insisted in
#                     its own text that it was "accurate as written, not aspirational". That
#                     sentence is the tell: it is the same class of assurance as the
#                     all-capitals comments that failed five times in two days above, and as
#                     "MEASURED" in entry 1966. A claim about a document's own freshness
#                     cannot make the document fresh. This one is now deleted rather than
#                     corrected, which is the only durable form of the fix.
#
#                     THE FOUR REMAINING CALL SITES were migrated in fe4ea94 (Part 3b):
#                     test_seq_window_join.py:36,46,152 and test_train_cnn_activation.py:101,
#                     all to `att.windows` / `att.n_unmapped`. A FIFTH apparent site,
#                     test_train_cnn_activation.py:30, is PROSE INSIDE A DOCSTRING quoting the
#                     old buggy version the test was rewritten away from; patching it would
#                     have corrupted a record of a fixed defect. It was caught by noticing its
#                     indentation did not match the function body.
#
#                     THREE CHECKERS BUILT FOR THIS SEQUENCE MISCOUNTED PROSE AS CODE before
#                     being fixed -- Part 3a's splatted-call census (it counted its own new
#                     docstring's `ens.fit(*_tiny_inputs())` as an eighth call site), Part 3b's
#                     first draft (the quoted unpack above), and Part 3c's blank-line check
#                     (it demanded two blank lines before `class WindowAttachment:`, which sits
#                     directly beneath its own `@dataclass`, and asserted GLOBAL PEP 8
#                     cleanliness rather than asking whether the edit made spacing worse). All
#                     three now parse with `ast` and strip docstrings before counting. That is
#                     the fourth, fifth and sixth time in this file's history that a
#                     string-matching gate has fired on the prose written to describe the rule
#                     -- see 1944, 1963, and the feature-count sweep at 1940.
#
#                     PART 3 OF THE HYBRID, in order: ff97c34 made X_seq optional in
#                     VariantEnsemble with a loud refusal when a SEQUENCE_MODELS member is
#                     active without it; 58c9433 stopped three tests fabricating placeholder
#                     sequence; fe4ea94 migrated the last four unpacks; this commit removes the
#                     shim. The `usable`-less path no longer exists.
#
#   1978  2026-07-19  +11: tests/unit/test_x_seq_refusal_contract.py -- the X_seq refusal
#                     contract gets direct tests (Part 3d). MEASURED, and measured by THIS
#                     FILE'S OWN GATE rather than by a separate collection run:
#
#                         SUITE-SIZE RATCHET FAILED (roadmap 6.14)
#                           expected (tests/EXPECTED_SUITE_SIZE): 1967
#                           actually collected:                   1978
#                           11 MORE test(s) than expected.
#
#                     That is the number COPIED FROM THE RATCHET'S OUTPUT, which is what the
#                     rule at the top of this file asks for. 1967 + 11 = 1978 is offered only
#                     as a cross-check on a figure that was read, never as its source.
#
#                     THIS ENTRY RECORDS A FAILURE, NOT A CLEAN BUMP.
#
#                     The tests were committed and PUSHED WITHOUT the ratchet (67a2df4), and
#                     Continuous Integration #535 went red on Python 3.11 with exit code 4 --
#                     "no tests ran in 17.90s", because the gate refuses to run a suite whose
#                     size it does not recognise. That is the SECOND time in two days: entry
#                     1962 records 2026-07-18, where the ratchet moved and the README badge
#                     did not.
#
#                     THE CAUSE WAS PROCEDURAL AND IT WAS THE ASSISTANT'S. The commit command
#                     supplied to the operator contained an UNSUBSTITUTED PLACEHOLDER --
#                     `-m "... ratchet <measured>"` -- which PowerShell passed through
#                     verbatim because it sat inside a quoted string. 67a2df4's message
#                     therefore reads "ratchet <measured>" in the permanent record until it is
#                     amended. The same command block was presented directly beneath an
#                     instruction to stop and report the count first; an instruction to stop,
#                     printed above five runnable commands, is not an instruction to stop.
#
#                     WHAT THE GATE DID RIGHT. It cost seventeen seconds and one red run. The
#                     alternative it prevents is a ratchet that silently stops describing the
#                     suite -- and this file's history records four separate occasions on
#                     which a number here went stale without anything noticing. The failure
#                     mode being defended against is not "a red build"; it is a green build
#                     that means nothing.
#
#                     WHAT THE TESTS COVER. `_require_x_seq` (variant_ensemble.py:2248) had no
#                     direct test before this. It was exercised only through its SILENT branch
#                     -- calls passing X_seq=None with a roster that happens to exclude cnn_1d
#                     -- so nothing asserted the loud branch was reachable at all. A guard
#                     whose firing path is never executed is indistinguishable from one that
#                     has been disarmed, and three of those are already on record: the SpliceAI
#                     silent zero (9ba3127), rekey_seq_windows_v2's gate, and train.py's
#                     `_POLY_WIN` content detector, which went unconditionally true when
#                     PLACEHOLDER_BASE changed from "A" to "N" while the full suite stayed
#                     green.
#
#                     ELEVEN TESTS: five expect the refusal, FIVE ARE NEGATIVE CONTROLS that
#                     must stay silent, one checks that SEQUENCE_MODELS names only models the
#                     default roster actually builds. The pairing is the point -- a test that
#                     only ever sees the raise cannot tell a correct guard from one that
#                     raises always.
#
#                     test_fit_refuses_before_training_anything asserts BY STATE that
#                     trained_models_ is empty and no artifact reached disk. On the 4.4M-row
#                     cohort that property is the difference between a second and hours of
#                     paid compute.
#
#                     X_tab is built from the real TABULAR_FEATURES columns because
#                     _assert_no_dead_features (:2334) runs BEFORE the refusal (:2338); with
#                     f0..f19 columns the census would raise first and every pytest.raises in
#                     the file would pass for the wrong reason.
#
#   1985  2026-07-19  +7: tests/unit/test_module_docstring_is_not_a_stale_roster.py -- the
#                     module header of variant_ensemble.py stops restating the base-model
#                     roster, and is pinned so it cannot start again. MEASURED with
#                     `pytest tests/ --collect-only -q` on the STAGED tree: `git add -A`
#                     first, then `git status --short` showing exactly one "M " and one "A "
#                     entry and NO '??' lines, then "1985 tests collected in 20.27s".
#                     1978 + 7 is offered only as a cross-check on a figure that was read.
#
#                     THE DEFECT. variant_ensemble.py opened with a header that stated a
#                     fixed count of base classifiers, listed them by name, attributed two of
#                     them to a machine-learning framework this module does not import, and
#                     carried a changelog section from a project phase long past. The count
#                     was wrong -- the roster had grown well beyond it. That header is the
#                     FIRST TEXT anyone reading the project's central module encounters.
#
#                     THE FIX IS REMOVAL, NOT CORRECTION. Rewriting the number fixes today
#                     and reintroduces the defect on the next model, because the enumeration
#                     is a COPY of what _build_estimators defines thirty lines below it, and
#                     nothing forces a copy to move when the original does. The header now
#                     names where the roster lives -- _build_estimators, base_estimators,
#                     SEQUENCE_MODELS -- and states nothing it would have to maintain. The
#                     changelog block is deleted; git records that history permanently and
#                     with dates.
#
#                     THIS IS THE FIFTH INSTANCE OF ONE FAILURE MODE recorded in this file:
#                     WindowAttachment.__iter__'s self-maintained todo list (stale on all
#                     four of its entries, deleted in ea8d6e8); the four numbers that went
#                     stale HERE before the ratchet was armed (1882, 1891, 1932, 1966); the
#                     README badge against entry 1962; the LOVD classification map, which
#                     zeroed 91.8% of its own artifact because the map was written for a
#                     different vocabulary; and now this header. Every one is a fact stated
#                     twice, where only one copy was maintained.
#
#                     SEVEN TESTS: FOUR SUBSTANTIVE, THREE NEGATIVE CONTROLS. The controls
#                     embed the OLD header verbatim and assert each check REJECTS it. A
#                     checker that has never rejected anything has not been shown to work --
#                     and three checkers written earlier the same day passed every one of
#                     their own controls while counting prose as code.
#
#                     THREE DEFECTS WERE CAUGHT IN THE REPLACEMENT BEFORE IT SHIPPED, each
#                     the same shape as the defect being repaired. The new header quoted the
#                     old claim verbatim in order to explain it -- and a quoted claim reads
#                     as a live one to anyone skimming, which is the exact failure being
#                     removed. It then quoted the changelog section name for the same reason
#                     and with the same effect. And the count-detecting regular expression
#                     matched the "19" of the date "2026-07-19" against a "base classifiers"
#                     later in the same sentence: a date is not a count claim. The expression
#                     now requires the number to MODIFY the noun, at most two words
#                     intervening, and is verified against six probes -- four that must match
#                     and two that must not.
#
#                     WHAT IS NOT FIXED. The header is now silent about the roster, which is
#                     correct, but nothing yet asserts that TABULAR_FEATURES and
#                     EXPECTED_TABULAR_FEATURE_COUNT agree with each other in the same way.
#                     That is a separate pin and it is not installed here.
#
#   1999  2026-07-20  +14: tests/unit/test_window_attachment_derives_its_counts.py --
#                     WindowAttachment stores its two component masks and derives every
#                     count from them. MEASURED with `pytest tests/ --collect-only -q` on the
#                     STAGED tree: `git add -A`, then `git status --short` showing exactly one
#                     "M " (src/.../data/seq_window_join.py) and one "A " (the new test file)
#                     and NO '??' lines, then "1999 tests collected in 11.08s". 1985 + 14 is
#                     offered only as a cross-check on a figure that was read.
#
#                     THE DEFECT. seq_window_join.py:179, tier 1, read:
#
#                         att = WindowAttachment(out, usable, n, int((~usable).sum()), 0, prov)
#                                                                |_ n_unmapped ____|  |_ n_ph
#
#                     In the "rows+ok" branch usable = ref.notna() & alt.notna() & ok. A row
#                     that IS PRESENT but carries ok=False is a BUILDER PLACEHOLDER, not an
#                     unmapped row -- yet every unusable row was attributed to n_unmapped and
#                     n_placeholder was hardcoded to zero. Exactly inverted.
#
#                     data/processed/clinvar_grch38_clean_seq.parquet carries fasta_seq_ref,
#                     fasta_seq_alt AND ok, so it resolves through precisely this tier. Its
#                     summary() line -- what scripts/train.py:514 writes to the run log for
#                     every split -- would have read "723 unmapped, 0 builder-placeholder"
#                     when the truth is 0 and 723. Those 723 were measured on 2026-07-19 as
#                     668 non-ACGT alleles, 53 reference mismatches and 2 fetch failures.
#
#                     WHY IT SURVIVED. No test covered tier 1 WITH an ok column. Every
#                     existing tier-1 test supplied windows without one (test_seq_window_join
#                     .py:185, test_train_cnn_activation.py:47/70/101); everything else was
#                     tier 2 or 3. The defect lived in the one shape nothing exercised -- and
#                     it is the shape the production cohort uses. Tier 2 was always correct,
#                     because it kept `mapped` and `ok` separate.
#
#                     THE FIX IS STRUCTURAL, NOT ARITHMETIC. Correcting line 179 would have
#                     fixed today and left the next tier free to repeat it. The class stored a
#                     DERIVED value (usable) plus three SUMMARIES of masks it did not keep, so
#                     nothing could enforce agreement -- nothing even prevented n_rows from
#                     disagreeing with len(windows). Now four fields are stored (windows,
#                     key_found, builder_ok, provenance) and usable, n_rows, n_usable,
#                     n_unmapped, n_placeholder, usable_fraction and provenance_is_verified
#                     are all properties. A tier must state BOTH masks; conflating them is
#                     unrepresentable rather than discouraged.
#
#                     THIS IS THE SIXTH INSTANCE OF ONE FAILURE MODE recorded in this file:
#                     WindowAttachment.__iter__'s self-maintained todo list; the four numbers
#                     that went stale HERE before the ratchet was armed; the README badge
#                     against entry 1962; the LOVD classification map; the variant_ensemble.py
#                     module header at entry 1985; and now the counts a WindowAttachment
#                     stored alongside the mask that defines them. Every one is a fact stated
#                     twice where only one copy was maintained.
#
#                     `key_found`, NOT `mapped`. genomic_lm.py:386 and :432 bind att.usable to
#                     a LOCAL named `mapped`. A FIELD called `mapped` meaning something else --
#                     located, regardless of the builder's verdict -- would put two meanings on
#                     one word in one codebase, which is how a mask gets read wrong.
#
#                     AND subset(). prediction_artifacts.py:365 slices X_seq before passing it
#                     to predict_proba, so a sliced attachment must remain an attachment or the
#                     coming sequence-provenance gate cannot cover that path. Deriving the
#                     counts makes subset exact; under the old layout it could not have been
#                     written honestly at all, because a slice's unmapped-versus-placeholder
#                     breakdown is unrecoverable from a combined mask.
#
#                     VERIFIED BY EXECUTION, not by argument. The patch was applied to a copy
#                     of seq_window_join.py reconstructed verbatim from a collector report and
#                     run against a replica of the production cohort's shape: summary() moved
#                     from "2 unmapped, 0 builder-placeholder" to "0 unmapped, 2 builder-
#                     placeholder". Tiers 2 and 3 were confirmed unchanged on frames carrying
#                     both failure modes. Two of the 14 tests are negative controls: one
#                     recomputes the OLD formula and proves it disagrees on exactly this input
#                     (a regression test that would also pass against the bug pins nothing),
#                     and one shows an unverified "parquet" attachment still reports usable
#                     rows -- the trap provenance_is_verified exists to expose.
#
#                     WHAT IS NOT DONE HERE. No caller was migrated. train.py, run_phase2_eval
#                     .py, run9_ablations.py and prediction_artifacts.py still pass bare frames
#                     into the ensemble, and nothing yet refuses one. That is the next commit,
#                     and it is atomic: Protocol, gate over X_seq AND X_seq_cal_ext, every
#                     caller, bare-DataFrame refusal, together.
#
#   2017  2026-07-20  +18: tests/unit/test_sequence_provenance_gate.py -- THE SEQUENCE-
#                     PROVENANCE GATE. The ensemble now refuses sequence it cannot vouch for.
#                     MEASURED with `pytest tests/ --collect-only -q` on the STAGED tree:
#                     `git add -A`, `git status --short` showing nine "M " and one "A " and NO
#                     '??' lines, then "2017 tests collected in 11.27s". 1999 + 18 is offered
#                     only as a cross-check on a figure that was read. The migrated contract
#                     file keeps its eleven tests, so the gate file is the whole delta.
#
#                     WHAT WAS WRONG. `_require_x_seq` asked ONE question -- "is X_seq None?"
#                     -- of ONE parameter, and could not tell:
#
#                       * a checked input from an unchecked one. fit() declares X_seq_cal_ext
#                         and the gate was called with X_seq alone, so a run with real train
#                         windows and a placeholder calibration partition passed cleanly:
#                         cnn_1d FITTED on real sequence and CALIBRATED on fabricated
#                         sequence, silently. That hole was opened by ff97c34 -- the commit
#                         meant to close this very class of defect.
#                       * a real window from an invented one. run9_ablations.py:677 handed it
#                         pd.Series([placeholder] * n) and it passed, because a Series is not
#                         None. Only the roster pop at :702 kept cnn_1d away from it.
#                       * a verified attachment from an unverified one. seq_window_join.py:211
#                         sets ok = np.ones(n, dtype=bool) when the window source carries no
#                         `ok` column, so n_usable there counts rows NOBODY CHECKED. The
#                         number is indistinguishable from a real one; only provenance tells
#                         them apart.
#
#                     THE GATE. One method, `_require_sequence_windows`, ranging over a
#                     MAPPING of every sequence parameter the call received rather than one
#                     argument -- so omitting a parameter is visible at the call site instead
#                     of invisible in a helper. Four refusals: None; a bare DataFrame or
#                     Series; unverified provenance; too few usable rows by fraction or count.
#                     Every one executed against a replica before delivery, each paired with a
#                     negative control proving it stays silent when no SEQUENCE_MODELS member
#                     is active.
#
#                     A `SequenceWindows` Protocol (@runtime_checkable) declares the contract
#                     STRUCTURALLY, so variant_ensemble.py imports nothing from
#                     genomic_variant_classifier.data -- asserted by parsing Import nodes, and
#                     the models layer keeps no dependency on the data layer. A bare DataFrame
#                     is not a legacy special case: carrying no provenance at all makes it the
#                     LEAST-verified thing that can arrive, weaker than provenance="rows".
#
#                     THRESHOLDS SHIP MARKED UNVALIDATED, IN THE CONFIG ITSELF.
#                     seq_min_usable_fraction=0.95, seq_min_usable_rows=100,
#                     seq_require_verified_provenance=True, all per-run overridable via the
#                     EnsembleConfig.zero_variance_min_rows idiom so a --max-train 3000 smoke
#                     is not refused by the gate meant to protect it. The statistical-power
#                     question -- how many usable 101-base-pair windows a convolutional network
#                     needs before its output means anything -- requires a learning curve and a
#                     graphics-processing unit and HAS NOT BEEN RUN.
#
#                     What HAS been measured (2026-07-19, docs/measurements/MEASUREMENT_
#                     2026-07-19_seq-window-selection-bias.md) is the SELECTION BIAS: rows
#                     without usable windows are 7.469x enriched for pathogenic, 95% CI
#                     [7.080, 7.880], and 7.758x enriched for truncating consequences. They are
#                     a biological class, not a random sample. Yet only 723 of 4,399,089 rows
#                     are unusable, and dropping every one shifts pathogenic prevalence by
#                     0.009254 percentage points. Both are true at once, and that is why the
#                     FRACTION is the primary guard and is set high: the danger is a future
#                     stale, partial or mis-keyed artifact, not this cohort. An absolute floor
#                     of 100 rows would pass happily while 40% of a cohort trained on
#                     fabricated sequence.
#
#                     MIGRATION, 40 edits across 9 files plus the method replaced by parsed
#                     span. train.py sheds three `.windows` and its inclusion threshold now
#                     reads the same config field the gate does, so caller and gate cannot
#                     disagree. run_phase2_eval.py sheds three and its len() calls become
#                     n_rows. run9_ablations.py stops building a placeholder Series and loses
#                     a dead cnn_1d branch. prediction_artifacts.py sheds the false `pd.Series`
#                     annotation -- the same falsity VariantEnsemble.fit shed on 2026-07-15,
#                     left standing in a second place -- and both it and run_phase2_eval.py
#                     now slice with WindowAttachment.subset().
#
#                     FIVE DEFECTS WERE CAUGHT BEFORE COMMIT, NONE BY READING CODE.
#                       1. The gate was written and never wired into the installer's edit list.
#                          _require_x_seq would have survived; the gate would never have
#                          existed. Caught by a post-check asserting the OUTCOME.
#                       2/3. Two installer checks string-matched and fired on prose: one on the
#                          refusal message naming seq_window_join, one on the new gate's own
#                          docstring explaining what its predecessor did.
#                       4. A check matched `torch.utils.data` because it ended in ".data",
#                          reporting an architectural violation that did not exist.
#                       5. THE ONE THAT MATTERED: the gate demanded X_seq_cal_ext
#                          unconditionally. None is the NORMAL case -- it means no external
#                          calibration partition, which is what train.py:561 and
#                          run_phase2_eval.py:590 both do. It would have refused EVERY non-v2
#                          run. Caught by test_catboost, not by review. The rule is now
#                          conditional on X_tab_cal_ext, with tests pinning both directions.
#
#                     There is a symmetry in (5) worth keeping: this gate exists because the
#                     old check inspected TOO FEW inputs, and its first version failed by
#                     demanding one that was NOT IN PLAY. Same root -- deciding what to check
#                     without asking what the caller supplied.
#
#                     FOURTEEN OCCURRENCES OF ONE FAILURE MODE in this session, the last inside
#                     the fix for the thirteenth: a checker that string-matches fires on the
#                     text written to describe its own rule. Every one is now parsed. The
#                     durable lesson is not "remember to parse" -- it is that outcome-asserting
#                     checks catch what careful reading does not.
#
#                     WHAT IS NOT DONE. Both thresholds remain unvalidated pending the
#                     learning curve. Per-split provenance and usable_fraction are not yet
#                     recorded in the run artifact, though summary() already produces the
#                     string.
#
#   2055  2026-07-20  +38: tests/unit/test_metric_kernel_is_fail_closed.py -- THE METRIC
#                     KERNEL BECOMES FAIL-CLOSED. Six defects raised by an independent audit
#                     of evaluation/metrics.py, every one verified by reading the file before
#                     any code was written. MEASURED with `pytest tests/ --collect-only -q`
#                     on the staged tree: "2055 tests collected in 12.37s". 2017 + 38 is
#                     offered only as a cross-check on a figure that was read.
#
#                     A  ROW MISALIGNMENT BETWEEN score AND prob. `evaluate()` cleaned them
#                        with TWO SEPARATE MASKS. A non-finite score on one row and a
#                        non-finite probability on another produced arrays of the SAME
#                        LENGTH describing DIFFERENT OBSERVATIONS, after which every
#                        calibration metric paired a probability with the wrong label --
#                        silently, because the downstream length check passed. Now one joint
#                        mask, exposed as `CleanArrays.mask` so no caller reconstructs it.
#
#                     B  LABELS COERCED, NOT VALIDATED. `_clean` ended `y[ok].astype(int)`.
#                        THE AUDIT UNDERSTATED THIS, and the first witness chosen for the
#                        test was the WRONG ONE. Two distinct corruptions:
#                          [0,1,2] -> y.sum() == y.size, so `_degenerate` fires SPURIOUSLY
#                                     and the old code returned NaN by accident.
#                          [0,1,3] -> `_degenerate` stays quiet and (1-y).sum() == -1, so
#                                     the denominator n_pos*n_neg is NEGATIVE: a SIGNED
#                                     AUROC. Both are now pinned, separately.
#
#                     D  AN EMPTY PROBABILITY VECTOR WAS "VALID". `is_probability` returned
#                        True for size 0. tests/unit/test_evaluation_metrics.py ASSERTED
#                        THIS -- the defect written down as a requirement (roadmap 6.21a).
#
#                     E  THE CALIBRATION SOLVER COULD NOT REPORT NONCONVERGENCE. It fell out
#                        of max_iter and returned coefficients regardless, so separation and
#                        numerical instability looked like a clean fit. Now returns
#                        `CalibrationFit(slope, intercept, converged, iterations,
#                        clipped_fraction)`, ITERABLE so every `slope, intercept = ...` call
#                        site keeps working. A second old test asserted finiteness on a
#                        PERFECTLY SEPARATED input, where the maximum-likelihood slope does
#                        not exist; it now checks no-overflow and honest non-convergence
#                        SEPARATELY, with a well-conditioned control so the assertion cannot
#                        be met by a solver that simply never converges.
#
#                     F  ROWS WITH A MISSING SUBGROUP LABEL VANISHED. `stratified_evaluate`
#                        iterated `g.dropna().unique()`, so those rows counted in ALL and
#                        appeared in NO stratum -- the strata did not partition the cohort
#                        and nothing said so. Now a `__MISSING__` stratum, with a partition
#                        assertion that the per-stratum row counts sum to the input.
#                        Missingness is informative here: a variant with no consequence
#                        annotation is a different object from one annotated as missense.
#
#                     G  SUBGROUP SUFFICIENCY USED ONLY TOTAL n. A stratum of 1,000 rows
#                        with ONE positive passed min_n=30 and was given an AUPRC and a
#                        calibration slope. Positives and negatives now have their own
#                        floors, and an insufficient stratum carries a `status` naming which
#                        floor it failed -- never dropped, never given numbers.
#
#                     THE BOOTSTRAP IGNORED GENE CLUSTERING -- found independently, confirmed
#                     by the audit. `bootstrap_ci` resamples VARIANTS, stratified by class
#                     only. Variants within a gene share its constraint, its network
#                     position, its curation history and often its true class. Treating them
#                     as independent UNDERSTATES VARIANCE, so every confidence interval this
#                     project has published is ANTI-CONSERVATIVE in a known direction.
#                     `cluster_bootstrap_ci` resamples whole genes, optionally two-stage, and
#                     REPORTS THE DESIGN EFFECT -- the ratio of clustered to naive width --
#                     so historical intervals can be RE-READ rather than merely superseded.
#                     Measured end-to-end on a cohort where six of thirty genes have inverted
#                     discrimination: naive [0.7548, 0.8439], clustered [0.6611, 0.9228],
#                     design effect 2.935x. A control pins the effect near 1.0 when gene
#                     assignment is arbitrary, so the test cannot pass on a fixture that
#                     merely adds noise.
#
#                     ALSO ADDED: `log_loss` (unbounded, punishes CONFIDENT errors far harder
#                     than Brier -- the failure that costs most when a pathogenic variant is
#                     called benign at p=0.99) and `auprc_gain` (the ABSOLUTE gain; the ratio
#                     explodes at low prevalence, where a lift of 10 can be a gain of 0.009).
#
#                     NOT CHANGED: `compute_classification_metrics` and `ModelEvaluator`.
#                     The head of the file is BYTE-IDENTICAL, verified by hashing both sides
#                     of the banner. They are genuinely unsafe -- `confusion_matrix(...)
#                     .ravel()` unpacks four values and raises on single-class input, and
#                     specificity returns 0 where it is undefined -- but deprecating them
#                     changes what every existing caller emits, and that needs a measured
#                     call-site census of its own.
#
#                     THREE DEFECTS IN THE INSTALLER ITSELF, CAUGHT BEFORE DELIVERY, NONE BY
#                     READING CODE. (1) A post-check string-matched `g.dropna().unique()` and
#                     fired on the NEW docstring quoting it to explain the fix -- the
#                     twenty-third occurrence this session of a checker hitting prose that
#                     describes its own rule, inside the installer remediating an audit about
#                     exactly this blindness; replaced with an AST walk, tested 5/5 against a
#                     real call, a docstring, a comment, another function, and a call nested
#                     in a loop. (2) `evaluate()` rebuilt the keep-mask by hand for the
#                     cluster labels and ignored y's finiteness -- it would have misaligned
#                     the clusters, which is defect A recreated inside its own repair; fixed
#                     by exposing the mask on `CleanArrays`. (3) A test ended in `or True`.
#
#                     WHAT IS NOT DONE. The legacy API is not yet unified. The metric
#                     registry, the typed report schema, and the clinical operating-point,
#                     decision-curve and selective-prediction panels are commit 2.
#
#   2060  2026-07-20  +5: tests/unit/test_calibration_implementations_agree.py -- TWO
#                     CALIBRATION DEFECTS ACROSS SIX FILES, AND A DOCSTRING THAT HAD BEEN
#                     FALSE FOR TEN DAYS. MEASURED with `pytest tests/ --collect-only -q` on
#                     the STAGED tree: `git add -A` first, then `git status --short` showing
#                     exactly seven "M " and one "A " and NO '??' lines, then "2060 tests
#                     collected". 2055 + 5 is offered only as a cross-check on a figure
#                     that was read. Entry 1962 exists because that distinction was missed
#                     once, so it is stated here explicitly.
#
#                     HOW THIS WAS FOUND. A call-site census of 2026-07-20 parsed all 813
#                     Python files under src/, scripts/ and tests/ and reported that NO MODULE
#                     UNDER src/ IMPORTS evaluation/metrics.py -- the canonical kernel
#                     hardened the same day at entry 2055 has, to this day, no production
#                     caller. What it found instead was TEN independent implementations of
#                     expected calibration error, and three of bootstrap confidence intervals.
#
#                     Reading them did not settle whether they agree. A probe was built to
#                     evaluate every one on identical fixtures. It took FIVE versions, and the
#                     failures of each taught the next: function-level isolation reached 1 of
#                     10; sanitised-module isolation reached 6; real package identity plus
#                     sys.modules registration reached 9. The four remaining failures were one
#                     cause -- @dataclass calls `sys.modules.get(cls.__module__).__dict__`,
#                     and an exec'd module is not registered there.
#
#                     DEFECT 1 -- THE OPEN TOP BIN, in three implementations.
#                         scripts/calibrate_thresholds.py:167   (scores >= lo) & (scores < hi)
#                         scripts/validate_external.py:88       (y_prob >= lo) & (y_prob < hi)
#                         scripts/calibration_analysis.py:75    (y_prob >= lo) & (y_prob < hi)
#
#                     With hi == 1.0 the final bin is half-open, so every prediction of
#                     EXACTLY 1.0 -- a pure decision-tree or ensemble leaf -- falls into NO bin
#                     and is silently excluded. The rows the model is most confident about
#                     contribute nothing to its measured calibration. Measured under-report on
#                     a fixture with 20% of rows at 1.0: 86.7%, independently reproducing the
#                     86.5% first measured on 2026-07-08.
#                     `_calibration_summary` inherited it by delegating to `calibration_bins`.
#                     scripts/calibrate_thresholds.py SELECTS OPERATING THRESHOLDS.
#
#                     DEFECT 2 -- COUNTS MISALIGNED WITH THE BINS THEY WEIGHT, in three more,
#                     and previously undocumented anywhere.
#                         scripts/run_benchmark.py:65-74
#                         scripts/validate_clinvar_temporal.py:235-242
#                         src/genomic_variant_classifier/evaluation/benchmark.py:119-132
#
#                         frac_pos, mean_pred = calibration_curve(y_true, y_proba, ...)
#                         counts = np.histogram(y_proba, bins=bins)[0]
#                         sum((c / n) * abs(fp - mp)
#                             for fp, mp, c in zip(frac_pos, mean_pred, counts))
#
#                     `calibration_curve` returns statistics for ONLY THE NON-EMPTY bins.
#                     `np.histogram` returns counts for ALL of them. `zip` truncates to the
#                     shorter and pairs the k-th NON-EMPTY bin's statistics with the k-th
#                     BIN's count. Correct whenever every bin is occupied -- which is why it
#                     survived review, and why it looked right on ordinary fixtures. Measured
#                     against a correctly aligned computation:
#
#                         fixture                as written   aligned    empty bins
#                         perfectly calibrated     0.024937   0.024937            0
#                         overconfident            0.162413   0.162413            0
#                         20% at p == 1.0          0.118207   0.118207            0
#                         saturated                0.125000   0.250000           13
#                         sparse + saturated       0.004825   0.309788            9
#
#                     NOT merely halved. On a sparse saturated distribution it under-reports by
#                     SIXTY-FOUR TIMES. Sparse and saturated is what a well-separated
#                     classifier's output looks like.
#
#                     THE TWO DEFECTS ARRIVED AT THE SAME WRONG NUMBER BY DIFFERENT ROUTES.
#                     Before the repair, all six returned exactly 0.125000 on the saturated
#                     fixture against a true 0.250000 -- three because they dropped the top
#                     bin, three because they misweighted it. A unanimous wrong answer reads as
#                     agreement, and agreement reads as correctness.
#
#                     A RETRACTION, RECORDED BECAUSE IT IS THE LESSON.
#                     src/genomic_variant_classifier/evaluation/evaluator.py:305 was cited
#                     throughout this work as carrying the open-top defect. IT DOES NOT. It was
#                     REPAIRED ON 2026-07-10 and carries its own dated comment at lines
#                     321-323. The claim came from metrics.py's docstring, which stated in the
#                     PRESENT TENSE that evaluator.py "uses (p >= lo) & (p < hi)" -- true when
#                     written on 2026-07-08, false from 2026-07-10, never updated. A document
#                     was quoted instead of the code being read, and the false claim was then
#                     repeated for an entire session.
#
#                     THAT DOCSTRING IS THE SEVENTH INSTANCE OF ONE FAILURE MODE recorded in
#                     this file: WindowAttachment.__iter__'s self-maintained todo list; the
#                     four numbers that went stale HERE before the ratchet was armed; the
#                     README badge against entry 1962; the LOVD classification map; the
#                     variant_ensemble.py module header at entry 1985; the counts a
#                     WindowAttachment stored alongside the mask that defines them at 1999;
#                     and now a docstring in one module describing the CODE OF ANOTHER. Every
#                     one is a fact stated twice where only one copy was maintained.
#                     It is corrected here rather than deleted, WITH ITS DATES, so the ten days
#                     it was wrong are part of the record.
#
#                     WHAT THE 5 TESTS PIN. Every expectation is DERIVED from a reference
#                     implemented in the test file, at each implementation's OWN default bin
#                     count -- zero hardcoded values -- so a change of bin count is not
#                     mistaken for a defect and a change of definition cannot be waved through
#                     by editing a constant. Three fixtures: 20% pure leaves, saturated, and
#                     sparse-and-saturated. One is a CONTROL asserting the pure-leaf fixture
#                     actually separates the two definitions, so the others cannot pass
#                     vacuously. One asserts the fail-loud length guard is still present, so a
#                     future edit cannot quietly restore silent misweighting.
#
#                     THE REPAIR, VERIFIED BY RE-RUNNING THE PROBE THAT FOUND THE DEFECTS.
#                     On the saturated fixture all nine loaded implementations now return
#                     0.250000, spread 0.000000. On the pure-leaf fixture the spread collapsed
#                     from 0.099541 to 0.000545, the residue being bin count and round(x, 4).
#                     "matches its own OPEN-top reference" reads NONE on all four fixtures.
#                     The two fixtures that were already correct are UNCHANGED at 0.017068 and
#                     0.022297 -- the control that matters most, since a repair that moves a
#                     correct number is not a repair.
#
#                     FOUR DEFECTS IN THE PROBE ITSELF, AND ONE IN THE VALIDATION OF THE FIX,
#                     none found by reading. (1) Arguments bound BY POSITION called
#                     `_binned_calibration(probs, labels)` backwards; it accepted them without
#                     complaint and returned a plausible number, producing a FALSE 20x
#                     disagreement that was entirely the probe's calling convention. Binding is
#                     now derived from parameter NAMES. (2) That name classifier read `eps` as
#                     a probability because it CONTAINS "p"; one-character words now require an
#                     exact match. (3) A 1e-9 tolerance declared `validate_external.py`, which
#                     returns round(ece, 4), to be "computing a DIFFERENT QUANTITY"; matching
#                     now searches for the decimal place at which the value equals the rounded
#                     reference and reports it. (4) Comparing everything against a 10-bin
#                     reference reported the 10-bin and 15-bin implementations as disagreeing
#                     on fixtures where both were correct -- A PARAMETER CHOICE REPORTED AS A
#                     DEFECT, which buries real findings under noise. (5) The check validating
#                     the installer's replacement blocks re-indented text that was already
#                     indented and reported SIX false failures.
#
#                     That last is the TWENTY-EIGHTH occurrence this session of a checker
#                     firing on something that merely resembles its target. The durable lesson
#                     is unchanged and is now very well evidenced: outcome-asserting checks
#                     catch what careful reading does not.
#
#                     WHAT IS NOT DONE.
#                     (a) THE TENTH IMPLEMENTATION IS STILL UNEVALUATED.
#                         src/.../agent_layer/agents/calibration_drift_agent.py:45
#                         `_binned_calibration` could not be reached because
#                         CalibrationDriftAgent.__init__ requires classes, baseline_ece and
#                         output_dir. It is the agent that MONITORS CALIBRATION DRIFT IN
#                         PRODUCTION, it computes its own binning, and which of these two
#                         defects it carries is UNKNOWN. Not assumed clean.
#                     (b) benchmark.py:125 computes `bin_midpoints` and never uses it.
#                     (c) The legacy metrics interface -- compute_classification_metrics and
#                         ModelEvaluator -- has ZERO callers outside metrics.py itself and its
#                         own test, measured by the same census. Removal is a live option and
#                         is a scope decision, not an installer's.
#                     (d) Three independent bootstrap implementations remain unreconciled:
#                         evaluator.py:284, report_generator.py:85, and the kernel's. The
#                         gene-cluster design effect measured at entry 2055 was 2.935x; whether
#                         the other two share the variant-level independence assumption has not
#                         been measured.
#   2071  2026-07-20  +11: tests/unit/test_preflight_run16_feature_contract.py -- THE
#                     ONLY UNTESTED CHECK WAS THE ONE THAT DRIFTED.
#                     scripts/preflight_run16_inputs.py carried five checks. Four were
#                     covered by tests/unit/test_preflight_run16_inputs.py; the fifth,
#                     check_feature_count, was covered by nothing. It pinned the literal
#                     EXPECTED_COUNT = 81 and compared it for equality against
#                     EXPECTED_TABULAR_FEATURE_COUNT, which had advanced 81 -> 88 -> 91 ->
#                     95 through patch_ve_maxentscan_delta.py,
#                     patch_tabular_omim_molecular.py and
#                     patch_variant_ensemble_finngen_r13.py. Because the module's exit
#                     convention is 0 all-pass / 2 any-fail, the ENTIRE Run-16 input
#                     preflight returned 2 on every invocation, on a clean tree, for the
#                     whole period after the 88 bump. A fail-loud guard that fails for a
#                     false reason is worse than no guard: it teaches the operator to
#                     ignore the four checks beside it that are correct.
#
#                     THE REPAIR DOES NOT RE-PIN THE NUMBER. Setting the literal to 95
#                     would reproduce the defect at the next roster growth. check_feature_
#                     contract() now asserts two properties that cannot go stale: the
#                     project's fail-loud invariant EXPECTED_TABULAR_FEATURE_COUNT ==
#                     len(TABULAR_FEATURES) with no duplicate names, and membership BY NAME
#                     of esm2_llr, maxentscan_delta and gene_constraint_oe -- the three
#                     features the gate's other checks exist to protect. A count never told
#                     us whether the right features were present; names do. check_feature_
#                     count() is retained as a delegating alias, because silently deleting
#                     a public name is a loss this project has had to reverse before.
#
#                     The eleven tests assert OUTCOMES against synthetic contracts injected
#                     into sys.modules, which is the only way to exercise a module that
#                     imports xgboost, lightgbm, catboost and torch at module scope. One
#                     case is impossible to construct any other way and is the reason the
#                     repair is shaped as it is: a DUPLICATE feature name hidden behind a
#                     matching count, which the old count-only gate PASSED.
#
#                     Recorded honestly: the first draft of the module-restore fixture in
#                     this file wrote `del sys.modules[m]` using a comprehension variable
#                     that does not leak in Python 3. All eleven tests reported PASSED while
#                     teardown raised NameError. Found by running it, not by reading it --
#                     the twenty-ninth instance of the same lesson.
#
#   2128  2026-07-21  +57: tests/unit/test_ordinal_conformal.py -- ORDINAL
#                     CONFORMAL PREDICTION: CONTIGUITY BY CONSTRUCTION, AND A
#                     GUARD THAT REFUSES THE COHORT WE ACTUALLY HAVE.
#                     src/genomic_variant_classifier/conformal/ordinal.py.
#
#                     THE PROBLEM. Split, Mondrian and grouped conformal all return
#                     {k : score_k <= q_hat}. On an UNORDERED label space that is
#                     correct. On the ACMG/AMP five-tier scale -- Benign < Likely
#                     benign < Uncertain significance < Likely pathogenic <
#                     Pathogenic -- nothing prevented the prediction set
#                     {Pathogenic, Benign}: an assertion that a variant is either
#                     the most harmful or the most harmless thing it could be and
#                     nothing between. That is not actionable, and it is not rare;
#                     it arises whenever the model is bimodal and the middle tier
#                     falls just above the threshold.
#
#                     THE CONSTRUCTION. Nested contiguous intervals. I_0 = [mode,
#                     mode]; each step extends to whichever neighbouring tier has
#                     the larger probability. The nonconformity score of tier k is
#                     the cumulative mass of the first interval containing it. Mass
#                     is non-decreasing in the step, so the level set {k : s(k) <=
#                     q_hat} IS one of the nested intervals -- contiguity is a
#                     property of the score, not a post-hoc repair. Under
#                     randomisation s(k) = u*M_(t-1) + (1-u)*M_t, a convex
#                     combination of consecutive cumulative masses, hence monotone
#                     in t. Contiguity is therefore structural, not empirical, and
#                     45,000 rows across varied alpha returned zero gapped sets.
#
#                     THE GUARD. The metric specification (Finding 1, Panel C) is
#                     explicit that this project has no five-class target: the
#                     pipeline collapses P and LP to 1, B and LB to 0, and excludes
#                     VUS. Tiers derived by thresholding one binary probability are
#                     contiguous TRIVIALLY, so the guarantee would be vacuous while
#                     the output looked genuine -- indistinguishable from the real
#                     thing by inspection. fit() therefore raises OrdinalLabelError
#                     when calibration labels carry fewer than three distinct
#                     values. ON TODAY'S COHORT IT FIRES. That is intended.
#                     allow_degenerate_labels exists only so the refusal can be
#                     tested and records its own use in degenerate_labels_.
#
#                     TWO DEFECTS FOUND BY RUNNING THE CODE, NOT BY READING IT.
#                     (1) An empty-set "repair" forced the modal tier into any
#                         empty set. Measured cost, 30 trials per level:
#                             alpha 0.05  0.9500 off -> 0.9555 on  (0.6 % forced)
#                             alpha 0.10  0.8990 off -> 0.9147 on  (1.6 % forced)
#                             alpha 0.20  0.7973 off -> 0.8411 on  (4.7 % forced)
#                             alpha 0.30  0.7005 off -> 0.7822 on  (9.1 % forced)
#                         OFF, the score is exactly calibrated at every level. ON,
#                         coverage inflates by precisely the fraction repaired and
#                         sets are wider than the data supports. Now opt-in via
#                         force_nonempty and always counted in n_forced_nonempty_.
#                         The code comment had claimed it was "recorded rather than
#                         silent" while nothing recorded it.
#                     (2) ordinal_report counted ABSTENTIONS as catastrophic errors,
#                         because distance_to_set scores an empty set as K, which
#                         exceeds any threshold. Found because n_catastrophic and
#                         n_empty were both exactly 64. An abstention is the method
#                         declining to assert; a catastrophic error is a confident
#                         assertion at the far end of the scale. Penalising the
#                         former as the latter would push a clinical system towards
#                         answering when it should decline. Now reported separately.
#
#                     Also recorded: the first draft of
#                     test_tie_break_actually_differs_on_an_exact_tie used
#                     [0.25, 0.25, 0.50, 0.00, 0.00] and called the neighbours tied.
#                     The modal tier is index 2, whose neighbours are 0.25 and 0.00.
#                     Not a tie. The test failed against CORRECT code. The thirtieth
#                     instance of the same lesson, this time with the fixture rather
#                     than the implementation at fault.
#
#                     THE STRONGEST TEST is a deliberately naive plain-Python
#                     reference implementation of the nested expansion, sharing no
#                     code with the vectorised one, compared row by row under both
#                     tie-break policies. An independent implementation disagreeing
#                     is the only reliable way to catch a subtle indexing error in
#                     vectorised code.
#
#                     OPEN, from the same work: per-tier coverage at alpha=0.10 was
#                     Benign 0.882, Likely benign 0.922, Uncertain 0.918, Likely
#                     pathogenic 0.919, Pathogenic 0.880 -- the two EXTREMES are
#                     under-covered while the middle is over-covered. That is the
#                     classic marginal-coverage failure and precisely what
#                     mondrian.py exists to fix. A class-conditional ordinal variant
#                     is the natural follow-on and it matters clinically: Pathogenic
#                     is the tier one least wants under-covered.
#
#                     ALSO OPEN: conformal/__init__.py imports scores, split,
#                     mondrian, grouped and coverage. It omits calibrate, and now
#                     ordinal. Verified by outcome: hasattr(conformal, 'calibrate')
#                     is False. Deliberately NOT fixed here -- it changes the
#                     package's public surface and belongs in its own commit with
#                     its own test.
#
#   2148  2026-07-21  +20: tests/unit/test_conformal_package_exports.py -- A
#                     LIST THAT WENT STALE TWICE NOW CANNOT GO STALE AGAIN.
#
#                     src/genomic_variant_classifier/conformal/__init__.py
#                     imported five of the seven modules that existed on disk.
#                     Verified by OUTCOME against commit c663c89:
#                         hasattr(conformal, 'calibrate') = False
#                         hasattr(conformal, 'ordinal')   = False
#                     calibrate had been unreachable since roughly 2026-05.
#                     ordinal was unreachable on the day it was added.
#
#                     THE FAILURE IS CONFUSING WHEN IT BITES, because the two
#                     import forms look equivalent to a reader and are not:
#                         from ...conformal import ordinal        -- works, Python
#                                                                    imports the
#                                                                    submodule
#                         conformal.ordinal.OrdinalConformalClassifier
#                                                                 -- AttributeError,
#                                                                    nothing bound
#                                                                    the name on the
#                                                                    package object
#
#                     ADDING TWO NAMES WOULD HAVE FIXED TODAY AND GUARANTEED A
#                     THIRD OCCURRENCE. That import line has gone stale twice
#                     because nothing connected "a file exists in this directory"
#                     to "its name appears on that line". Same shape as the Run-16
#                     preflight gate repaired earlier the same day: a literal kept
#                     correct by memory, with no mechanism to notice when it is not.
#
#                     THE DESIGN. Imports stay EXPLICIT. A pkgutil loop would never
#                     go stale but would make the public surface implicit, defeat
#                     static analysis and editor completion, and silently import
#                     anything dropped into the directory -- a scratch file, a
#                     half-finished module, an editor backup. Explicit is right; it
#                     needed a guard, not a replacement. __all__ was added so the
#                     public surface is declared rather than inferred.
#
#                     The guard is the test file. It WALKS THE PACKAGE DIRECTORY
#                     and asserts every module found is reachable as a package
#                     attribute, is declared in __all__, and appears in the package
#                     docstring; plus the reverse, that __all__ declares nothing
#                     absent from disk, which would break `import *` for every
#                     consumer at once. Adding a submodule without exporting it now
#                     turns the suite RED and the message names the file and the fix.
#
#                     VERIFIED BY SIMULATION, not by assertion: a placeholder
#                     risk_control.py -- one of the six conformal modules the
#                     2026-07-20 handoff lists as still missing, so a realistic
#                     case -- was dropped into the package. Five tests failed
#                     immediately with:
#                         conformal.risk_control is not reachable.
#                         src/.../conformal/risk_control.py exists on disk but
#                         'risk_control' is missing from the import in __init__.py.
#                         Fix: add 'risk_control' to both the `from . import (...)`
#                         block and __all__.
#                     The file was then removed and the suite returned to green.
#                     A guard that has never been observed to fire is not known to
#                     work.
#
#                     One test exists solely to stop the others passing vacuously:
#                     test_the_package_directory_is_readable_and_non_empty. Every
#                     other test here is parametrised over a directory listing, and
#                     an empty listing would produce zero test cases and a green
#                     suite -- the standard failure mode of data-driven tests.
#
#   2192  2026-07-21  +44: tests/unit/test_split_protocol_schema.py -- THE
#                     PARTITION SET BECOMES DATA, AND PROBABILITY CALIBRATION
#                     GETS ITS OWN PARTITION.
#                     src/genomic_variant_classifier/data/split_protocol_v2.py
#                     rewritten; scripts/forensics/verify_dtype.py repaired.
#
#                     THE DEFECT. The metric specification (Finding 2; Priority 2,
#                     "essential") requires probability calibration to be fitted on
#                     data untouched by model, method and alpha selection. Measured
#                     in the code 2026-07-21: scripts/train.py:131 and :550-551 fit
#                     the isotonic calibrator on the `tune` partition, which this
#                     module defined as "the model/method/alpha selection set". The
#                     calibrator was therefore fitted on the selection set. That
#                     affects every calibrated number the project reports.
#
#                     WHY A SCHEMA, NOT A FIFTH FRACTION FIELD. PARTITIONS was a
#                     hard-coded module constant referenced in five places, beside
#                     four separately-named fraction fields and a hand-written sum
#                     check. A fifth partition meant six coordinated edits; a sixth
#                     would mean six more. Same shape as three defects repaired the
#                     same week: the Run-16 gate's EXPECTED_COUNT = 81, the conformal
#                     package's five-of-seven import list, and the ratchet before it
#                     gained drift detection. The partition set is now DATA --
#                     PartitionSchema holds an ordered tuple of Partition(name,
#                     fraction, role) and every function derives behaviour from it.
#
#                     ROLES ARE THE POINT. train.py will ask for
#                     rows_for_role(CALIBRATE_PROBABILITY) rather than naming a
#                     partition. Under FIVE_WAY it receives a dedicated `calib`
#                     partition, gene-disjoint from `tune` and `conformal`. Under
#                     FOUR_WAY it receives None -- the honest answer. A silent
#                     fallback to `tune` would re-create the defect.
#
#                     BACKWARD COMPATIBILITY IS EXACT, AND WAS PROVEN AGAINST THE
#                     PREVIOUS REVISION, not merely asserted. The old module was
#                     loaded alongside the new one and bucket assignments compared:
#                     identical at seeds 42/7/123/2026, identical with non-default
#                     fractions, identical for group_shuffle on uniform gene sizes,
#                     and tests/test_split_protocol_v2.py passes unchanged (10/10).
#                     Had one gene moved bucket, stability invariant I8 would have
#                     broken across the upgrade and no pre-2026-07-21 result would be
#                     comparable to a later one. FOUR_WAY.hash_order is pinned to the
#                     historical (test, conformal, tune, train) by a test.
#
#                     TWO DEFECTS REPAIRED IN THE SAME PASS.
#                     (1) group_shuffle carve() rescaled by ROW counts while
#                         scikit-learn's GroupShuffleSplit interprets test_size as a
#                         proportion of GROUPS. Verified empirically: ten genes, one
#                         holding 100 of 109 rows, test_size=0.30 carved exactly 3 of
#                         10 genes -- 30 % of groups, 3 % of rows.
#                         The row ratio is an UNBIASED estimator of the gene ratio, so
#                         it is usually close; its variance grows with gene-size skew
#                         and it OVERFLOWS when a high-row-count gene is carved early.
#                         The clamp min(max(f, 1e-9), 1 - 1e-9) then turns it into
#                         0.999999999 and scikit-learn raises "n_samples=340,
#                         test_size=0.999999999", naming neither genes nor skew nor
#                         the partition. Measured, 12 seeds:
#                             skew                  rows           genes
#                             uniform               0.0000         0.0000
#                             heavy-tailed          0.0329         0.0000
#                             one gene = 90 % rows  0.0434, 3/12   0.0065, 0/12
#                                                   CRASH
#                         ClinVar per-gene variant counts ARE heavy-tailed, so the
#                         middle row is the realistic regime and the bottom row is
#                         reachable. carve() now rescales by gene counts; the ratio is
#                         bounded by construction so the overflow cannot arise, and
#                         genuine infeasibility gets an actionable message.
#                     (2) genes_are_stable_under_growth had an if/else whose branches
#                         were IDENTICAL, under a comment implying they differed.
#                         Dead code removed.
#
#                     RECORDED HONESTLY: the carve defect was hypothesised from
#                     reading, then TESTED AND REFUTED (worst deviation 0.013, better
#                     than hash mode at 0.038), and only found on a wider sweep over
#                     twelve seeds. The first adversarial test used a seed that did
#                     not trigger it. Reading produced the hypothesis; only running it
#                     across enough cases produced the evidence.
#
#                     scripts/forensics/verify_dtype.py:30 asserted the literal source
#                     text "for p in PARTITIONS:", which the schema-driven loop no
#                     longer contains. That check verified that code LOOKED a certain
#                     way, not that the dtype promotion WORKED -- a clean refactor
#                     failed it, and a silent breakage with intact loop text would
#                     have passed it. Both directions wrong. Replaced with a
#                     behavioural check: int32 fixture, FutureWarning escalated to
#                     error, real split, real remap, assert int64 out; plus a check
#                     that every partition IN THE SCHEMA is remapped and that genes
#                     unseen in train score zero (incident 2026-06-13). Proven by
#                     sabotage: removing the promotions fails 5 checks; reintroducing
#                     the leak is caught with 2,217 violations. The old text check
#                     passed both.
#
#                     NOT DONE HERE, DELIBERATELY: train.py is untouched. Wiring the
#                     calibrator to rows_for_role(CALIBRATE_PROBABILITY) and flipping
#                     --split-protocol off `legacy` (train.py:126) change the
#                     behaviour of every future run and belong in their own commit
#                     with their own test.
#
#   2270  2026-07-21  +78: tests/unit/test_clustering_metrics.py -- PANEL Q,
#                     AND FIVE REFUSALS THAT HAD TO BE PROVEN RATHER THAN
#                     ASSERTED.
#                     src/genomic_variant_classifier/evaluation/
#                     clustering_metrics.py (1,189 lines) +
#                     docs/PANEL_Q_unsupervised_structure.md (225 lines) added.
#
#                     WHAT PANEL Q IS. The metric specification has sixteen
#                     panels, A through P. Verified 2026-07-21 against
#                     project_metrics.txt (SHA-256 db987039...): the words
#                     Davies-Bouldin, silhouette, Calinski-Harabasz and Dunn
#                     appear ZERO times, and adjusted Rand index / adjusted
#                     mutual information / normalized mutual information appear
#                     nowhere as agreement metrics. Panel Q fills that gap. It
#                     evaluates whether a REPRESENTATION has coherent structure.
#                     It says nothing about whether predictions are correct, and
#                     nothing it produces may be reported as clinical
#                     superiority.
#
#                     THE MODULE IS ITS REFUSALS. Any of these metrics is three
#                     lines of scikit-learn; what makes the module worth having
#                     is that it declines to produce a number it cannot justify.
#                     Five properties, each proven by sabotage rather than by
#                     assertion -- the test suite passed on its first full run,
#                     which is exactly when a suite is least trustworthy:
#
#                     (1) THE SILHOUETTE GUARD REFUSES BEFORE ALLOCATING, and is
#                         expressed in MEMORY rather than a sample count because
#                         memory is the constraint and a count silently assumes a
#                         dtype. maximum_distance_matrix_gib=3.0 yields
#                         floor(sqrt(3*2^30/8)) = 20,066. Measured on
#                         1280-dimensional embeddings: 0.32 s at n=2,000; 7.53 s
#                         at 10,000; 42.44 s and 1.0 GiB at 30,000; ~27 GiB
#                         predicted at 60,000; 16.4 TiB at 1.5 million. A request
#                         above the ceiling returns COMPUTATIONALLY_DEFERRED in
#                         about a millisecond with the numbers in metadata.
#                         Removing the ceiling check made the test HANG -- it
#                         genuinely attempted 18.6 GiB. The failure mode the
#                         guard prevents, reproduced by accident.
#                     (2) NO SILENT NOT-A-NUMBER. Enforced in __post_init__, not
#                         only in the not_ok() factory: an earlier draft guarded
#                         one of two construction paths, so the raw dataclass
#                         constructor could still build an unexplained failure.
#                         aggregate() refuses to average across non-OK results
#                         and reports the full status census including the OK
#                         count, because numpy.nanmean would silently return the
#                         mean of whatever happened to work.
#                     (3) THREE EXCLUSION ROUTES, COUNTED SEPARATELY, IN A FIXED
#                         ORDER. Non-finite, then algorithm noise, then the
#                         minimum-cluster-size filter. A row qualifying for two
#                         counts ONCE, in the earliest. Reconciliation alone
#                         cannot detect a reordering because the total is
#                         identical either way, so bucket MEMBERSHIP has its own
#                         test. A single "noise_fraction" field would have
#                         reported 0.15 on the fixture and concealed 0.08.
#                     (4) THE TWO DAVIES-BOULDIN GEOMETRIES ARE DIFFERENT
#                         ESTIMANDS. For unit-normalized vectors the squared
#                         Euclidean distance is 2 - 2*cos, so pairwise Euclidean
#                         and cosine are monotone -- which holds for the
#                         silhouette coefficient and NOT for a centroid-based
#                         index, because the Euclidean mean of points on a sphere
#                         has norm below one and leaves the sphere. Measured on a
#                         directional fixture: 2.9188 Euclidean-on-normalized
#                         against 1.3538 spherical, with cluster mean norms near
#                         0.47. Checked against INDEPENDENT implementations
#                         written in the test file, not golden constants: a
#                         constant catches drift, an independent implementation
#                         catches wrongness. That is what caught the sabotage in
#                         which the spherical centroid silently became the
#                         arithmetic one.
#                     (5) THE CONFOUNDER GATE IS PURE AND FAIL-CLOSED.
#                         decide_confounder_gate() takes AgreementEstimate values
#                         and returns a verdict with no randomness, so the
#                         scientific policy is testable as arithmetic with
#                         hand-written intervals; evaluate_confounder_gate()
#                         measures, then calls it. Separating measurement from
#                         decision means a policy change cannot be made by
#                         accident while editing an estimator. Comparison is on
#                         INTERVALS: upper95(covariate) < lower95(target),
#                         strictly. Covariate upper 0.40 against target lower
#                         0.40 refuses; 0.3999 passes. A non-finite bound is not
#                         separation -- an unknown is not a pass.
#
#                     WHY A GATE AND NOT A DIAGNOSTIC. Clustering can rediscover
#                     gene identity, laboratory provenance or missingness pattern
#                     with considerable efficiency while looking biologically
#                     elegant, and a lower Davies-Bouldin index is exactly what
#                     that looks like. This cohort has a demonstrated reason for
#                     the concern: n_pathogenic_in_gene required a permutation
#                     ablation to establish it encoded genuine variant-level
#                     biology (observed AUROC 0.9666 against a permuted 95th
#                     percentile of 0.8016). Panel Q therefore refuses any
#                     solution whose adjusted mutual information with a technical
#                     covariate is not separated from that with the biological
#                     target.
#
#                     THE PERMUTATION NULL RESPECTS DEPENDENCE. Variants within a
#                     gene share a covariate, so permuting rows barely disturbs
#                     the association. Measured: gene-level null 95th percentile
#                     0.0516 against row-level 0.0011, a 47-fold difference. The
#                     row-level null would make a spurious association look far
#                     more significant than it is. permute_covariate_by_group is
#                     public so the MECHANISM is tested directly -- that every
#                     gene still holds one value and the multiset is preserved --
#                     which is more fundamental and less brittle than asserting a
#                     quantile that depends on seeds, numpy and scikit-learn.
#
#                     ONE DEFECT FOUND AND FIXED MID-BUILD. Constructing an
#                     AgreementEstimate with a non-finite POINT estimate raised
#                     deep inside decide_confounder_gate rather than at the
#                     boundary. The realistic case is a finite point with a
#                     non-finite INTERVAL -- too few usable subsample replicates
#                     -- so __post_init__ now requires a finite point estimate
#                     and documents that an unbounded interval is legitimate and
#                     handled fail-closed. Found because a deliberately
#                     unrealistic fixture exposed it.
#
#                     AND ONE TEST THAT COULD NOT FAIL. The first version of the
#                     silhouette-guard check used a 500-row fixture, where
#                     effective = min(100000, 500) = 500 is legitimately under
#                     the ceiling. Nothing could trigger the refusal. Rewritten
#                     against a 50,000-row cohort. This is the fourth time this
#                     session a check has been found unable to fail.
#
#                     NOT DONE HERE, DELIBERATELY: PartitionRole.STRUCTURE and
#                     the Panel Q orchestrator are absent. Panel Q's partition
#                     policy requires a gene-disjoint STRUCTURE partition so that
#                     cluster discovery -- which selects an algorithm, a distance,
#                     a cluster count and an interpretation, and is therefore
#                     model selection -- never touches the locked test set. That
#                     changes the shipped partition schema and belongs in its own
#                     versioned commit, with the orchestrator third, once it has
#                     a legitimate partition to run on.
#
#   2283  2026-07-21  +13: tests/unit/test_disk_census_walker.py -- THE TOOL THAT
#                     MEASURES THE DISK HAD BEEN WRONG THREE TIMES AND HAD NO
#                     TESTS. scripts/forensics/audit_disk_census.py added
#                     (renamed from a Downloads-only
#                     audit_disk_reclaim_v3_2026-07-20.py).
#
#                     WHY THIS EXISTS. The census tool had reported a materially
#                     wrong figure for data/ three versions running:
#                       v1 2026-07-20  161.38 GiB / 15,260 files
#                       v2 2026-07-20  every subdirectory 0.0 MiB -- the time
#                                      budget had expired and zeros were printed
#                                      as measurements
#                       v3 2026-07-21    3.21 GiB -- against a true 98.75 GiB
#                                      measured the same hour; short by 95.54 GiB
#                     Each fix was correct as far as it went. None shipped with a
#                     test. The tool also lived only in a Downloads folder, so
#                     the capability that resolved the 2026-07-20 drive emergency
#                     was one folder-clear away from being lost.
#
#                     DEFECT 5, THE ONE FIXED HERE. Walker keeps visited sets
#                     keyed on (st_dev, st_ino) so a CENSUS never counts an
#                     overlapping root twice -- without it the legacy junction
#                     C:\Documents and Settings made the census report 5.94x the
#                     volume. data_breakdown() then reused the SAME Walker that
#                     had already walked the whole volume. Every subdirectory of
#                     data/ was in _seen_dirs; size_of() skips any child already
#                     there and never checks the root it is handed. So each
#                     subtotal counted ONLY files sitting loose in that directory:
#
#                       directory        true    in subdirs  loose  v3 reported
#                       data/external   75.18G     75.18G    0.00G     0.003G
#                       data/processed   3.50G      0.54G    2.96G     2.950G
#                       data/raw        19.80G     19.80G    0.00G     0.000G
#                       data/_drift_ch   0.26G      0.00G    0.26G     0.264G
#
#                     Four for four -- the mechanism was characterised by
#                     prediction, not guessed. The de-duplication was never the
#                     bug; applying CENSUS semantics to a STANDALONE MEASUREMENT
#                     was. size_of() now takes an explicit `independent` flag and
#                     the two semantics are NAMED rather than implied. Cycle
#                     safety holds in both modes; only the SCOPE of the visited
#                     sets differs, and hardlink_savings is not mutated in
#                     independent mode because that statistic describes the
#                     census.
#
#                     WORSE THAN WRONG. The section printed "Compare the figure
#                     above against 161.38 GiB" directly beneath its own bad
#                     number, inviting the reader to conclude 158 GiB had been
#                     reclaimed. Nothing had. That line is replaced by the
#                     section's own error history, and a test asserts the old
#                     line is gone.
#
#                     WHAT WOULD NOT HAVE CAUGHT IT: asserting the census total
#                     matches the volume (it did); asserting termination (it did);
#                     asserting no exceptions (there were none). Only measuring a
#                     directory of KNOWN size AFTER walking its parent exposes it.
#                     Every load-bearing test here does exactly that.
#
#                     PROVEN BY SABOTAGE, all four detected:
#                       call site drops independent=True   ->  2 failed
#                       independent shares the dir set     ->  5 failed
#                       independent leaks into shared set  ->  5 failed
#                       shared de-dup removed entirely     ->  2 failed
#                     The last matters as much as the first: the tests pin the
#                     census property too, so the fix cannot be "achieved" by
#                     discarding the de-duplication that prevents 5.94x totals.
#
#                     RENAMED. A committed tool must not carry a version number
#                     and a date in its filename -- the project's own data-layout
#                     standard forbids version suffixes on directory names for the
#                     same reason. "reclaim" was also wrong: it deletes nothing.
#
#                     MEASURED CONTEXT, 2026-07-21: C: is the ONLY fixed volume,
#                     935.59 GiB, 83.01 free (8.87 %). data/ is 98.75 GiB, of
#                     which data/external/finngen is 57.64 GiB in three files.
#                     The suite showed a 1.87x wall-clock range on identical code
#                     (605.08 s to 1131.67 s), consistent with a host below the
#                     ten percent where Windows behaviour degrades. No storage
#                     decision is taken in this commit; the measuring instrument
#                     is repaired first.
#
#   2328  2026-07-21  +45: tests/unit/test_storage_guard.py -- THE GUARD THAT
#                     NEVER CHECKED FREE SPACE, AND THAT NOTHING EVER CALLED.
#                     configs/data_manifest.yaml gains a storage: policy block;
#                     scripts/maintenance/preflight_data_guard.py rewritten;
#                     scripts/preflight_run17.py gains storage_gate() in run_all.
#
#                     TWO DEFECTS, BOTH OF OMISSION.
#                     (1) preflight_data_guard.py verified structure -- dangling
#                     junction, stray file shadowing data/, canonical subtrees --
#                     and never looked at free space. The guard whose purpose is
#                     catching storage problems before a run could not catch the
#                     one the machine had. On 2026-07-20 the volume reached 0.716
#                     per cent free and a FAILING RUN, not a guard, is what
#                     noticed.
#                     (2) NOTHING EVER CALLED IT. A repository-wide search across
#                     every .py, .sh, .ps1 and .yaml on 2026-07-21 found ZERO
#                     invocations of assert_data_usable. Its own docstring said it
#                     was importable "to wire into run scripts / conftest". It
#                     never was, from 2026-06-14 to 2026-07-21. A guard that is
#                     not invoked is not a guard; it is a comment that happens to
#                     be executable. Defect (2) mattered more than (1): fixing
#                     only the first would have produced a better guard that still
#                     never ran.
#
#                     MEASURED CONTEXT (2026-07-21). C: is the ONLY fixed volume:
#                     935.59 GiB, 83.50 free, 8.925 per cent. data/ is 98.75 GiB,
#                     LARGER than the free space, so nothing large can be staged,
#                     duplicated or rebuilt in place. Nine suite runs on identical
#                     code spanned 605.08 s to 1131.67 s, a 1.87x range consistent
#                     with a host under storage pressure.
#
#                     THREE SEVERITIES, NOT ONE THRESHOLD. A single threshold must
#                     choose between crying wolf and staying silent until it is too
#                     late.
#                       free >= required                OK
#                       hard_floor <= free < required    WARN, run proceeds
#                       free < hard_floor                FAIL, refuse
#                     required = working_cache_gib + max(headroom_fraction *
#                     volume, headroom_min_gib) = 61.48 GiB on this volume, which
#                     reproduces EXACTLY what audit_disk_census.py printed. One
#                     model, not two.
#
#                     POLICY LIVES IN THE MANIFEST. DATA_LAYOUT_STANDARD.md already
#                     declares configs/data_manifest.yaml the single source the
#                     auditor, setup and sync scripts read. audit_disk_census.py
#                     keeps its OWN constants deliberately -- it must run standalone
#                     from any directory with the repository not importable -- and
#                     test_policy_matches_the_census_tool fails if the two ever
#                     disagree. Duplication as a considered trade, made safe by a
#                     test, rather than duplication by accident.
#
#                     WIRED AT THE POINT WHERE IT COSTS MONEY. storage_gate() joins
#                     run_all() in preflight_run17.py, which runs before a Vast.ai
#                     instance is created. It degrades to a FAIL ROW rather than an
#                     exception if the guard cannot load, because a preflight that
#                     dies on its first problem hides the second.
#
#                     PROVEN BY SABOTAGE, all seven detected:
#                       DEFAULT_POLICY drifts from manifest    ->  2 failed
#                       census constant drifts from manifest   ->  1 failed
#                       manifest changes, code copies do not   ->  4 failed
#                       hard floor stops being enforced        ->  6 failed
#                       __post_init__ validation removed       ->  7 failed
#                       storage_rows raises instead of a row   ->  1 failed
#                       free-space row dropped entirely        ->  4 failed
#
#                     A PYTHON INTERACTION WORTH RECORDING: @dataclass under
#                     `from __future__ import annotations` resolves annotations
#                     through sys.modules[cls.__module__]. A module loaded by
#                     importlib.util.spec_from_file_location must be REGISTERED in
#                     sys.modules BEFORE exec_module, or the decorator raises
#                     AttributeError on None. Both the test loader and
#                     storage_gate() do this, with the reason written down.
#
#   2378  2026-07-21  +50: tests/unit/test_partition_structure_role.py --
#                     PartitionRole.STRUCTURE and the SIX_WAY schema, which
#                     unblock the Panel Q orchestrator; plus a stale-list defect
#                     in PartitionSchema found while building them.
#
#                     WHY THE ROLE EXISTS. Panel Q's specification is explicit
#                     that unsupervised structure analysis is a MODEL-SELECTION
#                     activity: it chooses a representation, preprocessing,
#                     dimensionality, distance geometry, clustering algorithm,
#                     cluster count, noise handling, stability thresholds and a
#                     biological interpretation. Doing that on the locked test
#                     partition is selection on test. The specification requires
#                     a dedicated STRUCTURE partition "gene-disjoint from train,
#                     tune, probability calibration, conformal calibration, and
#                     test", with test admitting only a predeclared replication
#                     of a solution frozen on STRUCTURE. Panel Q shipped in
#                     5dcb932 with no partition to run on.
#
#                     A STALE LIST, FOUND BY PROBING. PartitionSchema capped role
#                     repetition by ENUMERATING three roles -- SELECT,
#                     CALIBRATE_CONFORMAL, TEST. That list was already stale:
#                     commit 5b1c82b added CALIBRATE_PROBABILITY the same morning
#                     and did not add it here. Two partitions could both claim
#                     it, the schema would be ACCEPTED, and name_for_role() would
#                     silently return whichever was declared first -- train.py
#                     fitting the probability calibrator on one partition while a
#                     second believed it held the role. A silent ambiguity in
#                     precisely the role the five-way schema exists to serve.
#                     Found by probing a six-partition schema, not by review.
#                     The repair removes the list rather than extending it: every
#                     role except TRAIN is capped, so a future enum member cannot
#                     be forgotten. The test is parametrized over PartitionRole
#                     ITSELF, so roles that do not exist yet are covered.
#
#                     THE MIGRATION GUARANTEE, AND A TEST FAILURE THAT EARNED IT.
#                     Hash intervals are assigned in hash_order, so a partition's
#                     genes depend on the cumulative fraction ahead of it.
#                     SIX_WAY keeps FIVE_WAY's first four intervals EXACTLY and
#                     splits its train interval in two:
#                       structure [0.45, 0.52)   train [0.52, 1.00)
#                     Verified across seeds 42/7/123/2026, migrating FIVE_WAY ->
#                     SIX_WAY moves EXACTLY ONE set of genes, out of train into
#                     structure:
#                       test, conformal, calib, tune  byte-identical
#                       structure   precisely the genes train gave up
#                       train       loses those and gains nothing
#                     So the locked test set is untouched, a fitted probability
#                     calibrator stays valid, and model selection need NOT be
#                     repeated. Only the training set shrinks, 0.55 -> 0.48,
#                     which is the honest cost of funding a structure partition.
#
#                     The FIRST hash order tried put structure between calib and
#                     tune, which shifted tune by 0.07 and would have silently
#                     invalidated every model-selection decision for no benefit.
#                     A test asserting the OPPOSITE property failed, and that
#                     failure is what found it. The guarantee is now the test.
#
#                     NO NEW SKIPS. An earlier draft skipped TRAIN inside the
#                     parametrized role test -- a permanent eighth skip in every
#                     run forever. Filtering the parametrize list instead keeps
#                     the enum-driven property at zero skip cost.
#
#                     PROVEN BY SABOTAGE, all five detected:
#                       role cap reverts to the enumerated list  ->  4 failed
#                       structure placed before tune             ->  6 failed
#                       structure funded from test not train     -> 13 failed
#                       STRUCTURE removed from the enum          ->  3 errors
#                       name_for_role substitutes instead of None-> 5 failed
#
#                     WHAT DID NOT CHANGE. FOUR_WAY and FIVE_WAY are untouched;
#                     PARTITIONS still aliases FOUR_WAY.names; the existing 54
#                     split tests pass unmodified. split(), the invariant checker
#                     and the leakage remap needed NO changes at all -- six
#                     partitions worked on the first probe, which is the payoff
#                     of the schema-as-data rewrite in 5b1c82b.
#
#   2408  2026-07-21  +30: tests/unit/test_calibration_carve.py -- THE ISOTONIC
#                     CALIBRATOR WAS FITTED ON GENES THE MODELS HAD TRAINED ON,
#                     IN EVERY PRODUCTION RUN.
#
#                     WHERE IT LIVED. run_phase2_eval.py:590 -- the production
#                     entry point, the one preflight_run17.emit_command() emits
#                     and every Run 14-17 launcher uses -- calls fit() with no
#                     *_cal_ext argument, taking the self-carve branch:
#                       _tts(idx, test_size=0.15, stratify=y_arr, ...)
#                     `stratify=`, no `groups=`. Measured 2026-07-21:
#                       500 genes        319/319 cal genes also in fit; 100.0% of
#                                        cal ROWS from trained-on genes
#                       8,000 genes    7,856/7,864 likewise; 100.0% of cal rows
#                     Twenty lines below, the inner CV is carefully gene-disjoint
#                     via GroupKFold citing INCIDENT_2026-06-13. Out-of-fold
#                     PREDICTIONS were gene-disjoint; the fold the CALIBRATOR was
#                     fitted on was not. One function, two standards.
#
#                     WHAT WAS NOT THE DEFECT. train.py's v2_conformal path fits
#                     the calibrator on `tune`, the SELECTION set -- also wrong,
#                     but NO launcher passes --split-protocol, so it has never
#                     run. Claude asserted three times that fixing train.py would
#                     "correct every calibrated number the project reports". That
#                     was WRONG: train.py is not the production entry point.
#                     Checking emit_command() before designing the fix is the only
#                     reason this commit touches variant_ensemble.py instead.
#
#                     THE FIX. EnsembleConfig.calibration_carve, default
#                     "gene_disjoint": carve whole GENES via GroupShuffleSplit.
#                     "legacy_stratified" reproduces the old behaviour BYTE-FOR-
#                     BYTE (identical indices at seeds 42/7/2026) so historical
#                     runs stay reproducible. Result: 100.0% -> 0.0% of
#                     calibration rows from trained-on genes, both cohorts.
#
#                     THE ROW-VERSUS-GROUP TRAP. GroupShuffleSplit's test_size is
#                     a proportion of GROUPS, not rows -- the same trap that
#                     crashed 3 of 12 seeds in split_protocol_v2.group_shuffle
#                     earlier the same day. The carve therefore VALIDATES the fold
#                     it produced (both classes present, >= calibration_min_rows)
#                     across a deterministic seed sequence, and RAISES with the
#                     full attempt log if none qualifies. Realized row fraction
#                     stayed at 14.9% and 14.7% against a 15% target.
#
#                     NO SILENT FALLBACK. calibration_carve_used_ records what
#                     actually happened: gene_disjoint:seed=N,
#                     legacy_stratified:configured, or
#                     legacy_stratified:no_gene_labels. run_phase2_eval reads
#                     gene_symbol from splits/meta_train.parquet, which may be
#                     absent -- that path now warns loudly rather than degrading
#                     in silence.
#
#                     WHAT THE FIX ACTUALLY BUYS, MEASURED OVER FIVE COHORTS --
#                     AND IT IS NOT WHAT CLAUDE PREDICTED. Claude said reported
#                     numbers would change and current values were optimistic.
#                     Measured Brier on a locked set of ENTIRELY UNSEEN genes:
#                       legacy        0.18063
#                       gene-disjoint 0.18064      difference +0.00001 (+0.00%)
#                     The REPORTED numbers barely move. What was optimistic is the
#                     INTERNAL estimate: the legacy calibration fold overstated
#                     its own quality in 5 of 5 cohorts, mean +0.00415 Brier;
#                     gene-disjoint mean +0.00048, essentially unbiased.
#                     CONSEQUENCE: Runs 14-17 reported test metrics DO NOT need
#                     retracting. The defect corrupted a diagnostic, not the
#                     headline. Synthetic cohorts; magnitude on the real cohort
#                     may differ, and the first real run should re-measure.
#
#                     A SABOTAGE THAT FAILED TO FAIL. Removing the both-classes
#                     guard left all 28 tests passing, because every cohort used
#                     happened to yield both classes. A cohort where the label is
#                     a property of the GENE (4 of 40 genes pathogenic) makes
#                     GroupShuffleSplit at random_state=42 return an 89-row
#                     ALL-BENIGN fold; isotonic regression on one class returns a
#                     constant and raises nothing. Two tests now pin it, and the
#                     fixture asserts it still forces the condition -- so the test
#                     cannot quietly stop testing. Fifth instance of the same
#                     lesson today, and the second in Claude's own work.
#
#                     PROVEN BY SABOTAGE, all six detected after the repair:
#                       default flips back to legacy        ->  5 failed
#                       disjoint branch uses stratified     ->  8 failed
#                       both-classes guard removed          ->  1 failed
#                       min-rows guard removed              ->  3 failed
#                       missing gene labels fall back mute  ->  1 failed
#                       legacy test_size drifts             ->  4 failed
#
#   2417  2026-07-21  +9 (2408 -> 2417): tests/unit/test_calibration_carve.py
#                     grows from 30 to 39. The 2408 bump was WITHDRAWN: the
#                     calibration carve it accompanied BROKE THE SUITE.
#
#                     WHAT BROKE. calibration_min_rows was a single absolute
#                     floor of 200. test_level2_leakfree_oof uses a 427-row
#                     cohort across 30 genes, where a 15 per cent carve of ANY
#                     kind yields about 64 rows. The floor refused gene-disjoint
#                     folds of 63-76 rows -- while the LEGACY stratified carve it
#                     replaced had been using a 62-row fold on that same cohort
#                     for the entire history of the project, without complaint.
#                     Refusing what the predecessor accepted is a regression, not
#                     a stricter standard. Full suite: 1 failed, 2400 passed.
#
#                     WHY NO TEST CAUGHT IT. Every cohort in the original 30
#                     tests had 500 or more genes. None could exercise the small-
#                     cohort path. The guard was correct in its intent and wrong
#                     in its instrument.
#
#                     THE REPAIR. The guard now asks "did the carve deliver
#                     roughly what was ASKED for?" rather than "is this fold
#                     small?" -- because that is the question the row-versus-group
#                     trap actually poses. Three thresholds:
#                       calibration_min_rows            = 25   absolute floor
#                       calibration_min_fraction_of_target = 0.5
#                       calibration_max_fraction_of_target = 2.0
#                       calibration_advisory_rows       = 200  WARN, not refuse
#
#                     THE TRAP RUNS BOTH WAYS -- found by probing the lower bound,
#                     not by review. A cohort of 11,000 rows whose largest gene
#                     held 9,000 produced a 9,300-row calibration fold against a
#                     1,650-row target: 85 per cent of the data, leaving the
#                     models 1,700 rows to fit on. An upper bound now refuses it.
#
#                     VERIFIED ACROSS THREE REGIMES:
#                       427 rows / 30 genes      -> 76-row fold, WARNS, proceeds
#                       11,000 rows, 1 dominant  -> REFUSED, both verdicts seen
#                       1,201,942 rows / 8,000   -> 180,458 rows, exactly 15.0%
#
#                     test_level2_leakfree_oof passes again (4 passed). The 14
#                     failures seen in the authoring sandbox for
#                     test_reclassification_* and test_split_protocol_schema are
#                     PRE-EXISTING there -- identical counts on an unmodified
#                     clone of c68934d -- and are artifacts of --noconftest plus
#                     absent packages, not of this change.
#
#   2446  2026-07-21  +29: tests/unit/test_calibration_validity_contract.py --
#                     TWO DEFECTS IN THE METRIC KERNEL EVERY PANEL BUILDS ON.
#
#                     DEFECT 1: calibration_valid asserted soundness it had not
#                     checked. It gated on is_probability(p) alone -- whether the
#                     values lay in [0,1] -- and never asked whether a calibration
#                     claim was SUPPORTABLE. Measured on y=[1,1,1,1],
#                     p=[.9,.8,.85,.95]:
#                       auroc NaN  auprc NaN      correct, ranking undefined
#                       cal_slope NaN  cal_intercept NaN
#                       brier 0.01875   ece 0.125     <- NUMBERS
#                       calibration_valid True        <- asserting they are sound
#                     That ECE is 1 - 0.875, the gap between the mean prediction
#                     and the only label present. The reliability diagram has one
#                     occupied row; it says nothing about calibration across the
#                     probability range.
#
#                     The flag's own documented invariant -- "False =>
#                     brier/log_loss/ece/cal_* are NaN by design" -- was ALREADY
#                     violated in the other direction: cal_slope and cal_intercept
#                     were NaN while the flag read True, so a reader would take an
#                     undefined estimand for a failed computation. The invariant is
#                     now ENFORCED in code, not merely written in a comment.
#
#                     Both classes present is a HARD requirement: without it the
#                     quantity is not calibration. Thin support is REPORTED, not
#                     refused -- DEFAULT_MIN_POS/NEG are the same floors
#                     stratified_evaluate already applied per subgroup, so
#                     identical data was called insufficient as a stratum and
#                     sound on its own. Refusing it outright would repeat the
#                     427-row regression made earlier the same day.
#                     New field calibration_support carries the machine-readable
#                     reason: sufficient | thin:... | single_class:... |
#                     not_probabilities | insufficient_rows.
#
#                     DEFECT 2: two evaluation contracts in one module.
#                     compute_classification_metrics and ModelEvaluator were
#                     REMOVED, not wrapped. The 2026-07-20 banner had called them
#                     "unsafe in ways this stack explicitly rejects" and deferred
#                     unification to a separate, separately-measured commit; this
#                     is that commit. Delegation would have preserved the contract
#                     that IS the problem: a dict of bare floats cannot express
#                     undefined, insufficient support, dependency unavailable or
#                     computationally deferred. It returned specificity=0 for an
#                     undefined quantity, and on a single-class cohort it did not
#                     even manage that -- confusion_matrix(...).ravel() yields one
#                     cell, not four, so it raised "not enough values to unpack
#                     (expected 4, got 1)".
#
#                     Neither had a production caller: one test pinned them, one
#                     notebook imported them from src.evaluation.metrics, a package
#                     path that has not existed since the rename. The __all__ note
#                     reading "original API -- do not remove" was itself stale.
#                     test_legacy_metrics_api_is_preserved is replaced 1:1 by
#                     test_the_legacy_metrics_api_is_gone, which pins their
#                     ABSENCE, so reintroducing a second contract requires deleting
#                     a test that explains why there is one.
#
#                     CHECK ORDER. An empty cohort first reported
#                     "not_probabilities" -- true, since is_probability([]) is
#                     correctly False, but the less useful of two true reasons.
#                     Checks now run most-specific first and it reports
#                     "insufficient_rows". Found by a test, not by review.
#
#                     PROVEN BY SABOTAGE, all three detected:
#                       calibration_valid reverts to is_probability -> 18 failed
#                       invariant unenforced                        ->  5 failed
#                       legacy evaluator restored                   ->  1 failed
#
#   2488  2026-07-21  +42: tests/unit/test_gene_block_permutation.py -- THE
#                     PERMUTATION NULL IS RENAMED, ITS PROVENANCE COMPLETED, AND
#                     ITS ONE UNACTED-ON DIAGNOSTIC NOW ACTS.
#
#                     THE RENAME. permute_covariate_by_group ->
#                     permute_covariate_by_gene_block, and permutation_unit
#                     "group" -> "gene_block". The old name said only that
#                     groups were involved, not that whole blocks are exchanged
#                     as units, which is the property the null rests on.
#                     "Gene-block permutation" is the standard name. Correcting
#                     an earlier description of this code as "labels shuffled
#                     WITHIN genes" -- wrong exchangeability unit AND wrong
#                     quantity, since it permutes a COVARIATE against a fixed
#                     clustering, not labels.
#
#                     A PROPERTY MEASURED, NOT A DEFECT FIXED. Gene-block
#                     permutation with UNEQUAL block sizes necessarily moves the
#                     row-level marginal: swapping a 10-row gene's value with a
#                     4-row gene's moves 10 rows out of one level and 4 into it.
#                     Measured on 1,149 rows / 150 genes with a PURELY
#                     gene-level covariate (zero genes carrying >1 value): mean
#                     total variation distance 0.0145 (sd 0.0076), and ZERO in 0
#                     of 200 permutations.
#
#                     TWO OF MY OWN CLAIMS WERE FALSE AND WERE CORRECTED FROM THE
#                     SAME DATA THAT PRODUCED THEM:
#                       "at zero within-gene variation the permutation is exact"
#                         -- false. Drift at p=0.00 was 0.0171, LARGER than at
#                         p=0.05 (0.0160). Unequal block SIZES are the cause;
#                         within-block heterogeneity only modulates it.
#                       "the relationship is monotone" -- false. Across p =
#                         0.00/0.05/0.15/0.35/0.60/1.00 drift ran 0.0171/0.0160/
#                         0.0264/0.0458/0.0630/0.0172, PEAKING near 0.60.
#
#                     WHY THE DEFAULT DID NOT CHANGE. Adjusted mutual information
#                     is chance-corrected against exactly the marginals that
#                     drift. Unstratified vs size-stratified p-values, 6 seeds:
#                       assoc 0.0   0.6024 vs 0.6013   -0.0011
#                       assoc 0.2   0.0659 vs 0.0548   -0.0111
#                       assoc 0.4   0.0033 vs 0.0033    0.0000
#                       assoc 0.6   0.0033 vs 0.0033    0.0000
#                     Restructuring buys nothing in the quantity anyone reads and
#                     costs frozen blocks. So the drift is MEASURED AND REPORTED
#                     per run (marginal_tvd_mean, marginal_tvd_max) instead.
#
#                     SIZE-STRATIFIED MODE, OPT-IN. size_strata=True restricts
#                     swaps to blocks of equal size and removes the drift
#                     EXACTLY -- 0.000000 in 200 of 200 permutations. Its cost:
#                     a block alone in its size stratum can only swap with
#                     itself, so its value is FROZEN. Reported as
#                     n_blocks_frozen_in_stratum. NOT assumed small: a synthetic
#                     cohort with sizes drawn uniformly 1-300 froze zero blocks,
#                     but that fixture does NOT model ClinVar's heavy tail, where
#                     large genes are frequently unique in size. A dedicated
#                     fixture gives every block a distinct size and proves the
#                     fully-frozen case reproduces the observed covariate exactly.
#
#                     A DIAGNOSTIC THAT NOW ACTS.
#                     n_groups_with_multiple_covariate_values was recorded from
#                     the start and NOTHING consumed it -- a repository-wide
#                     search found one test asserting it equals zero and no other
#                     reader. Above a 20 per cent advisory the run now WARNS and
#                     records REASON_BLOCK_COVARIATE_HETEROGENEOUS. It warns
#                     rather than refuses: refusing what the previous
#                     implementation accepted is a regression, the lesson learned
#                     hours earlier when an absolute calibration-fold floor broke
#                     a 427-row cohort.
#
#                     ALSO RECORDED: representative_rule ("first_value_per_block"
#                     -- a reader told only that twelve blocks carried multiple
#                     values still cannot tell WHICH was used),
#                     fraction_blocks_with_multiple_covariate_values, and
#                     p_value_sidedness ("one_sided_upper", left implicit until
#                     now, which invites a reader to halve or double it). The old
#                     keys n_groups and n_groups_with_multiple_covariate_values
#                     are RETAINED, because run manifests already carry them.
#
#                     PROVEN BY SABOTAGE, all six detected:
#                       size_strata silently ignored        -> 3 failed
#                       drift reported without measuring    -> 2 failed
#                       heterogeneity advisory removed      -> 2 failed
#                       representative rule mislabelled     -> 1 failed
#                       frozen-block count always zero      -> 1 failed
#                       scheme reverts to "group"           -> 3 failed
#
#   2544  2026-07-21  +56: tests/unit/test_capability_contract.py -- A FALSE
#                     GREEN IS NOW STRUCTURALLY IMPOSSIBLE TO CONSTRUCT.
#
#                     THE PROBLEM. A validation gate over a capability that does
#                     not exist does not block: it PASSES VACUOUSLY, because
#                     there is nothing to check and therefore nothing to fail. A
#                     green Panel H would then be cited as evidence a disease
#                     head was validated, when no disease head exists. That is
#                     the defect removed five times on 2026-07-21 --
#                     assert_data_usable called from nowhere; a stale enumerated
#                     role cap; a calibrator fitted on trained-on genes;
#                     calibration_valid asserting soundness it never checked; and
#                     n_groups_with_multiple_covariate_values with exactly one
#                     reader in the whole repository.
#
#                     THE CONTRACT. Four questions are separated, because
#                     collapsing them is what allows the false green: does the
#                     capability exist (CapabilityState); does it produce output
#                     (OUTPUT_AVAILABLE); is its target scientifically admissible
#                     (TargetState); has it passed validation (VALIDATED).
#
#                     THE INVARIANT IS IN THE CONSTRUCTOR, NOT THE GATE.
#                     CapabilityEvidence.__post_init__ refuses to build an OK
#                     capability that is not VALIDATED, whose target is not
#                     ADMISSIBLE, or which names no output artifact; and refuses
#                     any non-OK status without a machine-readable reason. There
#                     is no path to the object that avoids the check, so no
#                     caller can forget to consult it -- which is exactly how a
#                     well-tested assert_data_usable came to be called nowhere.
#
#                     WHY TargetState IS A TYPED AXIS AND NOT A REASON STRING.
#                     Gene ranking has a working capability, an available output
#                     AND an available target -- clingen_validity_score. It is
#                     still inadmissible, because that column is one of the
#                     model's own inputs; ranking genes with a model handed
#                     ClinGen's verdict and scoring against ClinGen is circular.
#                     An earlier draft encoded this in a reason STRING, where
#                     nothing could enforce it.
#
#                     TWO DELIBERATE DEPARTURES FROM THE PROPOSAL, BOTH DEFECTS
#                     FOUND BY CHECKING RATHER THAN READING:
#                       (1) MetricStatus is defined ONCE, here, and imported by
#                           clustering_metrics, which loses its local copy. The
#                           proposal defined a SECOND class of the same name --
#                           the divergence problem removed in b8275a0 that same
#                           day, where the legacy evaluator was deleted rather
#                           than wrapped for exactly this reason. The original
#                           six member VALUES are frozen and tested: existing run
#                           manifests on disk contain those strings.
#                       (2) (str, Enum) rather than StrEnum. StrEnum arrived in
#                           Python 3.11; pyproject declares
#                           requires-python = ">=3.10" and the CI matrix runs
#                           only 3.11 and 3.12, so a 3.10 install would fail at
#                           IMPORT with nothing in the pipeline to catch it. A
#                           test now scans the package for StrEnum and fails.
#
#                     THE THREE MEASURED STATES (against the repository, not
#                     estimated): regression -> IMPLEMENTED_NO_OUTPUT + ABSENT
#                     (gtex, rnaseq and functional_assays directories all
#                     absent); multilabel_disease -> NOT_IMPLEMENTED + ABSENT
#                     (ClinVar's disease field and phenotype identifiers are both
#                     unread); gene_ranking -> OUTPUT_AVAILABLE + CONTAMINATED.
#
#                     PROVEN BY SABOTAGE, all seven detected:
#                       OK invariant removed entirely       -> 27 failed
#                       OK no longer needs admissible target->  5 failed
#                       non-OK no longer needs a reason     -> 16 failed
#                       gate treats non-OK as satisfied     -> 13 failed
#                       empty release reports complete      ->  1 failed
#                       an original status value changed    ->  1 failed
#                       StrEnum reintroduced                ->  1 failed
#
#   2559  2026-07-21  +15: tests/unit/test_ci_failure_alert_workflow.py --
#                     THE RATCHET TURNED main RED AND NOBODY LOOKED.
#
#                     WHAT HAPPENED. At 12:58 on 2026-07-21 commit d7c4d35
#                     conflated two changes and left EXPECTED_SUITE_SIZE at 2417
#                     while the tree collected 2446. Continuous Integration run
#                     #562 aborted at COLLECTION time with pytest.UsageError --
#                     exit code 4, three minutes fourteen seconds against the
#                     usual fourteen minutes, because no test ever ran. main
#                     stayed red for roughly two hours before anyone noticed, and
#                     it was noticed by reading pasted local output, not by
#                     looking at Continuous Integration.
#
#                     THE DETECTION WORKED PERFECTLY. THE NOTIFICATION LOOP DID
#                     NOT EXIST. That is the same shape as every defect removed
#                     that day: a check that fires correctly and has no reader is
#                     only marginally better than no check at all.
#
#                     THE FIX. .github/workflows/ci_failure_alert.yml opens a
#                     GitHub issue when Continuous Integration fails on main,
#                     comments on that same issue for repeat failures instead of
#                     opening a pile of them, and CLOSES it when main goes green
#                     again -- so the issue list describes the current state
#                     rather than accumulating stale alarms. A cancelled or
#                     timed-out run changes nothing, because "the run did not
#                     finish" is neither "main is broken" nor "main is fine";
#                     reporting it as either is how a monitoring system lies, the
#                     lesson the drift monitor learned when UNKNOWN was reported
#                     as none.
#
#                     THE ALERT IS ITSELF TESTABLE. A guard that has never fired
#                     is a guard nobody knows works. workflow_dispatch exercises
#                     the whole path on demand and defaults to dry_run so
#                     exercising it creates no noise; with no workflow_run
#                     payload the script treats the situation as a FAILURE,
#                     because an alert tested only on its success branch is
#                     untested. These 15 tests parse the YAML and pin the
#                     trigger, the main-only branch filter, the exact
#                     permissions (contents: read, issues: write), the
#                     concurrency group, the issue reuse, the auto-close, and the
#                     diagnostic text naming exit code 4.
#
#                     A DEFECT CAUGHT IN THE TEST FILE ITSELF. The first draft
#                     used pytest.importorskip("yaml"). PyYAML is pinned in
#                     requirements.txt and imported at runtime by
#                     data/pipeline.py and utils/helpers.py, so it is a HARD
#                     dependency -- and a module-level importorskip collapses
#                     every test in a file into ONE skip entry, which is exactly
#                     how the graph-neural-network branch went untested for 508
#                     Continuous Integration runs. Replaced with a direct import.
#
#                     PROVEN BY SABOTAGE, all five detected:
#                       workflow name typo (never triggers) -> 1 failed
#                       branch filter dropped              -> 1 failed
#                       dispatch exercises success branch  -> 1 failed
#                       permissions widened                -> 1 failed
#                       never closes on recovery           -> 1 failed
#
#   2624  2026-07-21  +65: tests/unit/test_workflow_action_pins.py -- NODE.JS 20
#                     IS DEPRECATED, AND THE TAG IS NOT WHAT SHIPS.
#
#                     TRIGGER. The first run of the new CI failure alert
#                     succeeded and carried a warning: "Node.js 20 is deprecated.
#                     The following actions target Node.js 20 but are being
#                     forced to run on Node.js 24: actions/github-script@v7."
#                     Being forced onto Node 24 is a countdown, not a reprieve.
#
#                     THE AUDIT CORRECTED MY OWN FIRST PASS. I listed
#                     actions/checkout@v4 and actions/setup-python@v5 as exposed.
#                     WRONG: ci.yml and drift_monitor.yml already pinned those by
#                     commit SHA, and resolving the SHAs showed checkout v6.0.2,
#                     setup-python v6.2.0 and upload-artifact v7.0.0 -- all
#                     node24 already. Only data_freshness.yml still carried
#                     floating tags.
#
#                     TWO VERSION GUESSES WERE ALSO WRONG, AND BOTH WOULD HAVE
#                     SHIPPED A FIX THAT FIXED NOTHING:
#                       actions/upload-artifact v5 is STILL node20; v6 is the
#                         first node24 release.
#                       docker/build-push-action v6 is STILL node20; v7 is the
#                         first node24 release.
#                     Reading each action.yml AT THE EXACT SHA caught both. The
#                     tag is not what ships.
#
#                     WHAT CHANGED. Seven references across four workflows now
#                     pin node24 builds by 40-character commit SHA:
#                       ci_failure_alert.yml  github-script  v7 -> v8.0.0
#                       drift_monitor.yml     github-script  v7 -> v8.0.0
#                       data_freshness.yml    checkout/setup-python/
#                                             upload-artifact: floating tags ->
#                                             the same SHAs ci.yml already used
#                       ci.yml                login-action    v3 -> v4
#                                             metadata-action v5 -> v6.2.0
#                                             build-push-action v5 -> v7
#                     Every docker input in use (context, file, target, push,
#                     tags, labels, cache-from, cache-to) was confirmed present
#                     in the target manifests, and metadata-action v6 still emits
#                     the tags and labels outputs the next step consumes.
#
#                     TWO INVARIANTS, BOTH CHECKABLE OFFLINE. (1) Every non-local
#                     `uses:` is a full commit SHA, because a tag is mutable and
#                     re-makes a trust decision silently on every run. (2) Every
#                     SHA appears in EXPECTED_PINS with its resolved version and
#                     runtime, and every runtime is node24 -- so a test that only
#                     asked "is it a SHA?" cannot accept a node20 pin and report
#                     green. Adding a pin without recording what it resolves to
#                     fails the suite, forcing the lookup.
#
#                     CAVEAT RECORDED, NOT PAPERED OVER. ci.yml's push-ghcr job
#                     runs only on `release: published`, so the three docker
#                     upgrades cannot be exercised by an ordinary push. They are
#                     verified by manifest inspection, not by execution.
#
#                     PROVEN BY SABOTAGE, all six detected:
#                       a floating tag reappears           -> 1 failed
#                       an unrecorded SHA is introduced    -> 2 failed
#                       a pin relabelled node20            -> 2 failed
#                       upload-artifact back to v5         -> 1 failed
#                       build-push-action back to v6       -> 1 failed
#                       the regex stops matching           -> 2 failed
#
#   2658  2026-07-21  +34: tests/unit/test_representation_geometry.py -- PANEL R
#                     STAGE ONE. A CLUSTER PANEL CAN RATE A DEGENERATE
#                     REPRESENTATION AS EXCELLENT.
#
#                     THE MEASUREMENT. Two 128-dimensional two-class
#                     representations, 600 rows each. One separates its classes
#                     ANGULARLY and is near-isotropic; the other forces every
#                     vector into a narrow cone and encodes the class ONLY in the
#                     vector norm. Run through this repository's own Panel Q:
#
#                       Davies-Bouldin euclidean   3.934 -> 1.131    3.5x BETTER
#                       Calinski-Harabasz         38.49  -> 371.6    9.7x BETTER
#                       silhouette (euclidean)     0.057 -> 0.404    7.1x BETTER
#                       Davies-Bouldin spherical   1.324 -> 187.98 142x WORSE
#                       ---
#                       mean resultant length      0.238 -> 0.870
#                       effective rank (of 128)  105.9   -> 34.6
#
#                     THREE Panel Q metrics rate the DEGENERATE representation as
#                     substantially better, because radial separation is exactly
#                     what they reward.
#
#                     A CORRECTION TO THE SPECIFICATION. It listed Davies-Bouldin
#                     among the metrics a cone collapse can pass. True of the
#                     Euclidean variant, FALSE of the spherical one, which
#                     already exists at clustering_metrics.py:487 and moved
#                     142-fold. Panel R's warrant is therefore not "nothing
#                     detects this" but ONE METRIC ALARMS AND NOTHING DIAGNOSES:
#                     a spherical Davies-Bouldin of 188 cannot say whether the
#                     information was destroyed or merely badly arranged.
#
#                     WHAT SHIPPED. R1 (directional anisotropy: mean resultant
#                     length raw, centered, and their difference; norm mean, SD,
#                     coefficient of variation, max-to-median, zero-norm
#                     fraction) and R2 (effective rank, normalized effective
#                     rank, participation ratio, spectral entropy, top-k variance
#                     fractions, condition number). Pure functions of a matrix;
#                     every value is a MetricResult carrying its own status.
#
#                     WHAT DID NOT, AND WHY IT IS NOT A STUB. R3 through R7 need
#                     a stored representation. models/gnn.py:357 computes
#                     focal_embeddings and returns only
#                     classifier(focal_embeddings) -- the embedding is discarded.
#                     Those five stages are registered through
#                     CapabilityEvidence as NOT_IMPLEMENTED / ABSENT, and a test
#                     asserts none can satisfy a release gate. A probe panel over
#                     an absent representation would pass vacuously, and a green
#                     Panel R would then be cited as proof the geometry was
#                     checked.
#
#                     A DESIGN FLAW CAUGHT IN REVIEW. The first version returned
#                     UNDETERMINED for a detected collapse, conflating "could not
#                     evaluate" with "found a collapse and cannot classify its
#                     mechanism". Those are different findings and a gate must
#                     treat them differently -- it is the drift monitor's defect,
#                     where UNKNOWN was recorded as none and the notification
#                     never fired once. Split into COLLAPSE_UNDETERMINED and
#                     COLLAPSE_CONE_UNRESOLVED. Five outcomes are now distinct:
#                     healthy, common_direction_growth (linearly removable),
#                     cone_collapse_mechanism_unresolved, undetermined, and
#                     representation_explosion.
#
#                     THRESHOLDS ARE PLACEHOLDERS AND SAY SO. cone at mean
#                     resultant length > 0.5, thin at normalized effective rank
#                     < 0.5. Round numbers chosen in the absence of evidence, not
#                     derived from any representation this project has produced.
#                     Named module constants, overridable per call, with the
#                     three-reference calibration procedure documented for when
#                     an initialization baseline exists.
#
#                     A SABOTAGE THAT FAILED TO FAIL. Mutating n_zero_norm in the
#                     INSUFFICIENT_SUPPORT branch left the suite green: every
#                     assertion exercised the OK branch instead. A refusal must
#                     still report HOW BAD it was -- 31 of 32 zero-norm rows and
#                     1 of 32 are the same status and very different findings.
#                     Test added; the sabotage now fails.
#
#                     A TEST THAT PASSED FOR THE WRONG REASON. The
#                     rows-per-dimension floor was "tested" with 4 rows, which
#                     trips the absolute floor of 8 first and never reached the
#                     branch. Rewritten to use 100 rows against 128 dimensions,
#                     which clears one floor and fails the other, plus a
#                     complement proving the guard does not refuse everything.
#
#                     PROVEN BY SABOTAGE, all seven detected once retargeted:
#                       non-finite silently filtered      -> 2 failed
#                       zero-norm dropped without counting-> 1 failed
#                       rows-per-dimension floor removed  -> 1 failed
#                       unevaluable reports healthy       -> 4 failed
#                       detected collapse -> UNDETERMINED -> 3 failed
#                       an unbuilt stage claims OK        -> 3 failed
#                       norm_cv returns 0 at zero mean    -> 2 failed
#
#   2713  2026-07-21  +55: tests/unit/test_capability_lifecycle.py -- A CAPABILITY
#                     CANNOT CLAIM VALIDATION IT NEVER CLIMBED TO.
#
#                     THE GAP. capabilities.py checks that a declared state is
#                     internally consistent. It cannot check that the state was
#                     REACHED legitimately. Those are different questions, and
#                     only the second prevents a capability being flipped from
#                     "not built" to "validated" in one commit -- which means it
#                     never existed in the state where the interesting defects
#                     live: an artifact produced but wrong, a probe that runs but
#                     leaks, a metric computed on the wrong tensor.
#
#                     The instruction not to make that jump was written as prose.
#                     PROSE DOES NOT ENFORCE ITSELF, which is the same lesson the
#                     Continuous Integration suite-size comment taught when 330
#                     tests could have vanished behind a hand-maintained floor
#                     that still said PASS.
#
#                     THE RULE. Forward moves advance EXACTLY ONE RUNG along
#                     NOT_IMPLEMENTED -> IMPLEMENTED_NO_OUTPUT -> OUTPUT_AVAILABLE
#                     -> VALIDATED. Backward moves are legal at ANY distance, and
#                     DEPRECATED is reachable from anywhere, because discovering
#                     that an artifact is wrong must never be harder to record
#                     than claiming it is right -- a contract that made retraction
#                     awkward would quietly encourage not retracting. A DEPRECATED
#                     capability cannot be revived in place; it must be
#                     re-registered under a new name so the deprecation stays
#                     visible in the record.
#
#                     TransitionVerdict is never a bare boolean: an illegal
#                     verdict MUST carry a reason and a legal one MUST NOT, so a
#                     reader cannot skim a passing verdict's reason and mistake it
#                     for a warning.
#
#                     THE PANEL R MAP. panel_r_expected_ladder() records the rung
#                     each of R3-R7 must climb and what unblocks each step, so a
#                     future claim of "R3 is validated" can be checked against the
#                     PATH TAKEN rather than accepted on trust. It is a map, not a
#                     gate: a test asserts it does not contradict the live
#                     NOT_IMPLEMENTED registrations in representation_geometry.py.
#
#                     A CORRECTION TO AN EARLIER CLAIM. Panel R stage one said
#                     R3-R7 "cannot be built". That was an overstatement. They are
#                     blocked by an OBSERVABILITY gap -- gnn.py returns only
#                     classifier(focal_embeddings) -- and an observability gap is
#                     a refactor, not an impossibility. The reason constants now
#                     name the specific missing artifact rather than restating the
#                     status.
#
#                     TEN LAZY SKIPS REMOVED BEFORE THEY SHIPPED. The first draft
#                     parametrized backward transitions over a 4x4 cross-product
#                     and skipped the forward and identity cases, leaving ten
#                     permanent skips that existed only because the test was
#                     written carelessly. Replaced with a generated pair list, plus
#                     a guard asserting the generator yields n*(n-1)/2 pairs --
#                     an empty list would have made every backward test pass
#                     vacuously.
#
#                     PROVEN BY SABOTAGE, all seven detected:
#                       one-rung rule relaxed to any forward -> 4 failed
#                       a deprecated capability revived      -> 1 failed
#                       backward moves refused               -> 6 failed
#                       type checks removed                  -> 4 failed
#                       R3 and R4 given different blockers   -> 1 failed
#                       the map itself skips a rung          -> 2 failed
#                       a legal verdict carries a reason     -> 1 failed
#
#   2724  2026-07-21  +11: tests/unit/test_gnn_dependency_contract.py -- THE
#                     GNN BRANCH IS CI-COVERED, AND ITS COMMENTS NOW SAY SO.
#
#                     THE STALE COMMENT. Four graph-neural-network test files
#                     carried "# GNN tests need PyG; skip where absent (e.g. CI)"
#                     on their pytest.importorskip("torch_geometric") line:
#                     test_gnn_shared_graph.py, test_gnn_optim.py,
#                     test_gnn_tier2_denoise.py, test_gnn_gps.py. As of today that
#                     is FALSE. torch-geometric is pinned at 2.7.0 in
#                     requirements.txt, and ci.yml carries a hard gate ("Assert
#                     the coverage-critical dependencies are present") that
#                     sys.exit(1)s if torch_geometric is not importable. The GNN
#                     branch is fully covered in Continuous Integration; the skip
#                     fires only on an under-provisioned LOCAL machine.
#
#                     WHY IT WAS WORTH FIXING, NOT JUST COSMETIC. The comment
#                     asserted the exact belief that let the graph-neural-network
#                     branch go untested for 508 runs -- that Continuous
#                     Integration is allowed to skip these tests. A false comment
#                     beside a real safety net is worse than none: it tells a
#                     debugging reader the wrong thing with apparent authority.
#
#                     THE FIX, NOT A DELETION. The importorskip GUARD stays -- a
#                     developer without PyG should get a clean skip, not a
#                     collection error. The COMMENT is corrected to state the
#                     real contract: PyG is pinned, CI fails without it, so this
#                     skip is local-only. Each of the four diffs is one line
#                     removed and six added; the guard line is byte-identical.
#
#                     THE CONTRACT IS PINNED. test_gnn_dependency_contract.py
#                     asserts, without importing torch: (a) none of the GNN files
#                     reintroduce the stale phrasing or any "CI may skip"
#                     comment; (b) ci.yml's REQUIRED gate still lists
#                     torch_geometric; (c) that gate still sys.exit(1)s. So the
#                     comment cannot rot back to a lie, and the CI gate cannot
#                     quietly drop the dependency.
#
#                     TWO DEFECTS IN THE TEST FILE, CAUGHT BY SABOTAGE. First, the
#                     contract file matched its own test_gnn_* glob and scanned
#                     itself -- a false positive by construction; excluded by
#                     name. Second, the assertions searched the WHOLE ci.yml for
#                     "torch_geometric": and "sys.exit(1)", but torch_geometric
#                     also appears in the pip-check gate, so renaming it in the
#                     REQUIRED set alone still passed. Rescoped to the REQUIRED
#                     gate heredoc body, matched with an indentation-agnostic
#                     regex rather than a hard-coded space count.
#
#                     A BROAD-NET TEST WAS REMOVED, NOT ADDED. An earlier draft
#                     carried test_no_gnn_test_file_claims_ci_may_skip, a regex
#                     matching any comment with both "CI" and "skip". It
#                     false-positived on this project's OWN truthful comments --
#                     "Never skips in CI" and the quote-to-refute "do not read
#                     this as 'CI may skip'" -- and no bounded regex separates an
#                     assertion of a falsehood from its negation or its
#                     refutation without natural-language work. It was dropped.
#                     The precise STALE-phrase check catches the one dead phrasing
#                     that ever occurred; the REAL guarantee is the ci.yml gate,
#                     pinned below. A comment is documentation; the gate is the
#                     contract. Net test count is 11, not 12.
#
#                     PROVEN BY SABOTAGE, all four detected:
#                       stale phrase reintroduced          -> 1 failed
#                       torch_geometric removed from gate  -> 1 failed
#                       gate downgraded from sys.exit(1)   -> 1 failed
#                       an importorskip guard deleted      -> 1 failed
#
#   2740  2026-07-21  +16: tests/unit/test_gnn_typed_output.py -- THE GNN
#                     EXPORTS ITS REPRESENTATION WITHOUT CHANGING WHAT IT
#                     PREDICTS.
#
#                     THE DISCARD THIS UNDOES. models/gnn.py computed
#                     focal_embeddings = x[gene_idx] and returned only
#                     classifier(focal_embeddings). That single discard is why
#                     Panel R's R3-R7 are NOT_IMPLEMENTED: every probe, whitening
#                     ladder and trajectory needs a representation the model
#                     never handed out.
#
#                     THE DESIGN, AGAINST THE REAL CALLERS. The proposal in the
#                     specification had forward() ALWAYS return a typed output.
#                     Reading the caller inventory ruled that out: five sites do
#                     out = model(...); F.softmax(out) -- gnn.py:424/473/490 and
#                     ewc.py:186/379 -- and an always-on dataclass breaks all
#                     five in one commit. Instead a keyword-only
#                     return_embeddings=False leaves the default return a bare
#                     (n_focal, 2) tensor, byte-identical to before, so every
#                     existing caller is untouched; return_embeddings=True returns
#                     GNNOutput(logits, focal_embeddings). VariantGATGPS, the
#                     documented drop-in with an identical signature, gets the
#                     same treatment in lock-step so the swap stays valid.
#
#                     THE CONTRACT, AND THE TEST THAT PROVES IT. The exported
#                     tensor must be THE EXACT tensor the classifier consumed --
#                     not a copy, not an adjacent layer. A forward-pre-hook on the
#                     classifier captures its input; the test asserts it equals
#                     output.focal_embeddings bit-for-bit (rtol=0, atol=0). That
#                     holds WITHIN one forward pass, so it does not depend on
#                     dropout determinism and would catch a future refactor that
#                     exported a different layer. Exact-logit invariance is tested
#                     under eval() + inference_mode, where dropout is a no-op, and
#                     the test ASSERTS eval mode rather than assuming it. Gradient
#                     flow is checked in train mode: the encoder still receives
#                     gradients and the exported embedding stays attached to
#                     autograd (detachment is the extraction boundary's job, a
#                     later commit).
#
#                     WHAT THIS COMMIT DOES NOT DO. It does not persist anything,
#                     does not add R3-R7, and does not move any capability past
#                     NOT_IMPLEMENTED. It makes the representation OBSERVABLE.
#                     Per the capability ladder (test_capability_lifecycle.py),
#                     the next legal step for R3-R7 is IMPLEMENTED_NO_OUTPUT, and
#                     it is a separate commit.
#
#                     VERIFICATION BOUNDARY, STATED PLAINLY. These tests require
#                     torch, which the build environment for this delivery could
#                     not install. The control flow was verified with a mock
#                     torch (default -> bare tensor; opt-in -> GNNOutput with the
#                     classifier input); the numerical claims -- invariance,
#                     gradient flow, finiteness -- are proven by the real run on
#                     Monzia's machine before commit. The diff is exactly the
#                     added import, a keyword-only parameter, a docstring and the
#                     branch; the default path changes only
#                     'return classifier(...)' into 'logits = classifier(...);
#                     ...; return logits'.
#
#   2763  2026-07-21  +23: the extraction boundary. THE GNN'S FOCAL
#                     REPRESENTATION IS NOW PERSISTED WITH IMMUTABLE PROVENANCE
#                     AND A ROW-ORDER HASH, AND R3/R4/R5 CLIMB ONE RUNG.
#
#                     WHAT LANDED. src/.../evaluation/representation_artifact.py:
#                     RepresentationArtifact (frozen dataclass) + the
#                     extract_focal_embeddings boundary function. It turns a
#                     GNNOutput carrying focal_embeddings into a persisted,
#                     hashed, provenanced artifact. +20 tests in
#                     test_representation_artifact.py. representation_geometry.py:
#                     panel_r_capabilities advances R3/R4/R5 from NOT_IMPLEMENTED
#                     to IMPLEMENTED_NO_OUTPUT; +3 net tests.
#
#                     THE FOUR GUARANTEES, EACH PROVEN FALSIFIABLE BY SABOTAGE.
#                     (1) IDENTITY: the persisted matrix is exactly the exported
#                     embedding, detached to CPU float64. Sabotage: skip the
#                     detach -> 1 failed; drop float64 -> 1 failed. (2) ROW ORDER:
#                     a SHA-256 over ordered row keys; a reorder is detectable via
#                     verify_row_order. Sabotage: sort keys before hashing (making
#                     it order-insensitive) -> 2 failed. (3) IMMUTABILITY: frozen
#                     dataclass + read-only matrix. Sabotage: remove setflags ->
#                     1 failed. (4) PARTITION ROLE travels with the matrix and is
#                     validated. Sabotage: accept any role -> 1 failed. Plus the
#                     no-embeddings guard -> 1 failed, and the internal hash-
#                     consistency check -> 1 failed. All SEVEN detected; restored
#                     -> 20 passed.
#
#                     TWO TEST DEFECTS SELF-CAUGHT. A first sabotage pass had two
#                     that failed to fail. One was a MALFORMED sabotage (the
#                     replace target used chr(10) where the source has a real
#                     newline literal), re-run correctly and it fired. The other
#                     was a REAL test gap: the no-embeddings test asserted "some
#                     ValueError", and np.asarray(None, dtype=float64) yields
#                     array([nan]) shape (1,), which trips the 2-D shape check and
#                     raises ValueError from the WRONG place -- so removing the
#                     guard left the test green. Pinned the test to the guard's
#                     specific message; removing the guard now turns it red.
#
#                     THE LADDER DISCIPLINE. This commit PERSISTS and stops -- no
#                     probe, no whitening, no metric (a test asserts no such
#                     surface is exposed). Per capability_lifecycle.py the advance
#                     is NOT_IMPLEMENTED -> IMPLEMENTED_NO_OUTPUT, one rung,
#                     verified legal via transition_is_legal. R6 (needs a
#                     checkpoint series) and R7 (needs longitudinal outcomes) do
#                     NOT advance. A test asserts the advance did not make any
#                     stage releasable -- IMPLEMENTED_NO_OUTPUT is still far below
#                     VALIDATED.
#
#                     MIRRORS prediction_artifacts.py: atomic .tmp-then-rename +
#                     fsync writes, JSON manifest with git SHA, parquet not
#                     pickle, library-module logging. Built on the existing
#                     foundation, not a parallel one.
#
#   2786  2026-07-21  +23: Panel R stage R3 -- the norm-angle probe. A
#                     LABEL-FREE LINEAR MAP, FIT ON TRAIN ONLY, MEASURES WHETHER
#                     A COLLAPSED REPRESENTATION'S GEOMETRY IS RECOVERABLE.
#
#                     THE SCIENTIFIC QUESTION R3 ANSWERS. R1/R2 measured
#                     representation collapse; R3 asks whether it is recoverable.
#                     Each embedding splits into a NORM (radial magnitude) and an
#                     ANGLE (direction on the sphere). A cone collapse concentrates
#                     the angles while the norms may still separate structure.
#                     norm_angle_probe.py fits ZCA whitening -- mean and covariance,
#                     no labels -- and measures angular concentration (mean
#                     resultant length: 1=collapsed, 0=spread) before and after.
#                     On a known cone collapse the concentration drops from ~0.95
#                     to ~0.02: the collapse was a linear reparameterisation
#                     artifact a label-free map undoes. That is the answer.
#
#                     THE LEAKAGE GUARD, STRUCTURAL NOT ADVISORY. The whitening
#                     transform is fit on TRAIN only. Fitting mean/covariance on
#                     STRUCTURE or TEST leaks the evaluation distribution into the
#                     map EVEN WITH NO LABELS -- the second-moment structure of the
#                     held-out set is itself information. fit_whitening reads the
#                     RepresentationArtifact's bound partition_role and RAISES
#                     LeakageError on any non-TRAIN artifact. The TRAIN transform
#                     is then applied unchanged to held-out partitions via
#                     apply_whitening, which never refits. Sabotage: remove the
#                     guard -> 5 failed.
#
#                     PROVEN BY SABOTAGE, all seven detected: fit leakage guard
#                     removed -> 5 failed; report anchor guard -> 1; whitening
#                     identity broken -> 1; angular concentration forced to 0 -> 2;
#                     zero-norm rows not excluded -> 2; row-order verify skipped ->
#                     1; transform width check removed -> 1. Restored -> 22 passed.
#
#                     TWO TEST DEFECTS SELF-CAUGHT. Two guards (the report-level
#                     anchor check and the transform width check) were redundantly
#                     covered by a deeper ValueError, so removing them still raised
#                     -- the wrong-reason pattern. Both tests were pinned to their
#                     guards' SPECIFIC messages ("anchor", "cannot whiten shape"),
#                     so removing either now turns the suite red and the guards are
#                     proven to fire independently.
#
#                     THE LADDER. R3 advances IMPLEMENTED_NO_OUTPUT ->
#                     OUTPUT_AVAILABLE (its probe protocol now exists), verified
#                     legal via transition_is_legal. NOT MetricStatus.OK: the
#                     capabilities contract reserves OK for VALIDATED with an
#                     ADMISSIBLE target and a named artifact -- caught at
#                     construction, corrected to INSUFFICIENT_SUPPORT, because an
#                     output existing is not an output validated. R4/R5 stay at
#                     IMPLEMENTED_NO_OUTPUT (their probes are unbuilt); R6/R7 stay
#                     NOT_IMPLEMENTED. A coupled test in test_capability_lifecycle
#                     still passes: the ladder MAP is unchanged, only R3's present
#                     rung moved.
#
#   2800  2026-07-22  +14: record the NEGATIVE R3 transfer-validation result.
#                     THE APPARENT RECOVERY IS A MEAN-CENTERING ARTIFACT THAT DOES
#                     NOT SURVIVE HELD-OUT VALIDATION -- AND THAT IS THE FINDING.
#
#                     THE SCIENCE. R3's norm-angle probe reported a large in-sample
#                     angular recovery (~0.98 on a cone collapse). r3_validation.py
#                     tests it held-out and null-calibrated: on TEST the same
#                     TRAIN-fit whitening recovery is ~0 and does not exceed the
#                     reference null. A decomposition PROVES why: centering alone
#                     accounts for 99.6% of the in-sample drop; centering with the
#                     TRAIN mean recovers ~0.016 on TEST while centering with TEST's
#                     OWN mean recovers ~0.982. The "recovery" was the cone's common
#                     direction removed as a per-partition mean, unavailable at
#                     deployment. The capability contract WORKED: it stopped an
#                     appealing in-sample number from being promoted.
#
#                     THE THREE-CONCEPT RECORD (Monzia's design). ValidationArtifact
#                     separates implementation validity (the code computes the
#                     intended quantity: PASS), statistical/transfer validity
#                     (generalises held-out: FAIL), and admissibility (both: FALSE).
#                     A non-admissible metric MUST carry a finding -- enforced at
#                     construction. Three findings recorded: the transfer failure,
#                     the mean-centering decomposition, and a NULL-ADEQUACY finding.
#
#                     NULL ADEQUACY. The current rotation-only null is EXACTLY
#                     rotation-invariant for angular concentration (verified to
#                     machine precision), so it cannot separate the observed
#                     whitening from chance on this statistic. It is retained ONLY
#                     as an invariance control; a matched-spectrum null family
#                     (eigenvalue-permutation + random-orientation SPD, preserving
#                     the whitening gain spectrum while randomising covariance
#                     alignment) is the correct inferential reference and lands in a
#                     later methodological commit. The p-value is reported as a
#                     Monte Carlo matched-null tail probability, not an exact
#                     permutation p-value.
#
#                     PROVEN BY SABOTAGE (S1-S6 all fire): admissible-ignores-
#                     transfer, transfer-forced-True, p-value-inverted, leakage-
#                     guard-removed (message-pinned distinct from the inner guard),
#                     finding-requirement-dropped, own-mean-decomposition-faked.
#                     The identity-null sabotage is intentionally reclassified: it
#                     does not alter the rotation-invariant estimand, which is a
#                     property of the metric, not a test gap -- addressed by the
#                     matched-null commit. A PERMANENT REGRESSION TEST (common
#                     direction + noise + no transferable signal) locks in that the
#                     validator refuses to promote this class of false discovery.
#
#                     R3 STAYS OUTPUT_AVAILABLE (not VALIDATED): the output was
#                     checked and found NOT admissible. Its capability reason now
#                     cites the negative finding. Release gate unsatisfied. Nothing
#                     hidden, nothing promoted, nothing discarded.
#
2800
