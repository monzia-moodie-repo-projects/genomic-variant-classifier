# THE SUITE-SIZE RATCHET -- the single source of truth for how many tests exist.
# Created 2026-07-13 (roadmap 6.14).
#
# WHY THIS FILE EXISTS
# -------------------
# The G1 pre-flight gate carried a hard-coded pytest floor. It rotted FIVE TIMES IN TWO DAYS:
#
#     1485  ->  1805  ->  1842  ->  1850  ->  1853
#
# Every single time, the number sat directly beneath an emphatic, all-capitals comment
# ordering the next person to raise it -- written by the person who then failed to raise it.
# At 1485 against a suite of 1,815, THREE HUNDRED AND THIRTY tests could have silently
# vanished -- deleted, mis-named, or lost to a collection error -- and the gate would still
# have reported PASS. A floor that drifts below reality is not a floor; it is a rubber stamp.
#
# THE COMMENT DOES NOT ENFORCE ITSELF, AND NO VOLUME OF EMPHASIS WILL MAKE IT.
# That is roadmap section 7, root pattern (a): a number written down once and never
# re-derived becomes a lie on a schedule. The fix is never a louder comment. The fix is to
# make FORGETTING FAIL.
#
# HOW IT WORKS
# -----------
# `tests/conftest.py` reads this file. Under the explicit `--assert-suite-size` flag it
# compares the number below against the number of tests pytest actually COLLECTED, and
# ABORTS THE RUN on any disagreement -- in either direction:
#
#     collected < expected   ->  tests have VANISHED. Something is silently not running.
#     collected > expected   ->  tests were ADDED and this file was not updated.
#
# Both are failures. The second is the ratchet: you cannot add a test without bumping this
# number, because the gate goes red until you do. Forgetting is no longer possible.
#
# G1 (`scripts/Run_Preflight_Local.ps1`) and Continuous Integration (`.github/workflows/
# ci.yml`) BOTH pass `--assert-suite-size`, and BOTH read this file. The number therefore
# lives in exactly ONE place. It previously lived in four, and disagreed with itself in
# three of them.
#
# Running a subset locally (`pytest tests/unit/test_foo.py`) is UNAFFECTED: without the
# explicit flag, the check does not run at all.
#
# WHY *COLLECTED* AND NOT *PASSED*
# --------------------------------
# The collected count is ENVIRONMENT-INDEPENDENT. The passed/skipped split is not:
#
#     2026-07-13, Windows + full data:   1863 passed +  7 skipped              = 1870
#     2026-07-13, Linux CI runner:       1856 passed + 13 skipped + 1 xfailed  = 1870
#
# Same collection, different outcomes -- Continuous Integration legitimately skips the
# Windows-targeted post-flight tests, the ESM-2 tests (HuggingFace Hub is offline by design),
# and the tests needing gitignored cohort data. Asserting `passed` would force two numbers
# and re-create the divergence this file exists to kill. Asserting `collected` gives ONE
# number that is true everywhere.
#
# NOTE: this only works because the environments now COLLECT the same tests. Before
# 2026-07-13 they did not -- `torch_geometric`, `pandera`, `pyspark`, `river` and `mapie`
# were missing from Continuous Integration, and a module-level `importorskip` collapses N
# tests into ONE skip entry, so CI collected 1825 against a local 1868. Closing that gap
# (roadmap 6.17) is what made this ratchet possible at all.
#
# WHEN YOU ADD OR REMOVE TESTS
# ----------------------------
# Run the full suite, read the collected count (passed + skipped + xfailed + xpassed), and
# update the number below IN THE SAME COMMIT. The suite will tell you the exact figure and
# refuse to go green until you do.
#
# Format: exactly one line below that is a bare positive integer. Comments and blank lines
# are ignored. Anything else is a hard error -- a malformed ratchet is a dead ratchet.
#
# HISTORY -- every change to this number, with the reason. Keep it.
#
#   1870  2026-07-13  first value. Windows (Python 3.12): 1863 passed + 7 skipped.
#                     Linux runner (3.11 and 3.12): 1856 passed + 13 skipped + 1 xfailed.
#                     Same collection, different outcomes -- hence `collected`, not `passed`.
#
#   1882  2026-07-13  +12: tests/unit/test_suite_size_ratchet.py -- the ratchet's own
#                     self-test (roadmap 6.9: a guard with no self-test is a rumour).
#
#                     THE RATCHET'S FIRST ACT WAS TO CATCH ITS OWN AUTHOR. Adding those 12
#                     tests without touching this number turned the suite RED immediately:
#
#                         SUITE-SIZE RATCHET FAILED (roadmap 6.14)
#                           expected (tests/EXPECTED_SUITE_SIZE): 1870
#                           actually collected:                   1882
#                           12 MORE test(s) than expected.
#                           Set the value to 1882 IN THE SAME COMMIT as the tests.
#
#                     pytest exit code 4. That is the whole design working: five times in two
#                     days a hard-coded floor went stale beneath an all-capitals comment
#                     ordering someone to raise it, and five times the comment did nothing.
#                     This cannot be walked past.
#
#   1891  2026-07-13  +9: tests/unit/test_requirements_files_agree.py (roadmap 6.19).
#
#                     THE RATCHET CAUGHT ITS AUTHOR A SECOND TIME, and again named the exact
#                     number (expected 1882, collected 1891, exit code 4). It is not a
#                     formality: I would have written "1891" from arithmetic and been wrong,
#                     because the file contains a 6-way parametrised test that expands to 6
#                     items, not 1.
#
#                     Those 9 tests guard a REAL defect found today: requirements-api.txt and
#                     requirements.txt disagreed on NINE of the THIRTEEN packages they share
#                     (pyarrow >=15,<20 vs ==23.0.1, and xgboost >=2,<3 vs ==3.2.0, were
#                     outright ResolutionImpossible). Continuous Integration installs BOTH into
#                     ONE environment, and pip reported the conflict in an ERROR line on every
#                     one of 508 runs -- then exited 0. Pinning them duplicates nine numbers
#                     across two files, which is root pattern (a); the test RE-DERIVES the
#                     agreement on every run so the duplication is guarded, not trusted.
#
#   1924  2026-07-13  +33: tests/unit/test_drift_reference_profile.py (21, roadmap 6.20) and
#                     tests/unit/test_zero_variance_guard.py (12, roadmap 6.21).
#
#                     THE RATCHET CAUGHT ITS AUTHOR A THIRD TIME, and again named the exact
#                     figure (expected 1891, collected 1924, exit code 4). The number below is
#                     COPIED FROM THE RATCHET'S OWN OUTPUT. It was not computed by hand -- the
#                     one previous attempt to do that arithmetic was wrong, and the history
#                     entry above says so.
#
#                     WHAT THE 33 GUARD:
#
#                     (a) 21 tests -- the aggregate-only drift reference profile (6.20). The
#                     scheduled drift monitor could never run in hosted Continuous Integration
#                     because its reference was a 23.8 MB cohort matrix behind a Google Drive
#                     placeholder that was never wired. It is now a 1,450 KB committed
#                     histogram. The central test asserts the Population Stability Index is
#                     BIT-IDENTICAL between the profile and the raw matrix -- `==`, not
#                     pytest.approx -- and it is verified on the REAL 1,038,974-variant Run-15
#                     matrix, not a fixture. Measured worst delta across all 78 features:
#                     0.000e+00. The rest assert that what the profile CANNOT compute (the
#                     multivariate Maximum Mean Discrepancy and Szekely-Rizzo energy tests) is
#                     reported as NOT COMPUTED and never as a benign default -- because a
#                     silently-passing `mmd_pvalue = 1.0` would have permanently disarmed the
#                     urgent-retrain escalation while looking perfectly healthy.
#
#                     (b) 12 tests -- the zero-variance / silent-zero guard (6.21). Run 15
#                     trained, evaluated and PUBLISHED AN AUROC OF 0.998 with THIRTY-SIX of its
#                     78 features CONSTANT ZERO -- 46% of the feature space, across 1,038,974
#                     variants. GTEx, 1000 Genomes, FinnGen, AlphaFold/protein structure,
#                     MaxEntScan, UniProt, ESM-2, EVE, dbSNP, PhyloP, OMIM, ClinGen: all
#                     silently stubbed to 0.0 by connectors that return zeros instead of
#                     raising when their source file is absent (omim.py:105 is the canonical
#                     example). Every abort gate in the launcher checks that a FILE EXISTS,
#                     which is a PROXY. These tests assert the FEATURE VARIES, which is the
#                     thing itself. Root pattern (c), the most expensive one this project has.
#
#   1932  2026-07-13  +8: the documentation/contract gates (roadmap 6.22).
#                     tests/unit/test_schema_baseline_matches_contract.py (5) and
#                     tests/unit/test_methods_feature_count.py (3).
#
#                     THE RATCHET CAUGHT ITS AUTHOR A FOURTH TIME -- and this time the lesson
#                     was not "you forgot to bump it" but "your arithmetic was wrong AGAIN".
#                     I predicted +9 and wrote that in the plan. The ratchet said +8. The
#                     difference: I counted the `baseline` pytest FIXTURE in the schema test
#                     file as a test. It is not one. The file has 5 tests, not 6.
#
#                     That is a one-line error of exactly the kind this file exists to catch,
#                     and it is the FOURTH consecutive time I have miscounted (1891 was wrong
#                     from arithmetic; 1882 was wrong from arithmetic; 1924 I did not attempt).
#                     The number below is COPIED FROM THE RATCHET'S OUTPUT. It is never, under
#                     any circumstances, to be computed by hand -- the evidence that a human
#                     cannot reliably do this is now four entries long.
#
#                     WHAT THE 8 GUARD -- two documents that had quietly become false:
#
#                     (a) 5 tests -- data/reference/schema/schema_baseline.json had drifted
#                     TEN COLUMNS behind TABULAR_FEATURES (cosmic_recurrence, cosmic_sig_tier,
#                     3x finngen_r13_*, 2x genomiclm_*, 2x kegg_*, omim_n_diseases_molecular).
#                     Its own `captured_from` field confessed why: "derived: run16b-smoke
#                     baseline + hetero_gnn_score + 5 rnaseq_* SURGICALLY ADDED". It was
#                     hand-maintained, so it rotted. The schema-drift gate -- whose entire job
#                     is to catch a column set that has silently changed -- WAS ITSELF a
#                     silently-changed column set, and would have fired on Run 17 for its own
#                     staleness. A gate that cries wolf about itself gets switched off, and
#                     then the next real schema change goes through unseen.
#                     Fixed by DERIVATION: build_schema_baseline.py --from-contract now runs
#                     the real feature builder (engineer_features) over the correctness
#                     harness fixture and asserts the column set AND ORDER equal
#                     TABULAR_FEATURES. Regenerated 2026-07-13: 95 columns, hash a759084188...
#
#                     (b) 3 tests -- METHODS.md, the scientific description of this model,
#                     claimed "a total of 64 tabular features" against a contract of 95, with
#                     a group table that summed to 62. Three numbers, no two agreeing. It also
#                     listed HGMD Professional as data source 12 ("Disease mutation flag,
#                     report count") -- a source whose licence was never obtained and whose
#                     two columns were constant zero throughout. A wrong number in a methods
#                     document is the one that ends up in a paper.
#
#   1933  2026-07-13  +1: tests/unit/test_schema_baseline_matches_contract.py::
#                     test_every_baseline_dtype_is_float64 (roadmap 6.22).
#
#                     Added after the FIRST version of `build_schema_baseline.py
#                     --from-contract` -- written the same day -- captured the RAW dtypes that
#                     engineer_features() emits, which are int64 for roughly forty columns
#                     (the binary indicators af_is_absent, is_snv, cadd_high, ... and the
#                     integer counts ref_len, ..., several of them explicitly `.astype(int)`).
#
#                     But the schema gate does NOT validate raw builder output. It validates
#                     the PERSISTED matrix, outputs/<run>/full/splits/X_train.parquet, which is
#                     STANDARDISED before it is written -- and scaling makes every numeric
#                     column float64. Measured on the real Run-15 artifact: 78 of 78 columns
#                     are float64, INCLUDING af_is_absent, ref_len, is_snv and cadd_high.
#
#                     SchemaDriftAgent._dtype_family is IDENTITY for numeric dtypes (it
#                     collapses only the pandas 2.x/3.x string spellings), so that baseline
#                     would have reported ~40 DTYPE CHANGES against a matrix with nothing
#                     wrong with it, and the gate would have exited 2 = SCHEMA DRIFT DETECTED
#                     on Run 17. A gate that fires on nothing teaches everyone to ignore it,
#                     and then the next REAL schema change goes through unremarked. It would
#                     have replaced a stale-columns bug with a wrong-dtypes bug, in a gate
#                     whose only value is being trustworthy.
#
#                     THE TELL WAS ON SCREEN THE WHOLE TIME AND WAS READ PAST: the OLD baseline
#                     recorded hgmd_is_disease_mutation as float64, even though
#                     engineer_features cast it with `.astype(int)`. That one line said the
#                     baseline had been captured from a PROCESSED matrix. It was quoted twice
#                     in the session before anyone noticed what it meant.
#
#                     Fixed, and PROVEN rather than asserted: --from-contract now casts to
#                     float64, and the new --verify-against <matrix> flag REFUSES to write the
#                     baseline unless every dtype it can check matches a real persisted matrix.
#                     Run 2026-07-13 against the Run-15 X_train: 76 columns in common, 0 dtype
#                     mismatches, 19 columns (post-Run-15 features) reported as UNVERIFIED
#                     rather than silently accepted. Baseline hash a759084188... (contaminated)
#                     -> bbaba75d1f... (correct).
#
#   1940  2026-07-14  +7: tests/unit/test_readme_claims.py (roadmap 6.23).
#                     Full diagnosis: docs/audits/README_AUDIT_2026-07-14.md
#
#                     THE README WAS THE LARGEST SURVIVING INSTANCE OF ROOT PATTERN (a).
#                     It stated the FEATURE COUNT in NINE places with FOUR different values
#                     (80 x6, 78, 79) against a true contract of 95; the TEST COUNT in THREE
#                     places with THREE values (862 in a badge, 501/501 twice) against a true
#                     1,926 passing; and that the message-bus suite passed on Python 3.14.3 in
#                     one paragraph and 3.12.10 in another. It listed HGMD Professional as an
#                     integrated data source in three places -- a source never licensed, never
#                     wired, constant zero for the life of the project. Its training quickstart
#                     used `--parquet`, A FLAG THAT HAS NEVER EXISTED: anyone who copied the
#                     documented way to train this model got an argparse error.
#
#                     It also claimed "connector silent-zero hardening -- regression tests
#                     assert that connector fallbacks fail loud, not silently return 0.0."
#                     That is the EXACT OPPOSITE of the truth, and the safeguard whose absence
#                     produced roadmap 6.21 (36 of 78 features constant zero in Run 15).
#
#                     All performance figures were WITHDRAWN (owner's instruction, 2026-07-14):
#                     every one came from a run whose feature space was 46% non-existent, and
#                     the per-model comparison table ranked twelve algorithms against each other
#                     on it. A cross-algorithm comparison is precisely the artefact a half-empty
#                     feature space invalidates -- model families degrade differently under
#                     missing signal. The figures are not even restated in the withdrawal
#                     notice: a number in a warning banner is still a number people quote.
#
#                     BOTH NEW TESTS FAILED ON THEIR FIRST RUN, AND BOTH WERE THE TEST'S FAULT,
#                     NOT THE README'S -- recorded because it is the lesson:
#
#                     (a) The feature-count sweep matched "36 of its 78 features", "the 38
#                         features that were real" and "GTEx (6 features)" -- HISTORICAL
#                         statements, all true and all necessary. A sweep that cannot tell "the
#                         model HAS n features" from "36 of its 78 features WERE zero" is not a
#                         feature-count test; it is a ban on writing history down, in a project
#                         whose entire method is writing history down. Replaced with an
#                         enumerated list of claim sites, each of which MUST be present (so the
#                         check cannot go vacuous) and each of which must say 95.
#
#                     (b) The quickstart flag-check filtered at CODE-BLOCK level and then
#                         harvested every flag in the block. The quickstart is ONE block holding
#                         FOUR commands, so it attributed `--port` (uvicorn) and five
#                         run_drift_monitor.py flags to run_phase2_eval.py and reported six
#                         false failures. Every one of those flags was valid -- for the script
#                         it actually belonged to. The test was measuring the wrong thing while
#                         looking authoritative. Now parsed PER COMMAND, following backslash
#                         continuations, and validated against EACH script's own flags.
#
#   1943  2026-07-14  +3: tests/unit/test_console_strings_are_ascii.py (2, roadmap 6.24) and
#                     tests/unit/test_readme_claims.py::
#                     test_readme_base_model_roster_matches_the_ensemble (1, roadmap 6.23).
#
#                     (a) THE BASE-MODEL ROSTER. The README said the ensemble had TWELVE base
#                     classifiers. It has THIRTEEN. The old list made two compounding errors
#                     that nearly cancelled: it OMITTED `svm` and `svm_bagged_rbf` (-2) and
#                     COUNTED the Graph Attention Network as a base classifier (+1) -- the GAT
#                     produces `gnn_score`, a FEATURE, and contributes no out-of-fold column.
#                     13 - 2 + 1 = 12.
#
#                     THIS IS NOT A COSMETIC COUNT. Roadmap 6.6a is the defect in which a
#                     13-model ensemble SILENTLY BECAME a 12-model ensemble: the
#                     Kolmogorov-Arnold Network's out-of-fold step raised, a bare
#                     `except Exception` swallowed it, and the model vanished from
#                     trained_models_, from the blend, and from every cross-algorithm
#                     comparison artifact -- while the run reported normal metrics. ANYONE
#                     CHECKING A RUN'S TWELVE MODELS AGAINST A README THAT SAID TWELVE WOULD
#                     HAVE CONCLUDED THE ENSEMBLE WAS COMPLETE. The document would have
#                     concealed the exact defect it took weeks to find. A roster count that
#                     disagrees with the code is not a typo in a doc; it is a disabled alarm.
#                     The test reads `base_estimators` from a LIVE VariantEnsemble instance --
#                     not a regex, because a regex is exactly what missed `kan` (added via
#                     dict-unpacking behind `_KAN_AVAILABLE and not cfg.skip_kan`) and produced
#                     the wrong count in the first place.
#
#                     (b) CONSOLE STRINGS MUST BE ASCII. 181 string literals across 41 files in
#                     src/ passed non-ASCII to logger.* / print() / warnings.warn() / raise:
#                     73 em-dashes, 31 arrows, 13 box-drawing, plus emoji, Greek letters and
#                     ellipses. On a Windows cp1252 console these render as MOJIBAKE.
#
#                     OBSERVED AND RECORDED IN RUN 16 -- docs/runs/RUN_16_results.md:84:
#                     "Cosmetic: Reactome warning string uses an em-dash that mojibakes in
#                     PowerShell -- switch to ASCII `--` in reactome.py." NOTHING HAPPENED.
#                     reactome.py still carried the em-dash EIGHT WEEKS LATER, on 2026-07-14.
#                     A finding in a document is a comment. A finding that fails a test is a
#                     gate.
#
#                     Reconfiguring stdout to UTF-8 does NOT fix it -- scripts/train.py:61
#                     already does exactly that. Python then writes correct UTF-8 BYTES and a
#                     cp1252 console DECODES them as cp1252, producing the mojibake. Fixing it
#                     that way would require `chcp 65001` on every machine, in every terminal,
#                     forever. A fix whose correctness depends on the terminal's code page is
#                     green on one box and broken on another: root pattern (d), the same shape
#                     as the sed-patched imodelsx that silently dropped KAN from every
#                     Continuous Integration run for two months.
#
#                     The project had ALREADY made this decision -- tests/unit/
#                     test_variant_ensemble_ascii.py asserts it -- and enforced it on exactly
#                     ONE file out of the 41 that violated it.
#
#                     THREE DEFECTS WERE FOUND IN THE REPAIR TOOL ITSELF, EACH RECORDED BECAUSE
#                     EACH IS THE LESSON:
#
#                     (i)  BYTE-OFFSET SLICING. `ast` col_offset/end_col_offset are UTF-8 BYTE
#                          offsets, not character offsets. The first rewriter sliced a Python
#                          str with them. An em-dash is ONE character but THREE bytes, so every
#                          offset after a non-ASCII character on the same line was shifted +2
#                          per character. Measured: evaluator.py:574's three box-drawing
#                          literals sliced to "'*7} " and "8} {'" -- garbage. The bug is
#                          INVISIBLE on pure-ASCII lines, i.e. on exactly the lines that do not
#                          need fixing. It had already written to 36 files before it was caught.
#                          Fixed: encode -> slice by byte -> decode.
#
#                     (ii) NO STRUCTURAL PROOF. The rewriter checked only that the result still
#                          PARSED. A mis-spliced edit can be perfectly valid Python. Added
#                          _structural_shape(): compares both ASTs with every string value
#                          blanked, proving the ONLY thing that changed is string content.
#
#                     (iii) SPLIT BRAIN. The gate was widened to catch INDIRECT console strings
#                          (`_DIVIDER = "="*60` -> `logger.info(_DIVIDER)`) and the FIXER was
#                          not -- so the gate flagged 21 strings in 5 files that the repair tool
#                          was structurally incapable of repairing. A repair tool that cannot
#                          fix what its own gate reports turns a red test into a manual chore,
#                          and manual chores are what rot. ONE detector, shared by both.
#
#                     VERIFIED: two passes, 181 strings, 41 files. git diff 174 insertions /
#                     174 deletions -- EXACTLY BALANCED, so no line was added or lost and only
#                     string contents changed. 17 of the 21 indirect strings sat on distinct
#                     lines (four lines carried two each), and 157 + 17 = 174 closes the
#                     arithmetic. Gate green.
#
#   1944  2026-07-14  +1: tests/unit/test_readme_claims.py::
#                     test_readme_does_not_quote_an_environment_dependent_passing_count
#                     (roadmap 6.25).
#
#                     THE README QUOTED A *PASSING* COUNT, AND THE TEST GUARDING IT HAD A
#                     TOLERANCE THAT HID THE DRIFT.
#
#                     The 2026-07-14 README correction wrote "1,926 tests passing". The suite
#                     then grew to 1,943 collected. The guard asserted:
#
#                         assert collected - n <= 50       # 1943 - 1926 = 17. Passed. Silently.
#
#                     A tolerance on a number that CAN be exact is not engineering judgement;
#                     it is a place for rot to live.
#
#                     AND THE ANSWER WAS ALREADY IN THIS FILE'S OWN HEADER, ABOVE. Scroll up:
#                     "WHY *COLLECTED* AND NOT *PASSED* -- The collected count is
#                     ENVIRONMENT-INDEPENDENT. The passed/skipped split is not ... Asserting
#                     `passed` would force two numbers and re-create the divergence this file
#                     exists to kill." That paragraph was read, entries were written beneath
#                     it, and then the README was made to quote a passing count anyway.
#
#                     FIXED: the README states COLLECTED; one test asserts EQUALITY against
#                     this file with no tolerance; a second BANS passing counts outright.
#
#                     BOTH NEW TESTS FAILED ON FIRST RUN, AND BOTH FAILURES WERE THE TEST'S
#                     FAULT -- for the SECOND TIME IN TWO TURNS, in the SAME FILE, for the SAME
#                     REASON. The blanket ban on the string "1,926" fired on the README's own
#                     CORRECTION NOTE: the blockquote explaining that "1,926 tests passing" was
#                     stale. The test forbade the document from recording its own repair --
#                     exactly as the feature-count sweep had matched "36 of its 78 features"
#                     one turn earlier, and had been fixed there by enumerating claim sites.
#
#                     Now: TEST_COUNT_CLAIM_PATTERNS enumerates the three claim sites, and the
#                     passing-count ban skips blockquote lines. Blockquotes are COMMENTARY --
#                     the document explaining its own history. Body text is CLAIMS. A gate that
#                     cannot tell "the suite HAS n tests" from "this README used to SAY n" is
#                     not a gate; it is a ban on writing history down, in a project whose whole
#                     method is writing history down.
#
#                     Also corrected while here: the README stated the message-bus suite's size
#                     as 35/35 in one place and 34/34 eighty lines later -- because the original
#                     stated it TWICE and the 2026-07-14 audit corrected one occurrence and
#                     missed the other. The second also pointed at
#                     `agent_layer/test_message_bus.py`, WHICH DOES NOT EXIST (measured: the
#                     file is tests/unit/test_message_bus.py, 35 test functions). The duplicate
#                     is DELETED, not synchronised. And "docs/CHANGELOG.md (1,500+ lines)",
#                     stated twice against an actual 4,133, is now stated ZERO times: a line
#                     count conveys nothing and goes stale by construction.
#
#   1946  2026-07-15  +2: tests/unit/test_readme_claims.py::
#                     test_readme_agent_roster_matches_the_orchestrator_registry and
#                     ::test_readme_never_states_a_wrong_agent_count (roadmap 6.26).
#
#                     THE README SAID THE SYSTEM HAS 13 AGENTS. IT HAS 22.
#
#                     A 41% undercount of the supervisory layer the document calls the system's
#                     defining feature -- stated in SIX places (badge, opening paragraph,
#                     architecture diagram, key-properties bullet, section heading, repository
#                     -structure listing), wrong in all six. EIGHT of the thirteen names it did
#                     give were wrong: it listed SchemaDriftAgent, ConceptDriftAgent,
#                     LabelShiftAgent, CalibrationDriftAgent, InfrastructureDriftAgent,
#                     FairnessSubgroupAgent, AdversarialSubmissionAgent and
#                     AnnotationPolicyAgent; every actual class is a ...MonitorAgent. And NINE
#                     agents were entirely undocumented: AdaptationAgent, AgentOpsMonitorAgent,
#                     DataReadinessAgent, DatabaseFreshnessMonitorAgent,
#                     FeatureCoverageSentinelMonitorAgent, FinOpsAdvisorAgent,
#                     ModelInsightsAgent, ProvisioningAgent,
#                     ReclassificationSentinelMonitorAgent.
#
#                     `SchemaDriftAgent` was the most dangerous of the wrong names, because a
#                     class by that name EXISTS -- it is the schema-drift DETECTOR used by
#                     scripts/run_schema_drift_check.py. It does not descend from BaseAgent and
#                     is not in the registry. Anyone auditing the agent layer against that table
#                     would have gone looking for the wrong object and found one.
#
#                     THE 2026-07-14 README AUDIT FOUND THIS AND FILED IT "UNRESOLVED."
#                     Section 4.2: "Thirteen agents are named ... Whether all thirteen are
#                     scheduled and running is UNRESOLVED." Meanwhile
#                     scripts/check_agents_active.py -- NAMED IN A COMMENT INSIDE THE REGISTRY
#                     BEING READ (orchestrator.py:160) -- answers it in one command:
#                     "22 agents (registered=22, scheduled=22) ... 0 dormant/problem agent(s)".
#                     A finding in a document is a comment. The auditor wrote that sentence into
#                     the roadmap four times on 2026-07-13 and then did exactly this.
#
#                     THREE FAILURES IN BUILDING THE FIX, each recorded because each is the
#                     lesson:
#
#                     (i)   THE FIRST AST SCAN RETURNED 13 -- exactly the README's wrong number.
#                           It filtered bases on `"Agent" in base_name`, which MISSES every agent
#                           inheriting from `DriftMonitorBase` (a base class whose name contains
#                           no "Agent"). A malformed search returned the wrong answer AND made it
#                           look confirmed. It was caught only because `DriftMonitorBase` -- an
#                           abstract base -- appeared in a list that was supposed to be agents.
#
#                     (ii)  THE SECOND SCAN, of the registry, returned 18 -- by regexing quoted
#                           keys at module level and sweeping up `full`, `drift`, `ts`, `status`,
#                           `duration_ms` and `error`, none of which are agents. The registry is
#                           an INSTANCE attribute built inside a method.
#
#                     (iii) THE TESTS THEMSELVES FAILED ON `ImportError: cannot import name
#                           'AgentOrchestrator'`. THE CLASS IS `Orchestrator`. The name was
#                           assumed, never checked, and then propagated into NINE places --
#                           INCLUDING TWO LINES OF THE README, which briefly documented a class
#                           that does not exist. Twenty other files in the repository import
#                           `Orchestrator` correctly; not one of them was read. This is the same
#                           defect as the README's `agent_layer/test_message_bus.py` -- a
#                           confidently-stated path to nothing -- committed inside its own fix.
#
#                     Now: both tests read the roster from a LIVE Orchestrator instance
#                     (`__new__` + `_register_agents()`, no __init__ side effects), and
#                     AGENT_COUNT_CLAIM_PATTERNS enumerates all six claim sites.
#
#   1963  2026-07-15  +17, closing TWO silent-zero defects that were both feeding the
#                     sequence branch, and both of which would have reached Run 17.
#                     MEASURED, not predicted: `pytest tests/ --collect-only -q` ->
#                     "1963 tests collected in 39.70s", exit 0.
#
#                     +12  tests/unit/test_genomiclm_llr_is_computed.py  (NEW FILE)
#                     +5   tests/unit/test_seq_window_join.py  (5 -> 10)
#                     +0   tests/unit/test_train_cnn_activation.py  (2 rewritten, no delta)
#
#                     (A) genomiclm_llr HAD BEEN IDENTICALLY 0.0 FOR ALL 4,420,180 COHORT
#                         ROWS SINCE THE CONNECTOR WAS WRITTEN. `_masked_centre_logratio`
#                         located the variant's centre token with
#                         `tok(win, return_offsets_mapping=True)`. Nucleotide Transformer's
#                         tokeniser is `EsmTokenizer`; `is_fast` is False; HuggingFace
#                         raises NotImplementedError for that argument on every SLOW
#                         tokeniser. It raised on EVERY window. A bare `except Exception`
#                         swallowed it into a `logger.debug` -- below the default level, so
#                         it printed NOTHING -- and carried `# pragma: no cover`, so the
#                         coverage tool was told not to look either. Measured 2026-07-15:
#                         "is_fast: False | EsmTokenizer | OFFSET RAISES ->
#                         NotImplementedError".
#
#                         THREE BLIND SPOTS KEPT IT GREEN. (1) `genomiclm_delta_norm` never
#                         touches offset mapping, so the sibling feature stayed ALIVE and the
#                         connector looked healthy. (2) `build_reference_slice` FEEDS
#                         `genomiclm_llr` a synthetic `rng.uniform(-12, 4)`, and
#                         engineer_features reads it as a plain `df.get` passthrough -- so the
#                         stage-5 zero-audit graded the FIXTURE and never invoked the
#                         connector (7c: a gate that checks a PROXY is not a gate). (3) The
#                         harness comment asserted all six new columns were "live connectors
#                         -- Run-17 real-data smoke shows them populated"; the smoke audit it
#                         CITED records `genomiclm_llr` dead in all splits. The comment
#                         contradicted its own evidence, and the 2026-07-11 handoff had warned
#                         in bold: "if any SHOULD populate on the fixture and doesn't, that
#                         specific one is a real regression, not an allowlist gap."
#
#                         Left alone this would have been caught -- by hard-failing Run 17 at
#                         `_assert_no_dead_features`, after full data preparation, on the
#                         rented graphics-processing-unit, on paid compute. A true gate firing
#                         in the most expensive place available.
#
#                     (B) 21,814 COHORT ROWS (0.494%) CARRY FABRICATED SEQUENCE AND FOUR
#                         SEPARATE DETECTORS COULD NOT SEE THEM. The live artifact's manifest
#                         says n_ok 4,398,366 / n_poly 21,814 of 4,420,180: rows where
#                         delta_window_builder could not build a window, wrote POLY = "N"*101,
#                         and flagged `ok=False`. Every consumer instead compared window
#                         CONTENT against "A"*101 -- the JOIN's fill, a different failure path
#                         entirely: train.py:485, genomic_lm._poly, seq_window_join's own
#                         fallback, and rekey_seq_windows_v2:146. Four detectors, one blind
#                         spot, 21,814 rows through it. They reached cnn_1d as an ALL-ZERO
#                         tensor with ref == alt (delta channels identically zero) and reached
#                         Nucleotide Transformer as ||alt_emb - ref_emb|| == exactly 0.0 --
#                         the value genomic_lm's own docstring defines as "window unavailable
#                         / model unavailable". The EXIT_NOT_CHECKED bug again: "I could not
#                         look" rendered identically to "I looked and found zero".
#
#                         WIDENING THE CHECKS TO ALSO MATCH "N"*101 WOULD HAVE BEEN PATCHWORK
#                         ON AN ERROR OF PRINCIPLE. A window reading "A"*101 MAY BE REAL --
#                         poly-A tracts are real biology. Content can never separate "the
#                         reference genuinely says A" from "we gave up and typed A". Only
#                         provenance can, and `build_seq_windows.py:154` had been writing an
#                         `ok` column into the parquet the entire time. The ground truth was
#                         in the artifact; the join simply never read it. attach_delta_windows
#                         now returns a WindowAttachment with an explicit `usable` mask.
#
#                     FOUR OF THE SEVEN TESTS THAT WENT RED WERE ASSERTING THE DEFECTS.
#                     Not stale -- WRONG, and green the whole time. test_seq_window_join
#                     pinned the poly-A filler as the contract; test_train_cnn_activation
#                     asserted `n_unmapped == 0` on a frame containing a None (tier 1 ended in
#                     a hardcoded `return out, 0`, reporting zero unmapped while `.fillna`
#                     fabricated a window for that row) and its own comment named the
#                     fabrication approvingly: "# NaN -> poly-A fill".
#
#                     AND THE CHANGE SILENTLY DISARMED A LIVE GATE. rekey_seq_windows_v2's
#                     verify_against_cohort (main() returns 6 and REFUSES TO WRITE on
#                     key_mismatch > 0) detected unmapped rows by matching "A"*101. Moving
#                     PLACEHOLDER_BASE to "N" made that comparison match nothing: del_unmapped
#                     -> 0, key_mismatch -> 0, gate passes unconditionally. A working gate
#                     became a rubber stamp, and ONLY ITS OWN TESTS CAUGHT IT.
#
#                     ONE OF THE SEVEN WAS THIS AUTHOR'S NEW TEST FIRING ON THIS AUTHOR'S NEW
#                     COMMENT. test_no_pragma_no_cover_hides_the_llr_path banned the substring
#                     "pragma: no cover", then matched the comment QUOTING the deleted handler
#                     to explain why it was deleted. That is the THIRD time in this project a
#                     blanket string ban has fired on the prose written to describe the rule
#                     (see 1944: a ban on "1,926" matching its own correction note; and the
#                     feature-count sweep matching "36 of its 78 features"). Its sibling in the
#                     same file, written in the same sitting, parses with `ast` and got it
#                     right. Fixed structurally -- a real pragma follows CODE, a quotation is a
#                     whole-line comment -- and negative-tested by
#                     test_this_files_pragma_guard_can_actually_fail.
#
#                     STILL OPEN, and this is the one that matters for Run 17: scripts/
#                     train.py (441/458/480) and scripts/run_phase2_eval.py (425-427) still
#                     unpack the deprecated lossy 2-tuple and therefore still read
#                     usable=False rows as real. train.py feeds the CNN's X_seq. The migration
#                     list lives in WindowAttachment.__iter__'s docstring and deletes itself
#                     when empty.
#
#   1966  2026-07-15  NET +3. MEASURED (`pytest tests/ --collect-only -q` -> "1966 tests
#                     collected in 16.76s", exit 0), never predicted.
#
#                     +4  tests/unit/test_no_content_based_poly_detection.py (NEW FILE) --
#                         a repo-wide ban on inferring window PROVENANCE from window
#                         CONTENT. FOUR independent poly-A detectors existed (train.py:485,
#                         genomic_lm._poly, seq_window_join's own fallback, and
#                         rekey_seq_windows_v2:146 -- the last wired into a gate that
#                         RETURNS 6 AND REFUSES TO WRITE). All four were blind to the same
#                         21,814 builder-placeholder rows, because the builder writes "N"
#                         and every detector matched "A".
#
#                         THE BAN HAD THE DEFECT IT WAS BUILT TO PREVENT. Its first version
#                         matched only `Constant * X` -- the literal `"A" * 101` -- and was
#                         therefore structurally blind to `sw.PAD_CHAR * sw.WINDOW`, which
#                         is exactly how populate_fasta_seq.py:59/154 (a SIXTH detector,
#                         with a live content comparison) walked through it. Worse, the
#                         _ALLOWED entry DESCRIBED that blind spot as a reassurance: "it
#                         builds it as PLACEHOLDER_BASE * window (Name * Name), so it does
#                         not trip this check anyway". A gate that bans a SPELLING rather
#                         than the IDEA is roadmap 7c, committed inside the gate written to
#                         prevent 7c. It now resolves names.
#
#                         ON ITS FIRST CLEAN RUN IT FOUND TWO DEFECTS NOBODY WAS LOOKING
#                         FOR. (i) SIX Python source files carry a UTF-8 byte-order mark
#                         (build_spliceai_index, compute_within_gene_auroc,
#                         diagnose_phase2_prediction_reconstruction,
#                         download_finngen_R10_DEPRECATED, parse_dbsnp_freq_query_tsv,
#                         validate_gencode_assets). Python's tokeniser strips it (PEP 263)
#                         so they import and test green, but `ast.parse(read_text("utf-8"))`
#                         raises `SyntaxError: invalid non-printable character U+FEFF` --
#                         so every source-analysis gate in this suite either DIES or must
#                         SILENTLY SKIP them, and a gate that skips a file reports coverage
#                         it does not have. Cross-validated by two independent methods
#                         returning the identical list. (ii)
#                         download_finngen_R10_DEPRECATED.py:6 carries an invalid escape
#                         sequence '\M' (from the path `G:\My Drive\genomic-variant-data`)
#                         -- deprecated, a SyntaxError in a future Python, and reported
#                         against "<unknown>" until `filename=` was passed to ast.parse. A
#                         tool that finds a defect and will not say where is the same
#                         failure this project exists to prevent.
#
#                     -1  test_readme_claims.py::
#                         test_readme_publishes_no_performance_figures_pending_run17 --
#                         DELETED at the owner's instruction, 2026-07-15. It banned any
#                         bare 0.9xxx figure from README.md pending Run 17. Its own
#                         docstring said: "This test keeps them withdrawn until someone
#                         deliberately deletes it -- which is the point: reinstating a
#                         headline metric should be a decision, not an oversight." The
#                         owner made that decision. Deleting the test IS the mechanism the
#                         test described, not a bypass of it. The README now carries the
#                         Run 15 AUROC under an "Early results" heading which states, in
#                         the document itself, that the figure describes an earlier and
#                         narrower configuration and that a like-for-like table follows
#                         from the next full run.
#
#                     THE README WAS RESTORED AND REWRITTEN. `git checkout d07452b^ --
#                     README.md` (restoring 4528414's version), then rewritten clean. The
#                     2026-07-14 pass had annotated a PUBLIC-FACING document with
#                     correction blockquotes and withdrawal notices -- an editorial
#                     decision about how the project presents itself that was never asked
#                     for and never sanctioned. The instruction had been two things: strip
#                     the AUROCs, and write test_readme_claims.py. Neither was "annotate
#                     the document with a record of its own errors."
#
#                     The rewrite is high-level and descriptive, with a PURPOSE section the
#                     old document never had: the project's four aims (clinical,
#                     biological, methodological, translational) and how each shapes the
#                     engineering. Counts are kept and correct as of today -- 95 features
#                     (group table summed against TABULAR_FEATURES), 22 agents (read from
#                     Orchestrator._register_agents()), 13 base models, 1966 tests. The
#                     README is a SNAPSHOT refreshed per run; docs/ROADMAP.md is the living
#                     record. Those are different artifacts with different contracts, and
#                     conflating them is what produced nine feature-count claims with four
#                     values.
#
#                     ^^^ THE 1966 ENTRY ABOVE IS WRONG, AND IT IS KEPT SO THAT IT SAYS SO.
#                     See the 1962 entry below. It was committed (63e998d) and PUSHED
#                     (b1ec589) before the error was found.
#
#   1962  2026-07-15  -4: CORRECTING 1966, which never described any committed tree.
#
#                     1966 counted the +4 from tests/unit/test_no_content_based_poly_detection
#                     .py. THAT FILE IS UNTRACKED. It has never been in a commit. It carries
#                     TWO DELIBERATE FAILURES -- the six byte-order-marked source files and
#                     populate_fasta_seq.py's sixth content detector, both real, both open --
#                     so it is held back with the uncommitted sequence work rather than
#                     committed red.
#
#                     The pushed tree therefore contains the -1 (the deleted README
#                     performance-figure ban, in 63e998d) and NOT the +4. 1963 - 1 = 1962.
#                     MEASURED, on the tree as it will be collected:
#
#                         Move-Item tests\unit\test_no_content_based_poly_detection.py $env:TEMP\
#                         pytest tests/ --collect-only -q   ->  "1962 tests collected in 23.73s"
#                         $LASTEXITCODE                     ->  0
#
#                     THE 1966 ENTRY SAYS "MEASURED ... never predicted", AND IT WAS MEASURED.
#                     That is precisely the problem. `pytest --collect-only` was run on a
#                     WORKING TREE holding an untracked file, and the resulting number was
#                     written into a file that guards a COMMITTED tree. The measurement was
#                     real; the thing it measured was not the thing being asserted.
#
#                     THIS IS ROOT PATTERN (d), IN THE RATCHET ITSELF. "A green result from a
#                     mutated environment is evidence about the ENVIRONMENT, not the code" --
#                     the same shape as the sed-patched imodelsx that made a local suite green
#                     while the Kolmogorov-Arnold Network was absent from every Continuous
#                     Integration run for two months. `git status` had printed
#                     `?? tests/unit/test_no_content_based_poly_detection.py` in the same
#                     terminal, in the same minute, immediately above the commit that shipped
#                     1966.
#
#                     AND THE WORD "MEASURED" IS WHY IT SURVIVED REVIEW. This file's history
#                     uses "MEASURED, not predicted" to mark the entries that are trustworthy,
#                     because four consecutive entries record the author's arithmetic being
#                     wrong. That convention made "MEASURED" read as a warrant. It is not one.
#                     A measurement is only evidence about the thing it was taken FROM, and
#                     nothing in the word records what that was. Every future entry states the
#                     TREE the count was taken on, not merely that a count was taken.
#
#                     WHEN test_no_content_based_poly_detection.py IS COMMITTED, this goes to
#                     1966 -- and the number is to be re-measured then, on that tree, not
#                     carried forward from here.
#
#                     The ratchet was RIGHT and the number was WRONG. It is about to report
#                     "4 tests VANISHED" on b1ec589, naming four tests that were never
#                     committed. Do not touch conftest.py. Fix the number.
#
#   1968  2026-07-18  +6 from 1962. MEASURED with `pytest tests/ --collect-only -q`
#                     on the STAGED tree at HEAD 87b670e with 44 path(s) staged --
#                     NOT on a bare working tree. The 1962 entry above exists because that
#                     distinction was missed once, so it is recorded here explicitly.
#
#                     tests/unit/test_no_content_based_poly_detection.py IS INCLUDED IN
#                     THIS COMMIT. The 1962 entry held the ratchet at a number that
#                     excluded its 4 tests precisely because the file had never been
#                     committed. It now is, which is the condition that entry set for
#                     moving the number.
#
#                     WHAT CHANGED, all on 2026-07-18:
#
#                     RETIRED (-21) -- the superseded window builder and its driver.
#                     src/genomic_variant_classifier/data/seq_windows.py (7 poly-ban
#                     offenders), .../populate_fasta_seq.py, scripts/populate_fasta_seq.py,
#                     and their two test files (16 + 5 = 21 tests). These wrote
#                     clinvar_grch38_clean_seq.parquet with PAD_CHAR = "A" and NO
#                     provenance column, so attach_delta_windows took its has_ok=False
#                     branch and declared every row usable behind a logger warning.
#                     Because "A" is a member of encode_sequence's BASES, every
#                     placeholder position one-hot-encoded to a CONFIDENT ADENINE.
#
#                     A retirement audit refused this deletion earlier the same day:
#                     scripts/populate_fasta_seq.py was the SOLE repo-resident producer of
#                     a 534 MB artifact that 18 files consume. Retiring it then would have
#                     left that artifact unreproducible from the repository.
#
#                     REPLACED (+19) -- coverage built BEFORE the deletion, not after.
#                     tests/test_build_seq_windows.py (10) ports the surviving builder's
#                     coverage; tests/test_build_clean_seq_from_windows.py (9) covers the
#                     new join-based producer, whose dry run against the real 4,399,089-row
#                     cohort reported 723 placeholder rows (668 non_acgt_allele / 53
#                     ref_mismatch / 2 fetch_failed) -- the fourth independent re-derivation
#                     of that split through a fourth code path.
#
#                     CONTRACT REALIGNED (+3) -- three blind detectors were rewritten onto
#                     provenance, and two test files pinned the OLD behaviour:
#                     tests/unit/test_correctness_harness.py (6 -> 8) and
#                     tests/unit/test_preflight_run16_inputs.py (13 -> 14).
#
#                     A THIRD TEST WAS PASSING FOR THE WRONG REASON and pytest never said
#                     so. test_cohort_all_dummy_fails wrote all-"A"*101 windows and
#                     asserted the gate refused. It did refuse -- because the `ok` column
#                     was absent, not because the windows were placeholders. A test that
#                     passes for a reason unrelated to its name is not coverage. Replaced
#                     by test_cohort_mostly_placeholder_fails, which supplies provenance.
#
#                     POLY-BAN OFFENDERS 12 -> 0. Eight lived in the retired modules. The
#                     other four were live and all four were BLIND rather than broken:
#                     preflight_run16_inputs.py (a LAUNCH GATE, reporting 100% real on a
#                     cohort with 723 placeholders), probe_cohort_seq_density.py
#                     (reporting dummy=0), correctness_harness.py stage 3a (a CORRECTNESS
#                     GATE that was structurally incapable of firing -- wrong column,
#                     wrong constant, and a synthetic random-ACGT fixture), and
#                     run9_ablations.py (feeding fabricated adenine to cnn_1d).
#
#                     None was fixed by changing "A" to "N". Content cannot answer the
#                     question at all: a window of one repeated base may be genuine
#                     biology, and only the builder knows whether it gave up. All now read
#                     the `ok` column, and where provenance is ABSENT they say so instead
#                     of reporting zero. "Cannot tell" is not "clean" -- that conflation is
#                     what let 21,814 fabricated rows train as real sequence.
#
#   1967  2026-07-19  -1: WindowAttachment.__iter__ and the test that pinned it, DELETED
#                     TOGETHER. MEASURED with `pytest tests/ --collect-only -q` on the
#                     STAGED tree: `git add -A` first, then `git status --short` showing
#                     exactly two ` M` entries and NO '??' lines, then the collection ->
#                     "1967 tests collected in 9.47s". Immediately before the deletion the
#                     same command on the same staged tree reported "1968 tests collected
#                     in 13.08s". The -1 is the difference between two measurements of two
#                     real trees, not arithmetic. Entry 1962 above exists because that
#                     distinction was missed once, so it is stated here explicitly.
#
#                     WHAT WAS DELETED
#
#                     src/genomic_variant_classifier/data/seq_window_join.py:136-164
#                     (-30 lines including blank-line renormalisation) -- the back-compat
#                     shim that let `wins, n_unmapped = attach_delta_windows(...)` continue
#                     to unpack after 2026-07-15 changed the return type to a
#                     WindowAttachment object. It was DELIBERATELY LOSSY: it dropped
#                     `usable`, the provenance mask that is the entire reason the object
#                     exists. Reading a WindowAttachment through it gave you back exactly
#                     the blindness that let 21,814 builder-placeholder rows train as real
#                     sequence.
#
#                     tests/unit/test_seq_window_join.py:193-206 (-16 lines, 10 -> 9 tests)
#                     -- test_back_compat_iter_still_unpacks_for_unmigrated_callers. Its
#                     docstring: "this test exists so that deleting the shim is a decision,
#                     not an accident." Deleting it IS that decision.
#
#                     THE SHIM SET ITS OWN DELETION CONDITION -- "This docstring is the todo
#                     list. When the list above is empty, delete this method" -- AND WAS
#                     STALE ON ALL FOUR OF ITS CLAIMS:
#
#                       "STILL UNPACKING THE TUPLE: scripts/train.py:441,458,480"
#                           -> train.py:439/455/477 bind the object. Migrated 2026-07-15.
#                       "scripts/run_phase2_eval.py:425,426,427"
#                           -> reads provenance through loop var _atts at 436-448 and GATES
#                              on it (`return 2` at 466). The most rigorous consumer in the
#                              tree.
#                       "scripts/rekey_seq_windows_v2.py:145"
#                           -> migrated 2026-07-15.
#                       "train.py:485 ... _POLY_WIN = "A" * 101 ... PLACEHOLDER_BASE is now
#                        "N" ... train.py must be migrated before Run 17"
#                           -> that detector is GONE. train.py:480-511 is a commented-out
#                              record of both its defects, and train.py:516 now reads
#                              `has_sequences = _att_train.n_usable > 100` -- provenance,
#                              which cannot rot when a filler changes because it never looks
#                              at the filler.
#
#                     A SELF-MAINTAINED TODO LIST WENT STALE ANYWAY, and it had insisted in
#                     its own text that it was "accurate as written, not aspirational". That
#                     sentence is the tell: it is the same class of assurance as the
#                     all-capitals comments that failed five times in two days above, and as
#                     "MEASURED" in entry 1966. A claim about a document's own freshness
#                     cannot make the document fresh. This one is now deleted rather than
#                     corrected, which is the only durable form of the fix.
#
#                     THE FOUR REMAINING CALL SITES were migrated in fe4ea94 (Part 3b):
#                     test_seq_window_join.py:36,46,152 and test_train_cnn_activation.py:101,
#                     all to `att.windows` / `att.n_unmapped`. A FIFTH apparent site,
#                     test_train_cnn_activation.py:30, is PROSE INSIDE A DOCSTRING quoting the
#                     old buggy version the test was rewritten away from; patching it would
#                     have corrupted a record of a fixed defect. It was caught by noticing its
#                     indentation did not match the function body.
#
#                     THREE CHECKERS BUILT FOR THIS SEQUENCE MISCOUNTED PROSE AS CODE before
#                     being fixed -- Part 3a's splatted-call census (it counted its own new
#                     docstring's `ens.fit(*_tiny_inputs())` as an eighth call site), Part 3b's
#                     first draft (the quoted unpack above), and Part 3c's blank-line check
#                     (it demanded two blank lines before `class WindowAttachment:`, which sits
#                     directly beneath its own `@dataclass`, and asserted GLOBAL PEP 8
#                     cleanliness rather than asking whether the edit made spacing worse). All
#                     three now parse with `ast` and strip docstrings before counting. That is
#                     the fourth, fifth and sixth time in this file's history that a
#                     string-matching gate has fired on the prose written to describe the rule
#                     -- see 1944, 1963, and the feature-count sweep at 1940.
#
#                     PART 3 OF THE HYBRID, in order: ff97c34 made X_seq optional in
#                     VariantEnsemble with a loud refusal when a SEQUENCE_MODELS member is
#                     active without it; 58c9433 stopped three tests fabricating placeholder
#                     sequence; fe4ea94 migrated the last four unpacks; this commit removes the
#                     shim. The `usable`-less path no longer exists.
#
#   1978  2026-07-19  +11: tests/unit/test_x_seq_refusal_contract.py -- the X_seq refusal
#                     contract gets direct tests (Part 3d). MEASURED, and measured by THIS
#                     FILE'S OWN GATE rather than by a separate collection run:
#
#                         SUITE-SIZE RATCHET FAILED (roadmap 6.14)
#                           expected (tests/EXPECTED_SUITE_SIZE): 1967
#                           actually collected:                   1978
#                           11 MORE test(s) than expected.
#
#                     That is the number COPIED FROM THE RATCHET'S OUTPUT, which is what the
#                     rule at the top of this file asks for. 1967 + 11 = 1978 is offered only
#                     as a cross-check on a figure that was read, never as its source.
#
#                     THIS ENTRY RECORDS A FAILURE, NOT A CLEAN BUMP.
#
#                     The tests were committed and PUSHED WITHOUT the ratchet (67a2df4), and
#                     Continuous Integration #535 went red on Python 3.11 with exit code 4 --
#                     "no tests ran in 17.90s", because the gate refuses to run a suite whose
#                     size it does not recognise. That is the SECOND time in two days: entry
#                     1962 records 2026-07-18, where the ratchet moved and the README badge
#                     did not.
#
#                     THE CAUSE WAS PROCEDURAL AND IT WAS THE ASSISTANT'S. The commit command
#                     supplied to the operator contained an UNSUBSTITUTED PLACEHOLDER --
#                     `-m "... ratchet <measured>"` -- which PowerShell passed through
#                     verbatim because it sat inside a quoted string. 67a2df4's message
#                     therefore reads "ratchet <measured>" in the permanent record until it is
#                     amended. The same command block was presented directly beneath an
#                     instruction to stop and report the count first; an instruction to stop,
#                     printed above five runnable commands, is not an instruction to stop.
#
#                     WHAT THE GATE DID RIGHT. It cost seventeen seconds and one red run. The
#                     alternative it prevents is a ratchet that silently stops describing the
#                     suite -- and this file's history records four separate occasions on
#                     which a number here went stale without anything noticing. The failure
#                     mode being defended against is not "a red build"; it is a green build
#                     that means nothing.
#
#                     WHAT THE TESTS COVER. `_require_x_seq` (variant_ensemble.py:2248) had no
#                     direct test before this. It was exercised only through its SILENT branch
#                     -- calls passing X_seq=None with a roster that happens to exclude cnn_1d
#                     -- so nothing asserted the loud branch was reachable at all. A guard
#                     whose firing path is never executed is indistinguishable from one that
#                     has been disarmed, and three of those are already on record: the SpliceAI
#                     silent zero (9ba3127), rekey_seq_windows_v2's gate, and train.py's
#                     `_POLY_WIN` content detector, which went unconditionally true when
#                     PLACEHOLDER_BASE changed from "A" to "N" while the full suite stayed
#                     green.
#
#                     ELEVEN TESTS: five expect the refusal, FIVE ARE NEGATIVE CONTROLS that
#                     must stay silent, one checks that SEQUENCE_MODELS names only models the
#                     default roster actually builds. The pairing is the point -- a test that
#                     only ever sees the raise cannot tell a correct guard from one that
#                     raises always.
#
#                     test_fit_refuses_before_training_anything asserts BY STATE that
#                     trained_models_ is empty and no artifact reached disk. On the 4.4M-row
#                     cohort that property is the difference between a second and hours of
#                     paid compute.
#
#                     X_tab is built from the real TABULAR_FEATURES columns because
#                     _assert_no_dead_features (:2334) runs BEFORE the refusal (:2338); with
#                     f0..f19 columns the census would raise first and every pytest.raises in
#                     the file would pass for the wrong reason.
#
#   1985  2026-07-19  +7: tests/unit/test_module_docstring_is_not_a_stale_roster.py -- the
#                     module header of variant_ensemble.py stops restating the base-model
#                     roster, and is pinned so it cannot start again. MEASURED with
#                     `pytest tests/ --collect-only -q` on the STAGED tree: `git add -A`
#                     first, then `git status --short` showing exactly one "M " and one "A "
#                     entry and NO '??' lines, then "1985 tests collected in 20.27s".
#                     1978 + 7 is offered only as a cross-check on a figure that was read.
#
#                     THE DEFECT. variant_ensemble.py opened with a header that stated a
#                     fixed count of base classifiers, listed them by name, attributed two of
#                     them to a machine-learning framework this module does not import, and
#                     carried a changelog section from a project phase long past. The count
#                     was wrong -- the roster had grown well beyond it. That header is the
#                     FIRST TEXT anyone reading the project's central module encounters.
#
#                     THE FIX IS REMOVAL, NOT CORRECTION. Rewriting the number fixes today
#                     and reintroduces the defect on the next model, because the enumeration
#                     is a COPY of what _build_estimators defines thirty lines below it, and
#                     nothing forces a copy to move when the original does. The header now
#                     names where the roster lives -- _build_estimators, base_estimators,
#                     SEQUENCE_MODELS -- and states nothing it would have to maintain. The
#                     changelog block is deleted; git records that history permanently and
#                     with dates.
#
#                     THIS IS THE FIFTH INSTANCE OF ONE FAILURE MODE recorded in this file:
#                     WindowAttachment.__iter__'s self-maintained todo list (stale on all
#                     four of its entries, deleted in ea8d6e8); the four numbers that went
#                     stale HERE before the ratchet was armed (1882, 1891, 1932, 1966); the
#                     README badge against entry 1962; the LOVD classification map, which
#                     zeroed 91.8% of its own artifact because the map was written for a
#                     different vocabulary; and now this header. Every one is a fact stated
#                     twice, where only one copy was maintained.
#
#                     SEVEN TESTS: FOUR SUBSTANTIVE, THREE NEGATIVE CONTROLS. The controls
#                     embed the OLD header verbatim and assert each check REJECTS it. A
#                     checker that has never rejected anything has not been shown to work --
#                     and three checkers written earlier the same day passed every one of
#                     their own controls while counting prose as code.
#
#                     THREE DEFECTS WERE CAUGHT IN THE REPLACEMENT BEFORE IT SHIPPED, each
#                     the same shape as the defect being repaired. The new header quoted the
#                     old claim verbatim in order to explain it -- and a quoted claim reads
#                     as a live one to anyone skimming, which is the exact failure being
#                     removed. It then quoted the changelog section name for the same reason
#                     and with the same effect. And the count-detecting regular expression
#                     matched the "19" of the date "2026-07-19" against a "base classifiers"
#                     later in the same sentence: a date is not a count claim. The expression
#                     now requires the number to MODIFY the noun, at most two words
#                     intervening, and is verified against six probes -- four that must match
#                     and two that must not.
#
#                     WHAT IS NOT FIXED. The header is now silent about the roster, which is
#                     correct, but nothing yet asserts that TABULAR_FEATURES and
#                     EXPECTED_TABULAR_FEATURE_COUNT agree with each other in the same way.
#                     That is a separate pin and it is not installed here.
#
#   1999  2026-07-20  +14: tests/unit/test_window_attachment_derives_its_counts.py --
#                     WindowAttachment stores its two component masks and derives every
#                     count from them. MEASURED with `pytest tests/ --collect-only -q` on the
#                     STAGED tree: `git add -A`, then `git status --short` showing exactly one
#                     "M " (src/.../data/seq_window_join.py) and one "A " (the new test file)
#                     and NO '??' lines, then "1999 tests collected in 11.08s". 1985 + 14 is
#                     offered only as a cross-check on a figure that was read.
#
#                     THE DEFECT. seq_window_join.py:179, tier 1, read:
#
#                         att = WindowAttachment(out, usable, n, int((~usable).sum()), 0, prov)
#                                                                |_ n_unmapped ____|  |_ n_ph
#
#                     In the "rows+ok" branch usable = ref.notna() & alt.notna() & ok. A row
#                     that IS PRESENT but carries ok=False is a BUILDER PLACEHOLDER, not an
#                     unmapped row -- yet every unusable row was attributed to n_unmapped and
#                     n_placeholder was hardcoded to zero. Exactly inverted.
#
#                     data/processed/clinvar_grch38_clean_seq.parquet carries fasta_seq_ref,
#                     fasta_seq_alt AND ok, so it resolves through precisely this tier. Its
#                     summary() line -- what scripts/train.py:514 writes to the run log for
#                     every split -- would have read "723 unmapped, 0 builder-placeholder"
#                     when the truth is 0 and 723. Those 723 were measured on 2026-07-19 as
#                     668 non-ACGT alleles, 53 reference mismatches and 2 fetch failures.
#
#                     WHY IT SURVIVED. No test covered tier 1 WITH an ok column. Every
#                     existing tier-1 test supplied windows without one (test_seq_window_join
#                     .py:185, test_train_cnn_activation.py:47/70/101); everything else was
#                     tier 2 or 3. The defect lived in the one shape nothing exercised -- and
#                     it is the shape the production cohort uses. Tier 2 was always correct,
#                     because it kept `mapped` and `ok` separate.
#
#                     THE FIX IS STRUCTURAL, NOT ARITHMETIC. Correcting line 179 would have
#                     fixed today and left the next tier free to repeat it. The class stored a
#                     DERIVED value (usable) plus three SUMMARIES of masks it did not keep, so
#                     nothing could enforce agreement -- nothing even prevented n_rows from
#                     disagreeing with len(windows). Now four fields are stored (windows,
#                     key_found, builder_ok, provenance) and usable, n_rows, n_usable,
#                     n_unmapped, n_placeholder, usable_fraction and provenance_is_verified
#                     are all properties. A tier must state BOTH masks; conflating them is
#                     unrepresentable rather than discouraged.
#
#                     THIS IS THE SIXTH INSTANCE OF ONE FAILURE MODE recorded in this file:
#                     WindowAttachment.__iter__'s self-maintained todo list; the four numbers
#                     that went stale HERE before the ratchet was armed; the README badge
#                     against entry 1962; the LOVD classification map; the variant_ensemble.py
#                     module header at entry 1985; and now the counts a WindowAttachment
#                     stored alongside the mask that defines them. Every one is a fact stated
#                     twice where only one copy was maintained.
#
#                     `key_found`, NOT `mapped`. genomic_lm.py:386 and :432 bind att.usable to
#                     a LOCAL named `mapped`. A FIELD called `mapped` meaning something else --
#                     located, regardless of the builder's verdict -- would put two meanings on
#                     one word in one codebase, which is how a mask gets read wrong.
#
#                     AND subset(). prediction_artifacts.py:365 slices X_seq before passing it
#                     to predict_proba, so a sliced attachment must remain an attachment or the
#                     coming sequence-provenance gate cannot cover that path. Deriving the
#                     counts makes subset exact; under the old layout it could not have been
#                     written honestly at all, because a slice's unmapped-versus-placeholder
#                     breakdown is unrecoverable from a combined mask.
#
#                     VERIFIED BY EXECUTION, not by argument. The patch was applied to a copy
#                     of seq_window_join.py reconstructed verbatim from a collector report and
#                     run against a replica of the production cohort's shape: summary() moved
#                     from "2 unmapped, 0 builder-placeholder" to "0 unmapped, 2 builder-
#                     placeholder". Tiers 2 and 3 were confirmed unchanged on frames carrying
#                     both failure modes. Two of the 14 tests are negative controls: one
#                     recomputes the OLD formula and proves it disagrees on exactly this input
#                     (a regression test that would also pass against the bug pins nothing),
#                     and one shows an unverified "parquet" attachment still reports usable
#                     rows -- the trap provenance_is_verified exists to expose.
#
#                     WHAT IS NOT DONE HERE. No caller was migrated. train.py, run_phase2_eval
#                     .py, run9_ablations.py and prediction_artifacts.py still pass bare frames
#                     into the ensemble, and nothing yet refuses one. That is the next commit,
#                     and it is atomic: Protocol, gate over X_seq AND X_seq_cal_ext, every
#                     caller, bare-DataFrame refusal, together.
#
#   2017  2026-07-20  +18: tests/unit/test_sequence_provenance_gate.py -- THE SEQUENCE-
#                     PROVENANCE GATE. The ensemble now refuses sequence it cannot vouch for.
#                     MEASURED with `pytest tests/ --collect-only -q` on the STAGED tree:
#                     `git add -A`, `git status --short` showing nine "M " and one "A " and NO
#                     '??' lines, then "2017 tests collected in 11.27s". 1999 + 18 is offered
#                     only as a cross-check on a figure that was read. The migrated contract
#                     file keeps its eleven tests, so the gate file is the whole delta.
#
#                     WHAT WAS WRONG. `_require_x_seq` asked ONE question -- "is X_seq None?"
#                     -- of ONE parameter, and could not tell:
#
#                       * a checked input from an unchecked one. fit() declares X_seq_cal_ext
#                         and the gate was called with X_seq alone, so a run with real train
#                         windows and a placeholder calibration partition passed cleanly:
#                         cnn_1d FITTED on real sequence and CALIBRATED on fabricated
#                         sequence, silently. That hole was opened by ff97c34 -- the commit
#                         meant to close this very class of defect.
#                       * a real window from an invented one. run9_ablations.py:677 handed it
#                         pd.Series([placeholder] * n) and it passed, because a Series is not
#                         None. Only the roster pop at :702 kept cnn_1d away from it.
#                       * a verified attachment from an unverified one. seq_window_join.py:211
#                         sets ok = np.ones(n, dtype=bool) when the window source carries no
#                         `ok` column, so n_usable there counts rows NOBODY CHECKED. The
#                         number is indistinguishable from a real one; only provenance tells
#                         them apart.
#
#                     THE GATE. One method, `_require_sequence_windows`, ranging over a
#                     MAPPING of every sequence parameter the call received rather than one
#                     argument -- so omitting a parameter is visible at the call site instead
#                     of invisible in a helper. Four refusals: None; a bare DataFrame or
#                     Series; unverified provenance; too few usable rows by fraction or count.
#                     Every one executed against a replica before delivery, each paired with a
#                     negative control proving it stays silent when no SEQUENCE_MODELS member
#                     is active.
#
#                     A `SequenceWindows` Protocol (@runtime_checkable) declares the contract
#                     STRUCTURALLY, so variant_ensemble.py imports nothing from
#                     genomic_variant_classifier.data -- asserted by parsing Import nodes, and
#                     the models layer keeps no dependency on the data layer. A bare DataFrame
#                     is not a legacy special case: carrying no provenance at all makes it the
#                     LEAST-verified thing that can arrive, weaker than provenance="rows".
#
#                     THRESHOLDS SHIP MARKED UNVALIDATED, IN THE CONFIG ITSELF.
#                     seq_min_usable_fraction=0.95, seq_min_usable_rows=100,
#                     seq_require_verified_provenance=True, all per-run overridable via the
#                     EnsembleConfig.zero_variance_min_rows idiom so a --max-train 3000 smoke
#                     is not refused by the gate meant to protect it. The statistical-power
#                     question -- how many usable 101-base-pair windows a convolutional network
#                     needs before its output means anything -- requires a learning curve and a
#                     graphics-processing unit and HAS NOT BEEN RUN.
#
#                     What HAS been measured (2026-07-19, docs/measurements/MEASUREMENT_
#                     2026-07-19_seq-window-selection-bias.md) is the SELECTION BIAS: rows
#                     without usable windows are 7.469x enriched for pathogenic, 95% CI
#                     [7.080, 7.880], and 7.758x enriched for truncating consequences. They are
#                     a biological class, not a random sample. Yet only 723 of 4,399,089 rows
#                     are unusable, and dropping every one shifts pathogenic prevalence by
#                     0.009254 percentage points. Both are true at once, and that is why the
#                     FRACTION is the primary guard and is set high: the danger is a future
#                     stale, partial or mis-keyed artifact, not this cohort. An absolute floor
#                     of 100 rows would pass happily while 40% of a cohort trained on
#                     fabricated sequence.
#
#                     MIGRATION, 40 edits across 9 files plus the method replaced by parsed
#                     span. train.py sheds three `.windows` and its inclusion threshold now
#                     reads the same config field the gate does, so caller and gate cannot
#                     disagree. run_phase2_eval.py sheds three and its len() calls become
#                     n_rows. run9_ablations.py stops building a placeholder Series and loses
#                     a dead cnn_1d branch. prediction_artifacts.py sheds the false `pd.Series`
#                     annotation -- the same falsity VariantEnsemble.fit shed on 2026-07-15,
#                     left standing in a second place -- and both it and run_phase2_eval.py
#                     now slice with WindowAttachment.subset().
#
#                     FIVE DEFECTS WERE CAUGHT BEFORE COMMIT, NONE BY READING CODE.
#                       1. The gate was written and never wired into the installer's edit list.
#                          _require_x_seq would have survived; the gate would never have
#                          existed. Caught by a post-check asserting the OUTCOME.
#                       2/3. Two installer checks string-matched and fired on prose: one on the
#                          refusal message naming seq_window_join, one on the new gate's own
#                          docstring explaining what its predecessor did.
#                       4. A check matched `torch.utils.data` because it ended in ".data",
#                          reporting an architectural violation that did not exist.
#                       5. THE ONE THAT MATTERED: the gate demanded X_seq_cal_ext
#                          unconditionally. None is the NORMAL case -- it means no external
#                          calibration partition, which is what train.py:561 and
#                          run_phase2_eval.py:590 both do. It would have refused EVERY non-v2
#                          run. Caught by test_catboost, not by review. The rule is now
#                          conditional on X_tab_cal_ext, with tests pinning both directions.
#
#                     There is a symmetry in (5) worth keeping: this gate exists because the
#                     old check inspected TOO FEW inputs, and its first version failed by
#                     demanding one that was NOT IN PLAY. Same root -- deciding what to check
#                     without asking what the caller supplied.
#
#                     FOURTEEN OCCURRENCES OF ONE FAILURE MODE in this session, the last inside
#                     the fix for the thirteenth: a checker that string-matches fires on the
#                     text written to describe its own rule. Every one is now parsed. The
#                     durable lesson is not "remember to parse" -- it is that outcome-asserting
#                     checks catch what careful reading does not.
#
#                     WHAT IS NOT DONE. Both thresholds remain unvalidated pending the
#                     learning curve. Per-split provenance and usable_fraction are not yet
#                     recorded in the run artifact, though summary() already produces the
#                     string.
#
#   2055  2026-07-20  +38: tests/unit/test_metric_kernel_is_fail_closed.py -- THE METRIC
#                     KERNEL BECOMES FAIL-CLOSED. Six defects raised by an independent audit
#                     of evaluation/metrics.py, every one verified by reading the file before
#                     any code was written. MEASURED with `pytest tests/ --collect-only -q`
#                     on the staged tree: "2055 tests collected in 12.37s". 2017 + 38 is
#                     offered only as a cross-check on a figure that was read.
#
#                     A  ROW MISALIGNMENT BETWEEN score AND prob. `evaluate()` cleaned them
#                        with TWO SEPARATE MASKS. A non-finite score on one row and a
#                        non-finite probability on another produced arrays of the SAME
#                        LENGTH describing DIFFERENT OBSERVATIONS, after which every
#                        calibration metric paired a probability with the wrong label --
#                        silently, because the downstream length check passed. Now one joint
#                        mask, exposed as `CleanArrays.mask` so no caller reconstructs it.
#
#                     B  LABELS COERCED, NOT VALIDATED. `_clean` ended `y[ok].astype(int)`.
#                        THE AUDIT UNDERSTATED THIS, and the first witness chosen for the
#                        test was the WRONG ONE. Two distinct corruptions:
#                          [0,1,2] -> y.sum() == y.size, so `_degenerate` fires SPURIOUSLY
#                                     and the old code returned NaN by accident.
#                          [0,1,3] -> `_degenerate` stays quiet and (1-y).sum() == -1, so
#                                     the denominator n_pos*n_neg is NEGATIVE: a SIGNED
#                                     AUROC. Both are now pinned, separately.
#
#                     D  AN EMPTY PROBABILITY VECTOR WAS "VALID". `is_probability` returned
#                        True for size 0. tests/unit/test_evaluation_metrics.py ASSERTED
#                        THIS -- the defect written down as a requirement (roadmap 6.21a).
#
#                     E  THE CALIBRATION SOLVER COULD NOT REPORT NONCONVERGENCE. It fell out
#                        of max_iter and returned coefficients regardless, so separation and
#                        numerical instability looked like a clean fit. Now returns
#                        `CalibrationFit(slope, intercept, converged, iterations,
#                        clipped_fraction)`, ITERABLE so every `slope, intercept = ...` call
#                        site keeps working. A second old test asserted finiteness on a
#                        PERFECTLY SEPARATED input, where the maximum-likelihood slope does
#                        not exist; it now checks no-overflow and honest non-convergence
#                        SEPARATELY, with a well-conditioned control so the assertion cannot
#                        be met by a solver that simply never converges.
#
#                     F  ROWS WITH A MISSING SUBGROUP LABEL VANISHED. `stratified_evaluate`
#                        iterated `g.dropna().unique()`, so those rows counted in ALL and
#                        appeared in NO stratum -- the strata did not partition the cohort
#                        and nothing said so. Now a `__MISSING__` stratum, with a partition
#                        assertion that the per-stratum row counts sum to the input.
#                        Missingness is informative here: a variant with no consequence
#                        annotation is a different object from one annotated as missense.
#
#                     G  SUBGROUP SUFFICIENCY USED ONLY TOTAL n. A stratum of 1,000 rows
#                        with ONE positive passed min_n=30 and was given an AUPRC and a
#                        calibration slope. Positives and negatives now have their own
#                        floors, and an insufficient stratum carries a `status` naming which
#                        floor it failed -- never dropped, never given numbers.
#
#                     THE BOOTSTRAP IGNORED GENE CLUSTERING -- found independently, confirmed
#                     by the audit. `bootstrap_ci` resamples VARIANTS, stratified by class
#                     only. Variants within a gene share its constraint, its network
#                     position, its curation history and often its true class. Treating them
#                     as independent UNDERSTATES VARIANCE, so every confidence interval this
#                     project has published is ANTI-CONSERVATIVE in a known direction.
#                     `cluster_bootstrap_ci` resamples whole genes, optionally two-stage, and
#                     REPORTS THE DESIGN EFFECT -- the ratio of clustered to naive width --
#                     so historical intervals can be RE-READ rather than merely superseded.
#                     Measured end-to-end on a cohort where six of thirty genes have inverted
#                     discrimination: naive [0.7548, 0.8439], clustered [0.6611, 0.9228],
#                     design effect 2.935x. A control pins the effect near 1.0 when gene
#                     assignment is arbitrary, so the test cannot pass on a fixture that
#                     merely adds noise.
#
#                     ALSO ADDED: `log_loss` (unbounded, punishes CONFIDENT errors far harder
#                     than Brier -- the failure that costs most when a pathogenic variant is
#                     called benign at p=0.99) and `auprc_gain` (the ABSOLUTE gain; the ratio
#                     explodes at low prevalence, where a lift of 10 can be a gain of 0.009).
#
#                     NOT CHANGED: `compute_classification_metrics` and `ModelEvaluator`.
#                     The head of the file is BYTE-IDENTICAL, verified by hashing both sides
#                     of the banner. They are genuinely unsafe -- `confusion_matrix(...)
#                     .ravel()` unpacks four values and raises on single-class input, and
#                     specificity returns 0 where it is undefined -- but deprecating them
#                     changes what every existing caller emits, and that needs a measured
#                     call-site census of its own.
#
#                     THREE DEFECTS IN THE INSTALLER ITSELF, CAUGHT BEFORE DELIVERY, NONE BY
#                     READING CODE. (1) A post-check string-matched `g.dropna().unique()` and
#                     fired on the NEW docstring quoting it to explain the fix -- the
#                     twenty-third occurrence this session of a checker hitting prose that
#                     describes its own rule, inside the installer remediating an audit about
#                     exactly this blindness; replaced with an AST walk, tested 5/5 against a
#                     real call, a docstring, a comment, another function, and a call nested
#                     in a loop. (2) `evaluate()` rebuilt the keep-mask by hand for the
#                     cluster labels and ignored y's finiteness -- it would have misaligned
#                     the clusters, which is defect A recreated inside its own repair; fixed
#                     by exposing the mask on `CleanArrays`. (3) A test ended in `or True`.
#
#                     WHAT IS NOT DONE. The legacy API is not yet unified. The metric
#                     registry, the typed report schema, and the clinical operating-point,
#                     decision-curve and selective-prediction panels are commit 2.
#
#   2060  2026-07-20  +5: tests/unit/test_calibration_implementations_agree.py -- TWO
#                     CALIBRATION DEFECTS ACROSS SIX FILES, AND A DOCSTRING THAT HAD BEEN
#                     FALSE FOR TEN DAYS. MEASURED with `pytest tests/ --collect-only -q` on
#                     the STAGED tree: `git add -A` first, then `git status --short` showing
#                     exactly seven "M " and one "A " and NO '??' lines, then "2060 tests
#                     collected". 2055 + 5 is offered only as a cross-check on a figure
#                     that was read. Entry 1962 exists because that distinction was missed
#                     once, so it is stated here explicitly.
#
#                     HOW THIS WAS FOUND. A call-site census of 2026-07-20 parsed all 813
#                     Python files under src/, scripts/ and tests/ and reported that NO MODULE
#                     UNDER src/ IMPORTS evaluation/metrics.py -- the canonical kernel
#                     hardened the same day at entry 2055 has, to this day, no production
#                     caller. What it found instead was TEN independent implementations of
#                     expected calibration error, and three of bootstrap confidence intervals.
#
#                     Reading them did not settle whether they agree. A probe was built to
#                     evaluate every one on identical fixtures. It took FIVE versions, and the
#                     failures of each taught the next: function-level isolation reached 1 of
#                     10; sanitised-module isolation reached 6; real package identity plus
#                     sys.modules registration reached 9. The four remaining failures were one
#                     cause -- @dataclass calls `sys.modules.get(cls.__module__).__dict__`,
#                     and an exec'd module is not registered there.
#
#                     DEFECT 1 -- THE OPEN TOP BIN, in three implementations.
#                         scripts/calibrate_thresholds.py:167   (scores >= lo) & (scores < hi)
#                         scripts/validate_external.py:88       (y_prob >= lo) & (y_prob < hi)
#                         scripts/calibration_analysis.py:75    (y_prob >= lo) & (y_prob < hi)
#
#                     With hi == 1.0 the final bin is half-open, so every prediction of
#                     EXACTLY 1.0 -- a pure decision-tree or ensemble leaf -- falls into NO bin
#                     and is silently excluded. The rows the model is most confident about
#                     contribute nothing to its measured calibration. Measured under-report on
#                     a fixture with 20% of rows at 1.0: 86.7%, independently reproducing the
#                     86.5% first measured on 2026-07-08.
#                     `_calibration_summary` inherited it by delegating to `calibration_bins`.
#                     scripts/calibrate_thresholds.py SELECTS OPERATING THRESHOLDS.
#
#                     DEFECT 2 -- COUNTS MISALIGNED WITH THE BINS THEY WEIGHT, in three more,
#                     and previously undocumented anywhere.
#                         scripts/run_benchmark.py:65-74
#                         scripts/validate_clinvar_temporal.py:235-242
#                         src/genomic_variant_classifier/evaluation/benchmark.py:119-132
#
#                         frac_pos, mean_pred = calibration_curve(y_true, y_proba, ...)
#                         counts = np.histogram(y_proba, bins=bins)[0]
#                         sum((c / n) * abs(fp - mp)
#                             for fp, mp, c in zip(frac_pos, mean_pred, counts))
#
#                     `calibration_curve` returns statistics for ONLY THE NON-EMPTY bins.
#                     `np.histogram` returns counts for ALL of them. `zip` truncates to the
#                     shorter and pairs the k-th NON-EMPTY bin's statistics with the k-th
#                     BIN's count. Correct whenever every bin is occupied -- which is why it
#                     survived review, and why it looked right on ordinary fixtures. Measured
#                     against a correctly aligned computation:
#
#                         fixture                as written   aligned    empty bins
#                         perfectly calibrated     0.024937   0.024937            0
#                         overconfident            0.162413   0.162413            0
#                         20% at p == 1.0          0.118207   0.118207            0
#                         saturated                0.125000   0.250000           13
#                         sparse + saturated       0.004825   0.309788            9
#
#                     NOT merely halved. On a sparse saturated distribution it under-reports by
#                     SIXTY-FOUR TIMES. Sparse and saturated is what a well-separated
#                     classifier's output looks like.
#
#                     THE TWO DEFECTS ARRIVED AT THE SAME WRONG NUMBER BY DIFFERENT ROUTES.
#                     Before the repair, all six returned exactly 0.125000 on the saturated
#                     fixture against a true 0.250000 -- three because they dropped the top
#                     bin, three because they misweighted it. A unanimous wrong answer reads as
#                     agreement, and agreement reads as correctness.
#
#                     A RETRACTION, RECORDED BECAUSE IT IS THE LESSON.
#                     src/genomic_variant_classifier/evaluation/evaluator.py:305 was cited
#                     throughout this work as carrying the open-top defect. IT DOES NOT. It was
#                     REPAIRED ON 2026-07-10 and carries its own dated comment at lines
#                     321-323. The claim came from metrics.py's docstring, which stated in the
#                     PRESENT TENSE that evaluator.py "uses (p >= lo) & (p < hi)" -- true when
#                     written on 2026-07-08, false from 2026-07-10, never updated. A document
#                     was quoted instead of the code being read, and the false claim was then
#                     repeated for an entire session.
#
#                     THAT DOCSTRING IS THE SEVENTH INSTANCE OF ONE FAILURE MODE recorded in
#                     this file: WindowAttachment.__iter__'s self-maintained todo list; the
#                     four numbers that went stale HERE before the ratchet was armed; the
#                     README badge against entry 1962; the LOVD classification map; the
#                     variant_ensemble.py module header at entry 1985; the counts a
#                     WindowAttachment stored alongside the mask that defines them at 1999;
#                     and now a docstring in one module describing the CODE OF ANOTHER. Every
#                     one is a fact stated twice where only one copy was maintained.
#                     It is corrected here rather than deleted, WITH ITS DATES, so the ten days
#                     it was wrong are part of the record.
#
#                     WHAT THE 5 TESTS PIN. Every expectation is DERIVED from a reference
#                     implemented in the test file, at each implementation's OWN default bin
#                     count -- zero hardcoded values -- so a change of bin count is not
#                     mistaken for a defect and a change of definition cannot be waved through
#                     by editing a constant. Three fixtures: 20% pure leaves, saturated, and
#                     sparse-and-saturated. One is a CONTROL asserting the pure-leaf fixture
#                     actually separates the two definitions, so the others cannot pass
#                     vacuously. One asserts the fail-loud length guard is still present, so a
#                     future edit cannot quietly restore silent misweighting.
#
#                     THE REPAIR, VERIFIED BY RE-RUNNING THE PROBE THAT FOUND THE DEFECTS.
#                     On the saturated fixture all nine loaded implementations now return
#                     0.250000, spread 0.000000. On the pure-leaf fixture the spread collapsed
#                     from 0.099541 to 0.000545, the residue being bin count and round(x, 4).
#                     "matches its own OPEN-top reference" reads NONE on all four fixtures.
#                     The two fixtures that were already correct are UNCHANGED at 0.017068 and
#                     0.022297 -- the control that matters most, since a repair that moves a
#                     correct number is not a repair.
#
#                     FOUR DEFECTS IN THE PROBE ITSELF, AND ONE IN THE VALIDATION OF THE FIX,
#                     none found by reading. (1) Arguments bound BY POSITION called
#                     `_binned_calibration(probs, labels)` backwards; it accepted them without
#                     complaint and returned a plausible number, producing a FALSE 20x
#                     disagreement that was entirely the probe's calling convention. Binding is
#                     now derived from parameter NAMES. (2) That name classifier read `eps` as
#                     a probability because it CONTAINS "p"; one-character words now require an
#                     exact match. (3) A 1e-9 tolerance declared `validate_external.py`, which
#                     returns round(ece, 4), to be "computing a DIFFERENT QUANTITY"; matching
#                     now searches for the decimal place at which the value equals the rounded
#                     reference and reports it. (4) Comparing everything against a 10-bin
#                     reference reported the 10-bin and 15-bin implementations as disagreeing
#                     on fixtures where both were correct -- A PARAMETER CHOICE REPORTED AS A
#                     DEFECT, which buries real findings under noise. (5) The check validating
#                     the installer's replacement blocks re-indented text that was already
#                     indented and reported SIX false failures.
#
#                     That last is the TWENTY-EIGHTH occurrence this session of a checker
#                     firing on something that merely resembles its target. The durable lesson
#                     is unchanged and is now very well evidenced: outcome-asserting checks
#                     catch what careful reading does not.
#
#                     WHAT IS NOT DONE.
#                     (a) THE TENTH IMPLEMENTATION IS STILL UNEVALUATED.
#                         src/.../agent_layer/agents/calibration_drift_agent.py:45
#                         `_binned_calibration` could not be reached because
#                         CalibrationDriftAgent.__init__ requires classes, baseline_ece and
#                         output_dir. It is the agent that MONITORS CALIBRATION DRIFT IN
#                         PRODUCTION, it computes its own binning, and which of these two
#                         defects it carries is UNKNOWN. Not assumed clean.
#                     (b) benchmark.py:125 computes `bin_midpoints` and never uses it.
#                     (c) The legacy metrics interface -- compute_classification_metrics and
#                         ModelEvaluator -- has ZERO callers outside metrics.py itself and its
#                         own test, measured by the same census. Removal is a live option and
#                         is a scope decision, not an installer's.
#                     (d) Three independent bootstrap implementations remain unreconciled:
#                         evaluator.py:284, report_generator.py:85, and the kernel's. The
#                         gene-cluster design effect measured at entry 2055 was 2.935x; whether
#                         the other two share the variant-level independence assumption has not
#                         been measured.
#   2071  2026-07-20  +11: tests/unit/test_preflight_run16_feature_contract.py -- THE
#                     ONLY UNTESTED CHECK WAS THE ONE THAT DRIFTED.
#                     scripts/preflight_run16_inputs.py carried five checks. Four were
#                     covered by tests/unit/test_preflight_run16_inputs.py; the fifth,
#                     check_feature_count, was covered by nothing. It pinned the literal
#                     EXPECTED_COUNT = 81 and compared it for equality against
#                     EXPECTED_TABULAR_FEATURE_COUNT, which had advanced 81 -> 88 -> 91 ->
#                     95 through patch_ve_maxentscan_delta.py,
#                     patch_tabular_omim_molecular.py and
#                     patch_variant_ensemble_finngen_r13.py. Because the module's exit
#                     convention is 0 all-pass / 2 any-fail, the ENTIRE Run-16 input
#                     preflight returned 2 on every invocation, on a clean tree, for the
#                     whole period after the 88 bump. A fail-loud guard that fails for a
#                     false reason is worse than no guard: it teaches the operator to
#                     ignore the four checks beside it that are correct.
#
#                     THE REPAIR DOES NOT RE-PIN THE NUMBER. Setting the literal to 95
#                     would reproduce the defect at the next roster growth. check_feature_
#                     contract() now asserts two properties that cannot go stale: the
#                     project's fail-loud invariant EXPECTED_TABULAR_FEATURE_COUNT ==
#                     len(TABULAR_FEATURES) with no duplicate names, and membership BY NAME
#                     of esm2_llr, maxentscan_delta and gene_constraint_oe -- the three
#                     features the gate's other checks exist to protect. A count never told
#                     us whether the right features were present; names do. check_feature_
#                     count() is retained as a delegating alias, because silently deleting
#                     a public name is a loss this project has had to reverse before.
#
#                     The eleven tests assert OUTCOMES against synthetic contracts injected
#                     into sys.modules, which is the only way to exercise a module that
#                     imports xgboost, lightgbm, catboost and torch at module scope. One
#                     case is impossible to construct any other way and is the reason the
#                     repair is shaped as it is: a DUPLICATE feature name hidden behind a
#                     matching count, which the old count-only gate PASSED.
#
#                     Recorded honestly: the first draft of the module-restore fixture in
#                     this file wrote `del sys.modules[m]` using a comprehension variable
#                     that does not leak in Python 3. All eleven tests reported PASSED while
#                     teardown raised NameError. Found by running it, not by reading it --
#                     the twenty-ninth instance of the same lesson.
#
#   2128  2026-07-21  +57: tests/unit/test_ordinal_conformal.py -- ORDINAL
#                     CONFORMAL PREDICTION: CONTIGUITY BY CONSTRUCTION, AND A
#                     GUARD THAT REFUSES THE COHORT WE ACTUALLY HAVE.
#                     src/genomic_variant_classifier/conformal/ordinal.py.
#
#                     THE PROBLEM. Split, Mondrian and grouped conformal all return
#                     {k : score_k <= q_hat}. On an UNORDERED label space that is
#                     correct. On the ACMG/AMP five-tier scale -- Benign < Likely
#                     benign < Uncertain significance < Likely pathogenic <
#                     Pathogenic -- nothing prevented the prediction set
#                     {Pathogenic, Benign}: an assertion that a variant is either
#                     the most harmful or the most harmless thing it could be and
#                     nothing between. That is not actionable, and it is not rare;
#                     it arises whenever the model is bimodal and the middle tier
#                     falls just above the threshold.
#
#                     THE CONSTRUCTION. Nested contiguous intervals. I_0 = [mode,
#                     mode]; each step extends to whichever neighbouring tier has
#                     the larger probability. The nonconformity score of tier k is
#                     the cumulative mass of the first interval containing it. Mass
#                     is non-decreasing in the step, so the level set {k : s(k) <=
#                     q_hat} IS one of the nested intervals -- contiguity is a
#                     property of the score, not a post-hoc repair. Under
#                     randomisation s(k) = u*M_(t-1) + (1-u)*M_t, a convex
#                     combination of consecutive cumulative masses, hence monotone
#                     in t. Contiguity is therefore structural, not empirical, and
#                     45,000 rows across varied alpha returned zero gapped sets.
#
#                     THE GUARD. The metric specification (Finding 1, Panel C) is
#                     explicit that this project has no five-class target: the
#                     pipeline collapses P and LP to 1, B and LB to 0, and excludes
#                     VUS. Tiers derived by thresholding one binary probability are
#                     contiguous TRIVIALLY, so the guarantee would be vacuous while
#                     the output looked genuine -- indistinguishable from the real
#                     thing by inspection. fit() therefore raises OrdinalLabelError
#                     when calibration labels carry fewer than three distinct
#                     values. ON TODAY'S COHORT IT FIRES. That is intended.
#                     allow_degenerate_labels exists only so the refusal can be
#                     tested and records its own use in degenerate_labels_.
#
#                     TWO DEFECTS FOUND BY RUNNING THE CODE, NOT BY READING IT.
#                     (1) An empty-set "repair" forced the modal tier into any
#                         empty set. Measured cost, 30 trials per level:
#                             alpha 0.05  0.9500 off -> 0.9555 on  (0.6 % forced)
#                             alpha 0.10  0.8990 off -> 0.9147 on  (1.6 % forced)
#                             alpha 0.20  0.7973 off -> 0.8411 on  (4.7 % forced)
#                             alpha 0.30  0.7005 off -> 0.7822 on  (9.1 % forced)
#                         OFF, the score is exactly calibrated at every level. ON,
#                         coverage inflates by precisely the fraction repaired and
#                         sets are wider than the data supports. Now opt-in via
#                         force_nonempty and always counted in n_forced_nonempty_.
#                         The code comment had claimed it was "recorded rather than
#                         silent" while nothing recorded it.
#                     (2) ordinal_report counted ABSTENTIONS as catastrophic errors,
#                         because distance_to_set scores an empty set as K, which
#                         exceeds any threshold. Found because n_catastrophic and
#                         n_empty were both exactly 64. An abstention is the method
#                         declining to assert; a catastrophic error is a confident
#                         assertion at the far end of the scale. Penalising the
#                         former as the latter would push a clinical system towards
#                         answering when it should decline. Now reported separately.
#
#                     Also recorded: the first draft of
#                     test_tie_break_actually_differs_on_an_exact_tie used
#                     [0.25, 0.25, 0.50, 0.00, 0.00] and called the neighbours tied.
#                     The modal tier is index 2, whose neighbours are 0.25 and 0.00.
#                     Not a tie. The test failed against CORRECT code. The thirtieth
#                     instance of the same lesson, this time with the fixture rather
#                     than the implementation at fault.
#
#                     THE STRONGEST TEST is a deliberately naive plain-Python
#                     reference implementation of the nested expansion, sharing no
#                     code with the vectorised one, compared row by row under both
#                     tie-break policies. An independent implementation disagreeing
#                     is the only reliable way to catch a subtle indexing error in
#                     vectorised code.
#
#                     OPEN, from the same work: per-tier coverage at alpha=0.10 was
#                     Benign 0.882, Likely benign 0.922, Uncertain 0.918, Likely
#                     pathogenic 0.919, Pathogenic 0.880 -- the two EXTREMES are
#                     under-covered while the middle is over-covered. That is the
#                     classic marginal-coverage failure and precisely what
#                     mondrian.py exists to fix. A class-conditional ordinal variant
#                     is the natural follow-on and it matters clinically: Pathogenic
#                     is the tier one least wants under-covered.
#
#                     ALSO OPEN: conformal/__init__.py imports scores, split,
#                     mondrian, grouped and coverage. It omits calibrate, and now
#                     ordinal. Verified by outcome: hasattr(conformal, 'calibrate')
#                     is False. Deliberately NOT fixed here -- it changes the
#                     package's public surface and belongs in its own commit with
#                     its own test.
#
#   2148  2026-07-21  +20: tests/unit/test_conformal_package_exports.py -- A
#                     LIST THAT WENT STALE TWICE NOW CANNOT GO STALE AGAIN.
#
#                     src/genomic_variant_classifier/conformal/__init__.py
#                     imported five of the seven modules that existed on disk.
#                     Verified by OUTCOME against commit c663c89:
#                         hasattr(conformal, 'calibrate') = False
#                         hasattr(conformal, 'ordinal')   = False
#                     calibrate had been unreachable since roughly 2026-05.
#                     ordinal was unreachable on the day it was added.
#
#                     THE FAILURE IS CONFUSING WHEN IT BITES, because the two
#                     import forms look equivalent to a reader and are not:
#                         from ...conformal import ordinal        -- works, Python
#                                                                    imports the
#                                                                    submodule
#                         conformal.ordinal.OrdinalConformalClassifier
#                                                                 -- AttributeError,
#                                                                    nothing bound
#                                                                    the name on the
#                                                                    package object
#
#                     ADDING TWO NAMES WOULD HAVE FIXED TODAY AND GUARANTEED A
#                     THIRD OCCURRENCE. That import line has gone stale twice
#                     because nothing connected "a file exists in this directory"
#                     to "its name appears on that line". Same shape as the Run-16
#                     preflight gate repaired earlier the same day: a literal kept
#                     correct by memory, with no mechanism to notice when it is not.
#
#                     THE DESIGN. Imports stay EXPLICIT. A pkgutil loop would never
#                     go stale but would make the public surface implicit, defeat
#                     static analysis and editor completion, and silently import
#                     anything dropped into the directory -- a scratch file, a
#                     half-finished module, an editor backup. Explicit is right; it
#                     needed a guard, not a replacement. __all__ was added so the
#                     public surface is declared rather than inferred.
#
#                     The guard is the test file. It WALKS THE PACKAGE DIRECTORY
#                     and asserts every module found is reachable as a package
#                     attribute, is declared in __all__, and appears in the package
#                     docstring; plus the reverse, that __all__ declares nothing
#                     absent from disk, which would break `import *` for every
#                     consumer at once. Adding a submodule without exporting it now
#                     turns the suite RED and the message names the file and the fix.
#
#                     VERIFIED BY SIMULATION, not by assertion: a placeholder
#                     risk_control.py -- one of the six conformal modules the
#                     2026-07-20 handoff lists as still missing, so a realistic
#                     case -- was dropped into the package. Five tests failed
#                     immediately with:
#                         conformal.risk_control is not reachable.
#                         src/.../conformal/risk_control.py exists on disk but
#                         'risk_control' is missing from the import in __init__.py.
#                         Fix: add 'risk_control' to both the `from . import (...)`
#                         block and __all__.
#                     The file was then removed and the suite returned to green.
#                     A guard that has never been observed to fire is not known to
#                     work.
#
#                     One test exists solely to stop the others passing vacuously:
#                     test_the_package_directory_is_readable_and_non_empty. Every
#                     other test here is parametrised over a directory listing, and
#                     an empty listing would produce zero test cases and a green
#                     suite -- the standard failure mode of data-driven tests.
#
#   2192  2026-07-21  +44: tests/unit/test_split_protocol_schema.py -- THE
#                     PARTITION SET BECOMES DATA, AND PROBABILITY CALIBRATION
#                     GETS ITS OWN PARTITION.
#                     src/genomic_variant_classifier/data/split_protocol_v2.py
#                     rewritten; scripts/forensics/verify_dtype.py repaired.
#
#                     THE DEFECT. The metric specification (Finding 2; Priority 2,
#                     "essential") requires probability calibration to be fitted on
#                     data untouched by model, method and alpha selection. Measured
#                     in the code 2026-07-21: scripts/train.py:131 and :550-551 fit
#                     the isotonic calibrator on the `tune` partition, which this
#                     module defined as "the model/method/alpha selection set". The
#                     calibrator was therefore fitted on the selection set. That
#                     affects every calibrated number the project reports.
#
#                     WHY A SCHEMA, NOT A FIFTH FRACTION FIELD. PARTITIONS was a
#                     hard-coded module constant referenced in five places, beside
#                     four separately-named fraction fields and a hand-written sum
#                     check. A fifth partition meant six coordinated edits; a sixth
#                     would mean six more. Same shape as three defects repaired the
#                     same week: the Run-16 gate's EXPECTED_COUNT = 81, the conformal
#                     package's five-of-seven import list, and the ratchet before it
#                     gained drift detection. The partition set is now DATA --
#                     PartitionSchema holds an ordered tuple of Partition(name,
#                     fraction, role) and every function derives behaviour from it.
#
#                     ROLES ARE THE POINT. train.py will ask for
#                     rows_for_role(CALIBRATE_PROBABILITY) rather than naming a
#                     partition. Under FIVE_WAY it receives a dedicated `calib`
#                     partition, gene-disjoint from `tune` and `conformal`. Under
#                     FOUR_WAY it receives None -- the honest answer. A silent
#                     fallback to `tune` would re-create the defect.
#
#                     BACKWARD COMPATIBILITY IS EXACT, AND WAS PROVEN AGAINST THE
#                     PREVIOUS REVISION, not merely asserted. The old module was
#                     loaded alongside the new one and bucket assignments compared:
#                     identical at seeds 42/7/123/2026, identical with non-default
#                     fractions, identical for group_shuffle on uniform gene sizes,
#                     and tests/test_split_protocol_v2.py passes unchanged (10/10).
#                     Had one gene moved bucket, stability invariant I8 would have
#                     broken across the upgrade and no pre-2026-07-21 result would be
#                     comparable to a later one. FOUR_WAY.hash_order is pinned to the
#                     historical (test, conformal, tune, train) by a test.
#
#                     TWO DEFECTS REPAIRED IN THE SAME PASS.
#                     (1) group_shuffle carve() rescaled by ROW counts while
#                         scikit-learn's GroupShuffleSplit interprets test_size as a
#                         proportion of GROUPS. Verified empirically: ten genes, one
#                         holding 100 of 109 rows, test_size=0.30 carved exactly 3 of
#                         10 genes -- 30 % of groups, 3 % of rows.
#                         The row ratio is an UNBIASED estimator of the gene ratio, so
#                         it is usually close; its variance grows with gene-size skew
#                         and it OVERFLOWS when a high-row-count gene is carved early.
#                         The clamp min(max(f, 1e-9), 1 - 1e-9) then turns it into
#                         0.999999999 and scikit-learn raises "n_samples=340,
#                         test_size=0.999999999", naming neither genes nor skew nor
#                         the partition. Measured, 12 seeds:
#                             skew                  rows           genes
#                             uniform               0.0000         0.0000
#                             heavy-tailed          0.0329         0.0000
#                             one gene = 90 % rows  0.0434, 3/12   0.0065, 0/12
#                                                   CRASH
#                         ClinVar per-gene variant counts ARE heavy-tailed, so the
#                         middle row is the realistic regime and the bottom row is
#                         reachable. carve() now rescales by gene counts; the ratio is
#                         bounded by construction so the overflow cannot arise, and
#                         genuine infeasibility gets an actionable message.
#                     (2) genes_are_stable_under_growth had an if/else whose branches
#                         were IDENTICAL, under a comment implying they differed.
#                         Dead code removed.
#
#                     RECORDED HONESTLY: the carve defect was hypothesised from
#                     reading, then TESTED AND REFUTED (worst deviation 0.013, better
#                     than hash mode at 0.038), and only found on a wider sweep over
#                     twelve seeds. The first adversarial test used a seed that did
#                     not trigger it. Reading produced the hypothesis; only running it
#                     across enough cases produced the evidence.
#
#                     scripts/forensics/verify_dtype.py:30 asserted the literal source
#                     text "for p in PARTITIONS:", which the schema-driven loop no
#                     longer contains. That check verified that code LOOKED a certain
#                     way, not that the dtype promotion WORKED -- a clean refactor
#                     failed it, and a silent breakage with intact loop text would
#                     have passed it. Both directions wrong. Replaced with a
#                     behavioural check: int32 fixture, FutureWarning escalated to
#                     error, real split, real remap, assert int64 out; plus a check
#                     that every partition IN THE SCHEMA is remapped and that genes
#                     unseen in train score zero (incident 2026-06-13). Proven by
#                     sabotage: removing the promotions fails 5 checks; reintroducing
#                     the leak is caught with 2,217 violations. The old text check
#                     passed both.
#
#                     NOT DONE HERE, DELIBERATELY: train.py is untouched. Wiring the
#                     calibrator to rows_for_role(CALIBRATE_PROBABILITY) and flipping
#                     --split-protocol off `legacy` (train.py:126) change the
#                     behaviour of every future run and belong in their own commit
#                     with their own test.
#
#   2270  2026-07-21  +78: tests/unit/test_clustering_metrics.py -- PANEL Q,
#                     AND FIVE REFUSALS THAT HAD TO BE PROVEN RATHER THAN
#                     ASSERTED.
#                     src/genomic_variant_classifier/evaluation/
#                     clustering_metrics.py (1,189 lines) +
#                     docs/PANEL_Q_unsupervised_structure.md (225 lines) added.
#
#                     WHAT PANEL Q IS. The metric specification has sixteen
#                     panels, A through P. Verified 2026-07-21 against
#                     project_metrics.txt (SHA-256 db987039...): the words
#                     Davies-Bouldin, silhouette, Calinski-Harabasz and Dunn
#                     appear ZERO times, and adjusted Rand index / adjusted
#                     mutual information / normalized mutual information appear
#                     nowhere as agreement metrics. Panel Q fills that gap. It
#                     evaluates whether a REPRESENTATION has coherent structure.
#                     It says nothing about whether predictions are correct, and
#                     nothing it produces may be reported as clinical
#                     superiority.
#
#                     THE MODULE IS ITS REFUSALS. Any of these metrics is three
#                     lines of scikit-learn; what makes the module worth having
#                     is that it declines to produce a number it cannot justify.
#                     Five properties, each proven by sabotage rather than by
#                     assertion -- the test suite passed on its first full run,
#                     which is exactly when a suite is least trustworthy:
#
#                     (1) THE SILHOUETTE GUARD REFUSES BEFORE ALLOCATING, and is
#                         expressed in MEMORY rather than a sample count because
#                         memory is the constraint and a count silently assumes a
#                         dtype. maximum_distance_matrix_gib=3.0 yields
#                         floor(sqrt(3*2^30/8)) = 20,066. Measured on
#                         1280-dimensional embeddings: 0.32 s at n=2,000; 7.53 s
#                         at 10,000; 42.44 s and 1.0 GiB at 30,000; ~27 GiB
#                         predicted at 60,000; 16.4 TiB at 1.5 million. A request
#                         above the ceiling returns COMPUTATIONALLY_DEFERRED in
#                         about a millisecond with the numbers in metadata.
#                         Removing the ceiling check made the test HANG -- it
#                         genuinely attempted 18.6 GiB. The failure mode the
#                         guard prevents, reproduced by accident.
#                     (2) NO SILENT NOT-A-NUMBER. Enforced in __post_init__, not
#                         only in the not_ok() factory: an earlier draft guarded
#                         one of two construction paths, so the raw dataclass
#                         constructor could still build an unexplained failure.
#                         aggregate() refuses to average across non-OK results
#                         and reports the full status census including the OK
#                         count, because numpy.nanmean would silently return the
#                         mean of whatever happened to work.
#                     (3) THREE EXCLUSION ROUTES, COUNTED SEPARATELY, IN A FIXED
#                         ORDER. Non-finite, then algorithm noise, then the
#                         minimum-cluster-size filter. A row qualifying for two
#                         counts ONCE, in the earliest. Reconciliation alone
#                         cannot detect a reordering because the total is
#                         identical either way, so bucket MEMBERSHIP has its own
#                         test. A single "noise_fraction" field would have
#                         reported 0.15 on the fixture and concealed 0.08.
#                     (4) THE TWO DAVIES-BOULDIN GEOMETRIES ARE DIFFERENT
#                         ESTIMANDS. For unit-normalized vectors the squared
#                         Euclidean distance is 2 - 2*cos, so pairwise Euclidean
#                         and cosine are monotone -- which holds for the
#                         silhouette coefficient and NOT for a centroid-based
#                         index, because the Euclidean mean of points on a sphere
#                         has norm below one and leaves the sphere. Measured on a
#                         directional fixture: 2.9188 Euclidean-on-normalized
#                         against 1.3538 spherical, with cluster mean norms near
#                         0.47. Checked against INDEPENDENT implementations
#                         written in the test file, not golden constants: a
#                         constant catches drift, an independent implementation
#                         catches wrongness. That is what caught the sabotage in
#                         which the spherical centroid silently became the
#                         arithmetic one.
#                     (5) THE CONFOUNDER GATE IS PURE AND FAIL-CLOSED.
#                         decide_confounder_gate() takes AgreementEstimate values
#                         and returns a verdict with no randomness, so the
#                         scientific policy is testable as arithmetic with
#                         hand-written intervals; evaluate_confounder_gate()
#                         measures, then calls it. Separating measurement from
#                         decision means a policy change cannot be made by
#                         accident while editing an estimator. Comparison is on
#                         INTERVALS: upper95(covariate) < lower95(target),
#                         strictly. Covariate upper 0.40 against target lower
#                         0.40 refuses; 0.3999 passes. A non-finite bound is not
#                         separation -- an unknown is not a pass.
#
#                     WHY A GATE AND NOT A DIAGNOSTIC. Clustering can rediscover
#                     gene identity, laboratory provenance or missingness pattern
#                     with considerable efficiency while looking biologically
#                     elegant, and a lower Davies-Bouldin index is exactly what
#                     that looks like. This cohort has a demonstrated reason for
#                     the concern: n_pathogenic_in_gene required a permutation
#                     ablation to establish it encoded genuine variant-level
#                     biology (observed AUROC 0.9666 against a permuted 95th
#                     percentile of 0.8016). Panel Q therefore refuses any
#                     solution whose adjusted mutual information with a technical
#                     covariate is not separated from that with the biological
#                     target.
#
#                     THE PERMUTATION NULL RESPECTS DEPENDENCE. Variants within a
#                     gene share a covariate, so permuting rows barely disturbs
#                     the association. Measured: gene-level null 95th percentile
#                     0.0516 against row-level 0.0011, a 47-fold difference. The
#                     row-level null would make a spurious association look far
#                     more significant than it is. permute_covariate_by_group is
#                     public so the MECHANISM is tested directly -- that every
#                     gene still holds one value and the multiset is preserved --
#                     which is more fundamental and less brittle than asserting a
#                     quantile that depends on seeds, numpy and scikit-learn.
#
#                     ONE DEFECT FOUND AND FIXED MID-BUILD. Constructing an
#                     AgreementEstimate with a non-finite POINT estimate raised
#                     deep inside decide_confounder_gate rather than at the
#                     boundary. The realistic case is a finite point with a
#                     non-finite INTERVAL -- too few usable subsample replicates
#                     -- so __post_init__ now requires a finite point estimate
#                     and documents that an unbounded interval is legitimate and
#                     handled fail-closed. Found because a deliberately
#                     unrealistic fixture exposed it.
#
#                     AND ONE TEST THAT COULD NOT FAIL. The first version of the
#                     silhouette-guard check used a 500-row fixture, where
#                     effective = min(100000, 500) = 500 is legitimately under
#                     the ceiling. Nothing could trigger the refusal. Rewritten
#                     against a 50,000-row cohort. This is the fourth time this
#                     session a check has been found unable to fail.
#
#                     NOT DONE HERE, DELIBERATELY: PartitionRole.STRUCTURE and
#                     the Panel Q orchestrator are absent. Panel Q's partition
#                     policy requires a gene-disjoint STRUCTURE partition so that
#                     cluster discovery -- which selects an algorithm, a distance,
#                     a cluster count and an interpretation, and is therefore
#                     model selection -- never touches the locked test set. That
#                     changes the shipped partition schema and belongs in its own
#                     versioned commit, with the orchestrator third, once it has
#                     a legitimate partition to run on.
#
#   2283  2026-07-21  +13: tests/unit/test_disk_census_walker.py -- THE TOOL THAT
#                     MEASURES THE DISK HAD BEEN WRONG THREE TIMES AND HAD NO
#                     TESTS. scripts/forensics/audit_disk_census.py added
#                     (renamed from a Downloads-only
#                     audit_disk_reclaim_v3_2026-07-20.py).
#
#                     WHY THIS EXISTS. The census tool had reported a materially
#                     wrong figure for data/ three versions running:
#                       v1 2026-07-20  161.38 GiB / 15,260 files
#                       v2 2026-07-20  every subdirectory 0.0 MiB -- the time
#                                      budget had expired and zeros were printed
#                                      as measurements
#                       v3 2026-07-21    3.21 GiB -- against a true 98.75 GiB
#                                      measured the same hour; short by 95.54 GiB
#                     Each fix was correct as far as it went. None shipped with a
#                     test. The tool also lived only in a Downloads folder, so
#                     the capability that resolved the 2026-07-20 drive emergency
#                     was one folder-clear away from being lost.
#
#                     DEFECT 5, THE ONE FIXED HERE. Walker keeps visited sets
#                     keyed on (st_dev, st_ino) so a CENSUS never counts an
#                     overlapping root twice -- without it the legacy junction
#                     C:\Documents and Settings made the census report 5.94x the
#                     volume. data_breakdown() then reused the SAME Walker that
#                     had already walked the whole volume. Every subdirectory of
#                     data/ was in _seen_dirs; size_of() skips any child already
#                     there and never checks the root it is handed. So each
#                     subtotal counted ONLY files sitting loose in that directory:
#
#                       directory        true    in subdirs  loose  v3 reported
#                       data/external   75.18G     75.18G    0.00G     0.003G
#                       data/processed   3.50G      0.54G    2.96G     2.950G
#                       data/raw        19.80G     19.80G    0.00G     0.000G
#                       data/_drift_ch   0.26G      0.00G    0.26G     0.264G
#
#                     Four for four -- the mechanism was characterised by
#                     prediction, not guessed. The de-duplication was never the
#                     bug; applying CENSUS semantics to a STANDALONE MEASUREMENT
#                     was. size_of() now takes an explicit `independent` flag and
#                     the two semantics are NAMED rather than implied. Cycle
#                     safety holds in both modes; only the SCOPE of the visited
#                     sets differs, and hardlink_savings is not mutated in
#                     independent mode because that statistic describes the
#                     census.
#
#                     WORSE THAN WRONG. The section printed "Compare the figure
#                     above against 161.38 GiB" directly beneath its own bad
#                     number, inviting the reader to conclude 158 GiB had been
#                     reclaimed. Nothing had. That line is replaced by the
#                     section's own error history, and a test asserts the old
#                     line is gone.
#
#                     WHAT WOULD NOT HAVE CAUGHT IT: asserting the census total
#                     matches the volume (it did); asserting termination (it did);
#                     asserting no exceptions (there were none). Only measuring a
#                     directory of KNOWN size AFTER walking its parent exposes it.
#                     Every load-bearing test here does exactly that.
#
#                     PROVEN BY SABOTAGE, all four detected:
#                       call site drops independent=True   ->  2 failed
#                       independent shares the dir set     ->  5 failed
#                       independent leaks into shared set  ->  5 failed
#                       shared de-dup removed entirely     ->  2 failed
#                     The last matters as much as the first: the tests pin the
#                     census property too, so the fix cannot be "achieved" by
#                     discarding the de-duplication that prevents 5.94x totals.
#
#                     RENAMED. A committed tool must not carry a version number
#                     and a date in its filename -- the project's own data-layout
#                     standard forbids version suffixes on directory names for the
#                     same reason. "reclaim" was also wrong: it deletes nothing.
#
#                     MEASURED CONTEXT, 2026-07-21: C: is the ONLY fixed volume,
#                     935.59 GiB, 83.01 free (8.87 %). data/ is 98.75 GiB, of
#                     which data/external/finngen is 57.64 GiB in three files.
#                     The suite showed a 1.87x wall-clock range on identical code
#                     (605.08 s to 1131.67 s), consistent with a host below the
#                     ten percent where Windows behaviour degrades. No storage
#                     decision is taken in this commit; the measuring instrument
#                     is repaired first.
#
#   2328  2026-07-21  +45: tests/unit/test_storage_guard.py -- THE GUARD THAT
#                     NEVER CHECKED FREE SPACE, AND THAT NOTHING EVER CALLED.
#                     configs/data_manifest.yaml gains a storage: policy block;
#                     scripts/maintenance/preflight_data_guard.py rewritten;
#                     scripts/preflight_run17.py gains storage_gate() in run_all.
#
#                     TWO DEFECTS, BOTH OF OMISSION.
#                     (1) preflight_data_guard.py verified structure -- dangling
#                     junction, stray file shadowing data/, canonical subtrees --
#                     and never looked at free space. The guard whose purpose is
#                     catching storage problems before a run could not catch the
#                     one the machine had. On 2026-07-20 the volume reached 0.716
#                     per cent free and a FAILING RUN, not a guard, is what
#                     noticed.
#                     (2) NOTHING EVER CALLED IT. A repository-wide search across
#                     every .py, .sh, .ps1 and .yaml on 2026-07-21 found ZERO
#                     invocations of assert_data_usable. Its own docstring said it
#                     was importable "to wire into run scripts / conftest". It
#                     never was, from 2026-06-14 to 2026-07-21. A guard that is
#                     not invoked is not a guard; it is a comment that happens to
#                     be executable. Defect (2) mattered more than (1): fixing
#                     only the first would have produced a better guard that still
#                     never ran.
#
#                     MEASURED CONTEXT (2026-07-21). C: is the ONLY fixed volume:
#                     935.59 GiB, 83.50 free, 8.925 per cent. data/ is 98.75 GiB,
#                     LARGER than the free space, so nothing large can be staged,
#                     duplicated or rebuilt in place. Nine suite runs on identical
#                     code spanned 605.08 s to 1131.67 s, a 1.87x range consistent
#                     with a host under storage pressure.
#
#                     THREE SEVERITIES, NOT ONE THRESHOLD. A single threshold must
#                     choose between crying wolf and staying silent until it is too
#                     late.
#                       free >= required                OK
#                       hard_floor <= free < required    WARN, run proceeds
#                       free < hard_floor                FAIL, refuse
#                     required = working_cache_gib + max(headroom_fraction *
#                     volume, headroom_min_gib) = 61.48 GiB on this volume, which
#                     reproduces EXACTLY what audit_disk_census.py printed. One
#                     model, not two.
#
#                     POLICY LIVES IN THE MANIFEST. DATA_LAYOUT_STANDARD.md already
#                     declares configs/data_manifest.yaml the single source the
#                     auditor, setup and sync scripts read. audit_disk_census.py
#                     keeps its OWN constants deliberately -- it must run standalone
#                     from any directory with the repository not importable -- and
#                     test_policy_matches_the_census_tool fails if the two ever
#                     disagree. Duplication as a considered trade, made safe by a
#                     test, rather than duplication by accident.
#
#                     WIRED AT THE POINT WHERE IT COSTS MONEY. storage_gate() joins
#                     run_all() in preflight_run17.py, which runs before a Vast.ai
#                     instance is created. It degrades to a FAIL ROW rather than an
#                     exception if the guard cannot load, because a preflight that
#                     dies on its first problem hides the second.
#
#                     PROVEN BY SABOTAGE, all seven detected:
#                       DEFAULT_POLICY drifts from manifest    ->  2 failed
#                       census constant drifts from manifest   ->  1 failed
#                       manifest changes, code copies do not   ->  4 failed
#                       hard floor stops being enforced        ->  6 failed
#                       __post_init__ validation removed       ->  7 failed
#                       storage_rows raises instead of a row   ->  1 failed
#                       free-space row dropped entirely        ->  4 failed
#
#                     A PYTHON INTERACTION WORTH RECORDING: @dataclass under
#                     `from __future__ import annotations` resolves annotations
#                     through sys.modules[cls.__module__]. A module loaded by
#                     importlib.util.spec_from_file_location must be REGISTERED in
#                     sys.modules BEFORE exec_module, or the decorator raises
#                     AttributeError on None. Both the test loader and
#                     storage_gate() do this, with the reason written down.
#
#   2378  2026-07-21  +50: tests/unit/test_partition_structure_role.py --
#                     PartitionRole.STRUCTURE and the SIX_WAY schema, which
#                     unblock the Panel Q orchestrator; plus a stale-list defect
#                     in PartitionSchema found while building them.
#
#                     WHY THE ROLE EXISTS. Panel Q's specification is explicit
#                     that unsupervised structure analysis is a MODEL-SELECTION
#                     activity: it chooses a representation, preprocessing,
#                     dimensionality, distance geometry, clustering algorithm,
#                     cluster count, noise handling, stability thresholds and a
#                     biological interpretation. Doing that on the locked test
#                     partition is selection on test. The specification requires
#                     a dedicated STRUCTURE partition "gene-disjoint from train,
#                     tune, probability calibration, conformal calibration, and
#                     test", with test admitting only a predeclared replication
#                     of a solution frozen on STRUCTURE. Panel Q shipped in
#                     5dcb932 with no partition to run on.
#
#                     A STALE LIST, FOUND BY PROBING. PartitionSchema capped role
#                     repetition by ENUMERATING three roles -- SELECT,
#                     CALIBRATE_CONFORMAL, TEST. That list was already stale:
#                     commit 5b1c82b added CALIBRATE_PROBABILITY the same morning
#                     and did not add it here. Two partitions could both claim
#                     it, the schema would be ACCEPTED, and name_for_role() would
#                     silently return whichever was declared first -- train.py
#                     fitting the probability calibrator on one partition while a
#                     second believed it held the role. A silent ambiguity in
#                     precisely the role the five-way schema exists to serve.
#                     Found by probing a six-partition schema, not by review.
#                     The repair removes the list rather than extending it: every
#                     role except TRAIN is capped, so a future enum member cannot
#                     be forgotten. The test is parametrized over PartitionRole
#                     ITSELF, so roles that do not exist yet are covered.
#
#                     THE MIGRATION GUARANTEE, AND A TEST FAILURE THAT EARNED IT.
#                     Hash intervals are assigned in hash_order, so a partition's
#                     genes depend on the cumulative fraction ahead of it.
#                     SIX_WAY keeps FIVE_WAY's first four intervals EXACTLY and
#                     splits its train interval in two:
#                       structure [0.45, 0.52)   train [0.52, 1.00)
#                     Verified across seeds 42/7/123/2026, migrating FIVE_WAY ->
#                     SIX_WAY moves EXACTLY ONE set of genes, out of train into
#                     structure:
#                       test, conformal, calib, tune  byte-identical
#                       structure   precisely the genes train gave up
#                       train       loses those and gains nothing
#                     So the locked test set is untouched, a fitted probability
#                     calibrator stays valid, and model selection need NOT be
#                     repeated. Only the training set shrinks, 0.55 -> 0.48,
#                     which is the honest cost of funding a structure partition.
#
#                     The FIRST hash order tried put structure between calib and
#                     tune, which shifted tune by 0.07 and would have silently
#                     invalidated every model-selection decision for no benefit.
#                     A test asserting the OPPOSITE property failed, and that
#                     failure is what found it. The guarantee is now the test.
#
#                     NO NEW SKIPS. An earlier draft skipped TRAIN inside the
#                     parametrized role test -- a permanent eighth skip in every
#                     run forever. Filtering the parametrize list instead keeps
#                     the enum-driven property at zero skip cost.
#
#                     PROVEN BY SABOTAGE, all five detected:
#                       role cap reverts to the enumerated list  ->  4 failed
#                       structure placed before tune             ->  6 failed
#                       structure funded from test not train     -> 13 failed
#                       STRUCTURE removed from the enum          ->  3 errors
#                       name_for_role substitutes instead of None-> 5 failed
#
#                     WHAT DID NOT CHANGE. FOUR_WAY and FIVE_WAY are untouched;
#                     PARTITIONS still aliases FOUR_WAY.names; the existing 54
#                     split tests pass unmodified. split(), the invariant checker
#                     and the leakage remap needed NO changes at all -- six
#                     partitions worked on the first probe, which is the payoff
#                     of the schema-as-data rewrite in 5b1c82b.
#
#   2408  2026-07-21  +30: tests/unit/test_calibration_carve.py -- THE ISOTONIC
#                     CALIBRATOR WAS FITTED ON GENES THE MODELS HAD TRAINED ON,
#                     IN EVERY PRODUCTION RUN.
#
#                     WHERE IT LIVED. run_phase2_eval.py:590 -- the production
#                     entry point, the one preflight_run17.emit_command() emits
#                     and every Run 14-17 launcher uses -- calls fit() with no
#                     *_cal_ext argument, taking the self-carve branch:
#                       _tts(idx, test_size=0.15, stratify=y_arr, ...)
#                     `stratify=`, no `groups=`. Measured 2026-07-21:
#                       500 genes        319/319 cal genes also in fit; 100.0% of
#                                        cal ROWS from trained-on genes
#                       8,000 genes    7,856/7,864 likewise; 100.0% of cal rows
#                     Twenty lines below, the inner CV is carefully gene-disjoint
#                     via GroupKFold citing INCIDENT_2026-06-13. Out-of-fold
#                     PREDICTIONS were gene-disjoint; the fold the CALIBRATOR was
#                     fitted on was not. One function, two standards.
#
#                     WHAT WAS NOT THE DEFECT. train.py's v2_conformal path fits
#                     the calibrator on `tune`, the SELECTION set -- also wrong,
#                     but NO launcher passes --split-protocol, so it has never
#                     run. Claude asserted three times that fixing train.py would
#                     "correct every calibrated number the project reports". That
#                     was WRONG: train.py is not the production entry point.
#                     Checking emit_command() before designing the fix is the only
#                     reason this commit touches variant_ensemble.py instead.
#
#                     THE FIX. EnsembleConfig.calibration_carve, default
#                     "gene_disjoint": carve whole GENES via GroupShuffleSplit.
#                     "legacy_stratified" reproduces the old behaviour BYTE-FOR-
#                     BYTE (identical indices at seeds 42/7/2026) so historical
#                     runs stay reproducible. Result: 100.0% -> 0.0% of
#                     calibration rows from trained-on genes, both cohorts.
#
#                     THE ROW-VERSUS-GROUP TRAP. GroupShuffleSplit's test_size is
#                     a proportion of GROUPS, not rows -- the same trap that
#                     crashed 3 of 12 seeds in split_protocol_v2.group_shuffle
#                     earlier the same day. The carve therefore VALIDATES the fold
#                     it produced (both classes present, >= calibration_min_rows)
#                     across a deterministic seed sequence, and RAISES with the
#                     full attempt log if none qualifies. Realized row fraction
#                     stayed at 14.9% and 14.7% against a 15% target.
#
#                     NO SILENT FALLBACK. calibration_carve_used_ records what
#                     actually happened: gene_disjoint:seed=N,
#                     legacy_stratified:configured, or
#                     legacy_stratified:no_gene_labels. run_phase2_eval reads
#                     gene_symbol from splits/meta_train.parquet, which may be
#                     absent -- that path now warns loudly rather than degrading
#                     in silence.
#
#                     WHAT THE FIX ACTUALLY BUYS, MEASURED OVER FIVE COHORTS --
#                     AND IT IS NOT WHAT CLAUDE PREDICTED. Claude said reported
#                     numbers would change and current values were optimistic.
#                     Measured Brier on a locked set of ENTIRELY UNSEEN genes:
#                       legacy        0.18063
#                       gene-disjoint 0.18064      difference +0.00001 (+0.00%)
#                     The REPORTED numbers barely move. What was optimistic is the
#                     INTERNAL estimate: the legacy calibration fold overstated
#                     its own quality in 5 of 5 cohorts, mean +0.00415 Brier;
#                     gene-disjoint mean +0.00048, essentially unbiased.
#                     CONSEQUENCE: Runs 14-17 reported test metrics DO NOT need
#                     retracting. The defect corrupted a diagnostic, not the
#                     headline. Synthetic cohorts; magnitude on the real cohort
#                     may differ, and the first real run should re-measure.
#
#                     A SABOTAGE THAT FAILED TO FAIL. Removing the both-classes
#                     guard left all 28 tests passing, because every cohort used
#                     happened to yield both classes. A cohort where the label is
#                     a property of the GENE (4 of 40 genes pathogenic) makes
#                     GroupShuffleSplit at random_state=42 return an 89-row
#                     ALL-BENIGN fold; isotonic regression on one class returns a
#                     constant and raises nothing. Two tests now pin it, and the
#                     fixture asserts it still forces the condition -- so the test
#                     cannot quietly stop testing. Fifth instance of the same
#                     lesson today, and the second in Claude's own work.
#
#                     PROVEN BY SABOTAGE, all six detected after the repair:
#                       default flips back to legacy        ->  5 failed
#                       disjoint branch uses stratified     ->  8 failed
#                       both-classes guard removed          ->  1 failed
#                       min-rows guard removed              ->  3 failed
#                       missing gene labels fall back mute  ->  1 failed
#                       legacy test_size drifts             ->  4 failed
#
#   2417  2026-07-21  +9 (2408 -> 2417): tests/unit/test_calibration_carve.py
#                     grows from 30 to 39. The 2408 bump was WITHDRAWN: the
#                     calibration carve it accompanied BROKE THE SUITE.
#
#                     WHAT BROKE. calibration_min_rows was a single absolute
#                     floor of 200. test_level2_leakfree_oof uses a 427-row
#                     cohort across 30 genes, where a 15 per cent carve of ANY
#                     kind yields about 64 rows. The floor refused gene-disjoint
#                     folds of 63-76 rows -- while the LEGACY stratified carve it
#                     replaced had been using a 62-row fold on that same cohort
#                     for the entire history of the project, without complaint.
#                     Refusing what the predecessor accepted is a regression, not
#                     a stricter standard. Full suite: 1 failed, 2400 passed.
#
#                     WHY NO TEST CAUGHT IT. Every cohort in the original 30
#                     tests had 500 or more genes. None could exercise the small-
#                     cohort path. The guard was correct in its intent and wrong
#                     in its instrument.
#
#                     THE REPAIR. The guard now asks "did the carve deliver
#                     roughly what was ASKED for?" rather than "is this fold
#                     small?" -- because that is the question the row-versus-group
#                     trap actually poses. Three thresholds:
#                       calibration_min_rows            = 25   absolute floor
#                       calibration_min_fraction_of_target = 0.5
#                       calibration_max_fraction_of_target = 2.0
#                       calibration_advisory_rows       = 200  WARN, not refuse
#
#                     THE TRAP RUNS BOTH WAYS -- found by probing the lower bound,
#                     not by review. A cohort of 11,000 rows whose largest gene
#                     held 9,000 produced a 9,300-row calibration fold against a
#                     1,650-row target: 85 per cent of the data, leaving the
#                     models 1,700 rows to fit on. An upper bound now refuses it.
#
#                     VERIFIED ACROSS THREE REGIMES:
#                       427 rows / 30 genes      -> 76-row fold, WARNS, proceeds
#                       11,000 rows, 1 dominant  -> REFUSED, both verdicts seen
#                       1,201,942 rows / 8,000   -> 180,458 rows, exactly 15.0%
#
#                     test_level2_leakfree_oof passes again (4 passed). The 14
#                     failures seen in the authoring sandbox for
#                     test_reclassification_* and test_split_protocol_schema are
#                     PRE-EXISTING there -- identical counts on an unmodified
#                     clone of c68934d -- and are artifacts of --noconftest plus
#                     absent packages, not of this change.
#
#   2446  2026-07-21  +29: tests/unit/test_calibration_validity_contract.py --
#                     TWO DEFECTS IN THE METRIC KERNEL EVERY PANEL BUILDS ON.
#
#                     DEFECT 1: calibration_valid asserted soundness it had not
#                     checked. It gated on is_probability(p) alone -- whether the
#                     values lay in [0,1] -- and never asked whether a calibration
#                     claim was SUPPORTABLE. Measured on y=[1,1,1,1],
#                     p=[.9,.8,.85,.95]:
#                       auroc NaN  auprc NaN      correct, ranking undefined
#                       cal_slope NaN  cal_intercept NaN
#                       brier 0.01875   ece 0.125     <- NUMBERS
#                       calibration_valid True        <- asserting they are sound
#                     That ECE is 1 - 0.875, the gap between the mean prediction
#                     and the only label present. The reliability diagram has one
#                     occupied row; it says nothing about calibration across the
#                     probability range.
#
#                     The flag's own documented invariant -- "False =>
#                     brier/log_loss/ece/cal_* are NaN by design" -- was ALREADY
#                     violated in the other direction: cal_slope and cal_intercept
#                     were NaN while the flag read True, so a reader would take an
#                     undefined estimand for a failed computation. The invariant is
#                     now ENFORCED in code, not merely written in a comment.
#
#                     Both classes present is a HARD requirement: without it the
#                     quantity is not calibration. Thin support is REPORTED, not
#                     refused -- DEFAULT_MIN_POS/NEG are the same floors
#                     stratified_evaluate already applied per subgroup, so
#                     identical data was called insufficient as a stratum and
#                     sound on its own. Refusing it outright would repeat the
#                     427-row regression made earlier the same day.
#                     New field calibration_support carries the machine-readable
#                     reason: sufficient | thin:... | single_class:... |
#                     not_probabilities | insufficient_rows.
#
#                     DEFECT 2: two evaluation contracts in one module.
#                     compute_classification_metrics and ModelEvaluator were
#                     REMOVED, not wrapped. The 2026-07-20 banner had called them
#                     "unsafe in ways this stack explicitly rejects" and deferred
#                     unification to a separate, separately-measured commit; this
#                     is that commit. Delegation would have preserved the contract
#                     that IS the problem: a dict of bare floats cannot express
#                     undefined, insufficient support, dependency unavailable or
#                     computationally deferred. It returned specificity=0 for an
#                     undefined quantity, and on a single-class cohort it did not
#                     even manage that -- confusion_matrix(...).ravel() yields one
#                     cell, not four, so it raised "not enough values to unpack
#                     (expected 4, got 1)".
#
#                     Neither had a production caller: one test pinned them, one
#                     notebook imported them from src.evaluation.metrics, a package
#                     path that has not existed since the rename. The __all__ note
#                     reading "original API -- do not remove" was itself stale.
#                     test_legacy_metrics_api_is_preserved is replaced 1:1 by
#                     test_the_legacy_metrics_api_is_gone, which pins their
#                     ABSENCE, so reintroducing a second contract requires deleting
#                     a test that explains why there is one.
#
#                     CHECK ORDER. An empty cohort first reported
#                     "not_probabilities" -- true, since is_probability([]) is
#                     correctly False, but the less useful of two true reasons.
#                     Checks now run most-specific first and it reports
#                     "insufficient_rows". Found by a test, not by review.
#
#                     PROVEN BY SABOTAGE, all three detected:
#                       calibration_valid reverts to is_probability -> 18 failed
#                       invariant unenforced                        ->  5 failed
#                       legacy evaluator restored                   ->  1 failed
#
#   2488  2026-07-21  +42: tests/unit/test_gene_block_permutation.py -- THE
#                     PERMUTATION NULL IS RENAMED, ITS PROVENANCE COMPLETED, AND
#                     ITS ONE UNACTED-ON DIAGNOSTIC NOW ACTS.
#
#                     THE RENAME. permute_covariate_by_group ->
#                     permute_covariate_by_gene_block, and permutation_unit
#                     "group" -> "gene_block". The old name said only that
#                     groups were involved, not that whole blocks are exchanged
#                     as units, which is the property the null rests on.
#                     "Gene-block permutation" is the standard name. Correcting
#                     an earlier description of this code as "labels shuffled
#                     WITHIN genes" -- wrong exchangeability unit AND wrong
#                     quantity, since it permutes a COVARIATE against a fixed
#                     clustering, not labels.
#
#                     A PROPERTY MEASURED, NOT A DEFECT FIXED. Gene-block
#                     permutation with UNEQUAL block sizes necessarily moves the
#                     row-level marginal: swapping a 10-row gene's value with a
#                     4-row gene's moves 10 rows out of one level and 4 into it.
#                     Measured on 1,149 rows / 150 genes with a PURELY
#                     gene-level covariate (zero genes carrying >1 value): mean
#                     total variation distance 0.0145 (sd 0.0076), and ZERO in 0
#                     of 200 permutations.
#
#                     TWO OF MY OWN CLAIMS WERE FALSE AND WERE CORRECTED FROM THE
#                     SAME DATA THAT PRODUCED THEM:
#                       "at zero within-gene variation the permutation is exact"
#                         -- false. Drift at p=0.00 was 0.0171, LARGER than at
#                         p=0.05 (0.0160). Unequal block SIZES are the cause;
#                         within-block heterogeneity only modulates it.
#                       "the relationship is monotone" -- false. Across p =
#                         0.00/0.05/0.15/0.35/0.60/1.00 drift ran 0.0171/0.0160/
#                         0.0264/0.0458/0.0630/0.0172, PEAKING near 0.60.
#
#                     WHY THE DEFAULT DID NOT CHANGE. Adjusted mutual information
#                     is chance-corrected against exactly the marginals that
#                     drift. Unstratified vs size-stratified p-values, 6 seeds:
#                       assoc 0.0   0.6024 vs 0.6013   -0.0011
#                       assoc 0.2   0.0659 vs 0.0548   -0.0111
#                       assoc 0.4   0.0033 vs 0.0033    0.0000
#                       assoc 0.6   0.0033 vs 0.0033    0.0000
#                     Restructuring buys nothing in the quantity anyone reads and
#                     costs frozen blocks. So the drift is MEASURED AND REPORTED
#                     per run (marginal_tvd_mean, marginal_tvd_max) instead.
#
#                     SIZE-STRATIFIED MODE, OPT-IN. size_strata=True restricts
#                     swaps to blocks of equal size and removes the drift
#                     EXACTLY -- 0.000000 in 200 of 200 permutations. Its cost:
#                     a block alone in its size stratum can only swap with
#                     itself, so its value is FROZEN. Reported as
#                     n_blocks_frozen_in_stratum. NOT assumed small: a synthetic
#                     cohort with sizes drawn uniformly 1-300 froze zero blocks,
#                     but that fixture does NOT model ClinVar's heavy tail, where
#                     large genes are frequently unique in size. A dedicated
#                     fixture gives every block a distinct size and proves the
#                     fully-frozen case reproduces the observed covariate exactly.
#
#                     A DIAGNOSTIC THAT NOW ACTS.
#                     n_groups_with_multiple_covariate_values was recorded from
#                     the start and NOTHING consumed it -- a repository-wide
#                     search found one test asserting it equals zero and no other
#                     reader. Above a 20 per cent advisory the run now WARNS and
#                     records REASON_BLOCK_COVARIATE_HETEROGENEOUS. It warns
#                     rather than refuses: refusing what the previous
#                     implementation accepted is a regression, the lesson learned
#                     hours earlier when an absolute calibration-fold floor broke
#                     a 427-row cohort.
#
#                     ALSO RECORDED: representative_rule ("first_value_per_block"
#                     -- a reader told only that twelve blocks carried multiple
#                     values still cannot tell WHICH was used),
#                     fraction_blocks_with_multiple_covariate_values, and
#                     p_value_sidedness ("one_sided_upper", left implicit until
#                     now, which invites a reader to halve or double it). The old
#                     keys n_groups and n_groups_with_multiple_covariate_values
#                     are RETAINED, because run manifests already carry them.
#
#                     PROVEN BY SABOTAGE, all six detected:
#                       size_strata silently ignored        -> 3 failed
#                       drift reported without measuring    -> 2 failed
#                       heterogeneity advisory removed      -> 2 failed
#                       representative rule mislabelled     -> 1 failed
#                       frozen-block count always zero      -> 1 failed
#                       scheme reverts to "group"           -> 3 failed
#
#   2544  2026-07-21  +56: tests/unit/test_capability_contract.py -- A FALSE
#                     GREEN IS NOW STRUCTURALLY IMPOSSIBLE TO CONSTRUCT.
#
#                     THE PROBLEM. A validation gate over a capability that does
#                     not exist does not block: it PASSES VACUOUSLY, because
#                     there is nothing to check and therefore nothing to fail. A
#                     green Panel H would then be cited as evidence a disease
#                     head was validated, when no disease head exists. That is
#                     the defect removed five times on 2026-07-21 --
#                     assert_data_usable called from nowhere; a stale enumerated
#                     role cap; a calibrator fitted on trained-on genes;
#                     calibration_valid asserting soundness it never checked; and
#                     n_groups_with_multiple_covariate_values with exactly one
#                     reader in the whole repository.
#
#                     THE CONTRACT. Four questions are separated, because
#                     collapsing them is what allows the false green: does the
#                     capability exist (CapabilityState); does it produce output
#                     (OUTPUT_AVAILABLE); is its target scientifically admissible
#                     (TargetState); has it passed validation (VALIDATED).
#
#                     THE INVARIANT IS IN THE CONSTRUCTOR, NOT THE GATE.
#                     CapabilityEvidence.__post_init__ refuses to build an OK
#                     capability that is not VALIDATED, whose target is not
#                     ADMISSIBLE, or which names no output artifact; and refuses
#                     any non-OK status without a machine-readable reason. There
#                     is no path to the object that avoids the check, so no
#                     caller can forget to consult it -- which is exactly how a
#                     well-tested assert_data_usable came to be called nowhere.
#
#                     WHY TargetState IS A TYPED AXIS AND NOT A REASON STRING.
#                     Gene ranking has a working capability, an available output
#                     AND an available target -- clingen_validity_score. It is
#                     still inadmissible, because that column is one of the
#                     model's own inputs; ranking genes with a model handed
#                     ClinGen's verdict and scoring against ClinGen is circular.
#                     An earlier draft encoded this in a reason STRING, where
#                     nothing could enforce it.
#
#                     TWO DELIBERATE DEPARTURES FROM THE PROPOSAL, BOTH DEFECTS
#                     FOUND BY CHECKING RATHER THAN READING:
#                       (1) MetricStatus is defined ONCE, here, and imported by
#                           clustering_metrics, which loses its local copy. The
#                           proposal defined a SECOND class of the same name --
#                           the divergence problem removed in b8275a0 that same
#                           day, where the legacy evaluator was deleted rather
#                           than wrapped for exactly this reason. The original
#                           six member VALUES are frozen and tested: existing run
#                           manifests on disk contain those strings.
#                       (2) (str, Enum) rather than StrEnum. StrEnum arrived in
#                           Python 3.11; pyproject declares
#                           requires-python = ">=3.10" and the CI matrix runs
#                           only 3.11 and 3.12, so a 3.10 install would fail at
#                           IMPORT with nothing in the pipeline to catch it. A
#                           test now scans the package for StrEnum and fails.
#
#                     THE THREE MEASURED STATES (against the repository, not
#                     estimated): regression -> IMPLEMENTED_NO_OUTPUT + ABSENT
#                     (gtex, rnaseq and functional_assays directories all
#                     absent); multilabel_disease -> NOT_IMPLEMENTED + ABSENT
#                     (ClinVar's disease field and phenotype identifiers are both
#                     unread); gene_ranking -> OUTPUT_AVAILABLE + CONTAMINATED.
#
#                     PROVEN BY SABOTAGE, all seven detected:
#                       OK invariant removed entirely       -> 27 failed
#                       OK no longer needs admissible target->  5 failed
#                       non-OK no longer needs a reason     -> 16 failed
#                       gate treats non-OK as satisfied     -> 13 failed
#                       empty release reports complete      ->  1 failed
#                       an original status value changed    ->  1 failed
#                       StrEnum reintroduced                ->  1 failed
#
#   2559  2026-07-21  +15: tests/unit/test_ci_failure_alert_workflow.py --
#                     THE RATCHET TURNED main RED AND NOBODY LOOKED.
#
#                     WHAT HAPPENED. At 12:58 on 2026-07-21 commit d7c4d35
#                     conflated two changes and left EXPECTED_SUITE_SIZE at 2417
#                     while the tree collected 2446. Continuous Integration run
#                     #562 aborted at COLLECTION time with pytest.UsageError --
#                     exit code 4, three minutes fourteen seconds against the
#                     usual fourteen minutes, because no test ever ran. main
#                     stayed red for roughly two hours before anyone noticed, and
#                     it was noticed by reading pasted local output, not by
#                     looking at Continuous Integration.
#
#                     THE DETECTION WORKED PERFECTLY. THE NOTIFICATION LOOP DID
#                     NOT EXIST. That is the same shape as every defect removed
#                     that day: a check that fires correctly and has no reader is
#                     only marginally better than no check at all.
#
#                     THE FIX. .github/workflows/ci_failure_alert.yml opens a
#                     GitHub issue when Continuous Integration fails on main,
#                     comments on that same issue for repeat failures instead of
#                     opening a pile of them, and CLOSES it when main goes green
#                     again -- so the issue list describes the current state
#                     rather than accumulating stale alarms. A cancelled or
#                     timed-out run changes nothing, because "the run did not
#                     finish" is neither "main is broken" nor "main is fine";
#                     reporting it as either is how a monitoring system lies, the
#                     lesson the drift monitor learned when UNKNOWN was reported
#                     as none.
#
#                     THE ALERT IS ITSELF TESTABLE. A guard that has never fired
#                     is a guard nobody knows works. workflow_dispatch exercises
#                     the whole path on demand and defaults to dry_run so
#                     exercising it creates no noise; with no workflow_run
#                     payload the script treats the situation as a FAILURE,
#                     because an alert tested only on its success branch is
#                     untested. These 15 tests parse the YAML and pin the
#                     trigger, the main-only branch filter, the exact
#                     permissions (contents: read, issues: write), the
#                     concurrency group, the issue reuse, the auto-close, and the
#                     diagnostic text naming exit code 4.
#
#                     A DEFECT CAUGHT IN THE TEST FILE ITSELF. The first draft
#                     used pytest.importorskip("yaml"). PyYAML is pinned in
#                     requirements.txt and imported at runtime by
#                     data/pipeline.py and utils/helpers.py, so it is a HARD
#                     dependency -- and a module-level importorskip collapses
#                     every test in a file into ONE skip entry, which is exactly
#                     how the graph-neural-network branch went untested for 508
#                     Continuous Integration runs. Replaced with a direct import.
#
#                     PROVEN BY SABOTAGE, all five detected:
#                       workflow name typo (never triggers) -> 1 failed
#                       branch filter dropped              -> 1 failed
#                       dispatch exercises success branch  -> 1 failed
#                       permissions widened                -> 1 failed
#                       never closes on recovery           -> 1 failed
#
#   2624  2026-07-21  +65: tests/unit/test_workflow_action_pins.py -- NODE.JS 20
#                     IS DEPRECATED, AND THE TAG IS NOT WHAT SHIPS.
#
#                     TRIGGER. The first run of the new CI failure alert
#                     succeeded and carried a warning: "Node.js 20 is deprecated.
#                     The following actions target Node.js 20 but are being
#                     forced to run on Node.js 24: actions/github-script@v7."
#                     Being forced onto Node 24 is a countdown, not a reprieve.
#
#                     THE AUDIT CORRECTED MY OWN FIRST PASS. I listed
#                     actions/checkout@v4 and actions/setup-python@v5 as exposed.
#                     WRONG: ci.yml and drift_monitor.yml already pinned those by
#                     commit SHA, and resolving the SHAs showed checkout v6.0.2,
#                     setup-python v6.2.0 and upload-artifact v7.0.0 -- all
#                     node24 already. Only data_freshness.yml still carried
#                     floating tags.
#
#                     TWO VERSION GUESSES WERE ALSO WRONG, AND BOTH WOULD HAVE
#                     SHIPPED A FIX THAT FIXED NOTHING:
#                       actions/upload-artifact v5 is STILL node20; v6 is the
#                         first node24 release.
#                       docker/build-push-action v6 is STILL node20; v7 is the
#                         first node24 release.
#                     Reading each action.yml AT THE EXACT SHA caught both. The
#                     tag is not what ships.
#
#                     WHAT CHANGED. Seven references across four workflows now
#                     pin node24 builds by 40-character commit SHA:
#                       ci_failure_alert.yml  github-script  v7 -> v8.0.0
#                       drift_monitor.yml     github-script  v7 -> v8.0.0
#                       data_freshness.yml    checkout/setup-python/
#                                             upload-artifact: floating tags ->
#                                             the same SHAs ci.yml already used
#                       ci.yml                login-action    v3 -> v4
#                                             metadata-action v5 -> v6.2.0
#                                             build-push-action v5 -> v7
#                     Every docker input in use (context, file, target, push,
#                     tags, labels, cache-from, cache-to) was confirmed present
#                     in the target manifests, and metadata-action v6 still emits
#                     the tags and labels outputs the next step consumes.
#
#                     TWO INVARIANTS, BOTH CHECKABLE OFFLINE. (1) Every non-local
#                     `uses:` is a full commit SHA, because a tag is mutable and
#                     re-makes a trust decision silently on every run. (2) Every
#                     SHA appears in EXPECTED_PINS with its resolved version and
#                     runtime, and every runtime is node24 -- so a test that only
#                     asked "is it a SHA?" cannot accept a node20 pin and report
#                     green. Adding a pin without recording what it resolves to
#                     fails the suite, forcing the lookup.
#
#                     CAVEAT RECORDED, NOT PAPERED OVER. ci.yml's push-ghcr job
#                     runs only on `release: published`, so the three docker
#                     upgrades cannot be exercised by an ordinary push. They are
#                     verified by manifest inspection, not by execution.
#
#                     PROVEN BY SABOTAGE, all six detected:
#                       a floating tag reappears           -> 1 failed
#                       an unrecorded SHA is introduced    -> 2 failed
#                       a pin relabelled node20            -> 2 failed
#                       upload-artifact back to v5         -> 1 failed
#                       build-push-action back to v6       -> 1 failed
#                       the regex stops matching           -> 2 failed
#
#   2658  2026-07-21  +34: tests/unit/test_representation_geometry.py -- PANEL R
#                     STAGE ONE. A CLUSTER PANEL CAN RATE A DEGENERATE
#                     REPRESENTATION AS EXCELLENT.
#
#                     THE MEASUREMENT. Two 128-dimensional two-class
#                     representations, 600 rows each. One separates its classes
#                     ANGULARLY and is near-isotropic; the other forces every
#                     vector into a narrow cone and encodes the class ONLY in the
#                     vector norm. Run through this repository's own Panel Q:
#
#                       Davies-Bouldin euclidean   3.934 -> 1.131    3.5x BETTER
#                       Calinski-Harabasz         38.49  -> 371.6    9.7x BETTER
#                       silhouette (euclidean)     0.057 -> 0.404    7.1x BETTER
#                       Davies-Bouldin spherical   1.324 -> 187.98 142x WORSE
#                       ---
#                       mean resultant length      0.238 -> 0.870
#                       effective rank (of 128)  105.9   -> 34.6
#
#                     THREE Panel Q metrics rate the DEGENERATE representation as
#                     substantially better, because radial separation is exactly
#                     what they reward.
#
#                     A CORRECTION TO THE SPECIFICATION. It listed Davies-Bouldin
#                     among the metrics a cone collapse can pass. True of the
#                     Euclidean variant, FALSE of the spherical one, which
#                     already exists at clustering_metrics.py:487 and moved
#                     142-fold. Panel R's warrant is therefore not "nothing
#                     detects this" but ONE METRIC ALARMS AND NOTHING DIAGNOSES:
#                     a spherical Davies-Bouldin of 188 cannot say whether the
#                     information was destroyed or merely badly arranged.
#
#                     WHAT SHIPPED. R1 (directional anisotropy: mean resultant
#                     length raw, centered, and their difference; norm mean, SD,
#                     coefficient of variation, max-to-median, zero-norm
#                     fraction) and R2 (effective rank, normalized effective
#                     rank, participation ratio, spectral entropy, top-k variance
#                     fractions, condition number). Pure functions of a matrix;
#                     every value is a MetricResult carrying its own status.
#
#                     WHAT DID NOT, AND WHY IT IS NOT A STUB. R3 through R7 need
#                     a stored representation. models/gnn.py:357 computes
#                     focal_embeddings and returns only
#                     classifier(focal_embeddings) -- the embedding is discarded.
#                     Those five stages are registered through
#                     CapabilityEvidence as NOT_IMPLEMENTED / ABSENT, and a test
#                     asserts none can satisfy a release gate. A probe panel over
#                     an absent representation would pass vacuously, and a green
#                     Panel R would then be cited as proof the geometry was
#                     checked.
#
#                     A DESIGN FLAW CAUGHT IN REVIEW. The first version returned
#                     UNDETERMINED for a detected collapse, conflating "could not
#                     evaluate" with "found a collapse and cannot classify its
#                     mechanism". Those are different findings and a gate must
#                     treat them differently -- it is the drift monitor's defect,
#                     where UNKNOWN was recorded as none and the notification
#                     never fired once. Split into COLLAPSE_UNDETERMINED and
#                     COLLAPSE_CONE_UNRESOLVED. Five outcomes are now distinct:
#                     healthy, common_direction_growth (linearly removable),
#                     cone_collapse_mechanism_unresolved, undetermined, and
#                     representation_explosion.
#
#                     THRESHOLDS ARE PLACEHOLDERS AND SAY SO. cone at mean
#                     resultant length > 0.5, thin at normalized effective rank
#                     < 0.5. Round numbers chosen in the absence of evidence, not
#                     derived from any representation this project has produced.
#                     Named module constants, overridable per call, with the
#                     three-reference calibration procedure documented for when
#                     an initialization baseline exists.
#
#                     A SABOTAGE THAT FAILED TO FAIL. Mutating n_zero_norm in the
#                     INSUFFICIENT_SUPPORT branch left the suite green: every
#                     assertion exercised the OK branch instead. A refusal must
#                     still report HOW BAD it was -- 31 of 32 zero-norm rows and
#                     1 of 32 are the same status and very different findings.
#                     Test added; the sabotage now fails.
#
#                     A TEST THAT PASSED FOR THE WRONG REASON. The
#                     rows-per-dimension floor was "tested" with 4 rows, which
#                     trips the absolute floor of 8 first and never reached the
#                     branch. Rewritten to use 100 rows against 128 dimensions,
#                     which clears one floor and fails the other, plus a
#                     complement proving the guard does not refuse everything.
#
#                     PROVEN BY SABOTAGE, all seven detected once retargeted:
#                       non-finite silently filtered      -> 2 failed
#                       zero-norm dropped without counting-> 1 failed
#                       rows-per-dimension floor removed  -> 1 failed
#                       unevaluable reports healthy       -> 4 failed
#                       detected collapse -> UNDETERMINED -> 3 failed
#                       an unbuilt stage claims OK        -> 3 failed
#                       norm_cv returns 0 at zero mean    -> 2 failed
#
#   2713  2026-07-21  +55: tests/unit/test_capability_lifecycle.py -- A CAPABILITY
#                     CANNOT CLAIM VALIDATION IT NEVER CLIMBED TO.
#
#                     THE GAP. capabilities.py checks that a declared state is
#                     internally consistent. It cannot check that the state was
#                     REACHED legitimately. Those are different questions, and
#                     only the second prevents a capability being flipped from
#                     "not built" to "validated" in one commit -- which means it
#                     never existed in the state where the interesting defects
#                     live: an artifact produced but wrong, a probe that runs but
#                     leaks, a metric computed on the wrong tensor.
#
#                     The instruction not to make that jump was written as prose.
#                     PROSE DOES NOT ENFORCE ITSELF, which is the same lesson the
#                     Continuous Integration suite-size comment taught when 330
#                     tests could have vanished behind a hand-maintained floor
#                     that still said PASS.
#
#                     THE RULE. Forward moves advance EXACTLY ONE RUNG along
#                     NOT_IMPLEMENTED -> IMPLEMENTED_NO_OUTPUT -> OUTPUT_AVAILABLE
#                     -> VALIDATED. Backward moves are legal at ANY distance, and
#                     DEPRECATED is reachable from anywhere, because discovering
#                     that an artifact is wrong must never be harder to record
#                     than claiming it is right -- a contract that made retraction
#                     awkward would quietly encourage not retracting. A DEPRECATED
#                     capability cannot be revived in place; it must be
#                     re-registered under a new name so the deprecation stays
#                     visible in the record.
#
#                     TransitionVerdict is never a bare boolean: an illegal
#                     verdict MUST carry a reason and a legal one MUST NOT, so a
#                     reader cannot skim a passing verdict's reason and mistake it
#                     for a warning.
#
#                     THE PANEL R MAP. panel_r_expected_ladder() records the rung
#                     each of R3-R7 must climb and what unblocks each step, so a
#                     future claim of "R3 is validated" can be checked against the
#                     PATH TAKEN rather than accepted on trust. It is a map, not a
#                     gate: a test asserts it does not contradict the live
#                     NOT_IMPLEMENTED registrations in representation_geometry.py.
#
#                     A CORRECTION TO AN EARLIER CLAIM. Panel R stage one said
#                     R3-R7 "cannot be built". That was an overstatement. They are
#                     blocked by an OBSERVABILITY gap -- gnn.py returns only
#                     classifier(focal_embeddings) -- and an observability gap is
#                     a refactor, not an impossibility. The reason constants now
#                     name the specific missing artifact rather than restating the
#                     status.
#
#                     TEN LAZY SKIPS REMOVED BEFORE THEY SHIPPED. The first draft
#                     parametrized backward transitions over a 4x4 cross-product
#                     and skipped the forward and identity cases, leaving ten
#                     permanent skips that existed only because the test was
#                     written carelessly. Replaced with a generated pair list, plus
#                     a guard asserting the generator yields n*(n-1)/2 pairs --
#                     an empty list would have made every backward test pass
#                     vacuously.
#
#                     PROVEN BY SABOTAGE, all seven detected:
#                       one-rung rule relaxed to any forward -> 4 failed
#                       a deprecated capability revived      -> 1 failed
#                       backward moves refused               -> 6 failed
#                       type checks removed                  -> 4 failed
#                       R3 and R4 given different blockers   -> 1 failed
#                       the map itself skips a rung          -> 2 failed
#                       a legal verdict carries a reason     -> 1 failed
#
#   2724  2026-07-21  +11: tests/unit/test_gnn_dependency_contract.py -- THE
#                     GNN BRANCH IS CI-COVERED, AND ITS COMMENTS NOW SAY SO.
#
#                     THE STALE COMMENT. Four graph-neural-network test files
#                     carried "# GNN tests need PyG; skip where absent (e.g. CI)"
#                     on their pytest.importorskip("torch_geometric") line:
#                     test_gnn_shared_graph.py, test_gnn_optim.py,
#                     test_gnn_tier2_denoise.py, test_gnn_gps.py. As of today that
#                     is FALSE. torch-geometric is pinned at 2.7.0 in
#                     requirements.txt, and ci.yml carries a hard gate ("Assert
#                     the coverage-critical dependencies are present") that
#                     sys.exit(1)s if torch_geometric is not importable. The GNN
#                     branch is fully covered in Continuous Integration; the skip
#                     fires only on an under-provisioned LOCAL machine.
#
#                     WHY IT WAS WORTH FIXING, NOT JUST COSMETIC. The comment
#                     asserted the exact belief that let the graph-neural-network
#                     branch go untested for 508 runs -- that Continuous
#                     Integration is allowed to skip these tests. A false comment
#                     beside a real safety net is worse than none: it tells a
#                     debugging reader the wrong thing with apparent authority.
#
#                     THE FIX, NOT A DELETION. The importorskip GUARD stays -- a
#                     developer without PyG should get a clean skip, not a
#                     collection error. The COMMENT is corrected to state the
#                     real contract: PyG is pinned, CI fails without it, so this
#                     skip is local-only. Each of the four diffs is one line
#                     removed and six added; the guard line is byte-identical.
#
#                     THE CONTRACT IS PINNED. test_gnn_dependency_contract.py
#                     asserts, without importing torch: (a) none of the GNN files
#                     reintroduce the stale phrasing or any "CI may skip"
#                     comment; (b) ci.yml's REQUIRED gate still lists
#                     torch_geometric; (c) that gate still sys.exit(1)s. So the
#                     comment cannot rot back to a lie, and the CI gate cannot
#                     quietly drop the dependency.
#
#                     TWO DEFECTS IN THE TEST FILE, CAUGHT BY SABOTAGE. First, the
#                     contract file matched its own test_gnn_* glob and scanned
#                     itself -- a false positive by construction; excluded by
#                     name. Second, the assertions searched the WHOLE ci.yml for
#                     "torch_geometric": and "sys.exit(1)", but torch_geometric
#                     also appears in the pip-check gate, so renaming it in the
#                     REQUIRED set alone still passed. Rescoped to the REQUIRED
#                     gate heredoc body, matched with an indentation-agnostic
#                     regex rather than a hard-coded space count.
#
#                     A BROAD-NET TEST WAS REMOVED, NOT ADDED. An earlier draft
#                     carried test_no_gnn_test_file_claims_ci_may_skip, a regex
#                     matching any comment with both "CI" and "skip". It
#                     false-positived on this project's OWN truthful comments --
#                     "Never skips in CI" and the quote-to-refute "do not read
#                     this as 'CI may skip'" -- and no bounded regex separates an
#                     assertion of a falsehood from its negation or its
#                     refutation without natural-language work. It was dropped.
#                     The precise STALE-phrase check catches the one dead phrasing
#                     that ever occurred; the REAL guarantee is the ci.yml gate,
#                     pinned below. A comment is documentation; the gate is the
#                     contract. Net test count is 11, not 12.
#
#                     PROVEN BY SABOTAGE, all four detected:
#                       stale phrase reintroduced          -> 1 failed
#                       torch_geometric removed from gate  -> 1 failed
#                       gate downgraded from sys.exit(1)   -> 1 failed
#                       an importorskip guard deleted      -> 1 failed
#
#   2740  2026-07-21  +16: tests/unit/test_gnn_typed_output.py -- THE GNN
#                     EXPORTS ITS REPRESENTATION WITHOUT CHANGING WHAT IT
#                     PREDICTS.
#
#                     THE DISCARD THIS UNDOES. models/gnn.py computed
#                     focal_embeddings = x[gene_idx] and returned only
#                     classifier(focal_embeddings). That single discard is why
#                     Panel R's R3-R7 are NOT_IMPLEMENTED: every probe, whitening
#                     ladder and trajectory needs a representation the model
#                     never handed out.
#
#                     THE DESIGN, AGAINST THE REAL CALLERS. The proposal in the
#                     specification had forward() ALWAYS return a typed output.
#                     Reading the caller inventory ruled that out: five sites do
#                     out = model(...); F.softmax(out) -- gnn.py:424/473/490 and
#                     ewc.py:186/379 -- and an always-on dataclass breaks all
#                     five in one commit. Instead a keyword-only
#                     return_embeddings=False leaves the default return a bare
#                     (n_focal, 2) tensor, byte-identical to before, so every
#                     existing caller is untouched; return_embeddings=True returns
#                     GNNOutput(logits, focal_embeddings). VariantGATGPS, the
#                     documented drop-in with an identical signature, gets the
#                     same treatment in lock-step so the swap stays valid.
#
#                     THE CONTRACT, AND THE TEST THAT PROVES IT. The exported
#                     tensor must be THE EXACT tensor the classifier consumed --
#                     not a copy, not an adjacent layer. A forward-pre-hook on the
#                     classifier captures its input; the test asserts it equals
#                     output.focal_embeddings bit-for-bit (rtol=0, atol=0). That
#                     holds WITHIN one forward pass, so it does not depend on
#                     dropout determinism and would catch a future refactor that
#                     exported a different layer. Exact-logit invariance is tested
#                     under eval() + inference_mode, where dropout is a no-op, and
#                     the test ASSERTS eval mode rather than assuming it. Gradient
#                     flow is checked in train mode: the encoder still receives
#                     gradients and the exported embedding stays attached to
#                     autograd (detachment is the extraction boundary's job, a
#                     later commit).
#
#                     WHAT THIS COMMIT DOES NOT DO. It does not persist anything,
#                     does not add R3-R7, and does not move any capability past
#                     NOT_IMPLEMENTED. It makes the representation OBSERVABLE.
#                     Per the capability ladder (test_capability_lifecycle.py),
#                     the next legal step for R3-R7 is IMPLEMENTED_NO_OUTPUT, and
#                     it is a separate commit.
#
#                     VERIFICATION BOUNDARY, STATED PLAINLY. These tests require
#                     torch, which the build environment for this delivery could
#                     not install. The control flow was verified with a mock
#                     torch (default -> bare tensor; opt-in -> GNNOutput with the
#                     classifier input); the numerical claims -- invariance,
#                     gradient flow, finiteness -- are proven by the real run on
#                     Monzia's machine before commit. The diff is exactly the
#                     added import, a keyword-only parameter, a docstring and the
#                     branch; the default path changes only
#                     'return classifier(...)' into 'logits = classifier(...);
#                     ...; return logits'.
#
#   2763  2026-07-21  +23: the extraction boundary. THE GNN'S FOCAL
#                     REPRESENTATION IS NOW PERSISTED WITH IMMUTABLE PROVENANCE
#                     AND A ROW-ORDER HASH, AND R3/R4/R5 CLIMB ONE RUNG.
#
#                     WHAT LANDED. src/.../evaluation/representation_artifact.py:
#                     RepresentationArtifact (frozen dataclass) + the
#                     extract_focal_embeddings boundary function. It turns a
#                     GNNOutput carrying focal_embeddings into a persisted,
#                     hashed, provenanced artifact. +20 tests in
#                     test_representation_artifact.py. representation_geometry.py:
#                     panel_r_capabilities advances R3/R4/R5 from NOT_IMPLEMENTED
#                     to IMPLEMENTED_NO_OUTPUT; +3 net tests.
#
#                     THE FOUR GUARANTEES, EACH PROVEN FALSIFIABLE BY SABOTAGE.
#                     (1) IDENTITY: the persisted matrix is exactly the exported
#                     embedding, detached to CPU float64. Sabotage: skip the
#                     detach -> 1 failed; drop float64 -> 1 failed. (2) ROW ORDER:
#                     a SHA-256 over ordered row keys; a reorder is detectable via
#                     verify_row_order. Sabotage: sort keys before hashing (making
#                     it order-insensitive) -> 2 failed. (3) IMMUTABILITY: frozen
#                     dataclass + read-only matrix. Sabotage: remove setflags ->
#                     1 failed. (4) PARTITION ROLE travels with the matrix and is
#                     validated. Sabotage: accept any role -> 1 failed. Plus the
#                     no-embeddings guard -> 1 failed, and the internal hash-
#                     consistency check -> 1 failed. All SEVEN detected; restored
#                     -> 20 passed.
#
#                     TWO TEST DEFECTS SELF-CAUGHT. A first sabotage pass had two
#                     that failed to fail. One was a MALFORMED sabotage (the
#                     replace target used chr(10) where the source has a real
#                     newline literal), re-run correctly and it fired. The other
#                     was a REAL test gap: the no-embeddings test asserted "some
#                     ValueError", and np.asarray(None, dtype=float64) yields
#                     array([nan]) shape (1,), which trips the 2-D shape check and
#                     raises ValueError from the WRONG place -- so removing the
#                     guard left the test green. Pinned the test to the guard's
#                     specific message; removing the guard now turns it red.
#
#                     THE LADDER DISCIPLINE. This commit PERSISTS and stops -- no
#                     probe, no whitening, no metric (a test asserts no such
#                     surface is exposed). Per capability_lifecycle.py the advance
#                     is NOT_IMPLEMENTED -> IMPLEMENTED_NO_OUTPUT, one rung,
#                     verified legal via transition_is_legal. R6 (needs a
#                     checkpoint series) and R7 (needs longitudinal outcomes) do
#                     NOT advance. A test asserts the advance did not make any
#                     stage releasable -- IMPLEMENTED_NO_OUTPUT is still far below
#                     VALIDATED.
#
#                     MIRRORS prediction_artifacts.py: atomic .tmp-then-rename +
#                     fsync writes, JSON manifest with git SHA, parquet not
#                     pickle, library-module logging. Built on the existing
#                     foundation, not a parallel one.
#
#   2786  2026-07-21  +23: Panel R stage R3 -- the norm-angle probe. A
#                     LABEL-FREE LINEAR MAP, FIT ON TRAIN ONLY, MEASURES WHETHER
#                     A COLLAPSED REPRESENTATION'S GEOMETRY IS RECOVERABLE.
#
#                     THE SCIENTIFIC QUESTION R3 ANSWERS. R1/R2 measured
#                     representation collapse; R3 asks whether it is recoverable.
#                     Each embedding splits into a NORM (radial magnitude) and an
#                     ANGLE (direction on the sphere). A cone collapse concentrates
#                     the angles while the norms may still separate structure.
#                     norm_angle_probe.py fits ZCA whitening -- mean and covariance,
#                     no labels -- and measures angular concentration (mean
#                     resultant length: 1=collapsed, 0=spread) before and after.
#                     On a known cone collapse the concentration drops from ~0.95
#                     to ~0.02: the collapse was a linear reparameterisation
#                     artifact a label-free map undoes. That is the answer.
#
#                     THE LEAKAGE GUARD, STRUCTURAL NOT ADVISORY. The whitening
#                     transform is fit on TRAIN only. Fitting mean/covariance on
#                     STRUCTURE or TEST leaks the evaluation distribution into the
#                     map EVEN WITH NO LABELS -- the second-moment structure of the
#                     held-out set is itself information. fit_whitening reads the
#                     RepresentationArtifact's bound partition_role and RAISES
#                     LeakageError on any non-TRAIN artifact. The TRAIN transform
#                     is then applied unchanged to held-out partitions via
#                     apply_whitening, which never refits. Sabotage: remove the
#                     guard -> 5 failed.
#
#                     PROVEN BY SABOTAGE, all seven detected: fit leakage guard
#                     removed -> 5 failed; report anchor guard -> 1; whitening
#                     identity broken -> 1; angular concentration forced to 0 -> 2;
#                     zero-norm rows not excluded -> 2; row-order verify skipped ->
#                     1; transform width check removed -> 1. Restored -> 22 passed.
#
#                     TWO TEST DEFECTS SELF-CAUGHT. Two guards (the report-level
#                     anchor check and the transform width check) were redundantly
#                     covered by a deeper ValueError, so removing them still raised
#                     -- the wrong-reason pattern. Both tests were pinned to their
#                     guards' SPECIFIC messages ("anchor", "cannot whiten shape"),
#                     so removing either now turns the suite red and the guards are
#                     proven to fire independently.
#
#                     THE LADDER. R3 advances IMPLEMENTED_NO_OUTPUT ->
#                     OUTPUT_AVAILABLE (its probe protocol now exists), verified
#                     legal via transition_is_legal. NOT MetricStatus.OK: the
#                     capabilities contract reserves OK for VALIDATED with an
#                     ADMISSIBLE target and a named artifact -- caught at
#                     construction, corrected to INSUFFICIENT_SUPPORT, because an
#                     output existing is not an output validated. R4/R5 stay at
#                     IMPLEMENTED_NO_OUTPUT (their probes are unbuilt); R6/R7 stay
#                     NOT_IMPLEMENTED. A coupled test in test_capability_lifecycle
#                     still passes: the ladder MAP is unchanged, only R3's present
#                     rung moved.
#
#   2800  2026-07-22  +14: record the NEGATIVE R3 transfer-validation result.
#                     THE APPARENT RECOVERY IS A MEAN-CENTERING ARTIFACT THAT DOES
#                     NOT SURVIVE HELD-OUT VALIDATION -- AND THAT IS THE FINDING.
#
#                     THE SCIENCE. R3's norm-angle probe reported a large in-sample
#                     angular recovery (~0.98 on a cone collapse). r3_validation.py
#                     tests it held-out and null-calibrated: on TEST the same
#                     TRAIN-fit whitening recovery is ~0 and does not exceed the
#                     reference null. A decomposition PROVES why: centering alone
#                     accounts for 99.6% of the in-sample drop; centering with the
#                     TRAIN mean recovers ~0.016 on TEST while centering with TEST's
#                     OWN mean recovers ~0.982. The "recovery" was the cone's common
#                     direction removed as a per-partition mean, unavailable at
#                     deployment. The capability contract WORKED: it stopped an
#                     appealing in-sample number from being promoted.
#
#                     THE THREE-CONCEPT RECORD (Monzia's design). ValidationArtifact
#                     separates implementation validity (the code computes the
#                     intended quantity: PASS), statistical/transfer validity
#                     (generalises held-out: FAIL), and admissibility (both: FALSE).
#                     A non-admissible metric MUST carry a finding -- enforced at
#                     construction. Three findings recorded: the transfer failure,
#                     the mean-centering decomposition, and a NULL-ADEQUACY finding.
#
#                     NULL ADEQUACY. The current rotation-only null is EXACTLY
#                     rotation-invariant for angular concentration (verified to
#                     machine precision), so it cannot separate the observed
#                     whitening from chance on this statistic. It is retained ONLY
#                     as an invariance control; a matched-spectrum null family
#                     (eigenvalue-permutation + random-orientation SPD, preserving
#                     the whitening gain spectrum while randomising covariance
#                     alignment) is the correct inferential reference and lands in a
#                     later methodological commit. The p-value is reported as a
#                     Monte Carlo matched-null tail probability, not an exact
#                     permutation p-value.
#
#                     PROVEN BY SABOTAGE (S1-S6 all fire): admissible-ignores-
#                     transfer, transfer-forced-True, p-value-inverted, leakage-
#                     guard-removed (message-pinned distinct from the inner guard),
#                     finding-requirement-dropped, own-mean-decomposition-faked.
#                     The identity-null sabotage is intentionally reclassified: it
#                     does not alter the rotation-invariant estimand, which is a
#                     property of the metric, not a test gap -- addressed by the
#                     matched-null commit. A PERMANENT REGRESSION TEST (common
#                     direction + noise + no transferable signal) locks in that the
#                     validator refuses to promote this class of false discovery.
#
#                     R3 STAYS OUTPUT_AVAILABLE (not VALIDATED): the output was
#                     checked and found NOT admissible. Its capability reason now
#                     cites the negative finding. Release gate unsatisfied. Nothing
#                     hidden, nothing promoted, nothing discarded.
#
#   2803  2026-07-22  +3: codify the orthogonal invariance of angular
#                     concentration. THE STATISTIC IS EXACTLY ROTATION-INVARIANT,
#                     WHICH IS WHY A ROTATION-ONLY NULL IS A CONTROL, NOT A TEST.
#
#                     mean resultant length ||mean(v/||v||)|| rotates with the
#                     data and a norm is rotation-invariant, so an orthogonal map
#                     leaves the value unchanged to machine precision (verified
#                     across 4 inputs x 5 rotations, delta < 1e-10). This reframes
#                     the earlier identity-null "sabotage": substituting identity
#                     for a rotation null did not move the verdict because it
#                     CANNOT -- the estimand is rotation-invariant. That is the
#                     correct, expected result, not a gap.
#
#                     A contrast test shows a non-orthogonal RESCALING map DOES
#                     change angular concentration, which is exactly why a matched-
#                     spectrum rescaling null (preserving the whitening gain
#                     spectrum while randomising orientation) is informative where
#                     a rotation null is not -- motivating the null-family commits.
#
#                     Sabotage-verified: breaking rotation-invariance in
#                     angular_concentration turns the two invariance tests red; the
#                     rescaling-contrast test correctly stays green (different
#                     property). Pure test commit, no source change.
#
#   2819  2026-07-22  +16: the matched-spectrum null family. A NULL THAT
#                     MATCHES THE WHITENING'S STRENGTH AND DESTROYS ONLY ITS
#                     COVARIANCE ALIGNMENT -- THE RIGHT INFERENTIAL REFERENCE.
#
#                     WHY. A rotation null cannot test angular concentration (it
#                     is rotation-invariant -- Commit 2). The right null preserves
#                     the whitening GAIN SPECTRUM (singular values, determinant,
#                     Frobenius/operator norm, condition number -- the whole
#                     numerical character) while randomising the ALIGNMENT of the
#                     gains with the covariance eigen-directions, the one thing
#                     under test. Two nulls: PRIMARY eigenvalue-assignment
#                     permutation (keep U and gains, permute the gain-to-direction
#                     assignment -- a clean conditional-randomisation null, O(d)
#                     per draw); SECONDARY matched-spectrum random orientation
#                     (same gains, Haar-random orientation -- a broader alignment
#                     test). Verified: both share the observed condition number
#                     exactly and neither whitens the covariance (observed error
#                     ~0, null ~40).
#
#                     GAIN-SPREAD PRECONDITION. Under heavy shrinkage the gains
#                     flatten and permuting near-equal gains is uninformative. The
#                     permutation null REFUSES a gain coefficient of variation
#                     below a floor (FlatGainSpectrumError); the orientation null,
#                     which reorients rather than reassigns, is the fallback and
#                     still works. This is the robustness guard added to Monzia's
#                     design so a flat spectrum fails loud, not silently anti-
#                     conservative.
#
#                     ARCHITECTURE. WhiteningTransform gains two OPTIONAL fields
#                     (eigenvectors, gains), populated by fit_whitening, so ONE
#                     fitted object carries both the apply-able map and the factors
#                     a null needs -- no parallel object to keep in sync. W is
#                     built from the factored form so W == (U*gains)@U.T exactly. A
#                     null built from a transform lacking the factors raises
#                     MissingEigendecompositionError.
#
#                     PROVEN BY SABOTAGE (6 fire): permutation-not-permuting,
#                     orientation-is-identity (caught by a reorientation test
#                     asserting off-diagonal mass and draw-to-draw difference,
#                     since a diagonal still matches the spectrum), gain-spread-
#                     guard-removed, spectrum-match-always-passes, factor-
#                     requirement-dropped, null-uses-ones-not-gains. The p-value
#                     is a Monte Carlo matched-null tail probability, not exact.
#
#   2829  2026-07-22  +10: wire the matched null family into a TWO-NULL
#                     INTERSECTION admissibility rule. A RECOVERY CLAIM MUST CLEAR
#                     EVERY CONDITION -- NO NULL COMPENSATES FOR THE OTHER.
#
#                     validate_recovery_matched runs the held-out concentration
#                     statistic against a matched-spectrum null (primary
#                     eigenvalue-permutation or secondary orientation) instead of
#                     the rotation-only control. On the R3 cone case all three
#                     nulls agree: no transfer (observed ~ -0.013).
#
#                     decide_recovery_admissibility applies Monzia Moodie's section
#                     14 intersection: admissible iff implementation AND transfer
#                     AND primary-null AND secondary-null AND centering-not-dominant
#                     AND gene-cluster-uncertainty. Verified strict: the positive
#                     path passes only when ALL hold, and breaking ANY single one
#                     (primary, secondary, centering, cluster CI) fails it with the
#                     matching finding. A null passing requires a low tail
#                     probability AND a positive observed recovery, so a transform
#                     that makes geometry WORSE cannot be admitted on a tail fluke.
#
#                     UNCERTAINTY IS PENDING, NEVER DEFAULT-TRUE. The gene-cluster
#                     bootstrap does not exist yet, so cluster_ci_low defaults to
#                     None -> uncertainty_passed None -> admissible False with a
#                     gene_cluster_uncertainty_pending_infrastructure finding. The
#                     honest state is "not yet checked", which BLOCKS admissibility
#                     rather than silently passing. A real lower confidence bound
#                     activates the check (>0 passes, <=0 fails distinctly).
#
#                     PROVEN BY SABOTAGE (5 fire): PENDING-treated-as-pass,
#                     intersection-becomes-OR, centering-condition-dropped, primary-
#                     null-ignores-observed-sign, unknown-null-kind-silently-
#                     defaults. Reported figures are Monte Carlo matched-null tail
#                     probabilities, not exact permutation p-values.
#
#   2846  2026-07-22  +17: separate directional dispersion from covariance-
#                     ALIGNMENT recovery, and calibrate the matched null only
#                     against the alignment-sensitive estimand.
#
#                     THE FINDING. A matched-spectrum null showed angular
#                     concentration is ALIGNMENT-BLIND: it responds to whitening
#                     gain-spectrum MAGNITUDE (which the null preserves), not gain-
#                     to-direction ALIGNMENT (which the null scrambles), so it
#                     cannot beat the matched null and cannot evidence covariance
#                     alignment. The apparent angular "recovery" and the alignment
#                     question are two different estimands the single word
#                     "recovery" had conflated.
#
#                     THE ALIGNMENT ESTIMAND. whitening_alignment_recovery measures
#                     held-out covariance-SHAPE recovery from a TRAIN-fitted
#                     transform. The covariance is TRACE-NORMALISED before the
#                     identity distance, so shape_error is EXACTLY scale-invariant
#                     (verified: 25x rescaling leaves it unchanged) and a global
#                     variance contraction cannot masquerade as alignment recovery;
#                     scale is reported separately. It is fit TRAIN-only via a
#                     FittedWhiteningTransform whose provenance makes the leaking
#                     path (held-out mean) inexpressible; a non-TRAIN fit raises
#                     LeakageError with a stable reason.
#
#                     CALIBRATION. Against the matched null the alignment statistic
#                     controls Type-I error under isotropic no-transfer fixtures
#                     (0/40) and has power under shared-covariance transfer (40/40),
#                     while the angular statistic provably does NOT beat the matched
#                     null even where the alignment statistic does. CalibrationSummary
#                     stores numerator and denominator and refuses a rate that does
#                     not match its counts -- the arithmetic cannot drift.
#
#                     COMPATIBILITY CONTRACT. RecoveryStatisticSpecification types
#                     each statistic by sensitivity and its compatible null targets;
#                     pairing a gain-assignment null with the alignment-blind angular
#                     statistic RAISES, so a future reader cannot mistake structural-
#                     zero-power for a broken implementation.
#
#                     CAPABILITY. R3 splits into R3a (angular dispersion: OUTPUT_
#                     AVAILABLE, transfer FAILED, alignment-blind, non-admissible --
#                     the negative finding preserved) and R3b (whitening alignment:
#                     method-validated on SYNTHETIC fixtures, scientific validation
#                     NOT evaluated, non-admissible). Two orthogonal axes
#                     (MethodValidationState, ScientificValidationState); the base
#                     CapabilityState enum is untouched. Admissibility requires
#                     held-out genomic scientific validation, never synthetic alone.
#
#                     PROVEN BY SABOTAGE (6 fire): shape-not-trace-normalised,
#                     leakage-guard-removed, compatibility-check-vacuous, calibration-
#                     invariant-off, raw-covariance-uses-held-out-mean, R3b-admissible-
#                     on-synthetic-only. Figures are Monte Carlo matched-null tail
#                     probabilities. R3 as a whole is NOT validated; no genomic claim.
#
#   2848  2026-07-22  +2: wire the R3a/R3b split into the panel capability
#                     reason. THE PANEL NOW REPORTS TWO SUB-CAPABILITIES, EACH
#                     NON-ADMISSIBLE FOR ITS OWN, DISTINCT REASON.
#
#                     R3a (angular dispersion): does not transfer held-out (mean-
#                     centering artifact) AND is alignment-blind under the matched
#                     null -- non-admissible as alignment evidence, the negative
#                     finding preserved. R3b (whitening alignment): trace-normalised
#                     covariance-shape recovery, alignment-sensitive, beats the
#                     matched null on synthetic fixtures (method-validated on
#                     SYNTHETIC), but its scientific validation on real genomic data
#                     has not been run -- also non-admissible, pending held-out
#                     genomic data, gene-cluster uncertainty, and prespecified
#                     thresholds. R3 as a whole stays OUTPUT_AVAILABLE and non-
#                     releasable. The reason points to r3a_angular_dispersion_
#                     evidence() and r3b_whitening_alignment_evidence().
#
#                     Sabotage-verified: removing the R3a/R3b/alignment-blind
#                     markers from the reason, or flipping R3b to appear
#                     scientifically validated, turns the new tests red. Reason-
#                     string change only; no new source file.
#
#   2857  2026-07-22  +9: hand the R3b-validated recovery protocol to the R4
#                     conditioning ladder as a binding inheritance contract --
#                     WITHOUT fabricating output R4 does not have.
#
#                     R4 (conditioning recoverability) has no genomic
#                     representation to run on, so it stays IMPLEMENTED_NO_OUTPUT.
#                     recovery_protocol.py binds it, by contract, to REUSE the
#                     protocol R3b validated when it is eventually implemented: a
#                     TRAIN-fitted leakage-safe whitening transform, the alignment-
#                     SENSITIVE trace-normalised shape-recovery statistic (not the
#                     alignment-blind angular one), and the matched-spectrum null
#                     family, all calibrated on synthetic fixtures. RecoveryProtocol
#                     refuses a non-TRAIN fit and refuses an alignment-blind
#                     statistic paired with a gain-assignment null (reusing the R3b
#                     compatibility contract). ProtocolInheritance refuses an un-
#                     calibrated protocol and refuses a no-output stage without a
#                     recorded reason -- and does NOT promote R4.
#
#                     This is the honest completion of the seven-commit matched-
#                     null sequence: the validated protocol is handed to the next
#                     ladder rung as a contract, not turned into a result. R4's
#                     ladder OUTPUT_AVAILABLE description now cites the inheritance.
#
#                     PROVEN BY SABOTAGE (6 fire): non-TRAIN-fit-guard removed,
#                     compatibility-guard removed, calibration-inherit-guard
#                     removed, no-output-reason-guard removed, R4-promoted-to-has-
#                     output, and R4-ladder-inheritance-text removed.
#
#   2860  2026-07-22  +3: message-pin the spurious scikit-learn
#                     parallel.delayed thread-config warning, and pin the premise
#                     with a contract test so the filter stays honest.
#
#                     The warning ("sklearn.utils.parallel.delayed should be used
#                     with sklearn.utils.parallel.Parallel ... propagate the
#                     configuration ... to the joblib workers") is emitted by
#                     scikit-learn's OWN estimators (RandomForest n_jobs != 1) as a
#                     thread-config-propagation note. It is not a correctness signal
#                     and it is not this project misusing delayed. It is execution-
#                     order dependent -- observed at 448, 2499, and 0 occurrences on
#                     different full-suite runs, attributed to whichever file trips
#                     it first -- which trains readers to scroll past UserWarnings.
#                     A message-pinned filter removes it deterministically.
#
#                     The contract test pins the premise that makes silencing safe:
#                     every delayed(...) in the project is paired with a joblib
#                     Parallel(...) in the same module, and no module imports delayed
#                     from sklearn.utils.parallel. If a future edit introduces a bare
#                     joblib.delayed feeding a scikit-learn Parallel -- which WOULD
#                     make the warning our own signal -- the test fails and the filter
#                     must be reconsidered. The filter is pinned to the exact message,
#                     never broadened to ignore::UserWarning, so a different
#                     UserWarning still reaches us.
#
#                     PROVEN BY SABOTAGE (4 fire): filter removed from pyproject,
#                     delayed() without Parallel() in a module, delayed imported from
#                     sklearn.utils.parallel, and filter broadened to the bare
#                     category.
#
#   2867  2026-07-22  +7: add the teardown-abort diagnostic -- a dispatch-only
#                     workflow plus a probe script. NO test file was added; the
#                     seven come from test_workflow_action_pins.py, which is
#                     PARAMETRIZED over workflow files and over every `uses:`
#                     reference line (1 new file + 2 tests x 3 references = 7,
#                     measured: that file collected 65 before and 72 after).
#
#                     WHY. CI run 29962715186 (#585, commit 821a990) failed one
#                     test on Python 3.12 only: a child process of
#                     test_rnaseq_ablation_tools.py returned -6 (SIGABRT) with
#                     "terminate called without an active exception" AFTER printing
#                     its own success line -- the work completed and the process
#                     aborted during interpreter finalization. Re-running the
#                     identical commit passed, proving nondeterminism, and 730 local
#                     reproduction attempts at the exact pinned versions (numpy
#                     2.4.4 / pandas 2.3.3 / pyarrow 23.0.1) produced zero aborts.
#                     The culprit library is therefore NOT identified, and no
#                     production code is changed until it is.
#
#                     WHAT IT DOES. Nine parallel arms in the real CI environment.
#                     Bisect by construction: numpy alone, pandas without parquet,
#                     a pyarrow read, a pyarrow write, pyarrow without pandas, and
#                     the real production script -- the smallest arm that aborts
#                     names the culprit without symbol lookup. Bisect by mitigation:
#                     the real script with Arrow input/output threads, OpenMP
#                     threads, and all pools constrained to one -- an arm whose rate
#                     collapses identifies the pool causally. Core dumps are enabled
#                     and any core is backtraced with the GNU Debugger, which names
#                     the aborting frame directly. Rates are reported with the
#                     one-sided 95% rule-of-three upper bound so a zero-abort arm is
#                     read as a bound, not as proof of safety.
#
#                     SAFETY. The workflow is workflow_dispatch only: it never runs
#                     on push and cannot turn main red. The probe writes only inside
#                     a temporary directory and always exits 0, because finding an
#                     abort is a SUCCESS for a diagnostic; it reports, it does not
#                     gate. The probe was sabotage-verified: an injected arm that
#                     aborts during finalization is detected 6/6 with returncode -6,
#                     the preceding stdout captured, and the signature grouped on
#                     the C++ message rather than trailing faulthandler noise.
#
#   2874  2026-07-23  +7: fix the SIGABRT at interpreter teardown at its root.
#
#                     pandas.read_parquet hands Arrow a PYTHON file handle, which
#                     Arrow wraps in arrow::py::PyReadableFile. That wrapper holds a
#                     Python reference, so its destructor calls PyGILState_Ensure.
#                     Running on an Arrow background thread after finalisation has
#                     begun, CPython take_gil kills the thread with pthread_exit; the
#                     forced unwind crosses C++ destructor frames and libstdc++ calls
#                     std::terminate. Twenty-seven core dumps carry that exact chain,
#                     once each.
#
#                     pq.read_table opens the file natively in C++, so the aborting
#                     destructor is never constructed. Measured in ONE run at 5000
#                     executions per arm: pandas.read_parquet 27 aborts, the native
#                     read 0, twice, in two independent arms.
#
#                     The tests walk the syntax tree to prove pandas.read_parquet is
#                     not called, compare both readers frame to frame to prove the
#                     data is unchanged, and run all four modes end to end.
#                     Reintroducing pandas.read_parquet turns two of them red.
#
#                     Two hypotheses were wrong and are recorded in the incident
#                     note: OpenBLAS (bystander, zero frames on any abort path) and
#                     the Arrow-to-pandas conversion (refuted, 0/5000).
#
#                     docs/INCIDENT_2026-07-23_rnaseq_ablation_teardown_abort.md
#
#   2893  2026-07-23  +19: the typed contracts for Panel S0 (Mixture-of-
#                     Experts expert identity and mechanistic interpretive
#                     admissibility). The FIRST code artifact of the MoE track.
#
#                     evaluation/moe_identity.py turns the contract parts of the
#                     Panel S0 specification into enforced types, all constructed
#                     NON-ADMISSIBLE. Five commitments are structural, not merely
#                     documented: (1) the causal boundary -- BIOLOGICAL_MEDIATION
#                     can never be admissible through Panel S0 and a request raises
#                     InsufficientSupportError, because model-component
#                     interventions establish reliance, not biological causation;
#                     (2) the four routing quantities are distinct and mechanism
#                     evidence never enters the allocation; (3) admissibility is
#                     claim-specific and the expert-global summary is the HIGHEST
#                     supported claim, never a hidden minimum; (4) an unvalidated
#                     expert is expert_003 in every external report; (5) a lineage
#                     event resets the claim ledger.
#
#                     A dead report-label assertion was found and removed during the
#                     sabotage pass (a check that cannot fail is worse than none).
#
#                     PROVEN BY SABOTAGE (8 fire): mediation-guard removed, mechanism
#                     evidence into allocation, admissible-needs-scientific removed,
#                     highest-supported flipped to minimum, CONTINUED wrongly resets,
#                     computational-teacher passes as mechanism, non-mechanism class
#                     reaches admissible, uuid opacity removed.
#
#   2965  2026-07-24  +8: tests/unit/test_real_data_prep_strict.py -- STEP 1b
#                     REWIRE #2. real_data_prep stops defining its own
#                     REVIEW_STATUS_TIER map and the inline substring .map(lambda)
#                     that applied it, and consumes the canonical resolver via
#                     _build_strict_review_tier_lookup (Option C aggregate strict
#                     preflight, same shape as the augment rewire at 2957). No
#                     allow_unmatched path, no fabricated tier, no resolver change.
#
#                     The rewire was cleared by a row-identity measurement first
#                     (docs/measurements/LABEL_FILTER_MEASUREMENT_2026-07-24.txt,
#                     commit f4dcd7b). real_data_prep applies THREE filters in
#                     sequence and the tier filter is the SECOND: the label filter
#                     (keep clinical_sig in PATHOGENIC_TERMS or BENIGN_TERMS) runs
#                     first. On the 1,686,333 label-filtered rows the tier
#                     distribution is IDENTICAL under the old substring map and the
#                     unified resolver, and the kept-row symmetric difference is 0
#                     at every threshold 1..5 -- so the rewire produces a
#                     byte-identical training cohort. The 157,229 conflicting-
#                     classification rows the consumer probe flagged are dropped at
#                     the label filter before the tier map runs (0 of 161,376
#                     conflict-significance rows survive it), and zero labeled
#                     variants carry a review status where substring and exact
#                     disagree. A regression test pins that agreement.
#
#                     The content-based detector inventory shrinks 7 -> 6; the
#                     detector independently finds exactly 6 maps afterward
#                     (clean_cohort, clinvar_tracker, the canonical map, and the
#                     three read-only probe scripts). clean_cohort follows next in
#                     Step 1b; clinvar_tracker moves to Step 1c.
#
#   2957  2026-07-24  +7: tests/unit/test_augment_reviewstatus_strict.py -- STEP 1b
#                     REWIRE #1. scripts/augment_reviewstatus.py stops defining its
#                     own REVIEW_STATUS_TIER map and substring _tier_of, and consumes
#                     the single canonical resolver in review_status.py. Unknown VCF
#                     CLNREVSTAT vocabulary is validated in ONE aggregate strict
#                     preflight (Option C, 2026-07-24): every distinct raw value is
#                     resolved once, any unmatched are collected keyed by canonical
#                     normalised form with raw spellings and counts retained, and a
#                     SINGLE error reports the complete inventory. No allow_unmatched
#                     path, no fabricated fallback tier, no resolver change -- so the
#                     57 resolver tests are untouched and the whole delta is the 7
#                     new augment tests. MEASURED under Python 3.11 with
#                     `pytest --collect-only -q` on the new file: 7 collected.
#
#                     The content-based detector inventory shrinks 8 -> 7: the
#                     augment_reviewstatus entry leaves KNOWN_TIER_MAP_DEFINITIONS,
#                     and the detector independently finds exactly 7 maps afterward
#                     (clean_cohort, real_data_prep, clinvar_tracker, the canonical
#                     map, and the three read-only probe scripts). real_data_prep and
#                     clean_cohort follow in Step 1b; clinvar_tracker moves to Step 1c
#                     because a drift monitor must expose unknown vocabulary rather
#                     than fail closed on it.
#
#                     Governing measurement (docs/measurements/CONSUMER_PROBE_
#                     2026-07-24.txt, commit cd635b2): WOULD_RAISE = 0 for every
#                     consumer on the 4,399,089-row cohort, so this rewire raises on
#                     nothing present today. The acceptance test exercises
#                     _build_strict_tier_lookup directly (the pure function that
#                     carries the behaviour); the full main() is an integration run
#                     against the ClinVar VCF, which lives outside the repository.
#

# 3118 -- 2026-07-26 -- Option C commit 2, bootstrap inference reconciliation.
#   Previous value: 2991.  Delta: +127.
#
#   One canonical bootstrap engine. The resampling unit is now an explicit,
#   typed part of every confidence interval instead of an accident of which
#   caller produced it. Gene-cluster resampling is REQUIRED for a certified
#   interval; variant-level resampling is available only when asked for
#   explicitly, is never certification-eligible, and records the assumption it
#   rests on. There is no silent fallback between them. The gene-cluster design
#   effect measured 2.935x on the real cohort (ratchet entry 2055), so every
#   interval the retired row-level paths produced was too narrow by about that
#   factor.
#
#   New tests: tests/unit/test_cluster_resolution.py (67) and
#   tests/unit/test_bootstrap_reconciliation.py (60).
#
#   Two defects were found in the previously "proven" dispatcher while building
#   this. Its docstring failed test_no_module_uses_strenum..., an existing
#   guard. And its replicate accounting modelled a sampling scheme bootstrap_ci
#   does not use, reporting 0.506 of replicates degenerate against a theoretical
#   0.500 -- invisible because the status it fed was a hard-coded constant, so
#   the prior check passed for the wrong reason.
#

# 3121 -- 2026-07-26 -- handoff completion: the missing ESTIMATE pin.
#   Previous value: 3118.  Delta: +3.
#
#   Handoff section 7c group 1 required five pins on canonical-engine
#   agreement -- estimate, endpoints, n_valid, seed, unit. Only four were
#   delivered. Nothing asserted that the interval bounds the number the
#   report prints beside it.
#
#   The FIRST attempt at the missing pin was itself a check that could not
#   fail: it rebuilt the wrapper locally instead of calling the real
#   ClinicalEvaluator._nan_safe, so it compared scikit-learn against
#   scikit-learn. Proven by sabotage -- scaling the evaluator wrapper by
#   0.98 left it GREEN. Rewritten to call the real wrapper, it fails under
#   that same sabotage and passes on clean code.
#

# 3132 -- 2026-07-26 -- P6 R2 phase 1: freeze the evidence before restructuring.
#   Previous value: 3121.  Delta: +11.
#
#   An additive --emit-json capture on probe_clean_cohort_p6_2026-07-25.py, plus
#   tests/unit/test_p6_probe_contract.py pinning the probe on an eight-variant
#   synthetic cohort. The capture changes no computation: proven by comparing a
#   pristine origin/main baseline against the modified probe writing to the same
#   path -- artifact SHA-256 identical, stdout one line longer.
#
#   Reading the source first falsified a roadmap invariant. The 63 and 203 counts
#   are summed over DIFFERENT universes: a quarantined variant has a group
#   adjudicated label but no representative row. n01 + n11 == 203 is replaced by
#   n01 + n11 + n_na1 == 203, and representative_row_label_changed becomes
#   NULLABLE rather than a total boolean.
#
#   NEAR-MISS: the guard first used a module-level importorskip on pyarrow, which
#   turned eleven tests into "1 skipped" on an interpreter without it. pyarrow is
#   pinned at requirements.txt:89. The ratchet CANNOT catch that class -- collection
#   precedes skipping -- which is how the graph-neural-network branch went untested
#   for 508 runs. Replaced with a direct import so absence fails loudly.
#

# 3146 -- 2026-07-26 -- P6 R2 phase 2: the reconciliation.
#   Previous value: 3132.  Delta: +14.
#
#   A typed ProbeConfig, then load -> compute -> summarize -> render. One
#   per-variant PolicyDelta pass feeds Table A (the 2x2 where the
#   representative-label comparison applies) and Table B (where it does not).
#   n10 + n11 == representative-label changes;
#   n01 + n11 + n_na1 == group-adjudicated label changes.
#
#   Three defects in the new code, all caught by reading output rather than
#   exit codes: a two-value return unpacked as three; a supersession block
#   appended SEVENTY-EIGHT times because adjacent string literals concatenate
#   before * applies; and a report that claimed golden reproduction with no
#   golden loaded, because the checks were skipped so the failure list was
#   empty. An absent golden now prints NOT VERIFIED, exits 2, and refuses to
#   supersede -- an unverified run must never supersede verified evidence.
#

# 3150 -- 2026-07-26 -- P6 R2 phase 2b: replay, do not derive.
#   Previous value: 3146.  Delta: +4.
#
#   The first real reconciliation run FAILED its own gate: recomputed 53
#   against a frozen 63, exit 1, original untouched. Three hypotheses were
#   falsified by reading the source -- identical row order, identical label-map
#   construction, and legacy can NEVER quarantine where P6 does not because the
#   legacy-to-unified tier map MERGES tiers (L4->U3), making the unified
#   best-tier set a superset. No (None, False) fall-through exists for P0.
#
#   So the published figure is now REPLAYED exactly as probe lines 464 and
#   514-517 compute it, reproduced BY CONSTRUCTION, while the stricter
#   both-sides-present quantity stays derived from PolicyDelta. A BRIDGE
#   classifies every disagreement into four named categories with example
#   variant identifiers, so the next run MEASURES the cause.
#

# 3154 -- 2026-07-26 -- P6 R2 phase 2c: correct the R2 report itself.
#   Previous value: 3150.  Delta: +4.
#
#   The first successful run returned EXIT=0 and reproduced the golden exactly;
#   n11 = 29 and n_na1 = 17 measured for the first time. Reading its artifact
#   FALSIFIED a structural claim: legacy quarantines 107 and P6 quarantines 85,
#   so P6 NEVER newly quarantines -- it un-quarantines 22. select_repr_row keeps
#   a row only when the best tier holds one class AND that class is binary; a
#   best tier of only non-binary rows gives {None}, one class but not a subset
#   of {0,1}, so legacy quarantines while the unified best tier (a superset,
#   the map merging 4 into 3) can include a binary row.
#
#   FIVE report defects corrected, all of the disease the artifact exists to
#   cure: "both sides had a label" was false (29 of the 53 have a legacy row
#   whose label is None -- it means both had a ROW); Table B was labelled the
#   P6-quarantined universe when it is EITHER side missing, 85 and 22 reported
#   as one; its prose asserted a mechanism that measures zero; the overloading
#   note printed BETWEEN the reconciliation lines, orphaning the 203; and
#   TableB carried no decomposition at all.
#

# 3155 -- 2026-07-26 -- P6 R2 phase 2d: Table B must PARTITION.
#   Previous value: 3154.  Delta: +1.
#
#   The corrected report emitted TWO OVERLAPPING COUNTS under one universe:
#   "no P6 representative 85" beside "no legacy representative 107", under a
#   universe of 107. 85 + 107 = 192. Overlapping counts printed as a breakdown
#   read as a partition and are not one -- the same defect the artifact exists
#   to correct, committed while correcting it.
#
#   True partition, cross-checked against two independent counters
#   (p6_only_representative = 22, quarantine symmetric difference = 22):
#       neither side has a representative (BOTH quarantined)  85
#       legacy missing, P6 PRESENT   (legacy-only quarantine)  22
#       P6 missing, legacy PRESENT   (NEWLY quarantined)        0
#   So p6_quar is a STRICT SUBSET of base_quar: legacy 107, P6 85, and P6
#   never newly quarantines on this cohort.
#
#   check_invariants now REFUSES any set of Table B cells that fails to sum
#   to the universe.
#

# 3165 -- 2026-07-26 -- P6 R2 phase 2e: Table B becomes a 3x2 JOINT table.
#   Previous value: 3155.  Delta: +10.
#
#   Phase 2d produced the right partition (85 / 22 / 0 summing to 107) but
#   stored the ROW and COLUMN marginals INDEPENDENTLY. A cohort whose 17 label
#   changes fall among legacy-missing variants and one whose 17 fall among
#   neither-side variants serialise IDENTICALLY. Correct margins, different
#   science, and no invariant over those marginals can distinguish them.
#
#   TableB now stores SIX joint cells and derives every margin, cardinality
#   and the direction sentence from them. The direction is COMPUTED: the real
#   cohort reports P6 UN-QUARANTINES, the synthetic fixture reports P6 NEWLY
#   QUARANTINES, and a test asserts both.
#
#   The delta() test helper forced p6_quarantined = quar OR legacy_quar, making
#   a LEGACY-ONLY quarantine inexpressible -- exactly the 107-to-85 transition
#   the real cohort shows. A helper that cannot construct the case cannot test
#   it. PolicyDelta.__post_init__ now refuses inconsistent records outright.
#

# 3169 -- 2026-07-26 -- P6 R2 phase 2f: machine-readable sidecar; R2 closed.
#   Previous value: 3165.  Delta: +4.
#
#   CLEAN_COHORT_P6_AUDIT_2026-07-25_R2.json is emitted beside the text report
#   from the SAME Reconciliation instance -- render_report() for a reader,
#   serialize_json() for exact regression checks. Values are never
#   reconstructed independently. It stores the ten table cells and derives
#   every margin, so a gate asserts typed values instead of parsing prose.
#   quarantine_direction is a stable TOKEN beside the human sentence.
#
#   CLOSURE CRITERION: prose may describe only quantities derivable from the
#   persisted joint structure. Further narrative revision is rejected unless
#   accompanied by a failing invariant or a newly measured contradiction.
#

# 3185 -- 2026-07-27 -- MetricResult moves to the vocabulary layer.
#   Previous value: 3169.  Delta: +16.
#
#   Roadmap Tier 1 item 6, commit 1 of 3. The registry is NOT here.
#
#   MetricResult was defined inside a 1,326-line PANEL module and imported by
#   two others, while its __post_init__ depends on MetricStatus in the
#   vocabulary layer -- the dependency ran upward. It now lives in
#   capabilities.py and clustering_metrics.py re-exports THE SAME OBJECT.
#
#   np.isfinite kept, not swapped for math.isfinite: measured equivalent on
#   every scalar and different only on arrays, where numpy silently ACCEPTS a
#   one-element array as finite.
#
#   TWO DEFECTS OF MY OWN, both caught by tests: the first extraction ran to
#   the next @dataclass and silently DELETED `def aggregate` between them; and
#   cleaning a sabotage with `git checkout` restored from the INDEX and wiped
#   the uncommitted relocation. Never use git checkout to undo a sabotage while
#   the real work is uncommitted.
#
#   A GUARD THAT COULD NOT FAIL: of four sabotages, the np.isfinite one PASSED
#   because it used np.float64, where numpy and math agree. Rewritten around a
#   one-element array -- the only discriminating input -- it now fails.
#

# 3227 -- 2026-07-27 -- the declarative metric registry.
#   Previous value: 3185.  Delta: +42.
#
#   Roadmap Tier 1 item 6, commit 2 of 3.
#
#   A FROZEN tuple of typed descriptors with import-time validation, so a
#   malformed declaration fails the IMPORT rather than the run. Applicability
#   is evaluated BEFORE the kernel is invoked: an inapplicable metric is never
#   computed, proven by a test whose kernel raises if called.
#
#   THREE AXES KEPT SEPARATE: status, scientific interpretability, and
#   certification eligibility. brier_score on one class is OK with
#   certification_eligible=False.
#
#   SUPPORT ATTACHMENT on every result, refusals and failures included:
#   population_scope, n_observations, n_classes_observed, and n_clusters when
#   supplied. n_clusters is a COUNT, not an effective sample size -- that
#   lives in BootstrapResult beside the design effect. No threshold applied.
#
#   population_scope is REQUIRED. Support counts alone do not identify the
#   DENOMINATOR: 53 and 63 were both correct over universes differing by ten
#   variants, and 85 was printed beside 107 as a breakdown of 107.
#
#   PRESERVE RAW STATE UNTIL DIAGNOSTICS COMPLETE. Canonicalisation occurs
#   only after diagnostic measurements have been computed. Three defects this
#   session shared one shape: destroy the distinction, measure the destroyed
#   distinction, declare success.
#
#   metrics.evaluate() is UNTOUCHED and is not registered as a composite.
#

# 3247 -- 2026-07-27 -- controlled metadata vocabulary.
#   Previous value: 3227.  Delta: +20.
#
#   Roadmap Tier 1 item 6, commit 2b of 3. The foundation commit 3 needs.
#
#   MetricMetadataKey is the single source of spelling for seven metadata
#   keys, with six read-only accessors on MetricResult. It is a str-and-Enum,
#   NOT StrEnum, which is Python 3.11+ while the floor is 3.10.
#
#   ACCESSORS, NOT CONSTRUCTOR FIELDS -- measured. 53 MetricResult
#   construction sites; 35 of the 39 in src/ are POSITIONAL, in
#   representation_geometry.py and norm_angle_probe.py. Those are
#   mathematical probes over embedding spaces where population scope has no
#   epidemiological meaning. TWO SEMANTIC FAMILIES: cohort evaluation carries
#   population and support; representation probes carry matrix shape and
#   dimension. MetricResult stays a GENERIC contract.
#
#   The guards found one REAL defect -- registry.py used
#   "n_classes_observed" as a literal at two sites -- and two instructive
#   false positives, of which representation_geometry.py "n_rows" is Family
#   B's word for embedding-matrix rows, not a misspelling of n_observations.
#
#
# 3247 -> 3273 (+26)  2026-07-27  commit 2a: the fail-closed
#   prediction-input contract.
#
#   clean_arrays dropped non-finite rows on ONE JOINT MASK over labels,
#   scores and probabilities alike, so a metric returned a value over a
#   silently narrowed population while support() named the wider one.
#   Twenty non-finite probabilities in a thousand rows gave a Brier score
#   over 980 rows, reported as n_observations = 1000, status ok,
#   certification_eligible True.
#
#   Labels and predictions no longer share a mask. A withheld label is an
#   ordinary missing observation and stays first-class, selected upstream
#   by a NAMED transitional selector. A non-finite predicted probability
#   is a MODEL-OUTPUT FAILURE: refused before dispatch, status FAILED over
#   the full attempted population, diagnostics attached.
#
#   FINITENESS RAISES; RANGE DOES NOT. An out-of-range vector was never a
#   probability vector and is still a valid SCORE on the same rows. The
#   ORDER of the two guards is the contract and is pinned.
#
#   test_auroc_ignores_nonfinite asserted the defect approvingly -- roadmap
#   6.28s shape -- and was INVERTED, not renamed. The old expectation is
#   now the sabotage.
#
#   Sabotage: 12 breaks, 12 detected, 0 undetected. THE FIRST RUN LEFT FOUR
#   UNDETECTED: two real test defects (status alone could not separate a
#   gate refusal from a kernel raising; a tripwire accepted its keyword
#   from anywhere in the file) and two malformed breaks. All four closed.
#
#
# 3273 -> 3318 (+45)  2026-07-27  commit 2a-1: the evaluation
#   population contract.
#
#   metrics.select_finite_reference_labels is RETIRED. Label eligibility
#   is now an explicit, recorded restriction of an EvaluationPopulation:
#   a frozen claim about which rows a number describes, where narrowing
#   is the only operation offered.
#
#   Load-bearing invariants: a restriction must STRICTLY narrow, so an
#   unchanged population cannot acquire artificial lineage; and child
#   membership must be a genuine SUBSET, because smaller, ordered,
#   unique and in range is not enough -- parent [0,2,4,6] with child
#   [1,3] satisfies all four while re-admitting removed rows.
#
#   The membership fingerprint reaches what cardinality cannot: two
#   disjoint 500-row subsets have equal n, can carry an identical scope,
#   and differ in fingerprint.
#
#   Sabotage: 14 breaks, 14 detected, 0 undetected. First run left two --
#   one real test gap, one malformed break -- both closed.
#
#   THE MEASURED DELTA CAUGHT AN ACCIDENTAL DELETION. A test module
#   collected 25 before and 19 after, where three removals and five
#   additions should have given 27. An edit anchored to a test function
#   and extended to END OF FILE destroyed three functions appended after
#   that anchor in commit 2a -- eight test cases, including the
#   parametrised gate test that closed the B4 gap in commit 2a own
#   sabotage matrix. All restored verbatim. A COMPUTED ratchet would have
#   recorded the intended number and lost them silently.
#
#
# 3318 -> 3354 (+36)  2026-07-27  commit 2b-1: the calibration
#   binning convention, unified into one table.
#
#   expected_calibration_error documented "[lo, hi) with the top bin
#   closed" and implemented np.digitize(right=True), which is (lo, hi]
#   for EVERY bin. Every probability exactly on an interior decade edge
#   landed one bin low. The evaluator had implemented the documented
#   convention since 2026-07-10, so the two disagreed for seventeen days.
#   Measured separation on a constructed cohort: 0.3242857 against
#   0.0642857, 404.44%.
#
#   It hid because the statistic is invariant to regrouping when merged
#   groups share the sign of (accuracy - confidence), and because
#   test_calibration_implementations_agree held NO interior-edge value at
#   all. That module now holds one -- carried item (k), discharged.
#
#   ONE BINNING WAS NOT ENOUGH. After both paths binned through the shared
#   table they still differed by 3.5e-18, because the kernel kept its own
#   SUMMATION loop. Two summations are two implementations. Both now read
#   CalibrationBins and the difference is exactly zero.
#
#   No published figure moves: every published calibration number came
#   from the evaluator, which was already correct.
#
#   Sabotage: 10 breaks, 10 detected, 0 undetected -- clean on the first
#   pass, with detection counts 17/5/2/2/17/1/11/1/1/15.
#
#
# 3354 -> 3415 (+61)  2026-07-27  commit 2b-2: registry vocabulary
#   completion.
#
#   Not more descriptors: what is completed is the VOCABULARY every descriptor
#   speaks -- ResultKind, ThresholdParameters, immutable JSON-validated
#   parameters, REGISTRY_SCHEMA_VERSION 1 -> 2 enforced at import for EVERY
#   descriptor -- plus four new descriptors: maximum calibration error,
#   Matthews correlation coefficient, F1, prevalence.
#
#   ACCEPTANCE CRITERION: NOTHING MOVED. A snapshot of every registry output was
#   frozen on the 2b-1 tree BEFORE a line of this was written. 48 pre-existing
#   results x 8 fields = 384 comparisons, ZERO movements, no carve-outs.
#
#   A degenerate confusion margin is caught by APPLICABILITY, not by a kernel
#   NaN: compute() already rules that an applicable metric returning non-finite
#   is FAILED, an implementation defect rather than a property of the cohort.
#   Zero denominators are UNDEFINED -- scikit-learn returns 0.0 AND raises
#   UndefinedMetricWarning, its own warning being the evidence.
#
#   Sabotage: 12 breaks, 12 detected, 0 undetected; 4 fire at IMPORT because
#   _validate_registry refuses a malformed declaration outright. THE FIRST RUN
#   LEFT THREE UNDETECTED: one was the seventeen-day binning defect reproduced
#   inside the test written to prevent it, one left the kernels
#   zero-denominator branch unguarded, one was a malformed break. All closed.
#
#
# 3415 -> 3419 (+4)  2026-07-27  commit 2b-3: the descriptor
#   immutability audit, and a self-validating acceptance oracle.
#
#   2b-2 made descriptors the semantic authority. Two properties of that
#   authority were asserted but never proved.
#
#   (1) EVALUATION MUST NOT MUTATE A DESCRIPTOR. Frozen in type, but parameters
#   are reached through a mapping and ThresholdParameters can be edited through
#   object.__setattr__. An in-place edit would silently change what every LATER
#   evaluation means, and numerical tests never notice, because the run that did
#   the mutating still produces the right answer. The fingerprint includes the
#   OBJECT IDENTITY of kernel and predicate, because 2b-2 guarantees one shared
#   ThresholdParameters and a value-preserving swap would defeat value equality.
#
#   (2) THE ORACLE MUST COME FROM THE TREE IT CLAIMS. registry_schema_at_capture
#   is 1 and must NOT equal the current REGISTRY_SCHEMA_VERSION of 2. A stale
#   fixture is a visible failure; a silently REGENERATED one is a photograph of
#   the thing it was checking. The header was added without regenerating any
#   result: fixtures digest 09713c3ee9279f5b8d4fafe3d5e953ef, unchanged.
#
#   TWO GUARDS THAT COULD NOT FAIL, both caught by sabotage: a dict-literal
#   comparison ADDED the key under test, and a per-call lambda made every
#   id() comparison trivially unequal. Removing three fingerprint fields had
#   left the suite green.
#
#   NEW STANDING RULE, after the same failure recurred THREE times: every
#   regression fixture targeting an algorithmic distinction shall first
#   demonstrate that the injected defect changes observable behaviour.
#
#   Sabotage: 8 breaks, 8 detected, 0 undetected.
#
#
# 3419 -> 3445 (+26)  2026-07-28  commit 3a: the typed report surface
#   and schema version 3.
#
#   Commit 3 was SPLIT. Schema introduction corrupts artifacts when it fails;
#   computational retirement corrupts numbers. 3a retires nothing: evaluate()
#   still computes MCC, F1 and the calibration errors itself, still emits
#   version 2, still leaves metric_results empty.
#
#   ACCEPTANCE: NOTHING MOVED. Ten cohorts x 48 report fields = 480 values,
#   frozen on the untouched 2b-3 tree before a line of 3a was written. Zero
#   movements, NO declared movement set. One field added, none removed.
#
#   THE ORACLE HAD TO BE REPAIRED FIRST: the first five cohorts could not
#   distinguish a four-decimal from a five-decimal prevalence contract, because
#   every sample size divides cleanly. Two cohorts now separate them BY
#   CONSTRUCTION. Four of ten detect a rounding change.
#
#   PER-FIELD ROUNDING EXTRACTED, NOT IMPOSED: prevalence 4 decimals, the other
#   seven metric fields 5. prevalence became a registered metric in 2b-2, so it
#   is exactly where a plausible global rule silently disagrees.
#
#   THREE DEFECTS IN LANDED CODE: to_dict emits raw NaN while from_dict
#   documents reading null back as NaN and dump_strict_json refuses NaN, so
#   every refused result was unpersistable; deserialisation dropped every
#   enum-typed flat field, crashing every round trip; and my own first test
#   helper invented the twenty cross-validated interval fields.
#
#   Sabotage: 12 breaks, 12 detected, 0 undetected. THE FIRST RUN LEFT ONE
#   UNDETECTED -- the oracle-staleness test was written as though it carried
#   2b-3's decisive assertion, which does not transfer here.
#
#
# 3445 -> 3455 (+10)  2026-07-28  commit 3b-0: population attribution.
#
#   Prerequisite to the legacy projection. Commit 3b makes evaluate() build a
#   MetricContext, which needs an EvaluationPopulation, which needed a non-empty
#   source_id -- but evaluate() receives arrays and has no identity to give.
#
#   THREE WAYS OF INVENTING ONE WERE MEASURED AND ALL FAIL: a fixed sentinel
#   makes two DIFFERENT equal-length cohorts share a fingerprint, certifying an
#   equivalence nobody established; deriving from labels was ruled out
#   2026-07-27; a per-call unique id is non-deterministic and breaks every
#   byte-identity oracle in the project.
#
#   ABSENCE IS NOW ABSENCE. source_id is Optional. An unattributed population
#   has NO fingerprint -- not a fingerprint of nothing, the absence of one.
#   Comparison is THREE-VALUED: same / different / unknown, because a boolean
#   cannot express not-knowable and False would read as a claim.
#
#   THE TRAP CLOSED: None == None is True in Python, so comparing two absent
#   fingerprints directly concludes sameness. A test pins that the naive
#   comparison returns True AND the authoritative one returns UNKNOWN.
#
#   NOT TESTED, DELIBERATELY: that two cohorts collide under a sentinel. That
#   documents the defect but institutionalises it as intended behaviour.
#
#   All 41 pre-existing population tests pass unchanged. Report oracle: 480
#   values, ZERO movements. Sabotage: 8 breaks, 8 detected, 0 undetected.
#
#
# 3455 -> 3517 (+62)  2026-07-28  commit 3b-1a: calibration applicability
#   correction, and the compatibility layer made an auditable interpreter.
#
#   Commit 3b was split again. Running the legacy projection in SHADOW against
#   the frozen oracle BEFORE granting it authority surfaced six field-cohort
#   disagreements, which were TWO scientifically different questions.
#
#   CALIBRATION AND DISCRIMINATION ARE DIFFERENT ESTIMANDS. Calibration no
#   longer requires both reference classes. All-negative cohort predicted at
#   0.10, observed frequency 0.00, gap 0.10 -- systematic overprediction,
#   measured, not an empty number.
#
#   THREE DEFECTS FOUND: applicable-verdict metadata was silently discarded;
#   the occupancy theorem was necessary but NOT sufficient (1.0 mapped to bin
#   10 gave an occupied plausible table, status OK, no exception -- a RANGE
#   invariant was added); and a patch was written against text from the
#   WITHDRAWN first commit 3a, caught only by a single-match assertion.
#
#   ONE ARCHITECTURAL BLIND SPOT PRODUCED SIX SURVIVING MUTATIONS. The first
#   matrix survived 6 of 11, all from legacy_projection.py having no dedicated
#   test module. It is an INTERPRETER, now tested on DECISION PATHS. Two rules
#   legitimately produce the same 0.0 -- a measurement and a substitution --
#   which no value comparison can separate.
#
#   TWO ORACLES, correctly disagreeing: legacy report 480 values ZERO
#   movements; typed registry 10 DECLARED movements, identity-asserted. The
#   fixture was NOT regenerated; it documents the pre-correction state.
#
#   Sabotage: 13 mutations, 13 detected, 0 undetected, including two SWAP
#   mutations caught only by the decision matrix.
#
#
# 3517 -> 3529 (+12)  2026-07-28  commit 3b-1b: the derived single-class
#   AUPRC rule. SHADOW EQUALITY REACHED: 6 -> 2 -> 0.
#
#   AUPRC stays canonically UNDEFINED on single-class cohorts. It is a ranking
#   quantity built around the positive class: an all-negative cohort has no
#   positives to retrieve, and an all-positive cohort has no negatives to rank
#   against, so its conventional 1.0 is determined by class composition rather
#   than by discrimination. scikit-learn warns in both cases.
#
#   THE LEGACY VALUE IS DERIVED, NOT CONSTANT: prevalence 0.0 gives 0.0 and 1.0
#   gives 1.0, read from the REGISTERED prevalence result. Neither a second
#   AUPRC computation nor a table entry, but a schema-v2 serialisation rule
#   keyed on class composition. It fails closed on absent siblings, a missing
#   prevalence, a non-OK prevalence, and a prevalence that is not degenerate --
#   the last being a contradiction that must surface, not be papered over.
#
#   A THIRD PROJECTION SOURCE. typed_value, substitute and derived can all
#   produce 0.0 on one report: a measurement, a compatibility constant, and a
#   value derived from prevalence. No comparison of VALUES can separate them.
#   The derived rule is proved not to be a constant in disguise, since the same
#   rule yields 0.0 on one degenerate cohort and 1.0 on the other.
#
#   Legacy report oracle 480 values ZERO movements; typed registry oracle shows
#   only 3b-1a's declared set, nothing new. Sabotage: 9 mutations, 9 detected,
#   0 undetected, clean on the first pass.
#
#
# 3529 -> 3545 (+16)  2026-07-28  commit 3b-2: the authority switch and
#   evaluator retirement. TIER 1 ITEM 6 COMPLETE, fourteen commits.
#
#   The registry is now the ONLY computation path. Deleted from the report:
#   roc_auc_score, average_precision_score, matthews_corrcoef at a hard-coded
#   0.5, f1_score at the same hidden threshold, an inline Brier expression, a
#   private calibration loop, and the _calibration_error method itself.
#
#   ACCEPTANCE: 480 report values, 10 movements, ALL schema_version 2 -> 3,
#   declared BY IDENTITY. 470 byte-identical. Not one measured number changed
#   when the report stopped computing them. scikit-learn warnings fell 9 -> 3.
#
#   The diff could be almost entirely subtractive because equivalence was proved
#   BEFORE authority transferred: shadow mismatches 6 -> 2 -> 0 across the two
#   preceding commits, resolving six disagreements as separate scientific
#   questions rather than six simultaneous suspects.
#
#   GUARDS: an abstract-syntax-tree guard for duplication that is WRITTEN and
#   counting wrappers for duplication that is EXECUTED. Neither alone suffices.
#   Composition is distinguished from duplication by an explicit budget, because
#   auprc_gain legitimately calls auprc.
#
#   FOUR DEFECTS FOUND: the scikit-learn-free import chain broke (2b-2 built
#   adapters at module scope, performing the import the lazy pattern defers);
#   THE TYPED SURFACE WAS COMPUTED AND DISCARDED, found only by chasing a
#   surviving mutation; the calibration adapters read a module constant rather
#   than their own declared parameters; and a miscount of my own.
#
#   Twelve tests referencing the retired method were REWRITTEN, not deleted.
#   Sabotage: 8 mutations, 8 detected, 0 undetected.
#
#
# 3545 -> 3558 (+13)  2026-07-28  the verified carried-item register.
#
#   Fourteen commits declared carried items inside per-commit roadmap deltas and
#   recorded status changes in LATER deltas, so an item's state could only be
#   reconstructed by grepping a 2,400-line document in order. Two sources of
#   truth for one fact, no divergence detector -- the same defect the metric
#   stack spent fourteen commits removing from the evaluation path.
#
#   IT HAD ALREADY GONE WRONG: item CI-l was retired in commit 2a-1 and still
#   read as OPEN eleven commits later.
#
#   NAMESPACE COLLISION RESOLVED: ROADMAP.md uses (a)-(d) for ROOT PATTERNS and
#   (a)-(s) for CARRIED ITEMS, five hundred lines apart. Carried items now carry
#   the CI- prefix.
#
#   SELF-VERIFYING: every open item has a predicate decidable by running code,
#   and the test fails in BOTH directions -- a stale OPEN item and a RETURNING
#   discharged condition. Uncheckable items are listed as such.
#
#   CI-q IS WORSE THAN RECORDED: ClinicalEvaluator.evaluate is called inside
#   this package at evaluator.py:1238 without source_id, so its own batch path
#   produces unattributed, uncertifiable results.
#
#   THE REGISTER CAUGHT ITS OWN AUTHOR TWICE: a predicate checking for the wrong helper name
#   when the function filters through `clean_arrays`, and two sabotage
#   survivors from substring assertions. Both now assert structure.
#
#   Sabotage: 6 mutations, 6 detected, 0 undetected.
#
#
# 3558 -> 3589 (+31)  2026-07-28  CI-t: report-path input gates.
#
#   Prerequisite to CI-q, which could not be built on a loop that already died
#   unpredictably during scoring.
#
#   Five scikit-learn calls consume the same y and p and DISAGREE about what is
#   invalid: non-finite input makes roc_curve raise and calibration_curve
#   RETURN; out-of-range input makes calibration_curve raise and roc_curve
#   return. Validation therefore precedes dispatch.
#
#   THREE DEFECTS IN LANDED CODE, ascending in danger. roc_curve raises and
#   aborts the report after the point metrics succeeded. calibration_curve
#   neither raises nor warns -- it returns a degenerate one-point curve carrying
#   NaN that fails only at persistence. And THE OPERATING-POINT SWEEP SHIPPED A
#   WRONG NUMBER: p >= t is FALSE for a NaN, so unusable predictions became
#   predicted negatives and the reported threshold moved from (0.6366, sens
#   0.90, spec 1.00, ppv 1.00) to (0.0000, 0.50, 0.00, 0.33).
#
#   Ten call sites gated. Refusal is component-level, so a corrupt model still
#   yields a complete report. A `scores` channel validated WITHOUT a range
#   restriction: the same array is refused as a probability, accepted as a score.
#
#   Every production caller obtains its array from predict_proba, so enforcing
#   the unit interval is a correctness fix with zero compatibility break.
#
#   FIVE DEFECTS IN MY OWN WORK, each caught by measurement -- including gating
#   two of three operating points, and a fallback that left the registry ranking
#   an invalid array while the curve refused it.
#
#   Sabotage: 11 mutations, 11 detected, 0 undetected.
#
#
# 3589 -> 3594 (+5)  2026-07-28  CI-t was discharged prematurely; the
#   enumeration that proves it now.
#
#   Commit 19c19a1 declared CI-t discharged on a HAND COUNT of ten scikit-learn
#   call sites. A PARSED ENUMERATION FINDS TWELVE: _consequence_breakdown calls
#   roc_auc_score and average_precision_score directly and raises on a corrupt
#   model. That path is reached only when meta is supplied, and every
#   corrupt-model test written for CI-t passed meta=None. The fixture shape hid
#   it, as it hid the calibration binning defect for seventeen days.
#
#   THE FIX IS THE ENUMERATION, NOT THE GATE. The suite now parses the module,
#   finds every metric call, and requires a validator whose result GOVERNS A
#   BRANCH in the enclosing function. A hand count is a claim; this is a check.
#
#   THE GUARD WAS TOO WEAK TWICE. First it asked whether a validator NAME
#   appeared, so a gate disabled with a constant satisfied it -- the same
#   weakness already found in the register predicate. Then it followed only one
#   hop, flagging a legitimate three-hop chain; a guard that cries wolf on
#   correct code gets weakened until it catches nothing.
#
#   MEASURED FOR CI-q: format_ci renders all four interval states identically
#   and certified is False in all four, so ranking admissibility must come from
#   the typed AUROC result. And the comparison artifact has NO consumers.
#
#   Sabotage: 6 mutations, 6 detected, 0 undetected.
#
#
# 3594 -> 3627 (+33)  2026-07-28  CI-q: the shared-population model
#   comparison.
#
#   compare_models scored several models against one shared y_true and could
#   not prove it. Each model built its own population, so the comparison's
#   entire premise -- like for like -- was true in fact and unrecorded.
#
#   With one corrupt model the table read good 0.99937 / fair 0.74253 /
#   corrupt NaN. A RANKING WAS PRESENTED: the corrupt model sorted last on a
#   NaN comparison, and a reader could not tell evaluated-and-worst from
#   never-evaluated.
#
#   ONE population, handed to every model BY OBJECT. Admissibility BEFORE
#   ordering, from the TYPED result, because format_ci renders all four
#   interval states identically. The ranking REFUSED, never filtered, and no
#   sort runs at all. SHARED_BY_CONSTRUCTION kept distinct from
#   VERIFIED_BY_FINGERPRINT. Three certification axes never collapsed.
#
#   THE SABOTAGE FOUND THE CENTRAL CLAIM WAS FALSE: the shared population was
#   built and never handed over; fingerprints matched only because the same
#   source_id was passed to each -- equal by coincidence. The test now asserts
#   OBJECT IDENTITY.
#
#   THE REGISTER PREDICATE WAS DEFECTIVE. It scanned for the evaluate call as raw text
#   and matched six places, five of them docstrings or an unrelated function,
#   so the item would have reported open forever. Fourth malformed probe of
#   this session and the most serious, because it lived in the register.
#
#   A DEFECT THE SUITE COULD NOT SEE: the first package ran 3610
#   tests green and broke git add. A sidecar written beside os.devnull
#   became nul.metadata.json -- a Windows reserved-name entry that
#   lists but cannot be opened. A test that discards its output cannot
#   verify it; version control caught it one layer out.
#
#   Sabotage: 10 mutations, 10 detected, 0 undetected.
#
#
# 3627 -> 3630 (+3)  2026-07-28  CI-u-1: one serialiser, not two.
#
#   RunArtifactWriter.save_eval_report serialised through asdict, which walks
#   the dataclass and BYPASSES EvaluationReport.to_serializable. Commit 3a added
#   that method precisely because asdict cannot carry result_kind and does not
#   normalise a refused result -- and this writer was never updated.
#
#   THE WRITER'S OWN COMMENT CLAIMED THEY WERE BYTE-IDENTICAL. True when
#   written on 2026-07-26, false the moment to_serializable existed. A claim in
#   a comment is not a check, and this one stopped being true silently, two days
#   later, with nothing to notice. Both now emit 15,509 identical bytes.
#
#   FAMILY B IS NOT PERSISTENCE-REACHABLE. CI-p claimed a blast radius of five
#   call sites; only two dump_strict_json sites exist in the package and neither
#   references any Family B type. The constraint did not exist. CI-p rescoped.
#
#   NEW ITEM CI-u: on a single-class cohort the FLAT auroc is NaN and strict
#   JSON refuses it, so a valid evaluation over a degenerate cohort produces an
#   artifact that CANNOT BE WRITTEN AT ALL. Absence needs a representation, not
#   a rejection. Staged u-1 (this) / u-2 absence / u-3 schema and read path.
#
#   Sabotage: 4 mutations, 4 detected, 0 undetected. B1 -- reverting to asdict
#   -- is the exact divergence that occurred between 26 and 28 July.
#
#
# 3630 -> 3655 (+25)  2026-07-29  CI-u-2: the absence vocabulary.
#
#   dump_strict_json refuses a non-finite number, correctly -- but the flat
#   report surface had no way to say a value was ABSENT, so the whole file was
#   rejected rather than the one field recorded as missing. Measured at
#   2a1e7f6, THREE OF FIVE COHORTS produced reports that could not be written.
#
#   BARE NULL IS NOT ENOUGH. UNDEFINED_ON_COHORT is a property of the DATA and
#   a legitimate finding; WITHHELD_BY_INPUT_GATE is a property of the MODEL
#   OUTPUT and a defect to fix. Reporting both as missing tells a reader to
#   investigate the wrong thing.
#
#   CURVE-LEVEL ABSENCE IS MEASURED, NOT AESTHETIC. No curve in any degenerate
#   cohort mixes valid and non-finite entries, so element-level absence would
#   describe a state that CANNOT OCCUR. A test pins that premise.
#
#   Absence is PER-CURVE: on an all-negative cohort tpr_curve is absent while
#   three other curves remain valid. n_expected separates a withheld curve over
#   200 rows from an empty curve over an empty cohort.
#
#   Sabotage: 7 mutations, 7 detected, 0 undetected, clean first pass. B2 and
#   B7 matter most -- both would DESTROY real measurements rather than merely
#   fail to record absence.
#
#   Does NOT wire the vocabulary in. That is u-3, and needs the gate verdicts
#   threaded through: the cause is only knowable where the refusal happened.
#
#
# 3655 -> 3682 (+27)  2026-07-29  CI-u-3: explicit absence. CI-u COMPLETE.
#
#   Frozen oracle before the change: THREE OF FIVE COHORTS produced reports that
#   could not be written at all. strict JSON refuses a NaN, correctly, but the
#   flat surface had no way to say a value was ABSENT. All five now persist.
#
#   THE CAUSE IS THREADED FROM WHERE THE REFUSAL HAPPENED. A single-class cohort
#   yields UNDEFINED_ON_COHORT, a property of the data; CI-t's gates yield
#   WITHHELD_BY_INPUT_GATE, a property of the model output. The NaN is identical.
#
#   Schema 4 adds field_absence and curve_absence with a BICONDITIONAL: a scalar
#   is null if and only if it is declared absent.
#
#   FOUR DEFECTS OF MY OWN. The invariant was VACUOUS as first wired --
#   to_serializable nulled every declared-absent field then asserted they were
#   null, so deleting the call entirely survived sabotage. The scalar predicate
#   tested is-None when the report uses NaN; the curve predicate tested
#   emptiness when an absent curve is [nan, nan]; and the completeness half
#   fired on reports that simply have no curves.
#
#   THE REGISTER CAUGHT A REAL DISCHARGE -- a status change made in code before
#   the document caught up. First genuine detection rather than synthetic.
#
#   Sabotage: 9 mutations, 9 detected, 0 undetected, 0 anchor misses. Two
#   earlier rounds were DISCARDED rather than accepted.
#
#
# 3682 -> 3709 (+27)  2026-07-29  CI-p: the writer agrees with the reader.
#   THE LAST OPEN DEFECT ON THE REGISTER IS CLOSED.
#
#   MetricResult.to_dict emitted the raw NaN that a non-OK result is REQUIRED
#   to carry in memory, and strict JSON refuses a non-finite number by design,
#   so every refused result was unpersistable through to_dict alone. from_dict
#   had always documented reading null back as NaN. THE READER WAS RIGHT; ONLY
#   THE WRITER DISAGREED.
#
#   The internal NaN sentinel is UNCHANGED -- it is only in an ARTIFACT that a
#   NaN becomes an absent estimate wearing a number s clothes. The rule is
#   STATUS-AWARE: an OK result with a non-finite value is a defect, and nulling
#   it would disguise that defect as a legitimate absence.
#
#   TWO LAYERS MET: commit 3a normalised NaN at the REPORT layer because the
#   source emitted it. Fixing the source made 3a s line meet a None it was
#   never written for. Kept and made tolerant rather than removed.
#
#   THE CLAIMED BLAST RADIUS NEVER EXISTED. CI-p named five Family B call sites;
#   measurement found none is persistence-reachable.
#
#   Sabotage: 6 mutations, 6 detected, 0 undetected, 0 anchor misses.
#
#
# 3709 -> 3710 (+1)  2026-07-29  register predicate audit: CI-m and CI-n
#   discharged. Both stated defects were ALREADY FIXED and neither predicate
#   could observe it, because both measured a PROXY.
#
#   CI-m asked: does evaluate call clean_arrays? True by design, permanently.
#   MEASURED: n_input 6, n_dropped 1, survivors 5. The caller CAN tell how many
#   observations a number describes. The counts arrived with the fail-closed
#   work of 2026-07-20; the item was never updated.
#
#   CI-n asked: does the derivation ACCEPT a cohort_version parameter? Also
#   permanently true. The ordered variant-identifier sequence is already
#   incorporated, and the docstring says so. MEASURED: frames differing in
#   variants, or in ORDER alone, yield distinct identities.
#
#   CI-r audited too and is GENUINELY OPEN -- its predicate measures the stated
#   condition directly.
#
#   Sabotage: 6 mutations, 5 detected, ONE UNDETECTED and RECORDED. A predicate
#   that calls the code and IGNORES the result needs dataflow analysis to
#   catch; that boundary is written into the guard rather than claimed.
#
#   THREE predicates have now been found measuring proxies: CI-q, CI-m, CI-n.
#   The register exists to stop status drifting from code, and its own
#   predicates were the drift.
#
#
# 3710 -> 3711 (+1)  2026-07-29  CI-r discharged. Every predicated carried
#   item is now closed.
#
#   CI-r said the frozen oracle is blind to interval certification and that a
#   defect forcing it False would be invisible. THE FIRST HALF IS ACCURATE; THE
#   SECOND IS FALSE. Forcing the success-path assignment to False FAILS
#   test_evaluator_produces_a_certified_interval_when_genes_are_present.
#
#   An oracle blind to a property is only a gap when NOTHING ELSE asserts it.
#   test_bootstrap_reconciliation covers it in both directions.
#
#   THE FOURTH PROXY. Its predicate read a FROZEN fixture -- a file nobody
#   intends to change -- rather than a coverage gap. CI-q a text scan, CI-m a
#   call check, CI-n a parameter check, CI-r a frozen-fixture property.
#
#   AN EMPTY GUARD THAT SKIPPED. With the last predicated item discharged,
#   pytest skipped the parametrised test -- got empty parameter set -- a guard
#   reporting success while checking nothing, which would also have raised the
#   stable skip surface from seven to eight. Rewritten as a loop.
#
#
# 3711 -> 3711 (+0)  2026-07-29  an assertion that had never executed.
#   SKIP SURFACE 7 -> 6.
#
#   test_aleatoric_higher_near_decision_boundary asserts that aleatoric
#   uncertainty peaks near p = 0.5, where binary entropy is maximal. IT HAD
#   NEVER RUN: at five epochs the model was so undertrained that no prediction
#   reached an extreme band, the precondition failed, and it skipped silently.
#
#   MEASURED, seed 42:
#      5 epochs  range [0.283, 0.731]  boundary 252  extreme  0  SKIPS
#     25 epochs  range [0.066, 0.840]  boundary  81  extreme  3  spans
#     50 epochs  range [0.025, 0.919]  boundary  25  extreme 58  SPANS
#
#   IT IS UNDERTRAINING, NOT THE DATA. A corpus with DESIGNED margin structure
#   was tried and was WORSE -- thirty epochs rather than twenty-five. That
#   hypothesis was stated before measuring and was refuted.
#
#   The precondition is now ASSERTED rather than skipped: a corpus that stops
#   spanning is a FAILURE requiring the training budget to be re-measured.
#
#   THE ASSERTION PASSES -- 7.09s, confirmed twice, run from a scratch copy
#   before packaging. The decomposition genuinely exhibits the property it
#   claims; that was never in evidence before today.
#
#   Collection UNCHANGED: the test was always collected, merely skipped.
#
#
# 3711 -> 3711 (+0)  2026-07-29  the README did not know the evaluation
#   stack existed.
#
#   The badge was updated by fifteen installers of this series while the prose
#   went unread. MEASURED in 432 lines: ZERO occurrences of typed metric
#   registry, MetricResult, schema version, carried item, EvaluationPopulation,
#   certification, ModelComparison or legacy projection.
#
#   A CLAIM NEARLY CORRECTED WRONGLY: the README says 95 tabular features and
#   the remembered figure was 91. variant_ensemble.py:193 defines 95. The count
#   grew and the README tracked it -- the stale value was MINE.
#
#   Added the section Evaluation as evidence, in the document's declarative
#   voice. Every factual claim verified against the code, including literally
#   that a single-class cohort yields undefined with reason
#   binary_class_support_required.
#
#   No code touched. Ratchet unchanged; no test added or removed.
#
#
# 3711 -> 3851 (+140)  2026-07-29  METRIC REGISTRY, COMMIT 1: the declared
#   catalogue and the confusion family. Priority 1 of project_metrics.txt.
#
#   The 2026-07-20 handoff asked at line 454 for this decision to be made
#   EXPLICITLY and warned it should not be made implicitly twice. It had been,
#   across three sessions of metric-surface repair. Asked directly, the choice
#   was the registry.
#
#   THE CATALOGUE. Sixteen panels specified, two present; the other fourteen
#   were absent AND INVISIBLE. catalogue.py registers every specified metric
#   with a written formula, range, direction and status, so an unbuilt metric
#   is a REGISTERED ABSENCE. 23 specified, 17 built, 6 absent -- from 13.
#
#   SEVEN KERNELS, hand-computed from TP=3 FN=1 FP=1 TN=5 and matched exactly.
#   NaN never zero on an empty margin. Both the prevalence-DEPENDENT predictive
#   values and the prevalence-INDEPENDENT likelihood ratios, deliberately.
#   Registered but NOT in the report surface -- that would move the frozen
#   480-value oracle, which is a separate declared change.
#
#   THE REGISTRY'S OWN VALIDATORS CAUGHT ME TWICE: threshold identity, and
#   REPORT_METRIC_NAMES protecting the frozen oracle.
#
#   FOUR DEFECTS OF MINE. Balanced accuracy refused on a PERFECT classifier
#   because I borrowed the likelihood-ratio predicate to satisfy the identity
#   validator -- the 3b-1a over-restriction repeated. Two invented names. Two
#   display names disagreeing with the registry.
#
#   AND THE KERNELS HAD NO TESTS: hand-verified in a throwaway probe, never
#   committed. Sabotage found three surviving numerical mutations, one the exact
#   error the kernel's own docstring warns against.
#
#   JEPA DISK POSITION, CORRECTED 2026-07-30. The line previously here read
#   "JEPA IS NO LONGER DISK-BLOCKED: 56.01 GB free against ~14.7 GB needed" and
#   was wrong twice.
#
#     UNIT. 56.01 came from an idiom dividing by PowerShell's 1GB literal, which
#     is 1073741824 bytes. The figure was GIBIBYTES under a label reading GB.
#     Every free-space number recorded through that idiom is a gibibyte number.
#
#     REQUIREMENT. ~14.7 GiB is the cache-only estimate WITHDRAWN on 2026-07-20,
#     when the operating floor was added. Line 1944 of this file already records
#     the corrected requirement -- 61.48 GiB -- and notes that it "reproduces
#     EXACTLY what audit_disk_census.py printed". One line contradicted another
#     2,330 lines earlier.
#
#   MEASURED 2026-07-30 by three independent methods agreeing within 1.5 MB:
#   935.59 GiB volume, 83.50 GiB free, 8.925 per cent. Against the corrected
#   61.48 GiB requirement the margin is +22.02 GiB: SUFFICIENT.
#
#   AND A CAVEAT WORTH MORE THAN THE VERDICT. A reading of 55.36 GiB taken at
#   02:24 the same morning was an artefact of timing: it was taken immediately
#   after a full suite run, and a full suite transiently consumes about 28 GiB of
#   temporary files. The baseline is ~83 GiB, corroborated by lines 1898 and 1931.
#   A capacity decision must clear the MINIMUM observed across a working session,
#   and a suite run overlapping a cache build would collide.
#
#   The 14.7 GiB figure itself remains UNDERIVED: it is a bare literal at
#   scripts/forensics/audit_disk_census.py:137 with no embedding dimension, row
#   count, dtype width or replica count behind it. It should be re-derived before
#   it governs anything.
#
#   Sabotage: 9 mutations, 9 detected, 0 undetected, 0 anchor misses.
#
#
# RECONSTRUCTED HISTORY -- 2026-08-01
#
# The following six suite-size transitions occurred in committed history but
# received no contemporaneous ratchet-log heading. Reconstructed from:
#
#     git log -p --follow -- tests/EXPECTED_SUITE_SIZE
#
# This is a factual reconstruction of commit, date, transition and subject only.
# No contemporaneous sabotage record was located; none is inferred. A mutation
# run may have happened without being written down, and absence of a RECORD is
# not absence of an ACTIVITY.
#
# The number was maintained correctly throughout -- every one of these six
# commits bumped it, and continuous integration runs the gate under
# --assert-suite-size, so a stale value would have failed the push. What lapsed
# was the NARRATIVE. That is the second decay this file can suffer: the gate
# makes forgetting the NUMBER fail, and does not make forgetting the STORY fail.
# A bare +270 cannot afterwards be distinguished from 270 tests appearing by
# accident.
#
#   50bb9fa  2026-07-30  3851 -> 3856  (+5)
#     carried item CI-i predicate; derive the README badge
#
#   2044102  2026-07-30  3856 -> 3862  (+6)
#     repair 301 lines of double-encoding in the changelog
#
#   c4229f1  2026-07-30  3862 -> 3898  (+36)
#     metric registry, commit 2 -- Brier decomposition
#
#   27f6009  2026-07-30  3898 -> 3959  (+61)
#     three metrics the catalogue declared
#
#   c4f14fb  2026-07-30  3959 -> 4120  (+161)
#     risk_control -- bound a clinical risk
#
#   dfc9d74  2026-07-30  4120 -> 4121  (+1)
#     separate the run-gate allowance from the JEPA figure
#
#     Historical-placement correction:
#
#     The Joint-Embedding Predictive Architecture disk-correction prose dated
#     2026-07-30 was appended at lines 4274-4302 beneath the earlier
#     3711 -> 3851 entry, whose heading is dated 2026-07-29. It belongs
#     semantically to dfc9d74 / 4120 -> 4121. The original text is deliberately
#     LEFT IN PLACE; this pointer corrects its attribution without rewriting the
#     historical artifact. Moving it would make the file read as though it had
#     always been correctly organised, which is a weaker record than an additive
#     erratum: the filing error is itself evidence about how this log decays.
#
# Arithmetic:
#
#     5 + 6 + 36 + 61 + 161 + 1 = 270
#     3851 + 270 = 4121
#
# Sabotage: contemporaneous results unavailable; none reconstructed.
#
#
# 4121 -> 4140 (+19)  2026-08-01  POP-1a: explicit label-eligible evaluation
#   population.
#
#   THE OTHER HALF OF THE 2026-07-27 RULING. "No numerical kernel may select,
#   filter, normalise or redefine its evaluation population." Commit 2a enforced
#   that for predicted scores and probabilities and DELIBERATELY left the label
#   half standing, because withheld labels are first-class here -- carried as NaN
#   by CanonicalVariantTable -- and selecting on them is a POPULATION decision.
#   `population.py` was written as the replacement and, until this commit, had
#   ZERO call sites in production. Twenty-two in tests; none in src.
#
#   MEASURED 2026-08-01 against the installed package, before any change. With
#   y = [1, 1, 0, nan], registry.compute returned for positive_predictive_value:
#
#       value 1.0   status ok   reason None
#       CERTIFICATION_ELIGIBLE True   N_OBSERVATIONS 4
#       POPULATION_FINGERPRINT sha256:9ff577fc...
#
#   A value computed over THREE rows, carrying the FOUR-row population's size and
#   fingerprint, certified eligible, with no reason and no diagnostic. The
#   narrowing happened inside metrics.clean_arrays where nothing downstream could
#   see it. registry.py:530-535 records the same shape on the probability axis --
#   a Brier score over 980 rows reported as n_observations = 1000 -- which is the
#   incident the ruling was written to eliminate. The fingerprint is the sharpest
#   part: it attested to a membership set the value was not computed over.
#
#   THE WIRING. The population is built BEFORE the input gates, restricted with
#   scope "label_eligible" and reason "reference_label_withheld" -- the vocabulary
#   the suite already asserted at twenty-two call sites -- and every array is
#   projected exactly once. Fourteen consumers were enumerated from the source
#   before a line was written; a projection reaching thirteen of them produces a
#   fingerprint that is ACTIVELY WRONG rather than merely over-broad.
#
#   Gene clusters are resolved on the PROJECTED frame. `_resolved` refuses
#   all-or-nothing on any missing gene label and `partitions_equivalent` compares
#   whole columns, so resolving on the full frame would withhold a certified
#   interval because a row EXCLUDED from the evaluation lacked a gene symbol --
#   the 3b-1a over-restriction repeated. compare_models restricts ONCE and hands
#   down one object, because restricting per call would make the comparison
#   artifact record the ATTEMPTED fingerprint while every report it summarises
#   described the narrower set: this defect, one layer up.
#
#   THREE FAILURES LIE BEHIND AN EMPTY POPULATION, AND ARE GUARDED ONCE. An
#   all-withheld cohort projects to n = 0, and restrict permits it: it refuses a
#   mask removing NOTHING, not one removing everything. Downstream sit a division
#   by zero at the logger, a LegacyProjectionError from the single-class
#   area-under-the-precision-recall-curve rule -- which correctly refuses to rest
#   a derived value on a prevalence that was itself refused -- and an
#   uncharacterised multiplication in print_report. Guarding them one at a time
#   would be patchwork and the third was never even reached. The cohort is
#   refused once, with its lineage in the message. That is NOT the component-level
#   refusal the input gates perform: those withhold ONE quantity from a cohort
#   that exists. Here there is no cohort.
#
#   A DEFECT REPAIRED IN PASSING, DELIBERATELY DECLARED. `n_pos = int(y.sum())`
#   sat at line 817, ABOVE every input gate, and raised "cannot convert float NaN
#   to integer" on a withheld label -- measured on six of nine probed dtypes,
#   including a plain float array and a pandas Series, which is exactly what the
#   signature advertises. `evaluate` died there rather than reaching the gates
#   built for that input. Moving the line below the projection repairs it by
#   construction.
#
#   FOUR DEFECTS OF MINE, ALL CAUGHT BY SOMETHING OTHER THAN MY OWN READING.
#
#     THE SCORES REGRESSION. The first POP-1a projected `scores` BEFORE
#     validate_ranking_scores, so a mis-sized array raised from population.take
#     instead of being refused -- precisely the defect recorded at that gate on
#     2026-07-28 ("REFUSED MEANS NOT FORWARDED"), reintroduced one layer earlier.
#     Caught by the FULL SUITE, not by review: 1 failed, 4130 passed. `y` and `p`
#     are validated with n_expected=n because they are already projected;
#     `scores` arrives SOURCE-ALIGNED, so it is checked against n_source and
#     projected only once it validates. Only ONE of that test's two
#     parametrisations failed -- the correctly sized array passes take() and is
#     refused gracefully -- so a spot check would have missed it.
#
#     A VACUOUS TEST. The companion score test asserted only that the curves were
#     non-empty. On the label-eligible cohort [1, 1, 0] the fixture
#     [0.95, 0.05, 0.15] yields an area under the receiver operating
#     characteristic curve of 0.5 -- IDENTICAL to the probability channel
#     [0.9, 0.1, 0.2]. It would have passed while the evaluator validated the
#     array, projected it, and then ignored it. Replaced with
#     [0.95, 0.85, 0.15], which yields 1.0 and separates the channels.
#
#     TWO BLANK-LINE DEFECTS, from an appender idiom that stripped a block's own
#     leading newlines. Found by reading the applied diff. There is NO LINT GATE
#     in this repository -- no ruff, flake8, pycodestyle or pre-commit
#     configuration, and none of the five workflows runs one -- so formatting
#     discipline rests entirely on review. Recorded as a follow-up.
#
#   SABOTAGE. Fourteen mutations in a disposable git worktree. Two defences ran
#   first, because POP-1a was uncommitted and an editable install would have
#   resolved imports to the REAL src rather than the worktree, making every
#   mutation land on a file nothing imports and reporting 0 detected / 14
#   undetected -- the exact inverse of the truth. PYTHONPATH was pinned to the
#   worktree and evaluator.__file__ confirmed inside it; then a CANARY break was
#   required to turn the suite red, with the run aborting as HARNESS_ERROR
#   otherwise. The canary produced 37 failures. Restoration was verified against
#   the pristine digest after every one of fifteen runs.
#
#   M10 IS UNDETECTED AND RECORDED. Counting positives from the attempted labels
#   with np.nansum is EQUIVALENT on every fixture cohort: nansum ignores NaN and
#   the projection removes exactly the NaN rows. The two diverge only on an
#   INFINITE label, which np.isfinite excludes and np.nansum does not -- there the
#   projected sum is 2 and int(np.nansum(...)) raises OverflowError. That is a
#   real but narrow gap, and it raises a question deferred deliberately: an
#   infinity is not a MISSING label, it is a CORRUPT one, and treating it as
#   withheld pools it with genuine NaN.
#
#   Three mutations -- M04, M05, M13 -- were each detected by exactly ONE test,
#   test_the_metadata_frame_is_projected_with_the_arrays. Deleting that single
#   test would make three mutations invisible.
#
#   ACCEPTANCE: 19 tests in tests/unit/test_population_wiring.py, all green; the
#   full suite 4134 passed + 6 skipped = 4140 collected, skip set unchanged at 6;
#   test_existing_registry_results_do_not_move UNMOVED, its label_restricted
#   fixture having already applied this idiom before POP-1a existed.
#
#   FOLLOW-UPS RECORDED, NONE TOUCHED: ABS-1, the ranking channel's refusal
#   reported as undefined_on_cohort because _absence_maps takes ranking_check and
#   never reads it; roughly forty lines of dead absence computation in evaluate,
#   discarded at line 1240 and carrying a reason string that already disagrees
#   with the live one; eighteen ruff F821 undefined names, seven of them the
#   deliberate _ensure_sklearn global injection; no lint gate; ModelComparison
#   carrying a population fingerprint with no population scope beside it.
#
#   Sabotage: 14 mutations, 13 detected, 1 undetected, 0 anchor misses.
#
#
# 4140 -> 4152 (+12)  2026-08-02  POP-1b: the report names its evaluation
#   population. Schema version five.
#
#   POP-1a made `n_samples` the LABEL-ELIGIBLE count and deliberately added no
#   fields, because schema surface belongs in its own commit with its own version
#   bump. A reader of a version-4 artifact therefore could not tell a smaller
#   cohort from a narrowed one. Five fields say which, by how much, and what the
#   narrowing narrowed FROM: n_source, n_label_eligible,
#   n_reference_label_withheld, population_scope, population_parent_fingerprint.
#
#   THE FINDING THAT SHAPED THE WHOLE DESIGN, and it is invisible in the
#   dataclass. `from_serialized` filters a payload to known field names and calls
#   the constructor. A payload written before POP-1b contains none of the five,
#   so declared WITHOUT DEFAULTS they would raise TypeError on every version-1
#   through version-4 artifact ever written. It appears only when the
#   DESERIALISERS are read, which the preflight did and the dataclass could not.
#
#   The counts default to -1, not 0. Zero is a legitimate measurement for a
#   cohort that was attempted and yielded nothing; a historical artifact must not
#   read as though it recorded one. A negative sentinel is not a count.
#
#   `n_reference_label_withheld` reads `population.n_excluded_from_source`, an
#   EXISTING property whose docstring already said "relative to the ORIGINAL
#   frame, however many narrowings". The build specification proposed recomputing
#   `n_source - n` inline; that would have been a SECOND COPY of one quantity --
#   shape (a) inside the commit whose specification quotes shape (a).
#
#   THE THREE COUNTS ARE ENFORCED, NOT DECORATED. `__post_init__` asserts
#   n_source == n_label_eligible + n_reference_label_withheld, and refuses a
#   partial recording where two are measurements and one is a sentinel. A field
#   trio that must sum is exactly the shape that rots silently: any one can drift
#   and nothing notices. The battery constructs violations, because an invariant
#   nothing can violate in a test is an invariant nothing checks.
#
#   DEAD-2 REMOVED IN THE SAME COMMIT. `field_absence` and `curve_absence` were
#   declared TWICE in EvaluationReport, under two comment blocks both dated
#   2026-07-29. Python kept the SECOND -- the pair whose comment is three lines --
#   so the eight-line explanation of the biconditional was attached to
#   declarations Python discarded. POP-1b adds five fields immediately below that
#   region, which is how a third copy appears.
#
#   FOUND ONLY BY THE FULL SUITE. POP-1b's own battery and the two files it
#   repaired were green while FOUR tests failed in two files nothing in this
#   commit had opened: a cumulative field-growth guard, the readable-version set,
#   and two emitted-version assertions. The preflight had measured 347 test lines
#   matching `EvaluationReport|n_samples|schema`, which is why a schema change is
#   not declared done on the strength of its own tests.
#
#   THE 2b-3 SNAPSHOT FIXTURE WAS NOT REGENERATED. The growth guard reads
#   `tests/fixtures/report_snapshot_2b3.json` and lists every field added since.
#   Rebasing it onto today would have made it permanently blind to everything
#   added before now -- green while guarding nothing, which is worse than red.
#   The five names were APPENDED, in the order `dataclasses.fields` returns them,
#   measured from the live class. The installer verifies the fixture's digest
#   after writing and restores everything if it moved.
#
#   Three assertions were protected from change because they are TRUE and POP-1b
#   does not move them: `_TYPED == 3`, `_VERSION == 2`, and `removed == []`. Two
#   more elsewhere: `_ABSENCE == 4`, and the `4/4` contract-test count in
#   RUN_17_PLAN.md. Retargeting a true statement is how a correct document
#   becomes a false one, and this session nearly did it three times.
#
#   A TEST NAME THAT HAD ALREADY GONE STALE. `test_exactly_one_report_field_was_
#   added` asserted THREE: it was extended once and the name was left behind. That
#   name cost a full turn of this session, reading as a historical claim about one
#   commit when it is a running record. Renamed to
#   `test_no_report_field_appeared_unannounced`, which will not need renaming.
#
#   SIX DEFECTS OF THE AUTHOR'S, AND NOT ONE WAS CAUGHT BY REVIEW. An orphaned
#   comment that would have captioned the wrong constant; a docstring in a form
#   this file uses nowhere; two names used in repairs that the target file does
#   not import; a rename that would have DESTROYED the absence-maps guarantee it
#   was meant to preserve; a hand-built fixture supplying six of thirty-seven
#   required fields; and a line-count constant carried between two installers
#   that count differently, which refused a byte-identical file. Every one was
#   caught by an installer post-check, a rendered diff, or a real run.
#
#   A POST-CHECK MATCHED ITS OWN PROSE FOUR TIMES -- on `int(y.sum())`, on
#   `REFUSED MEANS NOT FORWARDED`, on the score fixtures, and on
#   `dataclasses.replace(`. Tuning the expected number fixes the instance and
#   leaves the class. The checks now tokenise and count over code with comments
#   and string literals removed. A count over source that discusses itself is
#   structurally unreliable.
#
#   SABOTAGE. Eleven mutations in a disposable worktree, behind the same two
#   defences as POP-1a: PYTHONPATH pinned with `evaluator.__file__` confirmed
#   inside it, and a canary required to turn the suite red or the run aborts.
#
#   M01 REMOVED A DEFAULT -- the mutation this battery existed for -- and was
#   DETECTED, breaking four tests. The defaults are genuinely pinned. The
#   distinction matters: the test that proves them FAILED on 2026-08-02 for an
#   unrelated reason, was repaired, and only then passed. A test that has passed
#   once is not the same as a test that can fail.
#
#   TWO UNDETECTED, AND THEY ARE REAL GAPS, NOT EQUIVALENT MUTANTS. POP-1a's one
#   undetected mutation was indistinguishable on any reachable cohort. These are
#   not.
#
#     M03 substitutes `n_excluded_from_parent` for `n_excluded_from_source`.
#     Every fixture population is ONE LEVEL deep, where the two coincide. They
#     diverge at two levels -- precisely the `compare_models` case the
#     specification cited as the reason for choosing the source distance. The
#     design decision is correct and entirely unprotected.
#
#     M07 makes the `print_report` narrowing guard always true, so a fully
#     labelled cohort gains a population line it should not have. Undetected
#     because NOTHING ASSERTS ON `print_report` OUTPUT AT ALL. The guard exists
#     to keep unrestricted output byte-identical and nothing checks that claim.
#
#   Both are recorded, neither is quietly repaired. Two tests would close them --
#   a two-level population, and a captured-output assertion -- and they belong in
#   their own commit with their own sabotage run, not retro-fitted to this one.
#
#   ACCEPTANCE: full suite 4146 passed, 6 skipped, 0 failed in 12m37s; 4152
#   collected; skip set unchanged at 6.
#
#   Sabotage: 11 mutations, 9 detected, 2 undetected, 0 anchor misses.
#
#
# 4152 -> 4157 (+5)  2026-08-03  REG-1: metadata ownership differs between the
#   refusal path and the success path, and the difference is now enforced.
#
#   `registry.compute` merged descriptor-supplied metadata two ways. The OK
#   branch derived a protected set, raised RegistryInvariantError on overlap, and
#   merged the verdict FIRST so it lost anyway. The REFUSAL branch merged
#   `verdict.metadata` LAST, so it WON, with NO collision check at all.
#
#   THE REFUSAL BRANCH WAS THE WORSE ONE TO LEAVE OPEN. `ctx.support()` supplies
#   POPULATION_SCOPE, POPULATION_FINGERPRINT, N_OBSERVATIONS and
#   N_CLASSES_OBSERVED, and its docstring says why they ride on refusals too: "an
#   INSUFFICIENT_SUPPORT on 3 rows and one on 300,000 point at different
#   problems." A refusal could claim a membership fingerprint it never examined,
#   on the branch whose whole purpose is stating its evidence base -- and
#   POPULATION_FINGERPRINT exists precisely because "n=980 beside n=980 says
#   nothing about WHICH 980".
#
#   LATENT ON 2026-08-01, LIVE AFTER POP-1b. The population keys were thinly
#   populated when this was found; `00e180c` made them real, populated and
#   load-bearing. The defect did not change. Its consequences did.
#
#   VERSION ONE WAS WRONG, AND THE SUITE SAID SO IN TWELVE MINUTES. It derived
#   ONE protected set and applied it to both branches. Twenty-nine tests went red:
#
#       verdict = Applicability(applicable=False, status=UNDEFINED,
#                               reason='binary_class_support_required',
#                               metadata={N_CLASSES_OBSERVED: 1, ...})
#       RegistryInvariantError: auroc: applicability metadata attempted to set
#       registry-owned key(s) ['MetricMetadataKey.N_CLASSES_OBSERVED']
#
#   `auroc` is a REGISTERED descriptor reporting the GROUND of its refusal --
#   "there is one class, therefore this metric is undefined" -- and the guard
#   called it a violation. A guard that treats two paths as symmetric when they
#   are not is a guard that REJECTS CORRECT BEHAVIOUR.
#
#   THE OWNERSHIP LINE WAS THEN MEASURED, NOT ARGUED. Probed across four cohort
#   shapes over every registered descriptor: 27 refusals observed;
#   N_CLASSES_OBSERVED claimed by SEVEN metrics; N_OBSERVATIONS,
#   POPULATION_FINGERPRINT and POPULATION_SCOPE claimed by NONE. The author had
#   listed N_OBSERVATIONS as registry-owned by argument beforehand -- the probe
#   CONFIRMED it, which is not the same as having been right, and had any
#   descriptor set it, version two would have failed exactly as version one did.
#
#   THE CONTRACT, ENFORCED:
#
#       key                        refusal path              success path
#       METRIC_NAME                registry-owned            registry-owned
#       POPULATION_SCOPE           registry-owned            registry-owned
#       POPULATION_FINGERPRINT     registry-owned            registry-owned
#       N_OBSERVATIONS             registry-owned            registry-owned
#       N_CLASSES_OBSERVED         DESCRIPTOR-owned evidence registry-owned
#       CERTIFICATION_*            not descriptor-supplied   registry-owned
#
#   ONE DERIVATION, ONE NAMED EXCEPTION. The protected set stays DERIVED from
#   `support()` on both paths, so a future key added there is protected the
#   moment it EXISTS rather than the moment somebody remembers. Only the
#   exception is named, and only the refusal path subtracts it. Neither merge
#   order changed: reordering also stops an overwrite, but SILENTLY, and the OK
#   branch has recorded since 2026-07-28 why that is the inferior fix.
#
#   TWO MUTATION ROUNDS ARE COMMITTED, AND THE FIRST IS PRESERVED UNCORRECTED.
#   docs/measurements/REG1_MUTATION_BASELINE_2026-08-03.txt records
#   6 mutations, 4 detected, 2 undetected -- INCLUDING two wrong rationales that
#   are themselves the finding:
#
#     * M05 was predicted detectable because "the pre-existing hijack test sets
#       N_CLASSES_OBSERVED". IT DOES NOT -- it sets POPULATION_SCOPE,
#       N_OBSERVATIONS and CERTIFICATION_ELIGIBLE. The list had been read during
#       the REG-1 preflight and the opposite was asserted from it. M05 was a REAL
#       MISSING TEST, not an equivalent mutant.
#
#     * M06 was called "the one thing a test cannot catch", on the reasoning that
#       no test can catch a number that is still correct. WRONG. A BEHAVIOURAL
#       test cannot; a STRUCTURAL one can, because the derivation is a property
#       of the SOURCE. Behavioural tests prove outputs and refusals; structural
#       tests prove ownership, derivation and authority paths.
#
#   Both gates were then FALSIFIED INDIVIDUALLY before the second round -- M05
#   alone gave "DID NOT RAISE", M06 alone gave "the refusal protected set was
#   HAND-LISTED instead of derived from ctx.support(), assert 0 == 1" -- because
#   inferring which test caught which mutation from an aggregate is weaker than
#   showing it.
#
#   ROUND TWO also verifies that the NAMED gate fired, not merely that the suite
#   went red: DETECTED_BY_EXPECTED is distinguished from DETECTED_BY_OTHER, and
#   the second would have been reported as the weaker result it is. All six were
#   caught by the test written to catch them.
#
#   FIVE DEFECTS OF THE AUTHOR'S IN THIS COMMIT ALONE, NONE CAUGHT BY REVIEW: a
#   raise-site check that counted the CLASS DEFINITION as a raise; a dead helper
#   written and then bypassed; a TAUTOLOGY comparing a value with itself, which
#   is the `_ABSENCE == _ABSENCE` defect repaired that same morning and
#   reproduced hours later; a count written against code the author MEANT to
#   write; and a fifth post-check matching its own prose, after which the checks
#   were tokenised rather than tuned.
#
#   ACCEPTANCE: full suite 4151 passed, 6 skipped, 0 failed in 17m49s; 4157
#   collected; skip set unchanged at 6.
#
#   Sabotage: 6 mutations, 6 detected, 0 undetected, 0 anchor misses.
#
#
# 4157 -> 4161 (+4)  2026-08-04  OP-0: characterize and clarify the legacy
#   operating-point selector semantics. NO numerical change. NO schema change.
#
#   PREPARATORY. OP-1 will replace this subsystem with a typed, population-
#   bearing one. Before that, the legacy contract is made TRUTHFUL and FROZEN so
#   the shadow comparison is scientifically interpretable. Without this, an OP-1
#   difference could be mistaken for a sweep defect, a tie-breaking defect, a
#   threshold-order defect, a regression in positive predictive value, or the
#   intended policy change -- five readings of one number.
#
#   THE DOCSTRING CLAIMED A POLICY THE CODE DOES NOT IMPLEMENT. It read
#   "Highest-sensitivity threshold where PPV >= min_ppv". The code walks
#   candidates from conservative to permissive and BREAKS at the first violation,
#   returning the last preceding candidate. Those differ whenever positive
#   predictive value is non-monotone in the threshold -- which it is.
#
#   MEASURED 2026-08-04, y = [1, 0, 1], p = [0.9, 0.8, 0.7], min_ppv = 0.60:
#
#       t=0.90   ppv=1.0000   sensitivity=0.5000   FEASIBLE        <- selected
#       t=0.80   ppv=0.5000   sensitivity=0.5000   violates floor  <- break fires
#       t=0.70   ppv=0.6667   sensitivity=1.0000   FEASIBLE, UNREACHABLE
#
#   The selected point has HALF the sensitivity of a candidate satisfying the
#   same floor. The legacy rule is legitimate -- the most permissive threshold
#   that has never dropped below the floor -- but it is not what its docstring
#   said.
#
#       legacy `_find_high_ppv_point`   OBJECTIVE B, conservative prefix
#       OP-1's policy for at_high_ppv   OBJECTIVE A, pointwise floor, maximise
#                                       sensitivity over ALL candidates
#
#   NEITHER NAME EXISTED ANYWHERE IN THE REPOSITORY BEFORE THIS COMMIT. Measured
#   by the OP-1 preflight: `Objective A` and `Objective B` returned ZERO
#   occurrences across source and documentation. The decision adopting Objective
#   A was taken 2026-08-01 and had never been written where the work happens --
#   a new instance of a familiar family: not a stored number that rotted, but an
#   ADOPTED DECISION never recorded outside the conversation that took it.
#
#   THE BREAK IS NOT REMOVED. Removing it would switch from B toward A before the
#   typed subsystem, exact sweep, population identity, refusal semantics and
#   shadow comparison exist to receive it, and the resulting difference would be
#   uninterpretable.
#
#   A NAMING DEFECT, REPAIRED. `n_neg = tp + fp  # n_flagged` stored the
#   PREDICTED-POSITIVE count under the name of the reference-negative count,
#   while the sibling `_find_operating_point` uses `n_neg` for the genuine
#   fp + tn. One identifier, two quantities, two adjacent functions. The
#   arithmetic was correct and the name was not -- one edit away from becoming
#   numerical. Renamed to `n_flagged`, which is already the public vocabulary: it
#   is a field on OperatingPoint. The sibling is untouched.
#
#   NO SABOTAGE LINE, DELIBERATELY. OP-0 changes NO ARITHMETIC, so there is
#   nothing behavioural to mutate. The installer proved it structurally: the
#   token stream of `_find_high_ppv_point` was compared before and after with the
#   renamed identifier normalised, and came to 418 tokens on both sides,
#   IDENTICAL. A text diff cannot show that, because the text also gains comments
#   and a rewritten docstring.
#
#   BOTH STRUCTURAL GUARDS WERE FALSIFIED INDIVIDUALLY, which is stronger than an
#   aggregate count for a commit of this kind. Reverting the rename in place gave:
#
#       AssertionError: line(s) [76] of _find_high_ppv_point store tp + fp under
#       the name n_neg ...   1 failed, 3 passed
#
#   The other three passed unchanged -- so the structural guard is not entangled
#   with behaviour it should not be watching. Restoration was verified by digest.
#
#   THE SUBSYSTEM ABOUT TO BE REWRITTEN HAS ALMOST NO COVERAGE. Measured across
#   the whole test tree: `_find_high_ppv_point` was exercised by NOTHING -- the
#   only match for either finder was a COMMENT mentioning `_find_operating_point`
#   in prose. The operating points themselves were asserted on seven lines across
#   four files. A shadow comparison against an unexercised selector proves very
#   little, and OP-1 should be read with that in mind.
#
#   A TOOL DEFECT FOUND AND FIXED IN PASSING. The `code_only` helper used by
#   these installers' post-checks filtered `tokenize.STRING` -- but ON PYTHON
#   3.12 F-STRINGS DO NOT TOKENISE AS STRING. They produce FSTRING_START,
#   FSTRING_MIDDLE and FSTRING_END, so f-string prose passed straight through a
#   filter whose entire purpose was excluding prose. Every earlier use in this
#   sequence had the same blind spot; the counts that passed did so because no
#   f-string happened to contain the token. Found by measuring where the
#   occurrences were rather than reasoning about the discrepancy a third time.
#
#   ACCEPTANCE: full suite 4155 passed, 6 skipped, 0 failed in 18m21s; 4161
#   collected; skip set unchanged at 6. Four new tests in
#   tests/unit/test_operating_point_semantics.py -- a NEW module, because both
#   candidate homes are about something else and this project has already paid
#   for one misfiled entry.
#
#
# 4161 -> 4165 (+4)  2026-08-04  REG-2: a vanishing denominator is UNDEFINED,
#   not INSUFFICIENT_SUPPORT. One line of source; four tests that are the first
#   thing in the repository able to notice it.
#
#   ONE LINE, TWO METRICS. `_requires_flagged_margin` serves BOTH predictive
#   values through its `flagged` parameter, so a single status change moves
#   exactly the two the preflight identified. The reason strings are composed as
#   `f"empty_{side}_set"` and are UNCHANGED: they were accurate before and after.
#
#       positive_predictive_value  TP+FP = 0  INSUFFICIENT_SUPPORT -> UNDEFINED
#       negative_predictive_value  TN+FN = 0  INSUFFICIENT_SUPPORT -> UNDEFINED
#
#   THE REGISTRY CONTRADICTED ITSELF, AND THIS WAS NOT A CLOSE CALL. Measured
#   across 24 registered descriptors and 6 cohort shapes:
#
#       undefined             binary_class_support_required     7 metrics
#       undefined             likelihood_ratio_unbounded        2 metrics
#       undefined             zero_confusion_margin             1 metric
#       insufficient_support  positive_class_support_required   1  CORRECT, stays
#       insufficient_support  negative_class_support_required   1  CORRECT, stays
#       insufficient_support  empty_predicted_positive_set      1  <- moved
#       insufficient_support  empty_predicted_negative_set      1  <- moved
#
#   TEN metrics already used UNDEFINED for a mathematically undefined state. Two
#   used INSUFFICIENT_SUPPORT for the same kind of state. REG-2 made two agree
#   with ten, and twelve agree now.
#
#   The enum's own contract: UNDEFINED is "mathematically undefined";
#   INSUFFICIENT_SUPPORT is "the machinery is ready and the science is not". A
#   quotient with a zero denominator is the first. And registry.py:954-973 states
#   the design intent this restores -- applicability must refuse EXACTLY where
#   the kernel would return NaN, with the requirement listed as `TP + FP > 0`,
#   marked threshold-dependent. That is a denominator condition.
#
#   WHY THIS IS ITS OWN COMMIT, AHEAD OF OP-1. OP-1's Oracle C exists to prove a
#   new count path reproduces the registry's status semantics. Making this change
#   INSIDE OP-1 would have been an intentional semantic movement disguised as an
#   implementation discrepancy: the oracle would report a difference and nobody
#   could tell which side was wrong.
#
#   THE GREEN SUITE WAS THE FINDING, NOT THE REASSURANCE. After the change:
#   4155 passed, 6 skipped, 0 failed -- a semantic correction to two metrics
#   changed NO test outcome anywhere. Then, measured directly, REVERTING it also
#   changed nothing: 54 passed before, 54 passed after. Nothing in the repository
#   asserted either status, so nothing would have noticed either drifting back.
#   That is the condition REG-2 exists to end, and it is why the four tests are
#   not optional.
#
#   FOUR TESTS, THREE JOBS. T1 and T2 pin the moved statuses AND their unchanged
#   reasons, because a status without its reason is half a contract. T3 pins the
#   BOUNDARY: `sensitivity` and `specificity` keep INSUFFICIENT_SUPPORT, since an
#   absent reference class is a property of the COHORT and not of a quotient --
#   without it the rule could be over-applied later and nothing would object. T4
#   is STRUCTURAL: it walks the live descriptor graph and fails if any refusal
#   naming a denominator-zero state carries anything but UNDEFINED.
#
#   T4 IS THE ONE THAT MATTERS MOST. T1 through T3 pin today's metrics; a metric
#   introduced next month with the old convention would pass all three. And in
#   the falsification T4 caught a case NEITHER T1 NOR T2 CONSTRUCTS -- the
#   negative predictive value's zero denominator on a single-row cohort.
#
#   THE FALSIFICATION IS THE SABOTAGE RUN. Reverting the change is the only
#   mutation that could exist for a one-line status correction, and its outcome
#   was predicted before it was run:
#
#       T1 FAIL   T2 FAIL   T4 FAIL   T3 PASS      3 failed, 55 passed
#
#   T3 passing under the reverted code is the load-bearing half: a boundary test
#   that failed alongside the others would have been measuring REG-2 rather than
#   the line REG-2 must not cross.
#
#   TWO INSTRUMENTS, WRITTEN BEFORE THE CHANGE, CONFIRM IT LANDED AND DID NOT
#   SPREAD. The denominator probe now exits 0 with both predictive values reading
#   `undefined`; the preflight reports a movement set of ZERO across all 24
#   descriptors. Both are committed under docs/measurements/.
#
#   ONE ARTIFACT IS DELIBERATELY SELF-CONTRADICTORY AND IS COMMITTED ANYWAY.
#   REG2_PREFLIGHT_AFTER exits 1 and prints "DECLARED MEMBERS NOT REPRODUCED",
#   offering two explanations -- unreachable cohorts, or an earlier wrong
#   measurement -- both of which are false. A third case obtains: the defect was
#   fixed. The instrument was written before the change and cannot distinguish
#   its own success from failure. Its message is honest about that limit, and
#   editing it to agree with the outcome would destroy the evidence.
#
#   COMPATIBILITY, RECORDED RATHER THAN WAVED PAST. No enum VALUE changed; both
#   strings already existed, and the enum's comment says those values are
#   load-bearing because historical run manifests contain them. Historical
#   manifests stay readable. What changes is that a NEW artifact reports
#   `undefined` where a PRE-REG-2 artifact reported `insufficient_support` FOR
#   THE SAME COHORT. A reader comparing the two sees a status change with no code
#   defect behind it.
#
#   THREE FINDINGS LOGGED AND DELIBERATELY NOT FOLDED IN. REG-2-b:
#   `_requires_interior_specificity` returns INSUFFICIENT_SUPPORT with reason
#   `specificity_undefined` -- the same mismatch, in a third place no cohort
#   shape reached. ICI-1: `integrated_calibration_index` is declared applicable
#   then returns non-finite, on three cohort shapes. F1-1: `f1` returns `ok` with
#   0.0 computed from an UNDEFINED positive predictive value -- the D5 shape, in
#   the registry rather than in the operating-point functions. Widening a
#   movement set to cover defects discovered while measuring it is the scope
#   creep the discipline prevents.
#
#   ACCEPTANCE: full suite 4159 passed, 6 skipped, 0 failed in 21m08s; 4165
#   collected; skip set unchanged at 6.
#
#
# 4165 -> 4171 (+6)  2026-08-04  THR-1a: the threshold vocabulary moves to a
#   bottom layer. ZERO MOVEMENT -- no behaviour, no value, no pre-existing test
#   outcome changed.
#
#   (The old value 4165 and today's PASS COUNT 4165 coincide. Different
#   quantities, unrelated.)
#
#   WHY THE LAYER EXISTS. OP-1's exact threshold sweep must describe each swept
#   candidate with a `ThresholdParameters`. A sweep living in a module that
#   imports `registry.py` would REVERSE THE LAYERING -- future registry
#   count-applicability code could then not import the sweep without a cycle.
#   `metrics.py` was the other candidate home and is worse: it imports
#   scikit-learn at module level, which would put a reusable algorithm behind
#   the import boundary `evaluation/__init__.py` exists to police, after commit
#   015ff94 broke exactly that.
#
#       capabilities.py / population.py
#                    |
#               thresholds.py        <- ThresholdOperator, ThresholdSource,
#                    |                  ThresholdParameters
#           +--------+--------+
#       registry.py       metrics.py
#
#   `thresholds.py` imports numpy, dataclasses, enum and logging. It imports
#   NEITHER registry, NOR metrics, NOR scikit-learn, and a STRUCTURAL test
#   asserts that over the source rather than at runtime -- a runtime check would
#   pass merely because scikit-learn happens to be installed.
#
#   RE-EXPORTED BY IDENTITY, NOT BY EQUALITY. `ThresholdParameters` documents
#   that one instance is shared by a descriptor, its kernel adapter and its
#   applicability predicate, "asserted BY IDENTITY at import time". A re-export
#   producing a DISTINCT class object would leave every isinstance() check
#   comparing against a different type, silently, until something asserted
#   identity -- and registry.py asserts it at import, so the import itself would
#   have failed rather than the suite.
#
#   THE CLASSES MOVED VERBATIM. The post-check compared
#   `before.replace(MOVED, "")` against `after.replace(REPLACEMENT, "")` to prove
#   everything outside the block was byte-identical -- a property that does not
#   depend on how difflib chooses to group hunks.
#
#   A SEQUENCING FAILURE OF MINE, RECORDED WITHOUT SOFTENING. A zero-movement
#   extraction needs an inventory BEFORE the move: every import of the classes,
#   every attribute lookup through `registry`, any package-level export, any
#   `__all__`, any use of `__module__`/pickle/qualified names, any monkeypatch,
#   any source-inspection test. SEVEN CHECKS. I ran none before the move and all
#   of them afterwards, when prompted.
#
#   Nothing broke, and the suite proves it. But "nothing broke" is the OUTCOME,
#   not the discipline: an inventory exists so a move is KNOWN safe rather than
#   FOUND safe, and running it afterwards converts a check into a postmortem.
#
#   THREE OF THE SEVEN WERE CLEAN BY ARRANGEMENT, NOT BY DESIGN.
#     * `registry.__all__` holds 12 entries and none of the three is among them,
#       measured from the LIVE module. Unchanged by the move -- they were never
#       declared there -- and all three remain reachable as attributes, which is
#       what every existing import uses.
#     * Every pickle and joblib reference across src, tests and scripts is a
#       MODEL artifact. None names a threshold type, so no historical artifact
#       refers to a class whose module moved.
#     * Three tests inspect `registry` source (test_metric_registry.py:437 and
#       :907, test_prediction_input_contract.py:294). All passed: none expected
#       the threshold definitions to be there.
#
#   THE FOURTH HAD GENUINELY CHANGED, AND WAS UNRECORDED. `__module__` moved from
#   `...evaluation.registry` to `...evaluation.thresholds`. Intended, and written
#   down nowhere. It affects repr, generated documentation, pickled output bytes
#   and type-name provenance, so an unrecorded change to it is an unrecorded
#   change to what an artifact says about itself.
#
#   AND THIS PROJECT ALREADY HAD THE PRECEDENT. `test_metric_result_relocation.py`
#   pins exactly this after exactly this kind of move: MetricResult was relocated
#   into `capabilities.py`, re-exported from `clustering_metrics`, and the tests
#   assert BOTH where it is defined and that consumers resolve the same object --
#   "must be DEFINED in the vocabulary layer, not re-exported into it". THR-1a
#   wrote the second half and omitted the first. The completing test uses the
#   precedent's wording, because it is the same claim about a different layer,
#   and it was falsified against `...registry` and `...capabilities` before
#   shipping.
#
#   NO SABOTAGE LINE. Nothing behavioural changed, so nothing behavioural can be
#   mutated; the two assertions above are the mutation-equivalent and both were
#   exercised.
#
#   THR-1b, NOT INCLUDED: adding `ThresholdSource.EVALUATION_SWEEP`. An additive
#   persisted-vocabulary change has a different failure mode from an extraction,
#   and combining them would give a regression two possible origins.
#
#   ACCEPTANCE: full suite 4165 passed, 6 skipped, 0 failed in 18m06s; 4171
#   collected; skip set unchanged at 6. Six new tests; EVERY PRE-EXISTING TEST
#   PASSED UNCHANGED, which is the whole acceptance criterion for a vocabulary
#   move.
#
#
# 4171 -> 4175 (+4)  2026-08-04  THR-1b: `ThresholdSource` gains
#   EVALUATION_SWEEP, and the vocabulary gains the gate that should already have
#   existed.
#
#   THE ADDITION IS ONE LINE. THE GATE IS THE SUBSTANCE. Measured in this
#   installer's own preconditions, before the change: across the whole test
#   suite, `for member in` appeared ZERO times in the threshold tests,
#   `ThresholdSource}` ZERO times, `_EXPECTED_THRESHOLD` ZERO times. FIVE tests
#   referenced `ThresholdSource` and NOT ONE ENUMERATED ITS MEMBERS -- they used
#   it as a VALUE.
#
#   So a member could have appeared, disappeared, or quietly changed its
#   serialised string, and nothing in 4,171 tests would have objected. That is
#   the REG-2 shape one layer down: there, a semantic correction to two metrics
#   changed no test outcome anywhere, and the repair was the assertions that
#   would notice it next time.
#
#   THE RENAME CASE IS WHY IT MATTERS.
#   `test_registry_vocabulary_completion.py:832` asserts
#   `mcc_print["threshold"] == (0.5, ">=", "fixed_default")` -- the descriptor
#   fingerprint EMBEDS the serialised source string in a tuple compared by
#   equality. A renamed value would silently orphan every historical record
#   carrying the old one, and nothing gated that until now.
#
#   THE GATE RUNS IN BOTH DIRECTIONS, on the pattern of
#   `test_conformal_package_exports.py`: the member set is asserted EXACTLY, so a
#   stealth addition and a stealth removal both fail, and so does a renamed
#   value. Exercised against all three before shipping, and FALSIFIED against the
#   live module afterwards -- a throwaway `SNEAKY = "sneaky"` gave:
#
#       Left contains 1 more item: {'SNEAKY': 'sneaky'}
#       AssertionError: the ThresholdSource vocabulary changed.
#       1 failed, 9 passed
#
#   THE OTHER NINE PASSED UNCHANGED, which is the load-bearing half: the
#   completeness gate is not entangled with identity, serialisation, ownership or
#   validation. A gate that fails alongside everything else is measuring the
#   change rather than the property.
#
#   COMPATIBILITY IS ASSERTED SEPARATELY FROM THE SET. The set test fails for ANY
#   change; a second test names the three members that predate THR-1b, so a
#   failure says immediately whether an EXISTING value moved -- which
#   reinterprets artifacts -- or a NEW one was added, which does not. All three
#   kept their strings, verified by post-check.
#
#   WHY `EVALUATION_SWEEP` AND NOT `EVALUATION_SELECTED`. A candidate exists
#   BEFORE selection: every point an exact sweep enumerates carries this source,
#   and at most one is ever chosen. "Selected" would make the vocabulary
#   temporally false for all but one candidate. And it keeps three facts apart --
#   SOURCE is where a candidate came from, POLICY is why it was chosen,
#   CERTIFICATION BLOCKERS are why its performance is not independently
#   validated. A field conflating them would leave an artifact unable to
#   distinguish "swept" from "chosen" from "unvalidated".
#
#   THIS RATCHET MEASURED ITS OWN TARGET. Every previous entry carried a
#   hand-written value, and that constant was wrong THREE TIMES TODAY: OP-0
#   predicted 4155 against a collected 4157, REG-2 predicted 48 test functions
#   against 49, and a THR-1a post-check expected 2 where 3 were measured. Each
#   was caught by a gate -- the machinery working -- but a target that is an
#   arithmetic guess is a stale constant waiting to happen. This installer ran
#   `pytest --collect-only` and wrote what it read. The prediction (4175)
#   was kept only as a cross-check, and agreed.
#
#   NO SABOTAGE LINE. The addition changes no existing value and no behaviour;
#   the falsification above is the mutation, with its outcome predicted before it
#   was run.
#
#   ACCEPTANCE: 4175 collected, measured by this installer. Four new tests.
#
#
# 4175 -> 4203 (+28)  2026-08-04  OP-1 STEP 1: the exact threshold sweep.
#   NO SELECTOR, and nothing imports it yet.
#
#   FOUR REGISTER DEFECTS CLOSE TOGETHER, WHICH IS WHY THIS IS A REWRITE AND NOT
#   FOUR PATCHES.
#
#       D1   the thousand-point grid misses achievable thresholds
#       D9   O(k*n): 2.25e12 element operations at 1.5M variants
#       D10  threshold-invariant work recomputed inside the sweep
#       D11  two incompatible notions of which thresholds exist
#
#   One sort with cumulative sums makes all four disappear at once, because THE
#   ACHIEVABLE THRESHOLDS ARE THE UNIQUE SCORE VALUES: there is no grid to miss
#   them, no per-candidate rescan, no loop to hoist invariant work out of, and
#   one sweep serves both selectors.
#
#       n           grid O(1000n)      unique O(k*n)      sort + cumsum
#       1,500,000   1,500,000,000  2,250,000,000,000     30,774,796
#
#   D1 DEMONSTRATED RATHER THAN ASSERTED. On scores spaced at 0.0001 -- finer
#   than the grid's 1/999 step -- the exact sweep finds FIVE distinct operating
#   points and `np.linspace(0, 1, 1000)` reaches TWO. Three of five are
#   unreachable by construction, and a test pins that comparison.
#
#   THE CANONICAL DOMAIN is {(max p, GREATER)} union
#   {(s, GREATER_OR_EQUAL) : s in unique(p)} -- k+1 candidates, NOT both
#   operators at every score. For adjacent distinct values `p > s` and `p >= s'`
#   induce THE SAME PARTITION, so enumerating both would duplicate candidates and
#   make indices representation-dependent.
#
#   The GREATER entry is the EMPTY CANDIDATE. `ThresholdParameters` constrains a
#   threshold to [0, 1], so "flag nothing" cannot be a value above the maximum
#   when the maximum is 1.0; GREATER at the maximum expresses it. THE GRID SWEEP
#   SILENTLY LACKED THIS OPERATING POINT ALTOGETHER.
#
#   CORRECTNESS IS AGREEMENT WITH THE DEFINITION, not with another clever
#   construction. Every candidate on eight cohorts -- all-distinct, one tied
#   pair, all tied, ties at the top, a single row, all-positive, all-negative,
#   and boundary scores of exactly 0.0 and 1.0 -- was compared against a
#   brute-force `(p >= t)` computation. ZERO MISMATCHES.
#
#   NOT A THIRD IMPLEMENTATION. The same construction exists TWICE in
#   `metrics.py`, in `auprc` and `_roc_points`, by different index arithmetic;
#   their raw count sequences were compared across eleven cohorts and found
#   identical (SWEEP-1). This is written to be the one those two can LATER be
#   rebuilt on -- which is why it lives in `thresholds.py` and imports no
#   scikit-learn.
#
#   STORAGE: OWNED IMMUTABLE ARRAYS, LAZY CANDIDATE VIEWS. `sweep[i]` builds a
#   `ThresholdSweepCandidate` on demand and NOTHING IS STORED PER CANDIDATE: a
#   1.5-million-row cohort with distinct scores has 1.5 million achievable
#   thresholds, and materialising that many frozen dataclasses would cost more
#   than the sweep computing them. Measured: bytes scale at EXACTLY 2.00x per
#   doubling (12,525 -> 25,025 -> 50,025).
#
#   The arrays are COPIED and marked read-only, and a test mutates the caller's
#   input afterwards to prove the sweep is unchanged. A sweep is EVIDENCE, and
#   evidence that can change after the fact is not evidence. Slicing is REFUSED
#   with a message pointing at the arrays, because it would materialise one
#   object per candidate -- the cost the array backing exists to avoid.
#
#   COUNTS ONLY, NO RATES. A rate can be undefined while a count never is:
#   `TP + FP = 0` makes the positive predictive value undefined, and reporting it
#   as 0.0 is the D2-D5 defect. REG-2 established on 2026-08-04 that such a state
#   is UNDEFINED rather than INSUFFICIENT_SUPPORT; step 2's typed outcome refuses
#   accordingly. `ConfusionCounts` REFUSES a float stored as a count, which is
#   how that defect begins.
#
#   EVERY REFUSAL RAISES rather than filtering and continuing: a non-finite score
#   or label, an empty cohort, scores outside [0, 1], mismatched lengths, a label
#   that is not 0 or 1, and a population whose size disagrees with the arrays.
#   `p >= t` evaluates FALSE for a NaN, and `evaluator.py` records that letting
#   one through moved a measured operating point from sensitivity 0.90 to 0.50
#   with no exception and no warning.
#
#   A DESIGN CORRECTION THE FIRST DRY RUN FORCED. The original append documented
#   an import of `EvaluationPopulation` that DID NOT EXIST -- it duck-typed
#   `population` through `getattr` -- and built a precondition around the
#   transitive scikit-learn risk of an import it never made. The behaviour
#   satisfied the ruling; THE TYPE CONTRACT DID NOT, and an unannotated field in
#   a type whose whole purpose is carrying identity is the weaker half. Now
#   imported and annotated at three sites, and the precautionary precondition
#   became load-bearing: `population.py` imports only __future__, dataclasses,
#   enum, hashlib, logging, numpy and typing.
#
#   GUARD-1, FOUND BEFORE THE WORK RATHER THAN AS A RED SUITE LATER.
#   `test_computation_path_guards.py:241-255` instruments threshold application
#   on the report path and asserts `all(t == (0.5, ">="))` with
#   `len(set(thresholds)) == 1`. The exact sweep applies EVERY unique score, and
#   its empty candidate uses GREATER. That guard does not fire now because step 1
#   WIRES NOTHING -- but step 6's cutover must either scope it to the legacy path
#   or extend it to accept swept candidates DELIBERATELY.
#
#   That is EXTRACT-1 paying for itself one commit after being named: the
#   pre-work inventory found a real constraint instead of a postmortem finding
#   it. The inventory also confirmed zero name collisions, no structural
#   assertion on `thresholds.py`, and 251 test modules in tests/unit -- so a
#   separate test file is the convention, and a sweep is NOT vocabulary.
#
#   NO SABOTAGE LINE. Nothing existing changed, and the correctness claim is
#   AGREEMENT WITH THE DEFINITION across eight cohorts and every candidate, which
#   is stronger than any mutation devisable for a new pure function.
#
#   19 TEST FUNCTIONS, 28 CASES. The difference is parametrisation: the
#   brute-force comparison runs over eight cohorts (+7) and the non-finite
#   refusal over nan, inf and -inf (+2). That arithmetic is the kind this
#   sequence has got wrong three times today, so THIS RATCHET MEASURED ITS OWN
#   TARGET -- it ran `pytest --collect-only` and wrote what it read, keeping the
#   prediction (4203) only as a cross-check.
#
#   ACCEPTANCE: full suite 4197 passed, 6 skipped, 0 failed in 16m07s;
#   4203 collected. NOTHING ELSE MOVED, because nothing imports the sweep.
#
#
# 4203 -> 4225 (+22)  2026-08-05  OP-1 STEP 2: the typed
#   operating-point outcome. STILL NO SELECTOR, and nothing imports it.
#
#   THE TWELVE-DEFECT REGISTER OF 2026-08-01 NOW HAS ONE ENTRY OPEN.
#
#       D7, D8       closed by OP-0 (d4b4259)
#       D1, D9, D10, D11   closed by step 1 (0030544)
#       D2-D5, D6    closed HERE
#       D12          remains -- the undeclared tie-break, which closes in
#                    step 4 when the selector declares and persists its rule
#
#   D2-D5 AND D6 CLOSE BY CONSTRUCTION, NOT BY CONVENTION. `MetricResult`
#   enforces in `__post_init__`, and its docstring states, that a non-OK status
#   REQUIRES a nonempty reason and CARRIES NaN, while an OK status carries a
#   finite value and no reason. So there is NO WAY to store a fabricated 0.0
#   where a refusal belongs -- the constructor refuses it, however the code is
#   later edited. That is a structural guarantee rather than a careful branch.
#
#   D5 IS BROKEN STRUCTURALLY. The legacy form computed F1 from a positive
#   predictive value that might itself be a fabricated 0.0 -- a second
#   fabrication derived from the first, with no record that either occurred.
#   2TP/(2TP+FP+FN) is the same quantity computed from COUNTS and cannot inherit
#   anything: measured on a cohort with nothing flagged, the positive predictive
#   value REFUSES while F1 is ok at 0.0, and a test pins exactly that.
#
#   D6 CLOSES BECAUSE NOTHING IS ROUNDED AT STORAGE. The legacy selectors stored
#   `round(value, 4)` and computed F1 from UNROUNDED inputs, so a stored F1 could
#   not be recomputed from the stored values it was supposedly derived from.
#   Measured here: stored 0.7551020408163265, recomputed 0.7551020408163266 --
#   agreeing to 1e-12, differing only in the last bit by floating-point
#   associativity. `round_for_display` returns a NEW mapping and leaves the
#   record alone, because rounding is a presentation concern and stays one.
#
#   THE REASONS AND STATUSES ARE THE REGISTRY'S, DELIBERATELY. Step 3's Oracle C1
#   exists to prove this count path reproduces the registry's status semantics,
#   and it can only do that if the two speak ONE vocabulary. Every reason string
#   here is one the registry already emits -- empty_predicted_positive_set,
#   empty_predicted_negative_set, positive_class_support_required,
#   negative_class_support_required, zero_f1_denominator, zero_confusion_margin
#   -- and the statuses follow REG-2 (afa7a90): a vanishing DENOMINATOR is
#   UNDEFINED, an absent reference CLASS is INSUFFICIENT_SUPPORT. A private
#   vocabulary would make the oracle compare two dialects and report every
#   difference as a defect.
#
#   PREVALENCE IS ABSENT, AND A TEST ASSERTS ITS ABSENCE. Decision 3 (2026-08-04):
#   it is a POPULATION statistic, not a threshold-derived one, and its canonical
#   value comes from the registry. Computing a second one here would invent two
#   prevalences that agree until a population bug makes them diverge.
#
#   POPULATION IDENTITY IS CARRIED ONCE, ON THE OUTCOME, in the
#   `MetricMetadataKey` vocabulary -- not pushed into every per-quantity result.
#   `MetricResult`'s own source records why it stays generic: 35 of its 53
#   construction sites are embedding-space probes for which population scope has
#   no epidemiological meaning, and forcing the field there "would make the
#   contract ceremonial exactly where it cannot be checked". Re-opening that
#   decision from here would have been the wrong layer to overrule it from.
#
#   `_is_finite` IS IMPORTED, NOT REIMPLEMENTED. Importing an underscore-prefixed
#   name across modules is a smell; the alternative is a SECOND implementation of
#   the exact predicate whose agreement governs serialisation, which is the
#   SWEEP-1 shape documented one commit earlier -- two correct implementations of
#   one thing, agreeing until they do not. The duplication is the worse of the
#   two, and the choice is recorded rather than left to look careless.
#
#   FOUR READS BEFORE A LINE WAS WRITTEN, AND EACH CHANGED THE DESIGN.
#     * The layering: `capabilities.py` and `population.py` import only stdlib
#       and NumPy, so importing MetricResult CANNOT close a cycle and the typed
#       outcome may sit beside the sweep. Had capabilities imported thresholds,
#       the outcome would have needed plain fields instead.
#     * MetricResult's enforced invariants: D2-D5 close BY CONSTRUCTION rather
#       than by branches I would otherwise have written carefully.
#     * MetricResult's generic-by-decision comment: population identity belongs
#       on the OUTCOME.
#     * `_is_finite` existing: import rather than duplicate.
#
#   That is EXTRACT-1 applied for a SECOND consecutive step, and this time it
#   CHANGED a design decision rather than confirming safety.
#
#   A TENTH PROSE-MATCHING POST-CHECK, RECORDED AS RECURRENCE. Three expectations
#   in this installer were wrong: `MetricResult.not_ok` counted 3 where 2 calls
#   exist (a docstring names it), `round(` counted 2 where 1 call exists (a
#   comment describes the legacy defect), and `prevalence` counted 1 where I
#   expected 2. EVERY EXPECTATION WAS RIGHT ABOUT THE CODE AND WRONG ABOUT THE
#   TEXT. The `code_only` tokenising helper existed in earlier installers and
#   this one did not carry it -- that is the recurrence, not the arithmetic.
#
#   NO SABOTAGE LINE. Nothing existing changed, and the guarantees are
#   CONSTRUCTOR-ENFORCED: a mutation that removed a careful branch would still be
#   refused by `MetricResult.__post_init__`, which is a stronger statement than
#   any mutation score.
#
#   ACCEPTANCE: full suite 4219 passed, 6 skipped, 0 failed in 17m07s;
#   4225 collected, measured by this installer (predicted 4225, and
#   they agree -- 22 tests, none parametrised, so functions and cases coincide).
#   NOTHING ELSE MOVED.
#
#
# 4225 -> 4271 (+46)  2026-08-05  OP-1 STEP 3a: ORACLE C1. A VERIFICATION
#   COMMIT -- no production code changed, and `git status` showed exactly one
#   untracked file. The entire change is evidence.
#
#   ORACLE C1 HOLDS. For the SIX estimands both paths compute --  sensitivity,
#   specificity, positive and negative predictive value, F1 and the Matthews
#   correlation coefficient -- `metrics_from_counts` and `registry.compute` agree
#   on STATUS, REASON and VALUE across six applicability regimes at the
#   registry's canonical threshold.
#
#   THAT VALIDATES A DECISION MADE DELIBERATELY IN STEP 2. Every reason string in
#   the count path is one the registry already emits, and the statuses follow
#   REG-2's measured boundary. Had step 2 coined a private vocabulary, C1 would
#   now report six failures per fixture AND NONE OF THEM WOULD BE DEFECTS -- the
#   oracle would be comparing two dialects. The vocabulary was chosen so it could
#   be tested; it has been.
#
#   WHY ONE THRESHOLD SERVES THE WHOLE COMPARISON. Measured against the live
#   registry: of 24 descriptors, NINE carry a `ThresholdParameters` and all nine
#   carry `(0.5, GREATER_OR_EQUAL, fixed_default)`. The other fifteen are ranking
#   or calibration metrics with no threshold. A descriptor pinned elsewhere would
#   need its own fixture family, and a test re-derives that declaration over the
#   live graph so a new one cannot arrive unnoticed.
#
#   THE FIRST RUN FAILED, AND THE DEFECT WAS MINE: 7 failed, 44 passed.
#
#       AssertionError: the Oracle C fixture must expose exactly one (0.5, >=)
#       candidate, found 0.
#
#   TWO REQUIREMENTS ACT ON EVERY C1 FIXTURE AND I SATISFIED ONLY ONE. The
#   comparison needs a candidate at the registry's parameters, so the cohort must
#   contain a score of EXACTLY 0.5 -- the sweep's candidates are the observed
#   score values. And the `no_predicted_positives` regime needs NOTHING flagged
#   at that threshold. Under GREATER_OR_EQUAL those are CONTRADICTORY, because
#   0.5 satisfies `p >= 0.5` and is therefore flagged.
#
#   I wrote every score below 0.5 to satisfy the second and removed the score the
#   first requires. The fixture was internally coherent and externally
#   impossible.
#
#   `>=` PLACES THE BOUNDARY SCORE ON THE FLAGGED SIDE. That single fact makes
#   one regime UNEXPRESSIBLE at the canonical threshold and its mirror image
#   trivial: `no_predicted_negatives` needs every score at or above 0.5, and 0.5
#   itself satisfies that. Verified directly -- scores [0.9, 0.5, 0.7, 0.6] clear
#   nothing and flag four.
#
#   AND I HAD READ THE DOCSTRING THAT SAYS SO. `ThresholdOperator` records that
#   ">= and > differ exactly at prob == threshold", quoted repeatedly across this
#   sequence, and I still wrote a fixture violating it. The guard caught what the
#   reading did not.
#
#   THE REGIME IS DROPPED, NOT REPLACED. A cohort could be built where 0.5 is
#   present and the regime differs -- every label negative, say, so TP + FP is
#   nonzero but the positive predictive value is 0 rather than undefined. That
#   would QUIETLY SUBSTITUTE A DIFFERENT TEST AND CALL IT THE SAME ONE. Six
#   regimes are claimed and six are tested.
#
#   THE REFUSAL ITSELF IS NOT LOST. Step 2 already pins it at the count level
#   with UNDEFINED and reason `empty_predicted_positive_set`. What C1 cannot do
#   is CORROBORATE that against the registry -- a limit of the oracle, not a gap
#   in the evidence.
#
#   AND THE IMPOSSIBILITY IS AN ASSERTION, NOT A COMMENT.
#   `test_the_empty_predicted_positive_regime_is_unreachable_at_the_canonical_threshold`
#   proves it directly, and is CONDITIONAL ON THE OPERATOR: if the registry ever
#   adopts GREATER, or a descriptor moves off 0.5, the regime becomes reachable
#   and the test fails -- which is the signal to add it back DELIBERATELY.
#
#   THE SURFACE PARTITION IS CODIFIED IN FOUR DISJOINT SETS, so every omission is
#   a TESTED SCOPE DECISION rather than an accidental gap:
#
#       shared, compared by C1     6 estimands
#       operating-point only       flagged_fraction -- no registry counterpart
#       registry-only, threshold   balanced_accuracy and the two likelihood
#                                  ratios -- derivable, deliberately not
#                                  duplicated
#       registry-only, population  prevalence (Decision 3, 2026-08-04)
#
#   THE THREE DERIVABLE METRICS STAY OUT BECAUSE THE FORMULAS ARE NOT THE
#   DIFFICULT PART. `_requires_interior_specificity` already decides that a
#   positive likelihood ratio with specificity at 1.0 is UNDEFINED with reason
#   `likelihood_ratio_unbounded` -- a scientific policy about an unbounded
#   quantity. Reimplementing the formula would create a second authority for that
#   policy, for the zero-denominator applicability, the status choice, the exact
#   reason string and the metadata. That is the SWEEP-1 shape.
#
#   A COMPLETENESS TEST CLOSES THE PARTITION: a NEW threshold-carrying descriptor
#   must be placed on a surface deliberately -- compared by C1, or excluded with a
#   reason -- or it fails.
#
#   PREVALENCE'S EXCLUSION WAS CONFIRMED BY AN INDEPENDENT ARTIFACT. Decision 3
#   reasoned from first principles that it is a population statistic; the live
#   registry declares it with NO THRESHOLD AT ALL. A decision made by argument,
#   later corroborated by measurement of something that knew nothing about it.
#
#   C2 IS DELIBERATELY NOT IN THIS COMMIT. `metrics_from_counts` builds plain
#   `MetricResult` instances; `registry.compute` ENRICHES and FINALISES them with
#   descriptor identity, support counts, certification metadata and population
#   keys. Full identity is unlikely to hold until both paths share one finaliser,
#   and MAKING C2 PASS BY COPYING REGISTRY METADATA INTO THE COUNT PATH WOULD
#   RECREATE A SECOND IMPLEMENTATION OF THE FINALISATION CONTRACT. Step 3b
#   measures the exact difference set; that measurement is a FINDING, not a fix.
#
#   ONE ILLUSTRATION CORRECTED AGAINST THE LIVE OBJECT. The adopted design's
#   helper reads `sweep.n_candidates`; `ExactThresholdSweep` implements `__len__`
#   and has no such attribute. FOURTH illustration-versus-reality gap in this
#   sequence, after `_two_class_context()`, `EvaluationPopulation.full(n_source=)`
#   by keyword, and an import documented but absent. An adopted design's code
#   sketches specify INTENT, not API.
#
#   NO SABOTAGE LINE. Nothing existing changed, and THE ORACLE'S OWN FIRST RUN
#   WAS THE FALSIFICATION: it failed, the failure was read rather than silenced,
#   and the correction removed an impossible cohort rather than a disagreement.
#
#   ACCEPTANCE: full suite 4265 passed, 6 skipped, 0 failed in 16m31s;
#   4271 collected, measured by this installer (predicted 4271).
#   46 cases from 11 functions: 6 metrics x 6 fixtures parametrised, plus 10.
#   NO PRODUCTION CODE CHANGED.
#
#
# 4271 -> 4278 (+7)  2026-08-05  CERT-1: an OK operating-point outcome
#   requires a population, and an UNATTRIBUTED one blocks certification with a
#   typed, explained blocker.
#
#   A CORRECTION TO SHIPPED CODE. `OperatingPointOutcome` landed in f0db01f two
#   commits earlier with an eligibility rule that CONTRADICTED THE REGISTRY'S:
#
#       registry     if not ctx.population.is_attributed:
#                        return False, "unattributed_population"
#
#       thresholds   return self.is_ok and not self.certification_blockers
#
#   The outcome NEVER CONSULTED THE POPULATION, so an OK outcome with
#   `population=None` and no blockers reported ELIGIBLE -- and a shipped test
#   POSITIVELY ASSERTED that. The registry's rule is older and better argued: "a
#   certified claim asserts something about a NAMED set of rows", and an
#   unattributed population's fingerprint is absent, so comparison with any
#   other returns UNKNOWN rather than SAME or DIFFERENT.
#
#   It also contradicted STEP 2'S OWN RATIONALE. Population identity was placed
#   on the outcome because "n=980 beside n=980 says nothing about WHICH 980". An
#   OK, certifiable outcome with no population at all is that argument abandoned.
#
#   HOW IT WAS FOUND: reading `_certification_eligibility` while inventorying for
#   step 3c. Not by a failing test -- the suite asserted the defect.
#
#   THREE STATES, NOW SEPARATED.
#       population absent        structurally invalid; the constructor REFUSES
#       population unattributed  numerically valid, certification BLOCKED
#       population attributed    eligibility depends on remaining blockers
#
#   THE MIDDLE ROW IS WHY THIS IS NOT SIMPLY A STRONGER BOOLEAN. Adding
#   `and self.population.is_attributed` would return the right answer and leave
#   the SERIALISED ARTIFACT UNABLE TO EXPLAIN WHY -- a reader would see
#   `certification_eligible: false` beside an EMPTY blocker list.
#
#   DECLARED AND DERIVED BLOCKERS STAY SEPARATE. `certification_blockers` is
#   what the caller declared; `effective_certification_blockers` adds what the
#   outcome's own state implies. Inserting the derived one via
#   `object.__setattr__` would make constructor arguments differ from the stored
#   object -- a caller passing `()` and reading back a one-element tuple.
#
#   ONE VOCABULARY: the blocker code is `unattributed_population`, matching the
#   registry's machine-readable reason CHARACTER FOR CHARACTER.
#
#   AND THE GATE THAT SHOULD HAVE EXISTED. CERT-1 added a member to
#   `OperatingPointCertificationBlocker` and found NO COMPLETENESS GATE guarding
#   it -- THR-1b built exactly that for `ThresholdSource` one day earlier. Three
#   gates added: the member set asserted exactly, prose agreement in both
#   directions, and the registry's own source read for the reason string.
#
#   FALSIFIED. Renaming the value to `no_population_identity` gave 4 FAILED,
#   25 passed. TWO MORE THAN PREDICTED, and the extras are correct: CERT-1's own
#   earlier tests already assert the string, from the serialised artifact and
#   directly. FOUR INDEPENDENT STATEMENTS OF ONE FACT, each from a different
#   angle, and a rename should break all four.
#
#   THE PROSE GATE CORRECTLY DID NOT FIRE -- the member still exists and still
#   has prose, so its subject is untouched by a value change. That is the T3
#   property: a gate failing alongside everything else measures the change
#   rather than the property.
#
#   TWO DEFECTS IN MY OWN INSTALLERS, FOUND BY REVIEW RATHER THAN BY A GATE.
#
#     * DEAD CODE. The CERT-1 installer defined `VOCAB_OLD`, `VOCAB_NEW` and
#       `VOCAB_TESTS`, checked the file existed, and NEVER READ, PATCHED, WROTE
#       OR MANIFESTED IT. Its docstring claimed "two edits to the test files";
#       one test file was edited. The discussion of updating a vocabulary gate
#       described work the installer never did -- and the gate it should have
#       built is the one added above.
#
#     * A CHECK WEAKER THAN ITS CLAIM. The fixture installer computed an AST dump
#       for every assertion and then compared only their COUNT. I described it as
#       establishing that "the repair cannot quietly alter what any test claims".
#       IT DID NOT: every assertion could be rewritten and the count would hold.
#       It also missed `pytest.raises` context managers entirely.
#
#       The claim was verified afterwards by `git diff`: ZERO removed assertion
#       lines, every hit an addition from the new tests. SO IT WAS TRUE BY LUCK
#       RATHER THAN BY CHECK, and the distinction belongs in the record.
#
#   FIVE MISCOUNTS OF MY OWN CODE, all caught by gates, three of which REFUSED A
#   WRITE. Every one was an ESTIMATE of code I had written rather than a
#   MEASUREMENT of it. The last remedy generalises: the final post-check derives
#   its expectation from the measured before-count -- `before - 3` -- rather than
#   hardcoding a number, and was verified across nine cases. THAT IS THE SAME
#   PRINCIPLE the finaliser design adopted: derive from what is there, do not
#   enumerate what you believe is there.
#
#   NO SABOTAGE LINE BEYOND THE FALSIFICATION ABOVE, which is the mutation and
#   was predicted before it ran -- imperfectly, in a direction that revealed more
#   coverage than expected rather than less.
#
#   ACCEPTANCE: full suite 4272 passed, 6 skipped, 0 failed in 17m20s;
#   4278 collected, measured by this installer (predicted 4278).
#   Assertions 53/53 unchanged through the fixture repair.
#

# 4278 -> 4300 (+22)  2026-08-05  OP-1 STEP 3c: the registry metadata prefix.
#   `MetricContext.support()` now has EXACTLY ONE caller in registry.py, and
#   `compute` has none.
#
#   ELEVEN OF THE TWELVE-DEFECT REGISTER OF 2026-08-01 ARE NOW CLOSED. D12, the
#   undeclared tie-break, remains and closes in step 4 when the selector
#   declares and persists its rule.
#
#   THE FIVE `MetricResult` CONSTRUCTION SITES SHARED EXACTLY TWO THINGS:
#   `{METRIC_NAME: d.name}` and `**ctx.support()`. Everything else differs by
#   branch. This is NOT "one finaliser both paths call" -- a phrase used in
#   earlier sessions that does not survive reading the sites.
#
#   THE SEAM WAS LATENT, NOT LIVE, AND THE DISTINCTION IS RECORDED RATHER THAN
#   BLURRED. `compute` called `support()` TWICE on each verdict-bearing path:
#   once for the guard's protected set, once for the attached metadata. Measured
#   2026-08-05, the twenty-seven lines between the OK-path snapshot at 1647 and
#   its guard at 1675 (that is, 1648-1674) are ENTIRELY COMMENT, and
#   `MetricContext` is a frozen dataclass, so the two snapshots cannot diverge
#   today. On the REFUSAL path `verdict.metadata` merges LAST, so a key the
#   first snapshot failed to protect would OVERWRITE a registry-owned key
#   attached from the second -- the forgery REG-1 exists to prevent, reachable
#   through a hole REG-1 did not close. The ruling is therefore prophylactic,
#   and says so.
#
#   OPTION F-strict, AFTER FIVE WERE COSTED AND FOUR REJECTED. A leaves the seam
#   and is dominated. B hoists the snapshot into `compute`, which makes the
#   helper take `support` -- wrong for the step-5 caller that holds a descriptor
#   and a context and would then have to call `support()` itself, reinstating
#   the second authority. C returns the mapping AND its keys, a second value
#   derivable from the first and discarded at three of five sites. D has the
#   guards derive from the returned mapping: correct, but it MOVES branch fields
#   ACROSS the support expansion at four of five sites. F keeps them where they
#   are by carrying them in `pre_support`.
#
#   INSERTION ORDER IS NOT COSMETIC HERE, WHICH IS WHY D WAS NOT ENOUGH.
#   `MetricMetadataKey` is a `(str, Enum)` mixin, so `hash(member) ==
#   hash(member.value)` and a plain string and its enum member are THE SAME
#   dictionary key. Confirmed three independent ways: the enum's own docstring
#   (2026-07-27), a Python 3.12 replica, and the live class. A plain-string
#   branch field can therefore collide with an enum support key, and insertion
#   order decides which value survives.
#
#   AN EXTRAS-ACCEPTING HELPER USING `**kwargs` CANNOT EXIST.
#   `MetricMetadataKey.CERTIFICATION_ELIGIBLE` is an enum member and cannot be a
#   keyword-argument name, so the OK site is unreachable through that shape.
#   `pre_support` is a Mapping for that reason, not by preference.
#
#   PRESERVED BYTE FOR BYTE, PROVEN BY EXECUTION BEFORE THE INSTALLER WAS
#   WRITTEN. A replica of all five literals and both protected-set expressions,
#   run on a clustered AND an unclustered context, asserted `list(old) ==
#   list(new)` -- key ORDER, not merely equality -- at every site; both
#   protected sets set-identical; `support()` called exactly once; the verdict
#   still winning `N_CLASSES_OBSERVED` on refusal; the certification blocker
#   still last on the OK path.
#
#   REJECTED, NOT SHADOWED. The helper refuses a caller-supplied key that
#   collides with the metric name or with the live snapshot. Merge order would
#   also prevent the overwrite, but silently -- the reasoning
#   `_reject_registry_owned_keys` records for descriptor metadata, applied here
#   to the registry's own call sites. No current site can trip it; it exists for
#   the next one.
#
#   A PRE-EXISTING STRUCTURAL GATE DETECTED THE SOURCE-SHAPE CHANGE, AND WAS
#   REWRITTEN RATHER THAN DELETED OR RELAXED.
#   `test_refusal_protected_keys_are_derived_from_ctx_support`, added 2026-08-03
#   after REG-1 mutation M06 went undetected, asserted that a literal
#   `ctx.support()` call appears INSIDE the refusal guard's protected-set
#   expression. Its protected property survived step 3c intact; its INSTRUMENT
#   recognised only one spelling of it. It is not a false positive, not harmless
#   staleness and not mere brittleness, and none of those labels is used,
#   because each erases the distinction between a valid property and an obsolete
#   instrument.
#
#   THE REPLACEMENT PROVES THE DERIVATION INSTEAD OF RECOGNISING A SPELLING: the
#   guard's protected set READS a local; that local's NEAREST PRECEDING
#   assignment calls the prefix; the prefix takes exactly ONE snapshot;
#   `compute` takes none. NEAREST is load-bearing -- the OK path rebinds `meta =
#   {**dict(verdict.metadata), **meta}` after its guard, so a check asking
#   merely whether `meta` is EVER assigned from the prefix would stay green if
#   the guard moved below that rebinding and began protecting a mapping that
#   already carried descriptor keys.
#
#   IT NOW COVERS BOTH GUARDED BRANCHES. Before step 3c the two derived
#   independently and only the refusal branch was pinned; after step 3c they
#   share one authority, so a gate covering one would certify half the
#   architecture. The pre-step-3c direct form is DELIBERATELY NOT accepted:
#   permitting it would allow a regression to two support authorities while
#   leaving the test green.
#
#   SABOTAGE ON THE GATE: 8 mutations, 8 detected, 0 undetected, with the
#   correct shape accepted first. The two that matter are the two a
#   name-recognising gate would be fooled by -- `base` assigned from an ordinary
#   dict literal, and the OK guard moved below the `meta` rebinding.
#
#   C2-1 IS NOW OBSERVED. The handoff recorded as permitted-but-never-measured
#   whether any metric is applicable on a single-class cohort while carrying
#   `reference_class_support`. Enumerating the live catalogue rather than naming
#   five metrics found `integrated_calibration_index` doing exactly that. The
#   five-name parametrised test it replaced never touched it.
#
#   DIAG-1 RECORDED, NOT FIXED. That descriptor is applicable, its kernel
#   returns nan, and `compute` takes the non-finite branch -- which does not
#   merge `verdict.metadata` at all. Neither does the exception branch. So a
#   FAILED result reports what the kernel returned but not the cohort fact that
#   explains it. PRE-EXISTING and unchanged by step 3c; altering site 3 or 4 is
#   a production change outside this step's scope. The absence is ASSERTED in
#   the test, so resolving DIAG-1 must update it deliberately rather than let it
#   pass by accident.
#
#   ZERO NEW SKIPS. A first version of the diagnostics test named five metrics
#   and SKIPPED the two that attach nothing on the cohort. Naming carriers is
#   guessing and a skip is not coverage: it reports success while asserting
#   nothing. Replaced by one loop over all 24 registered descriptors with its
#   non-vacuity assertion folded inside it, and `_missing_inputs` asserted empty
#   for every descriptor so a future one requiring a new input FAILS BY NAME
#   instead of dropping silently out of coverage. A second latent skip, on a
#   deterministic certification condition, became an assertion.
#
#   FOLLOW-UPS RECORDED, NONE TOUCHED. SUPPORT-1: `support()` depends only on
#   `ctx` yet is rebuilt once per descriptor inside `compute`, so a full
#   evaluation reconstructs one invariant mapping 24 times; `n_clusters` is an
#   uncached property computing `np.unique`, so that is O(n log n) per rebuild.
#   MERGE-1: the opposite merge orders are DERIVABLE, not accidental --
#   load-bearing on refusal where `N_CLASSES_OBSERVED` is descriptor-owned, and
#   unobservable on the OK path where a passing guard makes the two mappings
#   disjoint. JSONKEY-1: mixed enum and plain-string keys in serialised
#   metadata. RENDER-1: `str()` of a `(str, Enum)` member yields
#   `MetricMetadataKey.X`, not `x`, so one key renders two ways in diagnostics
#   -- enum machinery, NOT authored in capabilities.py, which is where a reader
#   would otherwise look. TYPING-1: `Callable`, `Mapping` and `Sequence`
#   imported from `typing` are deprecated aliases. DIAG-1 above.
#
#   THIRTEEN DEFECTS OF THE AUTHOR'S IN ONE SESSION, ALL CAUGHT BEFORE ANY
#   COMMIT, AND THE PATTERN MATTERS MORE THAN THE COUNT. Four were caught by
#   gates that REFUSED A WRITE: a repository-wide `*_manifest.json` sweep that
#   rejected three COMMITTED project artifacts; a guard-body statement count
#   asserted at 2 where the body has 3, which refused a patch that changed
#   nothing in the function it guarded; a fatal `\U` escape in a non-raw
#   docstring containing a Windows path -- the same class as the invalid escape
#   this file records at line 602; and a stale-name check written as a raw
#   substring search, which fired on the replacement's own docstring quoting the
#   name it replaced.
#
#   THE TENTH IS THE ONE WORTH THE SPACE. A stand-in file written to verify the
#   test patch IMPORTED `ast` at module level; the real
#   `test_metric_registry.py` does not, and imports it locally inside the bodies
#   that need it. The fixture was chosen to agree with its author, the patch was
#   reported verified, and it failed with NameError on the first real run. Not a
#   wrong belief encoded in a check -- a check that only looked where the author
#   had already looked. The repair removes the dependency entirely by nesting
#   the helpers, rather than adding an import to a module never read.
#
#   FIVE MORE, AND THE LAST TWO ARE THE SAME DISEASE AS THE TENTH. A diagnostics
#   test asserting a contract that never existed, since verdict metadata reaches
#   only two of five construction sites. A ratchet-entry format derived from the
#   MAXIMUM line width of the preceding entry, inheriting a single 92-character
#   outlier as the width for a whole new entry. A placeholder duration inside a
#   runnable command block, pasted verbatim -- the third time in this session
#   that a wrong value inside a pasteable block was pasted, with the instruction
#   to substitute it printed only in prose beneath; that class is closed by
#   measurement, this installer PARSING the suite output rather than accepting
#   numbers by argument. A LINE REFERENCE IN AN EARLIER DRAFT OF THIS ENTRY,
#   asserted from memory, AND THEN A SEARCH THAT CONFIRMED THE WRONG LINE. The
#   draft cited a prior invalid-escape defect at "line 1966" of this file, when
#   no part of these 5,501 lines had been read in the session except the last
#   entry and the final 1,200 characters. The search written to verify it used
#   needles including "download_finngen", which occurs at line 594 inside an
#   ENUMERATION OF SCRIPT NAMES -- so the check reported success on a line that
#   records nothing, and would have licensed a false sentence. A loose needle
#   matches prose: the lesson already held here as "search for the guard, not
#   its message", reproduced by a search written to prevent exactly it. Narrowed
#   to diagnostic PHRASES, the third attempt found the genuine record and it is
#   the one cited above. The recollection was RIGHT ABOUT THE DEFECT and WRONG
#   ABOUT THE LOCATION, the second attempt agreed with the author for the wrong
#   reason, and only the third MEASURED it.
#
#   ARITHMETIC: 4278 + 27 - 5 = 4300. The +27 is
#   tests/unit/test_registry_metadata_prefix.py as first delivered; the -5 is
#   the diagnostics repair. tests/unit/test_metric_registry.py contributes ZERO
#   -- one function replaced by one function. 18 functions, 22 cases.
#
#   ACCEPTANCE: full suite 4294 passed, 6 skipped, 0 failed in 14m36s; 4300
#   collected, measured by this installer (predicted 4300). The suite's stable
#   skip surface is UNCHANGED at 6; the two skips step 3c briefly introduced are
#   gone.
#
#
# 4300 -> 4353 (+53)  2026-08-06  OP-1 STEP 4: the two operating-point
#   selectors. D12 CLOSES, and with it the twelve-defect register opened on
#   2026-08-01 is fully closed. Neither legacy selector is touched and nothing
#   imports these yet; step 5 shadows the two implementations and step 6 wires
#   the replacement.
#
#   BASE 0a3041d -> RESULT 3ddf617. Added in the documentation
#   commit, because a result commit cannot be named by the commit that
#   creates it, and a placeholder in a runnable artifact is the defect
#   this file records at entry line 5659.
#
#   D12 WAS A TRAVERSAL DEPENDENCY, NOT A MISSING FEATURE. `if diff <
#   best_diff` is strict, so the FIRST candidate reaching the minimum wins --
#   and the two legacy selectors traverse OPPOSITE ways.
#   `_find_operating_point` walks `np.linspace(0, 1, 1000)` ascending,
#   permissive to conservative; `_find_high_ppv_point` walks
#   `np.sort(np.unique(p))[::-1]`, conservative to permissive.
#   `sweep_thresholds` runs conservative to permissive, so a selector written
#   naturally against it would have INVERTED the sensitivity-target tie policy
#   while appearing to preserve it, and step 5 would have reported a difference
#   nobody could attribute. The rule is therefore a DECLARED TOTAL ORDER and
#   the winner is the minimum under that key, not the first candidate to reach
#   it. Asserted directly: twenty random permutations plus the reversal, zero
#   divergent.
#
#   EVERY CRITERION NAMED IN THE PERSISTED POLICY CAN ACTUALLY DECIDE
#   SOMETHING. A four-stage draft ended each key with the canonical threshold
#   declaration and sabotage could not detect its removal. Measured over 400
#   random cohorts with duplicate scores forced: on a canonical sweep
#   `n_flagged` is STRICTLY increasing, so "fewer flagged" and "most
#   conservative threshold" induce THE SAME ORDER and the suffix was
#   unreachable. A policy code naming a stage that can never run would
#   overstate what chose the candidate, so the suffix is gone and the canonical
#   order is tested as a property of the SWEEP instead.
#
#   THE UNIQUENESS INVARIANT LIVES ON THE TYPE. The shortened keys are total
#   exactly when each candidate holds a distinct (true positive, false
#   positive) pair, so `ExactThresholdSweep.__init__` now refuses a sweep that
#   does not. A check repeated in every selector is one a future consumer
#   forgets, and the policy would then rest on an unstated producer convention
#   rather than a contract. Without it two identical states leave every key
#   tied, `np.lexsort` is stable, and the winner falls back to ARRAY ORDER --
#   D12 reopening through the back door in the commit that closes it. The
#   invariant rejects nothing the suite already builds: 300 random cohorts
#   through the live `sweep_thresholds`, zero violations.
#
#   TWO REQUIRED CHANGES THE HANDOFF'S CHANGE LIST DID NOT CONTAIN, both found
#   by reading the live module rather than the replica it was drafted against,
#   and both CALL-TIME failures invisible to `--collect-only`:
#     * `thresholds.py` imported only `EvaluationPopulation` from
#       `population.py`, while the selectors reference `PopulationComparison`
#       in a runtime expression. Unfixed, the first successful selection
#       raises NameError.
#     * `OperatingPointOutcome.refused()` took no `selection` argument, and
#       all four refusal paths pass one. Unfixed, every refusal raises
#       TypeError.
#
#   THE VERIFICATION WAS REDONE AGAINST THE REAL CLASS. The payloads were
#   drafted against a replica, and a replica agreeing with its author is the
#   defect that produced the step-3c NameError. The live
#   `OperatingPointOutcome` was reconstructed, the four edits applied to it,
#   and the whole battery re-run: 53 of 53 cases pass and 14 of 14 mutations
#   are detected against the PATCHED PRODUCTION CLASS, not the stand-in. The
#   replica could not have exposed either finding above -- it defines
#   `PopulationComparison` and accepts `selection` in the same file.
#
#   FOUR DEFECTS OF THE AUTHOR'S, ALL CAUGHT BEFORE ANY WRITE, AND ALL ONE
#   FAMILY: a conclusion that agreed with its author for the wrong reason.
#     * An abstract-syntax-tree probe matching only `ast.Name` calls, blind to
#       `T.ExactThresholdSweep(...)`; its empty result was consistent with the
#       claim but did not establish it.
#     * A recommendation to import `Optional`, reasoned from the import block
#       without reading the class that uses it -- which already carries three
#       such annotations and works under PEP 563. Withdrawn; recorded under
#       F821-1 instead of changed here.
#     * A ratchet-shape hypothesis inferred from a byte-count coincidence
#       (368,334 bytes is close to 4,300 node identifiers) rather than from
#       the file. Falsified by counting bare-integer lines: exactly one.
#     * An unquoted forward reference in the new `selection` field, correct
#       only because line 27 carries `from __future__ import annotations`.
#       Caught by EXECUTING the patched class, not by reading it; now quoted,
#       matching `refused()`'s own return annotation.
#
#   ARITHMETIC: 4300 + 53 = 4353. All 53 come from
#   tests/unit/test_operating_point_selection.py -- 27 functions, four
#   parametrised at x3, x20, x3 and x4.
#   tests/unit/test_operating_point_outcome.py contributes ZERO: it gained a
#   vocabulary-gate dictionary entry, not a test function.
#
#   ACCEPTANCE: full suite 4347 passed, 6 skipped, 0 failed in 18m20s; 4353
#   collected, measured by this installer (predicted 4353). The
#   suite's stable skip surface is UNCHANGED at 6. The 33 warnings are
#   pre-existing scikit-learn degenerate-cohort warnings from absence and
#   population tests; the selectors import no scikit-learn.
#
#
# 4353 -> 4417 (+64)  2026-08-07  REGISTRY-1: a model registry that was
#   referenced four times and never written, and the gate that would have
#   caught it the day it was written.
#
#   BASE 5298e90 -> RESULT 372cea1. Added in the documentation
#   commit, because a result commit cannot be named by the commit that
#   creates it, and a placeholder in a runnable artifact is the defect
#   this file records at its own entry for step 3c.
#
#   `ModelRegistry` was imported at continual_trainer.py:127 and :266 and in
#   drift_monitor.yml, and defined NOWHERE. Established three independent
#   ways: direct execution (ImportError), `git log --all -S "class
#   ModelRegistry" -- src/ scripts/` returning EMPTY -- never written, not
#   deleted -- and a 527-name import census in which it was the ONLY
#   unimportable name in the codebase.
#
#   IT SURVIVED BECAUSE BOTH IMPORTS ARE FUNCTION-LOCAL. Module collection
#   never executes them, and continual_trainer.py has no test coverage across
#   410 lines. The adaptive-retraining and model-promotion chain could not
#   run, and nothing said so. `models/registry.json` has never existed, so the
#   workflow guard exits 3 before reaching the ImportError: a correct
#   diagnosis of the symptom that conceals the cause.
#
#   A NEW MODULE, NOT AN ADDITION TO registry.py. `monitoring/registry.py` is
#   a DATA-SOURCE registry -- Category, Check, Source, Verdict, REGISTRY and
#   five accessors. The call sites specified an INTERFACE worth preserving;
#   they did not establish that their module placement was wise. Grafting
#   deployment state onto it would resolve an ImportError by creating a
#   semantic junk drawer, so the imports were corrected instead.
#
#   NO BARE SCALAR. `ModelRecord` has no `auroc` property, not even as a
#   convenience, and a test asserts the absence. A metric detached from its
#   protocol is what made 0.9988 unseen-gene look comparable with 0.9984
#   ordinary test. Callers write `record.evaluation.metrics["auroc"]`, which
#   keeps the number in the same expression as `record.evaluation.protocol`.
#   `promote_to_production` refuses on protocol mismatch, roster mismatch, a
#   non-durable artifact URI, a candidate not in shadow, an absent metric, or
#   a regression beyond tolerance -- each individually tested.
#
#   IDENTITY IS LINEAGE PLUS CONTENT. `ArtifactIdentity.measure` reads the
#   digest from disk; a caller cannot supply one. `record_id` is
#   `f"{run_id}-{sha256[:12]}"`, so a re-export under the same version
#   label is a different record. The roster is ENUMERATED with an
#   order-independent fingerprint, so ROSTER-1 becomes a promotion gate rather
#   than a comment.
#
#   THE REGISTRY LIVES IN A NEW `deployments/` NAMESPACE. `.gitignore:75`
#   ignores `/models/` wholesale, so `models/registry.json` could never be
#   committed and the Continuous Integration check could never find it.
#   `deployments/` is control-plane state: small, reviewable declarations, as
#   distinct from artifacts and from reference data. Its README records what a
#   committed registry can and cannot claim -- structural coherence of a
#   DECLARATION, never the health of a running deployment.
#
#   FIXING THE IMPORT DOES NOT ARM RETRAINING. A missing class is a terrible
#   safety mechanism, and removing it without replacing the boundary would be
#   worse. `AdaptiveRetrainingInputs` turns four measured findings into
#   constructor preconditions: LSIF-1 (the density ratio p_new/p_old is fitted
#   with the SAME rows in two different feature representations, so it has no
#   reference population), ROSTER-1 (`--skip-nn --skip-svm` changes the
#   architecture as well as the data), EVALPROV-1 (the new release's
#   VALIDATION split is registered as `holdout_auroc`), EWCSEL-1
#   (`best_score_` is set nowhere in src/, so the anchor is dictionary order).
#   PIPELINE-1 -- `InferencePipeline` has no `_prepare` -- is named in the
#   refusal too, and is the class of defect an import gate CANNOT see.
#
#   THE GATE WAS OBSERVED FAILING BEFORE IT WAS TRUSTED. The installer ran it
#   against the live tree before any edit and refused unless it reported
#   exactly one unresolved name, ModelRegistry, from two sites; then ran it
#   again after the edits and refused unless it reported zero. Red then green,
#   against the real repository, with the tree never left red.
#
#   THE GATE DOES NOT REIMPLEMENT IMPORT RESOLUTION. A first attempt used
#   `hasattr(module, name)` and reported ELEVEN working submodule imports as
#   broken -- `hasattr(email, "message")` is False while `from email import
#   message` succeeds. It now EXECUTES the statement in a child interpreter
#   and lets Python answer. A child, because importing this package in-process
#   applies the imodelsx Kolmogorov-Arnold Network repair and initialises
#   PyTorch, and a structural check must not reshape the interpreter the rest
#   of the suite runs in.
#
#   DOWNLOADSHADOW-1, FOUND BY THE GATE REFUSING. Its first run against the
#   live tree reported sixteen `AttributeError: module 'catalogue' has no
#   attribute 'create'` failures from inside thinc. The repository was fine:
#   `C:\Users\monzi\Downloads\catalogue.py` shadows the installed
#   `catalogue` distribution, and the installer runs from that folder. The
#   child now takes an explicit import path with the caller's own directory
#   removed, and every path entry is resolved to ABSOLUTE form first, because
#   an empty entry means the PARENT's working directory to the parent and the
#   CHILD's to the child.
#
#   ARITHMETIC: 4353 + 64 = 4417. 46 from
#   tests/unit/test_model_registry.py (40 functions, six parametrised) and 18
#   from tests/unit/test_import_resolution_gate.py (11 functions, three
#   parametrised). tests/unit/test_model_registry.py carries a 16-mutation
#   sabotage matrix result: 16 detected, 0 undetected, run before delivery.
#
#   ACCEPTANCE: full suite 4411 passed, 6 skipped, 0 failed in 16m01s; 4417
#   collected, measured by this installer (predicted 4417). The
#   suite's stable skip surface is UNCHANGED at 6.
#
#
# 4417 -> 4449 (+32)  2026-08-07  PROD-1 COMMIT A: the application
#   programming interface stops publishing constants nobody maintained, and
#   starts reporting what it is actually serving.
#
#   BASE 63e5da0 -> RESULT 4d334f9. Added in the documentation
#   commit, because a result commit cannot be named by the commit that
#   creates it.
#
#   api/main.py carried five provenance constants written in ae1853b on
#   2026-03-25, under a comment reading "update after each training run". They
#   were never updated -- through Runs 9 to 16. `HOLDOUT_AUROC = 0.9847` fused
#   a Run-8 sixty-four-feature figure with 154,404, the validation split size
#   of the Runs 10-14 cohort: two measurements, two eras, one line. The same
#   digits are Run 15's unseen-gene F1, so a reader reconciling them lands on
#   a third quantity. `/info` published all of it regardless of which artifact
#   was loaded, and FOUR OF THE FIVE WERE PINNED BY LITERAL IN THE SUITE -- so
#   the tests defended them, which is why they survived four and a half
#   months.
#
#   NOTHING HAND-MAINTAINED REPLACES THEM. Model identity is derived from the
#   digest of the bytes actually loaded, resolved against the committed
#   deployment declaration. `API_VERSION = "2.0.0"` covers the software
#   contract only; `PIPELINE_VERSION` was retired rather than narrowed,
#   because one symbol was serving as both the OpenAPI version and prediction
#   provenance, and that conflation is why "1.0.0" sat unexamined.
#
#   FOUR ORTHOGONAL AXES, because they answer four different questions:
#   ArtifactResolutionStatus (can these bytes be identified),
#   DeploymentAlignment (are they what the registry DECLARES),
#   RosterAlignment (does the executable roster match, allowing for a declared
#   serving projection), EvaluationApplicabilityStatus (may a metric measured
#   on that record be shown as evidence for THIS artifact). Collapsing any two
#   recreates the drift: a registered SHADOW artifact served by accident must
#   not look healthy merely because its digest resolves.
#
#   COMMIT A PUBLISHES NO METRIC AT ALL, and that is the scientific point.
#   `InferencePipeline.from_variant_ensemble` excludes `cnn_1d`, which needs a
#   FASTA context window unavailable over the REST path, so the deployable
#   artifact is a TWELVE-model projection of a THIRTEEN-model trained
#   ensemble. A metric measured on the record therefore does not
#   automatically describe these bytes. Resolving a digest authorises
#   IDENTITY, not EVIDENCE. A sealed evaluation naming this artifact digest
#   and this served-roster fingerprint is what will eventually authorise
#   publication, and that is Commit C.
#
#   THE PROJECTION IS DECLARED, NOT INFERRED. `ServingProjection` records
#   which models a derived serving artifact omits and why, so an intentional
#   omission is SERVING_SUBSET while an undeclared one is UNKNOWN and anything
#   further missing is INCONSISTENT. Without it a missing CatBoost and a
#   missing `cnn_1d` look identical at runtime, and this project has already
#   lost a model silently once.
#
#   LIVENESS AND READINESS ARE SEPARATED. A process that loaded bytes it
#   cannot identify is ALIVE and NOT READY: an orchestrator should stop
#   routing inference traffic to it rather than restart it. The existing test
#   fixture injects a pipeline object directly, so it constructs exactly that
#   world -- and the suite asserted it was "ok" until today.
#
#   THE ARTIFACT IS MEASURED AROUND THE LOAD, NOT BEFORE OR AFTER IT. A digest
#   taken only before describes bytes that may have been replaced during the
#   load; one taken only after describes bytes that may not be what was
#   deserialised. Two measurements, compared, and the second is reused by
#   registry resolution rather than reading a large artifact a third time.
#
#   TWO DEFECTS THE FIRST INSTALL'S OWN TEST RUN FOUND, both the author's:
#     * `ModelAttributionResponse` referenced in the /info handler and never
#       imported -- NameError on every call to that endpoint. No import check
#       and no collection sees it; it is F821's territory, and F821-1 is open.
#     * `InferencePipeline.save` is `joblib.dump`, and `MagicMock` will not
#       pickle, so the three tests built to drive the real serialisation path
#       never ran at all.
#
#   AND ONE IN THE CHECK WRITTEN TO CATCH THE FIRST. A conservative
#   undefined-name scan collected only the OUTER function's parameters, so a
#   `lambda f:` left `f` looking undefined; it reported that against main.py
#   and refused a correct fix. Its self-test had passed because the author
#   chose three cases and omitted lambdas and nested definitions -- the two
#   commonest binding forms in Python. The proof now runs EIGHT cases,
#   including one that fails if the checker is weakened to make the others
#   pass.
#
#   ARITHMETIC: 4417 + 32 = 4449. All 32 come from
#   tests/unit/test_runtime_attribution.py -- 29 functions, one parametrised
#   x4. tests/unit/test_api.py contributes ZERO: four of its tests were
#   REDIRECTED rather than added, because the world they construct (a pipeline
#   object with no artifact provenance) is a real and useful one, and three
#   were renamed because each claimed more than it checked.
#
#   A 12-MUTATION SABOTAGE MATRIX ran before delivery: 12 detected, 0
#   undetected -- after the matrix found a genuine hole. A test named
#   "a registered shadow artifact is not production" actually constructed "no
#   production declared", so the branch a mutation targeted never executed.
#   The test's NAME claimed more than the test checked.
#
#   ACCEPTANCE: full suite 4443 passed, 6 skipped, 0 failed in 18m34s; 4449
#   collected, measured by this installer (predicted 4449). The
#   suite's stable skip surface is UNCHANGED at 6.
#
#
# 4449 -> 4462 (+13)  2026-08-07  DOCKERCOPY-1: the container image
#   did not contain a module the web service imports, and nothing could have
#   noticed.
#
#   BASE e8de6a6 -> RESULT 2ccad69. Continuous Integration on the
#   result commit reported success on all seven jobs, INCLUDING the
#   Docker build smoke test that had been red for three commits.
#
#   `4d334f9` added api/attribution.py, which imports
#   monitoring.model_registry at module level. The Dockerfile's `api` stage
#   copies api/, models/, utils/ and the package __init__.py -- and NOT
#   monitoring/. Inside the image that import raised ModuleNotFoundError,
#   gunicorn never bound, the container exited, and the smoke step failed
#   sixteen seconds later with curl returning nothing. `4d334f9` and
#   `e8de6a6` were both red on origin/main.
#
#   NOTHING IN THE REPOSITORY COULD HAVE CAUGHT IT. The import-resolution
#   gate runs against the full source tree, where the import resolves
#   perfectly. So does the test suite. Only the IMAGE has the narrower file
#   surface, and the only thing that exercised the image was a smoke step
#   that piped /health into a grep for one field NAME -- not its value, not
#   the contract. It caught this ONLY because the container DIED. A service
#   that started and lied went green, and that step had been the sole
#   verification of the container for the life of the project.
#
#   The author enumerated the consumers of api.main IN PYTHON. The
#   container's file surface is a consumer expressed in a Dockerfile, and no
#   Python search finds it.
#
#   THE REPAIR IS THE GATE, NOT THE MISSING LINE.
#   tests/unit/test_docker_image_covers_the_api.py walks the static import
#   graph from api/main.py and asserts every reachable first-party module
#   lives under a path the api stage copies. Proved red against the
#   Dockerfile as it stood, green with the fix, and RED AGAIN for a near-miss
#   COPY of `monitor/` and for a renamed stage -- the last of those matters,
#   because a lazier parser finds no COPY lines after a rename and passes
#   vacuously.
#
#   ONE FILE, NOT THE DIRECTORY. monitoring/ also holds the drift detector,
#   the ClinVar tracker and the data-source registry, none of which inference
#   touches. It has no __init__.py -- an implicit namespace package -- so no
#   module body executes and the single file suffices. That is what line 94
#   of the Dockerfile already said it wanted: "only what inference needs".
#
#   THE SMOKE STEP NOW TESTS SOMETHING. It asserts the honest contract for an
#   image that ships no artifact: live true, ready false, model_loaded false,
#   status degraded. Asserting "ok" would demand a lie; asserting a field
#   name accepted anything. It dumps `docker logs` on failure, because this
#   outage gave a container identifier and then silence.
#
#   TWO STALE CLAIMS REMOVED FROM SURFACES NOTHING CHECKED. The image LABEL
#   published a validation area-under-the-curve figure into every image ever
#   built, and pointed org.opencontainers.image.source at a repository that
#   is not this one. scripts/run_benchmark.py claimed its results were
#   directly comparable to a published baseline that nothing publishes any
#   more. Neither figure is repeated in the replacement text: BASELINE-1
#   holds the number, because which experiment produced it is unestablished
#   -- the repository cited it under three incompatible descriptions.
#
#   connector_1kgp.py is DELIBERATELY UNTOUCHED. Its citation of the same
#   figure sits in a measured comparison whose arithmetic is internally
#   consistent; rewriting it would falsify a record and deleting it would
#   lose the motivation for the 1000 Genomes Project connector.
#
#   FOUR REFUSALS BEFORE ANYTHING WAS WRITTEN, ALL OF THEM POST-CHECKS OF THE
#   AUTHOR'S RATHER THAN THE REPAIR. Three were the same defect in three
#   files -- a text check satisfied by the comment explaining the removal --
#   and the fourth was the over-correction that followed: broadening
#   `grep -q '"status"'` to bare `grep -q`, which then refused a correct edit
#   because two lockfile steps use the idiom. The repair is precision about
#   WHAT is forbidden, plus a stage-0 self-check running one forbidden list
#   in BOTH directions: the replacements must not reproduce it, and the
#   edited files must not retain it. Proved by poisoning an edit and watching
#   it refuse.
#
#   ARITHMETIC: 4449 + 13 = 4462. All 13 from
#   tests/unit/test_docker_image_covers_the_api.py. Installer one's closing
#   hint predicted 4461, from before the gate gained a thirteenth case when
#   its parser was taught to join Dockerfile line continuations -- correct by
#   luck for one source path, wrong for two.
#
#   ACCEPTANCE: full suite 4456 passed, 6 skipped, 0 failed in 17m29s; 4462
#   collected, measured by this installer. The suite's stable skip surface is
#   UNCHANGED at 6.
#
#
# 4462 -> 4487 (+25)  2026-08-07  GATE-1, REGISTRY-1c, ALIGNMENT-1,
#   PIPELINE-1 and LSIF-1: four AUROC thresholds were never four gates.
#
#   BASE 3378659 -> RESULT b702777, LANDED 2026-08-08.
#   The header above reads 2026-08-07: that is when the work was done
#   and the payloads were built. Three installer refusals and one
#   correction round separated them, and the dates are left as they
#   fell rather than tidied, because a header rewritten to match the
#   commit would erase the gap it took to get there.
#
#   Continuous Integration on the result commit: success on both
#   workflows, including the container smoke test that exercises the
#   rewritten model_registry.py through the web service's import
#   closure, and the Docker coverage gate judging a NEW module.
#
#   GATE-1's original census counted four thresholds and called them four
#   policies. They are THREE CLASSES OF DECISION, and one of them is not a
#   policy at all:
#
#     0.90    score<->label alignment integrity   ScoreLabelAlignmentPolicy
#     0.97    absolute production floor           PromotionPolicy
#     0.002   maximum degradation                 PromotionPolicy
#     0.9842  copied arithmetic over a figure of  DELETED
#             unestablished provenance
#
#   CENSUS CORRECTION: the original inventory MISSED a fifth site.
#   scripts/forensics/verify_oof_alignment.py declared 0.90 independently,
#   with the comment "a correctly-joined base model should be well above
#   chance". That is the same integrity predicate as conformal calibration's,
#   not a new gate. Both now consume one ScoreLabelAlignmentPolicy from
#   evaluation/alignment.py, and the forensic script does NOT import
#   conformal to obtain it -- a general integrity check must not depend on a
#   specific statistical method for a number.
#
#   THE NUMBERS ARE TYPED, NOT JUSTIFIED. 0.97, 0.002 and 0.90 all carry
#   PolicyProvenance reporting LEGACY_PENDING_JUSTIFICATION, so
#   `is_justified` is False and stays false until somebody establishes them.
#   An architecture can be correct while its constants remain inherited, and
#   putting a threshold behind a good type does not validate it. The symbol
#   is a normal policy object rather than a constant named something
#   alarming, because a scary name becomes wallpaper the moment it is
#   imported.
#
#   REGISTRY-1c. The drift workflow imported ModelRegistry from a module that
#   never had it, read a path .gitignore excludes wholesale, and called an
#   attribute the registry deliberately does not provide. Three
#   incompatibilities, masked because the first branch exited before reaching
#   the third. It is now an EXIT-CODE ADAPTER over
#   validate_current_production: 3 not checked, 1 invalid, 0 valid -- the
#   semantics the file's own comments record as hard-won, preserved exactly,
#   but translated in Python where they can be tested. Continuous Integration
#   knows exit codes, not arithmetic.
#
#   THE STEP IS RENAMED to "Validate declared production registry state". A
#   committed file can establish that the DECLARED state satisfies policy; it
#   cannot establish that a running process serves those bytes. Calling it a
#   health check would let the workflow acquire stronger prose than its
#   evidence supports.
#
#   PIPELINE-1: three of four sites were mechanical. `InferencePipeline`
#   exposes `trained_models`, never `base_models`, so lines 404, 407 and 505
#   were API corrections -- 505 written by this author and shipped in
#   372cea1. THE FOURTH WAS NOT MECHANICAL.
#
#   LSIF-1 CLOSES, as a feature-space contract rather than a rename.
#   `current_pipe._prepare(pd.DataFrame(X_train_new))` supplied the NEW cohort
#   as the reference, and the source said so: "placeholder -- ideally pass
#   X_train_old". The defect was known when it was written. Feeding the real
#   reference THROUGH the serving pipeline would have been worse: reference
#   rows in the serving representation against new rows from
#   DataPrepPipeline gives compatible widths and an uninterpretable ratio. So
#   the call is removed, both cohorts must arrive already engineered at the
#   same stage, and COLUMN EQUALITY is checked -- a reordered column preserves
#   the width while permuting every row, and width is all the old code could
#   have checked.
#
#   `ReferenceTrainingFeatures` carries model_record_id, feature_names and an
#   optional population_fingerprint. When the fingerprints match there is no
#   covariate shift, and DensityRatioStatus.SAME_POPULATION yields weights of
#   one BY DECLARED POLICY rather than by fitting an estimator on p/p and
#   reporting its noise as adaptation.
#
#   EWCSEL-1 STAYS OPEN AND NOW FAILS CLOSED. Correcting the attribute name
#   would have turned `getattr(m, "best_score_", 0.0)` from unreachable into
#   silently arbitrary: `best_score_` is set nowhere in src/, so `max` over an
#   all-equal keyspace returns dictionary order. It raises instead.
#   UNRESOLVED_ADAPTIVE_RETRAINING_BLOCKERS names ROSTER-1, EVALPROV-1 and
#   EWCSEL-1 so that closing one blocker cannot open the path.
#
#   MEASURED, NOT ASSUMED: importing the evaluation package costs 749-926 ms
#   against 10-12 ms for model_registry. Placing the alignment policy under
#   evaluation/ therefore costs the conformal path about 0.8 s of one-time
#   import. Tolerable, and now a number in the record rather than a guess.
#
#   SIX AUTHOR DEFECTS BEFORE ANYTHING WAS WRITTEN, every one caught by an
#   installer refusing: a text rule applied where a syntax-tree rule belonged,
#   forbidding the comments that record what was removed; a digest pinned from
#   a scratch directory rather than from the tree; a forbidden string removed
#   from one of the two paragraphs carrying it; and a hand-escaped nested
#   string that produced an unterminated literal, rebuilt with repr() so the
#   quoting is generated rather than written by hand.
#
#   ARITHMETIC: 4462 + 25 = 4487. All 25 from
#   tests/unit/test_alignment_policy.py -- 14 functions, three parametrised.
#   tests/unit/test_model_registry.py contributes ZERO: its 46 cases were
#   REDIRECTED by the field rename, not added.
#
#   ACCEPTANCE: full suite 4481 passed, 6 skipped, 0 failed in 19m21s; 4487
#   collected, measured by this installer. Skip surface UNCHANGED at 6.
#


# 2026-08-11 -- 4487 -> 4605 (+118). Four scientific units, four commits:
#   b9738a4  canonicalisation integrity (AUDITCOUNT-1, STRICTNUMERIC-1,
#            PROVENANCE-ASSERT-1)
#   7161132  gnomAD constraint connector (DUPLICATE-1A, CONSTRAINTTRANSCRIPT-1,
#            CACHEIDENTITY-1/-2)
#   0a7a553  correctness harness (HARNESS-NULL-1)
#   48985d6  missingness contract (CONSTRAINTFILL-1, feature-engineering half)
#
#   WHAT WAS WRONG. A single connector fallback made two model features
#   BIT-IDENTICAL -- gene_constraint_oe and loeuf, identical=True, max abs
#   diff=0.0, correlation=1.0 across 1,038,974 rows. Transcript selection was
#   drop_duplicates(keep="first") under a comment claiming MANE ordering, which
#   disagrees with MANE Select for 5,468 of 17,473 genes (31.3%), 132 of them
#   crossing the 0.35 constrained boundary. Missing constraint was fabricated as
#   1.0 -- "completely tolerant of loss of function" -- at three levels, and a
#   blanket fillna(0.0) then filled whatever survived with MAXIMAL apparent
#   constraint. Stage 5 of the correctness harness computed its zero rate as
#   (s.fillna(0) == 0).mean(), so a column that was 200-of-200 MISSING was
#   reported as "100% zero, probable silent-zero connector".
#
#   Every one of those is the same defect: an ABSENCE rendered as a MEASUREMENT.
#
#   ARITHMETIC: 4487 + 118 = 4605. All 118 from four new files --
#   test_constraint_canonicalize.py 50, test_connector_gnomad_constraint.py 27,
#   test_feature_state.py 21, test_model_preprocessing.py 20. The five
#   superseded no-NaN assertions in test_api.py, test_core.py and
#   test_splice_ai_promotion.py contribute ZERO: they were REWRITTEN in place,
#   not added. An earlier prediction of 4628 double-counted them, and the
#   measured collection corrected it.
#
#   ACCEPTANCE: full suite 4598 passed, 6 skipped, 1 failed in 16m30s; 4605
#   collected. Skip surface UNCHANGED at 6 (five integration TODOs awaiting Run
#   15 cohort infrastructure, one POSIX-symlink platform conditional).
#
#   THE ONE FAILURE IS KNOWN AND RECORDED:
#   test_api.py::TestInferencePipeline::test_save_and_load_roundtrip.
#   api/pipeline.py fits LogisticRegression on a matrix that now legitimately
#   carries NaN. It awaits the preprocessor WIRING, which is deliberately not in
#   these commits. The ratchet counts COLLECTION, not passes, so it moves; the
#   failure is carried openly rather than hidden by deferring the bump.
#
#   EACH COMMIT WAS VERIFIED IN AN ISOLATED GIT WORKTREE containing exactly what
#   that commit contains -- HEAD plus the staged index, nothing from the working
#   tree. That method found ALPHAFOLD-SYSPATH-1: seven tests use
#   sys.path.insert rather than monkeypatch.syspath_prepend, which is a no-op at
#   C:\Projects\genomic-variant-classifier and an error at any other path. They
#   error in every worktree run and pass here. A fresh clone fails those seven.
#

# 2026-08-12 -- 4605 -> 4620 (+15). NEURALNAN-1 made visible.
#
#   variant_ensemble.py:1920 selected model columns with X.var(axis=0) > 0.0.
#   np.var over a column containing ANY NaN returns NaN, and NaN > 0.0 is
#   False, so a zero-variance guard silently reclassified "contains a missing
#   value" as "is degenerate" and DELETED the whole feature -- including the
#   360 observed values it still held at n=400.
#
#   MEASURED 2026-08-10 with a responsiveness control: tabular_nn, mc_dropout
#   and deep_ensemble showed a delta of EXACTLY 0.000000 on a NaN-bearing
#   column at n=8 AND n=400, while responding 0.28-0.99 to columns that were
#   never missing. Three of thirteen estimators trained on a different feature
#   space than the other ten, invisibly.
#
#   THE SAME COLUMNS ARE STILL DROPPED. Verified exhaustively over the five
#   reachable column kinds; old and new masks are IDENTICAL. Constancy is now
#   measured among OBSERVED values, so a column that varies where observed is
#   recorded as a MISSINGNESS loss and logged at WARNING with its name.
#   Imputing instead of dropping changes what these models learn and belongs
#   with the per-estimator rendering, not with a guard.
#
#   ARITHMETIC: 4605 + 15 = 4620. All 15 from tests/unit/test_feature_selection.py.
#   variant_ensemble.py contributes ZERO: its fit() was REDIRECTED to the new
#   selector, not extended.
#
#   ACCEPTANCE: tests/unit 4394 passed, 1 skipped, 0 failed.

# 2026-08-12 -- 4620 -> 4622 (+2). Value coverage for pipeline.py's first
# engineer_features call site, plus its control.
#
#   engineer_features is invoked TWICE in pipeline.py, in two methods that
#   duplicate forty lines and share no _prepare (PIPELINE-PREP-DUP-1). Commit
#   b22b63b applied the fitted missing-value policy at both. Only the second
#   had value coverage: test_save_and_load_roundtrip reaches it through
#   predict_single.
#
#   The first had CONTAINER COVERAGE ONLY. test_catboost.py builds its pipeline
#   from MagicMock estimators, which accept any input and return a canned array
#   -- they verify DataFrame-versus-ndarray dispatch and cannot detect NaN --
#   and its numeric_variant_df fixture populates gene_constraint_oe, so no
#   missing value ever reached an estimator.
#
#   The second test is a CONTROL: it demonstrates the same bare variant
#   genuinely raises ValueError on the same estimator when the policy is
#   absent. A passing assertion on a benign input is not evidence that a policy
#   works, only that nothing needed one.
#
#   ARITHMETIC: 4620 + 2 = 4622. Both from tests/unit/test_api.py.
#   ACCEPTANCE: tests/unit 4396 passed, 1 skipped, 0 failed.

# 2026-08-12 -- 4622 -> 4613 (-9). PHYLOP-SOURCE-OWNERSHIP-1 and PHYLOPTEST-DUP-1.
#
#   THE SUITE SIZE DECREASES, AND THAT IS DE-DUPLICATION, NOT LOSS. Every
#   assertion removed exists in a stronger form in test_phylop_block.py.
#
#   PhyloPConnector.annotate_dataframe wrote out["phylop_score"] = scores
#   unconditionally, replacing a column dbNSFP had populated with 17,706
#   distinct values -- and in stub mode, with no source configured at all,
#   replacing every value with 0.0. phyloP is signed: zero means NEUTRAL
#   EVOLUTION. The connector asserted neutral evolution across the cohort and
#   destroyed the evidence already present.
#
#   The collision was TESTED AS INTENDED, in TWO files.
#   test_core.py carried a byte-level copy of test_phylop_block.py; the
#   2026-05-26 relocation copied rather than moved. The first attempt at this
#   repair failed its gate with thirteen failures from that copy.
#
#   ARITHMETIC: test_phylop_block.py 18 -> 27 (+9); EIGHTEEN duplicated tests
#   in test_core.py removed (-18); the integration test rewritten for staged
#   ownership (0); the 7.2 survival assertion added to
#   test_real_scores_flow_through_to_features (an ASSERTION, not a collected
#   test, 0). Net -9. 4622 - 9 = 4613.
#
#   The first attempt asserted -8 from a miscount of the duplicates. The
#   de-duplication script REFUSED rather than removing what it found, which is
#   why this figure is measured from the class and not carried from a reading.
#
#   ACCEPTANCE: tests/unit 4387 passed, 1 skipped, 0 failed. 10 of 10 sabotage
#   mutations detected in isolated trees.

# 2026-08-12 -- 4613 -> 4641 (+28). PHYLOP-INGEST-INTEGRITY-1.
#
#   _build_index read the flat phyloP source with on_bad_lines="skip", so
#   malformed rows were discarded with no count and no record: the index was
#   smaller than the source and nothing said so. _parquet_to_index -- which
#   serves BOTH a parquet source and the connector's own cache -- carried the
#   same silent dropna. Neither reconciled rows read against rows indexed, and
#   both built a dictionary in which a duplicated locus resolved by FILE ORDER.
#
#   The header check was misdiagnosed twice before being executed. Measured on
#   pandas 3.0.2: without a dtype it fires correctly; with dtype Int64 read_csv
#   raises before it runs; and comment="#" drops a "#chrom" header regardless.
#   The defect is that a file's SHAPE was inferred from its CONTENT, decided by
#   a parsing argument elsewhere in the call. It is now a declared contract,
#   verified against the raw first line.
#
#   ARITHMETIC: 4613 + 28 = 4641. All 28 from the new
#   tests/unit/test_phylop_ingest.py -- 21 unit tests for strict ingestion and
#   7 exercising the connector's two ingest paths end to end. phylop.py
#   contributes ZERO: both methods were REPLACED, not extended.
#
#   ACCEPTANCE: tests/unit 4415 passed, 1 skipped, 0 failed.

# 2026-08-12 -- 4641 -> 4666 (+25). PHYLOP-CACHE-INTEGRITY-1.
#
#   The PhyloP index cache carried a filename and nothing else: no schema
#   version and no source digest. A cache built from one source could be served
#   for another and nothing in the artefact would contradict it -- the same
#   failure as CACHEIDENTITY-1 in the gnomAD constraint connector, where
#   identity came from the source FILENAME.
#
#   The read sat inside a bare try/except that collapsed three situations into
#   one outcome: no cache (normal), a cache describing a different source
#   (normal, but must be recorded), and a cache CLAIMING to describe this
#   source that cannot be read (a fault that rebuilding hides and that recurs
#   every run). The third now raises.
#
#   Identity is the schema version and the source DIGEST, never the path: the
#   same source moved is the same source, and a different source at the same
#   path is not.
#
#   ARITHMETIC: 4641 + 25 = 4666. All 25 from the new
#   tests/unit/test_phylop_cache.py -- 20 for cache identity and 5 exercising
#   the connector's round trip. phylop.py contributes zero, both methods having
#   been REPLACED rather than extended.
#
#   ACCEPTANCE: tests/unit 4440 passed, 1 skipped, 0 failed.

# 2026-08-12 -- 4666 -> 4685 (+19). PHYLOPPERF-1.
#
#   A1 declared DictPhyloPBackend transitional and recorded the substrate in a
#   constant so a temporary structure could not become permanent by inertia.
#   This is the commit that constant was written for: _lookup_backend is the
#   single seam between the connector and its lookup engine, so one method
#   changes and no contract from A1, A2 or A3 is reopened.
#
#   MEASURED, 200,000 queries against a 500,000-locus index: 0.387 s to
#   0.165 s, a 2.4x speedup with IDENTICAL results. An earlier draft implied an
#   order of magnitude; the first measurement was 1.8x, and profiling showed
#   .map(self._normalise) was 53.5% of the cost -- vectorising the lookup while
#   leaving normalisation per-row had moved the bottleneck, not removed it.
#
#   The equivalence verifier caught a real bug on its first run: the per-row
#   authority tests membership in ("X", "Y", "MT") CASE-SENSITIVELY, so
#   lowercase "y" is returned unchanged, while the first vectorised form
#   upper-cased it. A one-character difference in chromosome identity,
#   introduced by an optimisation. The two now agree on all 210 chromosome
#   tokens.
#
#   ARITHMETIC: 4666 + 19 = 4685. All 19 from the new
#   tests/unit/test_phylop_lookup.py. phylop.py contributes zero: one method
#   was REPLACED.
#
#   ACCEPTANCE: tests/unit 4459 passed, 1 skipped, 0 failed.

# 2026-08-12 -- 4685 -> 4715 (+30). PHYLOP-QUERY-INTEGRITY-1.
#
#   _query_bigwig carried two defects. `fillna=0.0` substituted at the LIBRARY
#   BOUNDARY, so the isnan guard on the next line could never fire: an
#   uncovered position returned 0.0, indistinguishable from a measured zero.
#   pyBigWig never had that behaviour, and _open_bigwig tries pybigtools first,
#   so the defective path is the one that ran. And every exception became the
#   sentinel, logged at DEBUG -- below the default threshold -- so a corrupt
#   asset and a neutral genome produced identical output.
#
#   MEASURED on a 659-byte bigWig written with pyBigWig and queried with both
#   libraries: an uncovered position gives 0.0 under fillna=0.0 and nan
#   without it, while 1:500 is a GENUINE zero giving 0.0 either way. pybigtools
#   0.3.0 emits a DeprecationWarning saying it CHANGED this default because it
#   was wrong; the repository restored the defective behaviour explicitly.
#
#   ARITHMETIC: 4685 + 30 = 4715. All 30 from the new test file. phylop.py
#   contributes zero: one method REPLACED and one call site updated.
#
#   ACCEPTANCE: tests/unit 4489 passed / 1 skipped with both bigWig libraries
#   installed, or 4480 / 10 without them -- nine tests are guarded by
#   _needs_libs. The installer MEASURES which, rather than assuming the
#   environment.

# 2026-08-13 -- 4715 -> 4807 (+92). DEPENDENCY-GOVERNANCE-1.
#
#   Three analyzers under one vocabulary: deps/model.py owns canonical
#   identities and the capability/deployment-profile scope model;
#   requirements_parse.py and dependency_census.py measure against it.
#
#   The instrument failure that started it: a parser reported a 310,494-byte,
#   180-package hash-pinned lock as ZERO packages, because it split lines
#   before joining continuations and swallowed every resulting failure. That
#   reading nearly supported deleting the artifact.
#
#   MEASURED: a naive .lower() disagrees with packaging.utils.canonicalize_name
#   on six of ten sampled distribution names; the hyphen-to-underscore
#   distribution-to-module guess disagrees with installed metadata on four of
#   seven, including pyBigWig -- this project's own dependency, whose module
#   keeps its capitals while Python imports are case-sensitive.
#
#   ARITHMETIC: 4715 + 92 = 4807. 31 + 28 + 33 collected cases, zero skipif
#   decorators, no third-party import beyond pytest -- so none can skip.
#
#   ACCEPTANCE: tests/unit 4572 passed, 10 skipped, 0 failed.

# 2026-08-13 -- 4807 -> 4840 (+33). DEPENDENCY-ONTOLOGY-1.
#
#   deps/governance.py records six package classifications measured by an
#   import census over src, scripts and tests: 941 files walked, 941 parsed,
#   zero failures. NOT ONE was correctly scoped by the file it sits in.
#
#   seaborn and jinja2 are REPORTING dependencies -- unguarded imports in
#   report_generator.py with sns.set_style() at module scope -- declared
#   development-only and absent from requirements-api.lock while matplotlib,
#   two lines above them, is present. pyfaidx is REFERENCE_SEQUENCE with 25
#   imports across 23 scripts. pre-commit is DEV_TOOLING, and its zero imports
#   are the CORRECT result for a console script. httpx and anyio remain
#   UNRESOLVED: reached only through fastapi.testclient, and whether their
#   versions are part of the tested surface has not been measured.
#
#   Scope is two axes. "seaborn = development" and "seaborn = runtime" are both
#   false; it is a REPORTING dependency absent from the API profile, which
#   `python -X importtime -c "import api.main"` established directly.
#
#   ARITHMETIC: 4807 + 33 = 4840. Thirty plain cases plus three parametrized
#   over the six CLASSIFICATIONS, so the count is a function of the data;
#   verified against the parse tree and pytest, both 33.
#
#   ACCEPTANCE: tests/unit 4605 passed, 10 skipped, 0 failed.

# 2026-08-13 -- 4840 -> 4845 (+5). REQFILES-NONASCII-1, PYTEST-ANYIO-REDIRECT-1.
#
#   Three EM DASH characters removed from two TRACKED requirements files, all
#   in section-header comments, replaced with the " -- " that requirements.txt
#   already uses at lines 160, 166 and 177. Nothing was broken -- pip reads
#   UTF-8 -- but these files pass through Set-Content, Copy-Item and Windows
#   editors where encoding is not always preserved, and every guarded installer
#   here already refuses a non-ASCII payload.
#
#   pytest-anyio>=0.0.0 removed. A 3,559-byte redirect package at version 0.0.0
#   whose PyPI summary says the plugin is built into anyio; never installed
#   here; its only mention anywhere was that one line. PROVEN sufficient:
#   anyio 4.13.0 declares exactly one entry-point group, pytest11 ->
#   anyio.pytest_plugin, and pytest --trace-config shows it REGISTERED.
#
#   ARITHMETIC: 4840 + 5 = 4845. Three of the five tests can skip via a git
#   helper -- a fact missed by checking each function body for pytest.skip --
#   so both skip conditions were measured: git ls-files exit 0 with 9 tracked
#   files, anyio pytest11 present. Neither skips.
#
#   ACCEPTANCE: tests/unit 4610 passed, 10 skipped, 0 failed.

# 2026-08-13 -- 4845 -> 4853 (+8). REPORTS-EAGER-IMPORT-1.
#
#   reports/__init__.py imported report_generator eagerly, so touching the
#   namespace required seaborn and jinja2 -- neither in requirements-api.lock,
#   while matplotlib two lines above them in the same block IS present -- and
#   executed sns.set_style() at module scope. Latent, because api.main does not
#   reach reports/, established with -X importtime rather than inferred.
#
#   MEASURED FIRST: all ten consumers import the SUBMODULE directly and zero
#   use a re-exported name, so the import block served no caller while
#   guaranteeing the activation. Replaced with a PEP 562 module __getattr__.
#
#   The tests measure in a FRESH interpreter, with two probes split by measured
#   cost: the namespace import is 0.03 s and the full one 1.91 s, a 64x
#   difference. Consolidating them into one expensive probe made the file
#   SLOWER than the version it replaced -- 11.17 s against 5.75 -- which only
#   timing the result revealed. Split correctly: 3.63 s.
#
#   ACCEPTANCE: tests/unit 4618 passed, 10 skipped, 0 failed.

# 2026-08-14 -- 4853 -> 4872 (+19). AGENT-ROOT-ANCHOR-1.
#
#   FIVE agents defaulted root="." so their output location depended on the
#   process working directory. scripts/apply_data_readiness_root_fix.py had
#   already diagnosed and repaired this for a sixth; three generated reports
#   sit inside src/ as its residue.
#
#   MEASURED by CONSTRUCTING each agent with a stub shared_state and reading
#   _root: five reported '.', DataReadinessAgent reported PROJECT_ROOT.
#
#   My first census found FOUR. A search across all of src and scripts found
#   provisioning_agent.py:45 as the fifth. Four detector function defaults are
#   deliberately excluded: they are pure functions whose callers all pass root
#   explicitly, and anchoring them would invert a layer dependency.
#
#   All eight existing test call sites inject root=str(tmp_path), so the
#   DEFAULT was unexercised and any change would have passed the suite. The new
#   file drives it: 11 cases fail before the repair, 19 pass after.
#
#   ACCEPTANCE: tests/unit 4637 passed, 10 skipped, 0 failed.

# 2026-08-14 -- 4872 -> 4885 (+13). REPORTS-DIR-IGNORED-1.
#
#   .gitignore:101 read `reports/` with no leading slash, so it matched at any
#   depth and swallowed src/genomic_variant_classifier/reports/ -- a SOURCE
#   PACKAGE. Proven by probe: an untracked .py there was ignored and invisible
#   to git status. Its two files survive only because they predate the rule.
#
#   Anchored to /reports/, with one explicit rule for the notebook output the
#   anchor would otherwise expose. Three stray artifacts moved out of src/,
#   each classified by READING it: two routine reports, and one preserved as
#   incident evidence under a disambiguating name because the root already
#   holds the correct 23:37 run of the same date.
#
#   The tests probe BEHAVIOUR via `git check-ignore --no-index`, not the rule
#   text: a text assertion would pass against rules reordered into uselessness.
#   Six cases fail against the bare rule; all thirteen pass after.
#
#   ACCEPTANCE: tests/unit 4650 passed, 10 skipped, 0 failed.

# 2026-08-14 -- 4885 -> 4910 (+25). RUNTIME-PATHS-1.
#
#   One typed authority for repository, artifact and state locations, replacing
#   five independent conventions. project_root is identity; artifact_root is
#   destination; state_root is mutable state. Conflating the first two is
#   OUTPUT-ROOT-CONFLATION-1; conflating the first and third put state inside
#   src/ twice.
#
#   Discovery verifies IDENTITY: three sentinels in conjunction plus the
#   declared name from pyproject.toml. There is no fallback -- explicit, then
#   GVC_PROJECT_ROOT, then discovery, then RAISE. config.py:17 returned one
#   author's Windows literal on every machine because it had one, and
#   GVC_PROJECT_ROOT is set nowhere.
#
#   The two state stores are named for their OWNERS -- literature_scout and
#   orchestrator -- so the agent_state.json ambiguity that nearly merged two
#   unrelated schemas cannot recur.
#
#   ADDITIVE: nothing imports this yet. Later units adopt it one consumer at a
#   time, each separately gated.
#
#   ACCEPTANCE: tests/unit 4675 passed, 10 skipped, 0 failed.

# 2026-08-15 -- 4910 -> 4932 (+22). STATE-STORE-1.
#
#   A JSON state store that is atomic, identified and fails closed, answering
#   version_monitor_agent.py:58-85: a cwd-relative path, a direct write_text,
#   and corruption swallowed into {} which the next save then PERSISTS. Those
#   compound -- a crash mid-write truncates, truncation reads as empty, empty
#   is written over the original, and the change-detection baselines are gone.
#
#   SharedState's atomic write was READ FIRST and is reproduced deliberately
#   and identically, with fsync added, so a second subtly-different
#   implementation does not appear beside a correct one.
#
#   Legacy bare payloads are readable when asked for: data/agent_state.json is
#   a 25-key dict with no envelope, verified by running the store against the
#   real file before this landed.
#
#   ADDITIVE: nothing imports it yet.
#
#   ACCEPTANCE: tests/unit 4697 passed, 10 skipped, 0 failed.

# 2026-08-15 -- 4932 -> 4949 (+17). LITERATURE-STATE-CWD-RELATIVE-1.
#
#   version_monitor_agent.py:58 read Path("data/agent_state.json") -- relative,
#   resolved against the working directory, both read and written. The agent is
#   live in two pipelines, and two divergent copies exist as a result.
#
#   Its corruption handler returned {} and the next save PERSISTED that
#   emptiness, so a crash mid-write destroyed the ClinVar and AlphaMissense
#   baselines the agent exists to keep. Six definitions become a store anchored
#   to RuntimePaths; the dead _set is dropped; the three call sites are
#   untouched.
#
#   /.gvc-state/ added to .gitignore, ANCHORED. Measured: git check-ignore
#   returned NOTHING for the canonical state path, so the first agent run would
#   have left mutable state untracked in git status. The leading slash keeps a
#   nested .gvc-state under src/ VISIBLE.
#
#   The two pre-existing tests stub _run_watch_targets with dry_run=True and
#   never reach the store; the new file drives it through an injected store.
#
#   ACCEPTANCE: tests/unit 4714 passed, 10 skipped, 0 failed.

# 2026-08-15 -- 4949 -> 4964 (+15). STATE-FILE-DUPLICATES-1.
#
#   A migration, not a file copy: two divergent literature-scout stores
#   reconciled into the canonical location with an immutable record carrying
#   every digest, the key-set comparison, and the reasoning.
#
#   Same 25 keys, no key unique to either, five values differing, every one the
#   nested copy being a later observation. Two copies existed because
#   version_monitor_agent.py:58 resolved a RELATIVE path against the working
#   directory (closed at a734ea1).
#
#   The migration REFUSES on differing key sets -- a newer copy that has lost a
#   key is not a superset -- on equal or non-string ordering values, and on any
#   source digest that has changed since the comparison. It deletes NOTHING:
#   both legacy files are retained and named in the record as the only
#   surviving evidence of what the defect produced.
#
#   THE MIGRATION IS NOT RUN BY THE INSTALLER. It writes live state; that is a
#   separate deliberate act.
#
#   ACCEPTANCE: tests/unit 4729 passed, 10 skipped, 0 failed.

# 2026-08-15 -- 4964 -> 4997 (+33). PREFLIGHT-TOKEN-SUBSTRING-1.
#
#   preflight_check.py:259 read `if "GITHUB_TOKEN=" in content` -- a substring
#   search over the WHOLE FILE. It accepted a commented line, an empty value, a
#   placeholder, and a name merely ENDING with GITHUB_TOKEN. It reported a
#   usable credential TWICE on 2026-08-15 when .env held only placeholder text,
#   and every cloud run is gated on that check.
#
#   The .env branch now parses lines, matches the name exactly, strips quotes,
#   rejects placeholder shapes, applies a length floor, reports a LENGTH rather
#   than the value, and distinguishes "cannot read" from "no token".
#
#   The floor is THIRTY, derived from two measured lengths: 22, the fragments
#   Add-Content produced when it split a pasted token across two lines; and 40,
#   the real token. Every current GitHub format is at least 40. I first
#   asserted 20, which does not reject 22 -- both wrong floors are sabotage
#   cases and the verifier enforces 22 <= floor < 40.
#
#   ACCEPTANCE: tests/unit 4762 passed, 10 skipped, 0 failed.
4997
