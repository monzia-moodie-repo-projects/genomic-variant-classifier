
=== 1. ProteinStructurePipeline: does it have annotate_dataframe (what line 1018 calls)? ===
public methods: ['annotate_dataframe']
has annotate_dataframe: True
has annotate: False

=== 2. What does line 1018 actually call? (show the real line) ===
1017|         protein = ProteinStructurePipeline(cache_dir=ac.protein_cache_dir)
1018|         df = protein.annotate_dataframe(df)
1019|         logger.info(
1020|             "Score annotation 14/17 (protein structure): %d missense variants annotated.",

=== 3. AlphaFoldConnector: confirm the method step-14a calls (annotate_dataframe) exists + signature ===
has annotate_dataframe: True
signature: (self, df: 'pd.DataFrame') -> 'pd.DataFrame'
has fetch: True

=== 4. Was line 1018 changed by our edit, or pre-existing? (git diff of the step-14 region) ===

@@ -297,6 +297,8 @@ class AnnotationConfig:
+    alphafold_path: Optional[Path] = None  # Phase D: AlphaFold cohort structural-feature parquet
+    alphafold_uniprot_index_path: Optional[Path] = None  # Phase D: UniProt index (gene->accession + sequence) for AF wt_aa cross-check
@@ -999,6 +1001,19 @@ class DataPrepPipeline:
         from genomic_variant_classifier.pipelines.protein_pipeline import ProteinStructurePipeline
+        # 14a. AlphaFold structural features (Phase D) -- parquet-first path.
+        # When ac.alphafold_path is set, the prebuilt cohort parquet supplies
+        # alphafold_plddt / solvent_accessibility / secondary_structure_context /
+        # with a wt_aa cross-check (fail-closed). The ProteinStructurePipeline below
+        if ac.alphafold_path is not None:
+            from genomic_variant_classifier.data.alphafold import AlphaFoldConnector
+            _af = AlphaFoldConnector(
+                parquet_path=ac.alphafold_path,
+                uniprot_index_path=ac.alphafold_uniprot_index_path,
+            df = _af.annotate_dataframe(df)
         protein = ProteinStructurePipeline(cache_dir=ac.protein_cache_dir)
         df = protein.annotate_dataframe(df)


=== 5. Full git diff stat (what exactly changed across all 4 files) ===
 scripts/launch_run17_baseline.sh                      | 11 +++++++++++
 scripts/run_phase2_eval.py                            | 19 +++++++++++++++++++
 src/genomic_variant_classifier/data/real_data_prep.py | 15 +++++++++++++++
 .../models/variant_ensemble.py                        |  2 +-
 4 files changed, 46 insertions(+), 1 deletion(-)

=== 6. Does BaseConnector define the cache interface AlphaFoldConnector uses? (_load_cache/_save_cache/config.cache_dir) ===
BaseConnector methods: ['_cache_path', '_get', '_load_cache', '_save_cache', '_to_canonical', 'fetch', 'source_name']
FetchConfig has cache_dir: True

=== 7. Instantiate AlphaFoldConnector in stub mode + annotate a 2-row frame (no parquet) -- real smoke ===
AlphaFoldConnector: parquet_path not set -- returning sentinel defaults. Build with scripts/build_alphafold_parquet.py.
cols added: ['alphafold_plddt', 'solvent_accessibility', 'secondary_structure_context', 'dist_to_active_site']
stub values: {'alphafold_plddt': 50.0, 'solvent_accessibility': 0.5, 'secondary_structure_context': 0.0, 'dist_to_active_site': 100.0}

=== DONE -- upload protein_interface_verify.txt ===
