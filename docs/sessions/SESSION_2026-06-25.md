# SESSION 2026-06-25 — Drive root consolidation + DataFreshnessAgent audit

**Owner:** Monzia Moodie · **Repo HEAD at close:** caf5ecd (origin/main)

---

## Objective
Unify two Drive roots into the canonical `genvarcla:genomic-variant-classifier/` store, eliminate scatter (G: and C:), verify end-to-end data integrity (all human chromosomes, no gaps), and audit the data-freshness agent system.

## What was done

### Drive migration (COMPLETE)
- Confirmed canonical root = `genomic-variant-classifier/data` (matches `configs/data_manifest.yaml` base). Decision: Monzia.
- Migrated from retired `genomic-variant-data/` into canonical, server-side (rclone, metadata-only):
  - **external/** 15,009 files → `data/external/` (eve 14,933; gtex 6; dbsnp 4; clingen 2; omim 2; gencode 8; hgnc 1; phylop 1; revel; rnaseq 14; + gnomad 24 VCFs; finngen R12+R13+docs; reference).
  - **models/** (4 trained pipelines: v1, phase2, phase4×2) → repo-level `models/`.
  - **runs/** (run9–15 ensembles + run15_full.tar) → `outputs/runs/`.
  - **experiments/** (1) → `outputs/experiments/`.
  - **manifests/** (14 provenance CSV/sha256) → `results/manifests/`.
  - **cache/** (esm2_cache.sqlite) → `data/cache/`.
- **gnomAD 24/24 re-verified in new home** — 3 MD5s exact (chrY/chr7/chrX). All chromosomes, no gaps.
- Dedups hash-confirmed (finngen R12, reference .fa identical across roots → source copies auto-deleted on move).
- **Source root `genomic-variant-data/` RETIRED** — verified empty, `rmdirs` removed it.

### REVEL phantom (resolved)
- Persistent "object not found" on a listing file `revel-v1.3_all_chromosomes.zip`. `rclone lsjson --recursive` diagnosed it: real 667 MB zip (ID `1zwZrn81uKScmti7kiVe46hpDuwl5DvHb`), located **directly under `external/`**, not under `external/revel/` as assumed → path mismatch caused every delete/move to miss. `moveto` with corrected path relocated it into canonical `data/external/revel/`. Source root then empty → retired.

### C: reconciliation
- `model/` confirmed pykan artifact, already gitignored (line 99).
- `manifests/` gitignored + committed (caf5ecd) — regenerable layout-audit output, distinct from Drive `results/manifests/`.
- `lovd/` (7 JSON) identified as stale subset of `data/external/lovd/raw/` (which `parse_lovd_all.py` reads via `RAW_DIR=data/external/lovd/raw`). Pending per-file hash-confirm → delete.
- `agent_data/` (85) = old `arch_cleanup_stage*` scripts/logs, untracked. Leave/archive.

### Agent audit — DataFreshnessAgent fully verified
- **Wired:** registered in orchestrator (`_register_agents`, AGENT_CLASSES, `PIPELINE_DEFINITIONS["data_freshness"]`).
- **Imports clean** (retired GCP config names still resolve).
- **Activated + runs live:** dry-run polled ClinVar/gnomAD/LOVD(403, graceful)/AlphaMissense end-to-end.
- Confirmed the registry-driven `DatabaseFreshnessMonitorAgent` supersedes it and covers the WHOLE registry (writes dated `reports/data_freshness/FRESHNESS_<date>.md`, flags MISSING/CRUFT, HITL re-acquire, emits DATA_UPDATED).

## Bugs / gaps found (non-blocking — staged for post-Run-17)
1. **gnomAD poll URL stale (v4.0).** Both legacy agent (`data_freshness_agent.py:67`) and registry gnomad/gnomad_constraint sources point at `release/4.0/...v4.0.sites.chr1...tbi` (frozen). Data is v4.1 → would never detect v4.1→v4.2. **Silent-miss defect.**
2. **Registry probe gaps.** ~10 sources are `check=MANUAL url=(empty)` but are publicly pollable (dbsnp, gtex, string, uniprot, phylop, vep, kgp_1000, clingen, reactome, eve) → silently never flag updates. (Distinct from correctly-MANUAL license-gated: hgmd/cosmic/tcga/omim/dbnsfp/finngen/spliceai.)
3. **Registry verdict drift.** `phylop=STUB`, `vep=STUB` but data is on disk; reconcile verdicts vs disk truth.
4. **`.OOMbak` cruft false-positive.** `_CRUFT_MARKERS` flags the intentionally-kept `dbnsfp_full_index.parquet.OOMbak`. Needs an allowlist.

## Repo
- `caf5ecd` — chore: gitignore regenerable layout-audit `manifests/` output. Pushed to origin/main.

## Next session
- Confirm REVEL zip in canonical revel/; hash-confirm + delete C:\lovd subset.
- Run 17 fresh preflight (`new_run_preflight.py` → RunNN_Preflight.ps1; `select_vast_offer.py`; `check_agents_active.py`; ALL-MODELS smoke). Then GPU run + postflight.
- POST-Run-17: apply registry+agent freshness patch (gnomAD v4.0→v4.1, wire probeable URLs, reconcile verdicts, OOMbak allowlist) with unit tests; THEN update ROADMAP.md/.docx + README (reflect whole-genome framing + all changes).
