# SESSION 2026-06-13 — Run 16 (launch, monitor, completion)

## Summary
Launched, monitored, and completed Run 16 — the first full-cohort training with real ESM-2 650M (delta + LLR) plus the full 13-model ensemble + stacking meta-learner. Best model ENSEMBLE_STACKER, test AUROC 0.99835 [0.99816–0.99852]. Full results in `docs/runs/RUN_16_results.md`.

## What was done
- Launched via `scripts/launch_run16_vm.sh` (ENV gate + PYTHONPATH=src) on Vast.ai 4090 (40728494); committed the L22 PYTHONPATH launcher fix (f349fae) and the monitor banner fix (fff4c09).
- Ran the feature-population audit (`audit_smoke_feature_population.py`): PASS on the 6 FAIL-severity sources; 35/81 columns constant (catalogued in `PHASE_2_FEATURES.md`).
- Confirmed **gene-disjoint splits** (12,385 train / 3,539 test genes, 0 overlap).
- scp'd all artifacts back to laptop; verified; destroyed the instance.

## What worked
- ESM-2 650M delta + LLR ran on GPU (~22 min, 188,352/188,983 scored).
- LOVD populated at full scale (362 > 0; smoke had 0) — contract gate 4.2 passed.
- All 13 base models trained + checkpointed; blend + stacker completed.
- Monitor banner fix: flatten ssh stderr + filter `Welcome to vast.ai` / `Have fun!` — clean output restored.

## What failed / needed fixing
- L21/L22: train.py died on `ModuleNotFoundError: genomic_variant_classifier` (src-layout package not installed). Fixed: vm.sh exports `PYTHONPATH=$REPO/src` + `pip install -e . --no-deps` + ENV_CHECK includes the package.
- Stale-Downloads collisions (L17 recurrence): versioned download names, then switched to in-place writers for the monitor.
- `eval_report.json` `consequence_breakdown`/`gene_errors` EMPTY — `meta_test` not passed to `ClinicalEvaluator.evaluate()`. **Fix next run** (one-line). Missense stratification recovered from saved OOF.

## Lessons
- `nothing to commit` is ambiguous (already-deployed vs stale copy); disambiguate by reading origin/main directly, not by inference.
- A port must be >= the original (don't drop safeguards); the launcher must verify the PROJECT package, not just third-party deps.
- The feature-population audit's `nondefault` counter is unreliable on a standardized matrix (counts vs raw sentinel) — true coverage comes from the annotation log (e.g., LOVD 362, not 1.49M).

## Findings (see RUN_16_results.md for detail)
- Ensemble ≈ best single model (CatBoost); stacking lift marginal.
- cnn_1d the clear outlier (test AUROC 0.8219, AUPRC 0.5670) — input-representation review pending.
- Leakage: gene memorization ruled out (disjoint); gene-prevalence secondary in importance; residual ClinVar predictor-circularity caveat remains.
- Calibration ECE 0.0054 (excellent) but MCE 0.118 (one weak bin).

## Next
- Apply the `meta_test` eval fix + cache PHASE-4 test predictions (halve eval time) + drop constant columns before training.
- Independent (non-ClinVar) label holdout to address circularity.
- Run 17 scope: GNN (`--string-db`) + 1KGP (`--kg-path`) activate `gnn_score` + `af_1kg_*`.
