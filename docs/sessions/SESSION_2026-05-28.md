# Session 2026-05-28 - G1 local pre-flight gate built and CLEARED (PM13 chain)

**Duration**: PM session (PS terminal active throughout)
**Outcome**: Charter v1.1 gate G1 (local pre-flight) built from scratch, debugged, and cleared GREEN (54 pass / 1 warn / 0 fail). Pre-Run-15 path advanced; next gate is G2 (VM-side, bash, Option A).
**Primary commits**: 3cf287a (G1 created), 8dd3285 (LOVD+pykan fixes), ee06b08 (kan.py docstrings), 3cfdd4d (G1 S6 harness fix)

---

## 1. Pre-session state
- HEAD: 8efa30d (PM12-docs, RUN_15_PLAN sync).
- Gates F G1/G2 scripts did not yet exist; only Run14_Preflight.ps1 (adaptation basis) present.
- Run 15 data flow unconfirmed (re-prep-from-raw vs run9_ready splits).

## 2. Data-flow confirmation (launch_run11_vm.sh probe)
- CONFIRMED Run 15 re-preps from raw on the VM: launch passes --clinvar/--gnomad + annotation sources to run_phase2_eval.py -> prep.run(). run9_ready splits NOT used; meta_train.parquet is a runtime output.
- --unseen-gene-holdout (PM11b) wired in launch ARGS (L206, unconditional). GNN via raw STRING pair -> --string-db auto.
- 9 required local prep inputs enumerated (FAIL-if-missing); finngen/1kg optional WARN (B.D1/B.D2 deferred).

## 3. G1 built (PM13, 3cf287a)
- Created scripts/Run_Preflight_Local.ps1 from the Run14_Preflight.ps1 skeleton. 14 sections (0-13): venv, git pushed+clean, KAN kan.py fix, imodelsx launch patch, requirements, KAN smoke, pytest, data files, ssh+vastai, disk, package imports, Run-15 wiring (C.8/C.9/GNN), postflight/destroy gates, RUN_15_PLAN <DECISION> count.
- Deltas vs Run 14: S1 verifies HEAD==origin/main (no hash pin); S7 adds lovd + STRING .info; S11-S13 new. Parser self-test 0 errors.

## 4. First fast pass - two FALSE-POSITIVE fails diagnosed
- Fast pass: 50 pass / 3 warn / 2 fail.
- LOVD 0.3 MB < 1 MB floor: legitimate. 18,006 variants / 10 cancer-predisposition genes (APC, BRCA1/2, MLH1, MSH2, MSH6, NF1, PTEN, RB1, TP53); 0.254 MB is the real gene-scoped extract; MinMB=1 was an arbitrary sentinel. Sub-finding: ref/alt null in 47.3% of LOVD rows (logged for on-VM data-quality audit).
- import pykan fails: a G1 bug, not the env. pykan 0.2.8 IS installed; its PyPI dist imports as module 'kan'. kan.py fallback correctly uses 'from kan import KAN' (L59).

## 5. PM13b (8dd3285) - fix the two false positives
- S7 LOVD floor 1 -> 0.1 MB. S10 import probe pykan -> kan.

## 6. PM13c (ee06b08) - kan.py stale docstrings
- L6/L81 claimed "FastKAN is/replaces primary" - false; live code selects imodelsx first (L51-53). Corrected to imodelsx (efficient-kan) primary; pykan/efficient-kan fallbacks. Run 10a pykan-runaway history (L8/L104) left intact.

## 7. First full G1 run - harness crash + skip miscount (S6)
- Two harness bugs (both mine, not project code):
  1. CRASH: S6 locals failed/passed collided CASE-INSENSITIVELY with script-scope accumulators Failed/Passed, clobbering the arrays to ints; next Fail() threw InvalidCastFromStringToInteger on 0 += "string". Fast pass never tripped it (-SkipPytest skips the block).
  2. WRONG FLOOR: MinPytest=566 is the COLLECTED total, but 6 tests are intentionally skipped, so only 560 pass.
- pytest itself HEALTHY: 560 passed / 6 skipped / 0 failed / 142s.

## 8. Skip-reason verification
- All 6 skips legitimate: 5 in test_mc_dropout_calibration.py (explicit TODOs pending Run 15 cohort/holdout/ECE/Spearman/gene-family-disjoint infra); 1 conditional coverage skip in test_tabular_nn_mc_dropout.py:232. No regressions.
- Forward note: the 5 MC-dropout calibration tests become runnable after Run 15 produces the cohort/holdout (post-Run-15 un-skip item).

## 9. PM13e (3cfdd4d) - S6 fix
- Renamed S6 locals to nFail/nPass/nSkip. Skip-aware gate: 0 failed/errored AND nPass>=560 AND collected(nPass+nSkip)>=566. Spliced the 8-line block by content match (CRLF-robust); diff confirmed S6-only; parser self-test 0 errors.

## 10. G1 CLEARED
- Full G1 at 3cfdd4d: 54 pass / 1 warn / 0 fail -> exit 0. S6: 560 passed, 6 skipped, 0 failed. S7 LOVD 0.254 MB passes; S10 import kan OK. Single WARN (1kg optional, deferred B.D1/B.D2) expected.

## 11. Process lessons
- PowerShell variable names are CASE-INSENSITIVE: a local $failed IS $script:Failed; never reuse an accumulator's bare name as a local.
- "collected" != "passed": a pytest pass-count floor must account for intentional skips (gate on 0-failed + passed-floor + collected-floor).
- A pre-flight harness can carry its own logic bugs that a parser self-test will not catch; only the full real-path run surfaces them. Run the full gate, not just the fast pass, before trusting it.

## 12. Side investigation - GPU-accelerated KAN backends (research)
- Produced a research report on candidate KAN backends for Run 10+ (FastKAN, efficient-kan, imodelsx, PolyKAN). Conclusions: imodelsx (wraps efficient-kan) is the right primary IF batching is patched for large N; FastKAN (RBF, ~3.3x faster than efficient-kan) recommended new #2 over pykan; PolyKAN (fused CUDA kernels, PPoPP 2026) promising but unreleased.
- Decision (this session): run verification-first steps (imodelsx fit() inspection + 10k/100k/250k/1M memory-time harness) BEFORE Run 15.
- warpKAN raised but confirmed a non-entity (warp-lang = NVIDIA Warp simulation framework, not a KAN); never a candidate.

## 13. Findings / tech-debt logged (not fixed this session)
- docs/CHANGELOG.md encoding mojibake: em-dashes, en-dashes, multiplication signs render as multi-byte garbage (UTF-8 misread as Windows-1252, compounded), from prior default-encoding writes. New entries written ASCII-clean + no-BOM UTF-8; bulk de-mojibake is a future cleanup.
- variant_ensemble.py L435-465: pandas .fillna downcasting FutureWarning (behavior changes in a future pandas).
- Meta-learner LogisticRegression lbfgs ConvergenceWarning on small fixtures; revisit max_iter/scaling for the real cohort.
- S5 KAN smoke message cluttered by imodelsx tqdm/stderr noise (cosmetic; check passes).

## 14. Open items - next
- Build G2 (scripts/Run_Preflight_VM.sh, bash on VM - Option A): GPU/CUDA, torch_geometric import, data arrival, repo commit; complement launch_run11_vm.sh's existing data/checkpoint checks.
- KAN backend initiative BEFORE Run 15: PM13d (persist report to docs/research/), imodelsx fit() source inspection, 10k/100k/250k/1M memory-time harness; confirm KAN-250K subsample wiring.
- Update RUN_15_PLAN gate F: G2 name Run_Preflight_VM.ps1 -> .sh (fold into G2 build commit).
- Then provision Vast.ai -> SCP -> G2 -> launch_run11_vm.sh -> monitor -> SCP back -> destroy (separate confirmed paste).

## 15. Commits this session (in order, all pushed)
- 3cf287a feat(preflight): add G1 local pre-flight Run_Preflight_Local.ps1 for Run 15 (PM13)
- 8dd3285 fix(preflight): correct LOVD size floor (1 -> 0.1 MB) and pykan import name (pykan -> kan) in G1 (PM13b)
- ee06b08 docs(kan): correct stale backend docstrings (L6, L81) to reflect imodelsx primary (PM13c)
- 3cfdd4d fix(preflight): G1 S6 - fix var-name collision + skip-aware pytest gate (PM13e)
- (PM14: this session doc + CHANGELOG entry)

## 16. KAN deep-audit (post-PM14) - backend memory/correctness from source
- Goal: before building G2 + committing to Run 15, verify the KAN path is memory-safe and class-correct (Run 10a had a ~17.9 GB pykan runaway).
- Findings (all source-level; supersede the earlier research-report risk ranking):
  - imodelsx KANClassifier.fit() BATCHES (kan_sklearn.py L60-93): batch_size=512, DataLoader; X/y stay CPU tensors, only the model + per-batch tensors hit the GPU. Peak GPU memory bounded by batch size, NOT dataset size. The 17.9 GB event was pykan-specific (full-batch spline grids) and cannot recur on the imodelsx path.
  - max_fit_samples default = 100_000 (kan.py L118); NO override in src/ or scripts/, so Run 15 uses the default.
  - _fit_imodelsx subsamples 1.2M -> 100k via sklearn.utils.resample(stratify=y, random_state=...): class balance preserved on the primary path. Minor nit: default replace=True = stratified bootstrap (~4% dup at 100k/1.2M); pykan path uses replace=False. Cosmetic; not changed.
  - imodelsx v1.0.13 bare-name bug handled twice (kan.py L181-183 instance attrs + launch L191-197 sed-patch); local .venv312 already patched.
- Decision: NO pre-Run-15 backend swap; imodelsx stays primary. FastKAN = future speed-only, off critical path. Recorded in docs/research/KAN_BACKEND_EVAL_2026-05-28.md (PM13d).

## 17. G2 scoping - launch preflight vs the existing preflight_vm.sh
- launch_run11_vm.sh own preflight: hard aborts on data (exit 2), patch-verify (exit 3), import smoke (exit 4); its §5 GPU/torch/polars/optuna/sklearn+LGBM is ONE non-aborting python -c (WARNS on no-CUDA, L129) and never imports torch_geometric. Repo reaches the VM via SCP of the whole working tree (.git included; RUN_11_SESSION_GUIDE L168) -> git rev-parse works on the VM. Canonical paths: REPO=/workspace/genomic-variant-classifier, DATA=/workspace/data, OUTDIR=/workspace/outputs/run11/full.
- Found scripts/preflight_vm.sh (326 lines, 2026-05-13): a real comprehensive VM preflight that ALREADY has the hard CUDA gate (L62-65) and a workflow-aware torch_geometric check (L196). BUT stale for Run 15: §5 checks the raw ClinVar VCF (Run 15 uses the processed parquet); relative data paths assume data under the repo (Run 15 data is at /workspace/data); run9/10 workflow naming. As-is it would false-FAIL on a Run 15 VM and overlaps launch's data checks.
- Decision (G2-A): new lean scripts/Run_Preflight_VM.sh (env/hardware gate only), lifting the proven GPU/PyG snippets; data-file checks stay launch's job; preflight_vm.sh kept with a deprecation header as an optional deep audit.

## 18. G2-A built (commit 4989a70)
- NEW scripts/Run_Preflight_VM.sh (set -uo pipefail, no -e): GPU+CUDA hard gate + VRAM floor (20000 MiB); torch_geometric+networkx; imodelsx.kan.kan_sklearn + KANClassifier; free disk (>=150 GB); RAM total (>=50 GB); repo HEAD (optional EXPECTED_HEAD + clean-tree warn). All floors env-overridable. Validated: bash -n clean (sandbox + on-machine), VRAM/disk/RAM/empty-array primitives runtime-tested, written LF/no-BOM, git ls-files --eol = i/lf w/lf.
- launch_run11_vm.sh L8/L119 FastKAN comments corrected to imodelsx/dependency (matches kan.py PM13c); 0 FastKAN remaining.
- RUN_15_PLAN.md gate-F live checklist .ps1 -> .sh (historical 05-27 entry left intact).
- preflight_vm.sh deprecation header inserted (still bash -n clean).

## 19. PM13d - KAN evaluation persisted (commit 6c192c1)
- Created docs/research/ + KAN_BACKEND_EVAL_2026-05-28.md (decision of record, corrected conclusions). validate_docs.py does not cover docs/research/, so free-form (no frontmatter).

## 20. State at wrap + open items
- HEAD: 6c192c1. Charter gates F: G1 (scripts/Run_Preflight_Local.ps1) CLEARED; G2 (scripts/Run_Preflight_VM.sh) BUILT + bash -n clean (runs on the VM before launch).
- Commits since PM14 (in order, pushed): 4989a70 (G2-A), 6c192c1 (PM13d). PM14 docs = 7dae7cf.
- Tech-debt (carried): CHANGELOG mojibake bulk-cleanup; variant_ensemble.py .fillna FutureWarning (L435-465); meta-learner lbfgs ConvergenceWarning on small fixtures; imodelsx resample replace=True nit (cosmetic); PM11c (cnn_1d closure), PM11d (decouple --skip-cnn from gnomad-constraint block); .md CRLF-on-touch (benign; only *.sh pinned eol=lf).
- Next (live, Monzia-driven): provision Vast.ai (RTX 4090, dlperf>=80 pcie_bw>=12, CUDA 12.x) -> SCP whole repo (.git) + data to /workspace -> bash scripts/Run_Preflight_VM.sh <HEAD> -> bash scripts/launch_run11_vm.sh -> monitor -> Run15_Postflight.ps1 (SCP artifacts back) -> Vastai_Destroy_Confirmed.ps1 (SEPARATE paste).
