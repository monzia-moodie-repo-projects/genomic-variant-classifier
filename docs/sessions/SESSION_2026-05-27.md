# Session 2026-05-27 - Pytest sys.modules pollution: diagnosis chain B.1-B.9 + testpaths fix landed

**Duration**: full session (PS terminal active throughout)
**Outcome**: Memory rule #4 G1 gate (local pytest collection errors = 0) cleared. Pre-Run-15 path unblocked.
**Primary commit**: `9eec8eb` - fix(pytest): restrict discovery to tests/ to stop sys.modules["torch"] pollution

---

## 1. Pre-session state

- HEAD: `8662597` (gitignore anchor fix from 2026-05-26).
- Memory rule #4 standing as of 2026-05-27: "STANDING RULE - ALL RUNS, ALL PROJECTS: Exhaustive pre-flight. ALL prior-run anomalies CLOSED or DEFERRED-with-justification. ALL <DECISION> placeholders resolved. Local pytest collection errors = 0..."
- Local pytest reported 12 collection errors. Charter v1.1 G1 gate not green.
- 21 <DECISION> placeholders unresolved in RUN_15_PLAN.md (A2-A8 anomaly sweep).
- A1 anomaly closure shipped in `3a166f6` (2026-05-26), but its 7 regression tests had never actually run under pytest because they were among the 12 erroring files.

## 2. The 12-error symptom

`python -m pytest --collect-only -q` produced 12 ERROR lines. 11/12 shared:

    TypeError: issubclass() arg 2 must be a class, a tuple of classes, or a union
    at scipy/_lib/array_api_compat/common/_helpers.py:69 in _issubclass_fast

The 12th (`test_esm2_activation.py`) had:

    ValueError: torch.__spec__ is not set

INCIDENT_2026-05-26_scipy-torch-array-api-compat.md (written previous session) hypothesized torch was broken locally. That hypothesis was disproved on 2026-05-27 by B.1.3 (`torch.__file__`, `__version__`, `__spec__`, `Tensor` all imported cleanly in plain Python). The doc needs correction - tracked as D13.

## 3. Phase B.1-B.4 - systematic hypothesis ruleout

| Phase | Hypothesis | Result |
|---|---|---|
| B.1 | torch installation corrupt | DISPROVED (plain Python imports torch.Tensor as a class) |
| B.2 | typeguard / pytest-cov plugin | DISPROVED (same 12 errors with -p no:typeguard, -p no:cov, both) |
| B.3 | anyio/faker/langsmith/assertion-rewriting/conftest | DISPROVED (kitchen-sink combination still 12 errors) |
| B.4 | per-file structural issue | DISPROVED (each of 12 files collects cleanly in isolation) |

Across B.1-B.4, plain Python import-order tests all succeeded. The bug manifested only under pytest's full-suite collection.

## 4. Phase B.5-B.6 - smoking gun via source grep, confirmed by exclusion

B.5.5 ran the equivalent of `Select-String -Path "src/**/*.py" -Pattern 'sys\.modules\['`. Two hits, both in `src/genomic_variant_classifier/agent_layer/test_message_bus.py` (L84, L88). At L87-89:

    for _mod in ("ewc_utils", "shap", "torch", "feedparser", "requests"):
        if _mod not in sys.modules:
            sys.modules[_mod] = MagicMock()

B.6.4 confirmed by exclusion:

| Run | Tests collected | Errors |
|---|---|---|
| Baseline (full rootdir) | 416 | 12 |
| Minus `test_message_bus.py` | 552 | 0 |

The +136 tests = exactly the 12 victim files' individual counts (17+10+10+3+18+10+2+7+10+11+22+16 from B.4.1). Test-count arithmetic was decisive.

Mechanism: pytest auto-discovery (default `test_*.py` pattern) imports `src/.../test_message_bus.py` during full-suite collection. Module-level code at L87-89 replaces `sys.modules["torch"]` with a MagicMock. Subsequent collection of 12 downstream files triggers scipy.stats's array_api_compat `_issubclass_fast`, which calls `getattr(sys.modules["torch"], "Tensor")` and gets a MagicMock back (hashable so it passes scipy's lru_cache key check, but NOT a class), causing `issubclass(cls, parent_cls)` to raise TypeError. The `test_esm2_activation.py` ValueError on `torch.__spec__` is the same pollution via a different lookup path.

## 5. Phase B.7 - pyproject.toml fix

Added `[tool.pytest.ini_options]` with `testpaths = ["tests"]` to restrict pytest auto-discovery to the canonical `tests/` tree.

- Pre-edit: 1068 chars, 32 lines.
- Post-edit (after B.8 style fix): 1939 bytes, 47 lines.
- Post-fix collection: 526 tests, 0 errors.

`test_message_bus.py` is unmodified. Per its docstring L10-14 ("Run from agent_layer/: python test_message_bus.py") it was designed as a standalone script. It remains runnable by explicit path.

## 6. Phase B.8 - side-effect audit

B.7 reported 526 tests vs the 552 expected from B.6.4. Investigation:

- B.8.1: root-level `test_catboost.py` (17718 B, untracked per `.gitignore` L95, in "Scratch and generated files" section per L92) contains exactly 26 tests.
- The file is intentionally untracked. Cloud/CI runs on Vast.ai never saw it.
- The canonical tracked sibling at `tests/unit/test_catboost.py` (20551 B) is in tests/ and continues to participate in default discovery.

Conclusion: the testpaths fix aligns local pytest behavior with cloud pytest behavior. Correctness improvement, not a regression.

B.8.5-B.8.7 added a blank line between `[tool.setuptools.packages.find]` and `[tool.pytest.ini_options]` for TOML style.

## 7. Phase B.9 + B.9.8 - commit, push, audit

| Step | Outcome |
|---|---|
| B.9.0 state pin | Threw "HEAD drift" - prior B.9 block had already committed; not a real failure |
| B.9.1 explicit-path verification | `pytest test_catboost.py` -> 26 tests, rc=0; `pytest src/.../test_message_bus.py` -> 0 tests, rc=5 (no error) |
| B.9.5 git commit | Said "nothing to commit, working tree clean" - because prior B.9 block had already landed it |
| B.9.6 push | `9eec8eb` already on origin/main; "Everything up-to-date" |
| B.9.7 final seal | 526 tests collected in 29.18s, tree clean, local==origin |
| B.9.8 post-commit audit | Surgical commit (only pyproject.toml), origin synced, FIX block PRESENT (B.9.8.6 with multiline regex), Run-ID trailer ABSENT |

The Run-ID trailer is absent because the prior (unrefined) B.9 block committed before the refined version with the trailer ran. The `check_run_id_trailer.py` hook accepted the commit anyway - confirms the hook is permissive or doesn't require trailers.

## 8. Test runs verified (first time post-fix)

- `tests/unit/test_mc_dropout_uncertainty.py`: 7 passed in 5.35s. Closes A1 regression test gap (shipped in `3a166f6` but never ran under pytest until this session).
- `tests/unit/test_alphamissense.py`: 17 passed in 20.28s.
- `tests/unit/test_eve.py`: 18 passed in 20.73s.
- `tests/unit/test_prediction_artifacts.py`: 11 passed in 5.13s.

## 9. Process lessons

1. **G1 gate paid for itself**. Run 15 would have launched on Vast.ai (Linux, where the polluter file is also importable) and discovered this issue only after compute spend.

2. **`pytest --collect-only -q` silently suppresses files with 0 tests AND 0 errors**. `test_message_bus.py` was being imported (running its sys.modules pollution) but wasn't enumerated in `-q` output, hiding the polluter from grep on pytest logs. The smoking gun came from greping the SOURCE FILE for `sys.modules[`, not the pytest log. Future audits should use `--collect-only -v` for per-file visibility.

3. **PowerShell array splat is `@var`, never `@$var`**. The wrong form gives a silent 1.4s no-op (B.5.1-B.5.3 false negative). Always validate: pytest invocation <2s elapsed time = red flag.

4. **`Select-String` regex misses produce empty arrays silently**. Downstream `foreach` over the empty array runs zero times silently (B.8.2-B.8.4 had this bug; B.8.1 caught the answer through a different path). Defensive pattern: count results, throw if empty when non-empty is expected.

5. **`Get-Content -Raw` after `Out-File -Encoding utf8` introduces CRLF on Windows**. B.9.8.1 false-negative on FIX block was caused by exactly this. Fixed in B.9.8.6 by joining `git log --format=%B`'s raw output array with LF explicitly. Lesson: for inspecting committed-message bodies, never round-trip through `Out-File`; either keep the array of lines or pipe through `-replace "\r",""`.

6. **`[System.IO.File]` uses .NET CWD, not PowerShell `Get-Location`**. B.11.2 wrote to `C:\Users\monzi\docs\sessions\...` instead of `C:\Projects\genomic-variant-classifier\docs\sessions\...` because the relative path was resolved against .NET's CWD (the PS-startup directory), not the post-cd PS CWD. Either use absolute paths with .NET APIs, or set `[Environment]::CurrentDirectory = (Get-Location).Path` once per session. This was caught and recovered by B.11.RETRY.

## 10. Commits this session

| Order | SHA | Subject |
|---|---|---|
| baseline | `8662597` | (prior session) fix(gitignore): anchor scratch-file patterns to root |
| 1 | `9eec8eb` | fix(pytest): restrict discovery to tests/ to stop sys.modules["torch"] pollution |
| 2 | (this commit) | docs(session): SESSION_2026-05-27 - B.1-B.9 testpaths fix |

## 11. Open items - next session entry

| Item | What | Effort |
|---|---|---|
| D13 | Append corrections to `INCIDENT_2026-05-26_scipy-torch-array-api-compat.md` (torch-broken hypothesis disproved by B.1.3; document verified root cause; update status OPEN -> RESOLVED) | 30 min |
| CHANGELOG | Append entries for `088797a`, `8662597`, `9eec8eb`, and this session commit to `docs/CHANGELOG.md` | 30 min |
| D12 | Refactor `test_message_bus.py` L87-89 sys.modules pollution into pytest `monkeypatch.setitem` fixtures with proper teardown | 1-2 hrs (post-Run-15) |
| D14 | Standing-rules update: add the 6 process lessons from Section 9 to memory | 15 min |
| Phase C | Anomaly sweep A2-A8 (21 `<DECISION>` placeholders in RUN_15_PLAN.md) - per memory recommend A7 first | 3-5 hrs |
| Phase D | Deferred-issue triage D1-D14 catalog in RUN_15_PLAN.md | 1-2 hrs |
| Phase E | `scripts/preflight_run15.py` G1-G15 master gate script | 1 hr |
| Phase F | Vast.ai provision -> SCP up -> train -> SCP back -> destroy | per memory #29/#30 |

Memory rule #4 stays in force: no run begins until all gates green.

---

## 12. PM continuation — Phase C anomaly closures (4 PM sessions)

After section 11's morning work landed `9eec8eb` (pytest testpaths fix — the title of this doc reflects the morning's primary outcome), the session continued through four PM batches addressing the Phase C anomaly sweep + Run 15 prep blockers. Detail in `docs/CHANGELOG.md`; this section is the session-level narrative.

### 12.1 A7 closure (B.O4 / C.3) — observability rewrite

- `da41f27` (code) + `526cb3f` (docs)
- `scripts/run14_observability.py` log-grep was overfitted to shell `==>` echo style; never matched Python-logger output (0 matches in `outputs/run14/run14_master.log`).
- Fix: read structured outputs (`per_model_metrics.csv`, `per_model_metrics_val.csv`, `models/*_meta.json`) directly; log-grep regex relaxed (drop `==>` prefix requirement) as fallback. 7-case regression test at `tests/unit/test_run14_observability.py`, all passing.

### 12.2 A3 closure (C.4) — launch script imodelsx_patch tee dedupe

- `9628463` (code) + `a3298e3` (docs)
- `scripts/launch_run11_vm.sh` L200 had `fi | tee -a "$LOG"` after an if/else where inner echoes already piped to `tee`. Each status line logged twice. Empirically confirmed via `outputs/run14/run14_master.log` (success-branch 2x, else-branch 0x).
- Fix: 1-line change replacing L200 outer-tee with `fi` + forensic comment.
- Process finding: PS `throw` inside `& { }` exits only the script block, not the surrounding paste. Resolved in 12.3 via top-level `try { ... } catch { ... return }` wrapper.

### 12.3 C.5 + C.6 + C.7 — postflight + destroy infrastructure

- `f7febbb` (C.5+C.6) + `6107e56` (C.7) + `d8baaa9` (docs)
- Closes the Run 14 procedural-failure class (A8 root cause): postflight gates wired (Test-ArtifactPresent helper now actually called), `models/` SCPed (Run 14 silently dropped this — the directory containing `ensemble.joblib`), destroy script separated from postflight inline-print.
- `scripts/Run15_Postflight.ps1` (194 lines): 7 Test-ArtifactPresent gates, writes `.gate_exit_code`, SCPs `models/` + `per_model_metrics*.csv`, prints pointer to destroy script. 5 explicit `exit 1` paths.
- `scripts/Vastai_Destroy_Confirmed.ps1` (114 lines): 4 independent refusal layers (stdin-not-redirected, gate-file-exists, gate-content-zero, "DESTROY"-typed-exactly). Pipes `'y' |` to handle vastai CLI's interactive prompt.
- First paste to use top-level `try/catch return`. Pattern is now the default for all subsequent pastes.

### 12.4 D17 — scripts/run15_observability.py + D16 audit

- `486c680` (code) + `8542d7a` (docs)
- D17 was the last hard blocker for Run 15 launch (Run15_Postflight.ps1 L80 exits 1 if `scripts/run15_observability.py` is missing locally).
- Approach: byte-level clone of `scripts/run14_observability.py` via Python patcher with 4 deterministic string-replace transforms. Test file cloned with single targeted replacement (data-source refs intentionally preserved).
- Verification: 19/19 PS sanity checks PASS, 14/14 pytest PASS in 7.67s (both regression on the old test and functional equivalence on the new test).
- **D16 audit finding**: while working D17, a `.gitattributes` audit revealed D16 (`*.sh text eol=lf` rule) was already CLOSED in `bd75ed5` from **2026-05-11** — 16 days before today. The CHANGELOG entries on L54/L98 of d8baaa9 listing D16 as open are stale; per append-only convention they were NOT modified, but `8542d7a` documents the audit finding for future grep.
- 4-iteration apply sequence (each iteration codified one new failure mode):
  - Attempt 1: TypeError on `Path.read_text(newline="")` — Python 3.12 vs 3.13 API gap (`read_text` accepts `newline=` only since 3.13; `write_text` since 3.10, asymmetric).
  - Attempt 2: Patcher fixed (`read_bytes`/`write_bytes`); Phase 4 false-failed because PS 5.1 `String.Split(string)` is the char-array overload (single-string overload added in .NET 5+, only in PS 7+). Files left on disk because catch printed recovery but didn't execute.
  - Attempt 3: Verification fixed (`-not $var.Contains('substr')`); state pin threw because Attempt 2's untracked files were still present.
  - Attempt 4: SUCCESS via self-healing Phase 1 (selective auto-cleanup for the exact known-stale `?? scripts/run15_observability.py` + `?? tests/unit/test_run15_observability.py` entries; refuses for any unexpected dirty entry).

## 13. End-of-day state (post-`8542d7a`)

**Phase C code-level: complete.** Run 15 launch infrastructure ready:
- `scripts/Run15_Postflight.ps1` (`f7febbb`)
- `scripts/Vastai_Destroy_Confirmed.ps1` (`6107e56`)
- `scripts/run15_observability.py` (`486c680`)

| Item | Status | Latest commit(s) |
|---|---|---|
| A1 / B.O2 | CLOSED (prior session) | `3a166f6` |
| A3 / C.4 | CLOSED today | `9628463` + `a3298e3` |
| A7 / B.O4 / C.3 | CLOSED today | `da41f27` + `526cb3f` |
| C.5 / C.6 / C.7 | CLOSED today | `f7febbb` + `6107e56` + `d8baaa9` |
| D16 (`*.sh text eol=lf` rule) | CLOSED 2026-05-11 (audit-discovered today) | `bd75ed5` |
| D17 (run15_observability.py) | CLOSED today | `486c680` + `8542d7a` |

Remaining items (none block the code-presence side of Run 15 launch): A2, A4, A6, E budget, H_Run15, D15 (memory codification).

## 14. PM process lessons (full detail in CHANGELOG entries)

1. **PS `throw` inside `& { }` doesn't halt paste** — fix: top-level `try { ... } catch { Write-Host ...; return }` wrapper.
2. **Sanity-check design is its own quality dimension** — over-specified anchors produce false positives that mask the next paste's real problems. Use word boundaries; walk lines for comment classification.
3. **Python 3.12 vs 3.13 API gap**: `Path.read_text(newline=)` was only added in 3.13. Use `read_bytes()` + `.decode("utf-8")` + `.encode("utf-8")` + `write_bytes()` for Python <= 3.12 portability AND byte-level autocrlf bypass.
4. **PowerShell 5.1 `String.Split(string)` is the char-array overload**: `.Split('run14')` on PS 5.1 splits on any of chars `r`/`u`/`n`/`1`/`4`, NOT on the substring. Single-string overload added in .NET 5+, only available in PS 7+. For substring-presence checks on PS 5.1, use `-not $var.Contains('substr')`.
5. **"Print recovery, hope human runs it" is structurally unreliable** — when a catch handler prints recovery commands as text but does not execute them, the next paste typically re-runs the same block and re-throws the same way. Build self-healing into the state-pin phase: detect exact known-stale artifacts, REFUSE for any unexpected dirty entry, selectively clean.
6. **Audit-finding pattern**: when memory/notes claim an item is OPEN but the repo shows otherwise, verify via `git log --oneline -- <path>` + `git show <commit>`. D16 was today's canonical example (already closed in `bd75ed5` from 2026-05-11; CHANGELOG entries claiming "open" were stale).
7. **Recursive `__pycache__` cleanup scope**: traversal from project root descends into `.venv*/` and clears site-packages pycache too (720 dirs cleared in one PM 4 paste). Future canonical idiom: filter out `.venv*` paths from the cleanup list.
8. **Python f-string invalid escape sequence**: literal backslash-followed-by-character in `f"..."` triggers SyntaxWarning in 3.12 and will SyntaxError in 3.14. The PM 4 docs patcher's `cl_entry` had this benign warning fire on a documentation string (the literal text rendered correctly; the warning was just code-quality nuisance). Use raw `rf"..."` or double the backslash when including regex-like patterns in documentation strings.

## 15. Commits added this PM (in order, all pushed)

| # | SHA | Subject |
|---|---|---|
| 1 | `da41f27` | fix(observability): A7 close - rewrite per_model parser to read structured files |
| 2 | `526cb3f` | docs(plan,changelog): record A7 closure (da41f27) |
| 3 | `9628463` | fix(scripts): A3 close - dedupe imodelsx_patch logging in launch_run11_vm.sh |
| 4 | `a3298e3` | docs(plan,changelog): record A3 closure (9628463) |
| 5 | `f7febbb` | feat(scripts): C.5+C.6 - Run15_Postflight.ps1 with Test-ArtifactPresent gates |
| 6 | `6107e56` | feat(scripts): C.7 - Vastai_Destroy_Confirmed.ps1 with 4-layer refusal |
| 7 | `d8baaa9` | docs(plan,changelog): close C.5/C.6/C.7 (f7febbb, 6107e56) |
| 8 | `486c680` | feat(scripts): D17 - scripts/run15_observability.py + tests for Run 15 |
| 9 | `8542d7a` | docs(plan,changelog): close D17 (486c680) + record D16 audit finding |

Plus this commit (SESSION update). **~10 commits today across morning + PM.**

## 16. Open items — actual (supersedes Section 11 for PM-overlapping items)

| Item | What | Effort | Notes |
|---|---|---|---|
| **D15** | Memory codification (the 8 lessons in Section 14) via `memory_user_edits` | 10-15 min | Requires user confirmation per the memory-tool standing rule. Equivalent to D14 in Section 11; numbering drifted as new D-items appeared during PM work. |
| **D13** | Append corrections to `INCIDENT_2026-05-26_scipy-torch-array-api-compat.md` (from Section 11) | 30 min | Still open; morning status unchanged. |
| **A4 / B.O1** | KAN subsample decision | 30 min | Decision-only; required before Run 15 launch. |
| **A2 / B.O3 / C.2** | TabularNN MC-dropout: implement or drop | 2-4 hrs OR 5 min | Decision drives effort. |
| **A6 / B.D1-B.D6** | 6 data-source decisions | 4-8 hrs total | License review needed for some (HGMD Pro). |
| **E budget** | Wall-clock + cost for Run 15 | 15 min | Required before launch. |
| **H_Run15** | Hypothesis lockdown for Run 15 | 15 min | Required before launch. |
| **Phase F** | Vast.ai provision -> SCP up -> train -> SCP back -> destroy | Run-15-day | After all above. |

Section 11's items D12 (test_message_bus.py refactor) and D14 (now D15) remain open from morning. The other Section 11 items: Phase C code work is now complete; Phase D triage and Phase E preflight remain.
