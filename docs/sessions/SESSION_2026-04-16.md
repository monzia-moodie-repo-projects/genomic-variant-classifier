# Session 2026-04-16 — Lambda A10 Setup, Phase 2 Feature Promotion, Run 8 Pre-flight

**Date:** 2026-04-16
**Instance:** Lambda Labs gpu_1x_a10, 147.224.51.197, $1.29/hr

## What Was Accomplished

### 1. Lambda Labs instance launched and verified
- A10 GPU, 23GB VRAM, CUDA 13.0, torch 2.11.0+cu130
- SSH key: ~/.ssh/id_lambda_run8 (ed25519)
- ssh-keygen -N "" fails in PowerShell; fix: run interactively, press Enter twice

### 2. SyntaxError in variant_ensemble.py fixed
- Root cause: Phase 2 feature code pasted inside unclosed assert ( expression
- Fix: write repair script to .py file via Set-Content, locate block structurally, delete broken fragment, verify with ast.parse()

### 3. TABULAR_FEATURES updated 21 -> 78 features
- engineer_features() had grown to 78 columns across Phase 2 sessions but TABULAR_FEATURES was frozen at 21
- Fix: regex-replace entire TABULAR_FEATURES block via fix_tabular_features.py written with Set-Content
- Test result: 205 passed, 3 skipped, 0 failed

### 4. Lambda environment provisioned
- pip --index-url replaces PyPI (wrong); fix: --extra-index-url for torch, separate invocation for non-torch
- torch 2.11.0+cu130, pandas 2.3.3, PyG 2.7.0 confirmed

### 5. GCS authenticated on Lambda
- SA key uploaded via scp, gcloud installed, bucket gs://genomic-variant-prod-outputs/ accessible

## Pending
- Run 8 training not yet launched — data sync and script execution are next actions
- Commit session docs + CHANGELOG

## Key Lessons
- assert ( multiline: assignments inside cause SyntaxError. Compute first, assert last.
- Never use python -c for multi-line Python with quotes/backslashes in PowerShell. Use Set-Content to .py file.
- TABULAR_FEATURES must stay in sync with engineer_features() output. The assert at end is the single guard.
- --index-url is destructive; --extra-index-url is additive.
- Lambda billing starts at launch. Commit and push before creating the instance.

## Files Modified
- src/models/variant_ensemble.py: SyntaxError fixed; TABULAR_FEATURES 78 features; _parse_codon_position(); Phase 2 feature computations
- src/data/real_data_prep.py: _join_spliceai(), _join_alphamissense(), Phase 2 in _engineer_features()
- tests/unit/test_core.py: test_phase2_features_promoted
- scripts/lambda_run8_startup.sh: new Lambda startup script
