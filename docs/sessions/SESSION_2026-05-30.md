# Session 2026-05-30 - train.py sequence/label realignment (PM11c/PM11d)

**Duration**: PM session (PS terminal active throughout)
**Outcome**: Closed carried tech-debt PM11c + PM11d in scripts/train.py. Test-side CNN sequences realigned to the gene-aware split; train-side guarded against silent misalignment. Regression test added; full unit suite green (562 pass / 1 skip / 0 fail). Anchored to HEAD 2a63210.
**Primary change**: scripts/train.py sequence block (was lines 259-272)

---

## 1. Pre-session state
- HEAD: 2a63210 (main == origin/main), clean tree.
- Carried tech-debt from PM-G2 (2026-05-28): PM11c (cnn_1d closure), PM11d (decouple --skip-cnn from gnomad-constraint block).

## 2. Ground-truth audit
- Migration consolidate-package-layout SHIPPED: agent_layer now at src/genomic_variant_classifier/agent_layer/ (29 files). Root Test-Path agent_layer -> False is expected.
- No scripts/preflight_review.py; real preflight surface = Run_Preflight_Local.ps1 (G1), Run_Preflight_VM.sh (G2), preflight_check.py (local), preflight_vm.sh (DEPRECATED Run 15+).
- DataPrepPipeline.run() returns 8-tuple (no meta_train). _save_splits persists meta_train only when passed (Patch 6b). meta_train.parquet absent in models/v1/splits (runtime output per SESSION_2026-05-28).
- X_train.parquet: 1197216x73, zero identity columns. meta_test.parquet carries variant_id/chrom/pos/ref/alt/fasta_seq, row-aligned to y_test (349067 == 349067).
- Production clinvar_grch38.parquet: fasta_seq present, notna=0.

## 3. Change (Option B-narrow-final-A; one file, no run() signature change)
- Test side: X_seq_test = meta_test["fasta_seq"].reset_index(drop=True).
- Train side: raise NotImplementedError if real training sequences enabled (no signature-free realignment; X_train has no key, run() does not return meta_train).
- has_sequences keyed on meta_test. PM11c dummy series retained only on no-sequence (production) path.
- NEW tests/unit/test_train_sequence_alignment.py.

## 4. Validation
- Regression test: 2 passed. Old-logic test proves bug (agreement < 0.85); meta_test-source test proves fix (== 1.0).
- Full unit suite: 562 passed, 1 skipped, 0 failed (327s). No regression.
- ast.parse OK; 0 residual iloc[:len(y_ slices; 3 expected anchors present. git diff +27/-7.

## 5. Plan forward
- Task 2: AutoKernel-style 5-stage correctness harness wired into G1/G2 + training_lifecycle_agent.py, extending preflight_vm.sh section 12 smoke fit.
- Task 3: ScienceClaw artifact ledger + deterministic policy gate on shared_state.py / message_bus.py / orchestrator.py.
- Deferred: Option-B-wide (return meta_train from run()) -- only if CNN-on-real-sequences is enabled.

## 6. References
- docs/incidents/INCIDENT_2026-05-30_train-sequence-misalignment.md
- Related: docs/incidents/INCIDENT_2026-05-23_cnn1d-0.5-auroc.md