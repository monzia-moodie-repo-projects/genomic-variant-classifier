# SESSION 2026-06-04 — Run 15 launch, monitoring, retrieval, teardown

## Scope
Launched, monitored to completion, retrieved, verified, and decommissioned
**Run 15** — the de-leaked gene-disjoint baseline (`launch_run15_baseline.sh`,
full-signal, `--unseen-gene-holdout`). Vast.ai instance 39391596 (RTX 4090,
$0.9852/hr).

## Outcome
- STATUS: `TRAIN_OK after 29525s | GNN_FAIL` (run process exit 0 @ 19:43:14 UTC).
- Wall-clock ~8.2 h; cost ~$8 (credit $37 → $28.63).
- Self-stop wrapper worked: instance stopped cleanly on completion.
- Data retrieved as a single tar, **MD5-verified** identical to the VM
  (`fefa30910559a89b2b62aa133d7b7e1c`, 1,660,497,920 bytes, **121 files**),
  extracted to `outputs/run15_baseline/full/`, backed up to
  `genvarcla:run15_baseline/`. Instance **destroyed**; exposed VM API key
  **rotated**.

## Headline results
| Split | AUROC | AUPRC | F1 | MCC | Brier | n |
|---|---|---|---|---|---|---|
| Test (main) | 0.9983 | 0.9934 | 0.9829 | 0.9657 | 0.0069 | 304,711 |
| Val (main) | 0.9984 | 0.9923 | 0.9804 | 0.9607 | 0.0071 | 146,329 |
| **Unseen-gene holdout** | **0.9988** | 0.9945 | 0.9847 | 0.9693 | 0.0058 | 213,436 (2,407 genes) |

**Key scientific finding.** The unseen-gene holdout AUROC (0.9988) ≈ main test
(0.9983). The model does not collapse on 2,407 genes never seen in training —
strong evidence for cross-gene generalization rather than gene-prevalence
memorization. C3 falsifier (b) PASS (≥ 0.95). Caveat: interpretation depends on
how `n_pathogenic_in_gene` is computed for held-out genes → elevate the
`n_pathogenic_in_gene` ablation to a named pre-publication experiment.

## Per-model comparison (test AUROC)
catboost 0.9984 (best single) · ENSEMBLE_STACKER 0.9983 · xgboost 0.9982 ·
deep_ensemble 0.9982 · lightgbm 0.9982 · mc_dropout 0.9981 · tabular_nn 0.9981 ·
gradient_boosting 0.9979 · random_forest 0.9977 · logistic_regression 0.9963 ·
cnn_1d 0.8219. Holdout/val orderings match.

Findings: (1) the **stacker did not beat the best single model** (catboost
0.9984 vs stacker 0.9983) — at this saturation/correlation, stacking adds
nothing; report it. (2) **cnn_1d is the consistent outlier** (~0.82,
sequence-only modality), well above chance, a legitimate weak-modality data
point. (3) Comparison is **10 of 12** — KAN and GNN are missing (see incidents).

## Failures (both non-fatal, both block a complete comparison)
1. **KAN failed in both ensembles** — `name 'test_size' is not defined`.
   imodelsx patch (launch_run11_vm.sh:193-194) was not ported into
   launch_run15_baseline.sh. See
   `INCIDENT_2026-06-04_kan-test-size-baseline-launcher.md`.
2. **GNN gnn_score degenerate** (constant 0.5, std=0 on all splits). Root cause
   confirmed in live source: `gnn_df` has no `variant_id`, so
   `GNNScorer.from_trainer` builds an empty `gene_scores` and every lookup
   returns the 0.5 default; gene-disjoint splits compound it. The GNN trained
   fine (val AUC 0.6509); its scores never reach the matrix. See
   `INCIDENT_2026-06-04_gnn-score-injection-degenerate.md`.

## Feature-importance map (data-completeness audit)
Top signal: `n_pathogenic_in_gene` 391 (≈1.5× next), loeuf 262, mis_z 243,
af_raw 219, syn_z 203, cadd_phred 191, pli 167, consequence_severity 166,
revel 160, alphamissense 149, af_log10 137, splice_ai 137, gerp 119.

**~38 features are exactly 0.0** (defined in `TABULAR_FEATURES` but never
populated): `gnn_score` (degenerate), `esm2_delta_norm`, `eve_score`,
`phylop_score`, all 7 GTEx, both HGMD, both OMIM, `clingen_validity_score`,
`dbsnp_af`, `alphafold_plddt` + 3 protein-structure, all 5 1KG AFs, all 3
FinnGen, `codon_position`, `maxentscan_score`, `exon_number`,
`dist_to_splice_site`, `is_canonical_splice`. The 78-feature matrix is
effectively ~40 features with signal. `PHASE_2_FEATURES` is `[]` but these zeros
are de facto placeholders — the data roadmap (HGVSp parser → ESM-2/EVE; GNN
injection fix → gnn_score; GTEx/OMIM/ClinGen/dbSNP/PhyloP/1KG/FinnGen
connectors) is quantified here.

## Housekeeping notes
- `variant_ensemble.py` docstrings are stale ("8 base classifiers", "65
  features") vs the actual ~12 estimators / 78 features — cosmetic, fix when
  touched.
- KAN-failure handling in `VariantEnsemble.fit` is correct (ERROR log, OOF→0.5,
  column dropped via `valid_cols`) — the blend is not corrupted; the issue is
  only the missing data point.

## Forward plan (next session) — all behind the ALL-MODELS smoke gate
1. KAN patch ported into the baseline launcher (+ `./model/` in .gitignore).
2. GNN inductive all-node scorer + `std>0` hard-abort + variant_id provenance.
3. SVM stays every run: Nyström/RFF full-data headline + exact-RBF bagged
   secondary (m=15k, K≤25, reported at plateau); remove the >100k binary skip.
4. ALL-MODELS smoke gate (tiny `--max-train`, no `--skip`, fails launch if any
   model errors/skips/degenerate).
5. Named pre-publication experiments: `n_pathogenic_in_gene` ablation;
   stacker-vs-best-single saturation confirmation once KAN+GNN are present.
6. Source still needed to build fixes: `kan.py`, head of `launch_run11_vm.sh`,
   `verify_gnn_score.py`, `splits.py`.
