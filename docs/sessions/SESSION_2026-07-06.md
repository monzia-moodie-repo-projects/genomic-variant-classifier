# SESSION 2026-07-06 — Connector expansion (NT/COSMIC/KEGG → 97 features), full-run launcher wiring, staging

Project: GenAssoc missense variant pathogenicity classifier, `C:\Projects\genomic-variant-classifier`.
Environment: Windows 11, PowerShell 7.6.3, Python 3.12.10, venv `.venv312`, CPU-only. VM: Vast.ai GPU,
repo at `/workspace/genomic-variant-classifier`. Drive remote: `genvarcla:` (rclone; never bulk-write G:).

This session continued the connector-expansion arc and ended with the full-run launcher wired for all
three new connectors and the required data staged to Drive. No GPU run was launched (infra step, deferred
to the user's timing). All code changes are guarded-installed, SHA-pinned, and behaviour-verified.

---

## 1. FEATURE-COUNT PROGRESSION (verified on disk)
91 (pre-session baseline) → 93 (Nucleotide Transformer) → 95 (COSMIC CMC) → **97 (KEGG)**.
Enforcement: the single fail-loud guard `EXPECTED_TABULAR_FEATURE_COUNT` in `variant_ensemble.py` (now 97).
`run_phase2_eval.py` reads `X_train.shape[1]` dynamically (no hardcoded count). No other count assertion
exists in the run17 tooling (grep-verified) — EXCEPT the stale preflight schema gate (see §7, inert at launch).

## 2. CONNECTORS BUILT + WIRED THIS SESSION

### Nucleotide Transformer (genomic LM) — genomiclm_* (→93)
Delivered earlier in the session-chain; activated via `--seq-windows` (already in the launcher). Two
features: `genomiclm_delta_norm`, `genomiclm_llr`. Smoke coverage: 1002/1003 variants (near-total).
NOTE: ranked #1 in feature importance on the smoke — flagged for the post-run leakage audit (a new
feature dominating a small slice earns MORE scrutiny, not a pass).

### COSMIC Cancer Mutation Census — cosmic_* (→95)
- Connector `connector_cosmic.py` (standalone, variant-keyed chrom:pos:ref:alt). Features
  `cosmic_recurrence`, `cosmic_sig_tier`. Data: `CancerMutationCensus_AllData_v104_GRCh37.tsv.gz`
  (~301 MiB; the GRCh37 release carries a GRCh38 position column — connector keys GRCh38).
- Cache built during activation: `cosmic_cmc_grch38_index.parquet` (37.9 MB, 5,447,351 substitutions).
  Activation coverage: 22.23% on a 50k cohort slice.
- Wiring installer applied cleanly; count 93→95.

### KEGG pathway membership — kegg_* (→97)  [PRIMARY NEW WORK THIS SESSION]
- Connector `src/genomic_variant_classifier/data/kegg.py` (SHA `d3495ca0`), standalone, gene-keyed on
  HGNC gene_symbol. Features: `kegg_pathway_count`, `kegg_disease_pathway_flag` (1.0 if gene in any
  hsa05xxx "Human Diseases" pathway). 12/12 in-container framework checks passed (merge, dedup, unknown-gene
  default, stub, missing-file, no-gene-column, re-run idempotency).
- Builder `scripts/build_kegg_parquet.py` (SHA `d99efdb8`): pulls KEGG REST API (rest.kegg.jp
  /list/pathway/hsa, /link/hsa/pathway, /list/hsa), Entrez→symbol from KEGG's own hsa list; disease
  pathways = hsa05xxx range (overridable). Fail-loud self-check (TP53/BRCA1 present + disease-flagged).
  Ran on box: **9,283 genes, 3,136 disease-flagged; TP53=51 paths, BRCA1=7 paths**; self-check green.
- Activation on 50k real variants: `pathway>0: 84.2%`, `disease-flag>0: 52.9%`.
  [FLAG] the 52.9% disease-flag rate is high — must pass the lone-feature leakage audit before it is
  "trusted" (broad hsa05xxx maps + disease-enriched ClinVar cohort = plausible, but unverified).
- Wiring installer `Install_kegg_wiring.ps1` applied on box (SHA-gated, behaviour-tested to 97):
  real_data_prep `cf6e0fd4`→`e5127b52`, variant_ensemble `79f7f78d`→`262c04cb`, run_phase2_eval
  `d1620ad7`→`8f450d81`. Verified: engineer_features emits 97 cols; cosmic_* + genomiclm_* intact;
  PHASE_4_FEATURES untouched.

## 3. TCGA — SCOPED AS AN RNA-EXPRESSION DATA TASK (NOT a connector)
Decision + evidence: TCGA tumor-vs-normal differential expression (DE) would populate the EXISTING
`rnaseq_log2fc`/`rnaseq_de_neglog10p` features (same feature type), so it is NOT a new connector. The
existing builder `build_rnaseq_canonical_real.py` is already parameterized on `--de de_features.tsv`; to
swap in TCGA pan-cancer DE, produce a TCGA `de_features.tsv` and re-run the builder — no code change.
Current DE lineage is GTEx (per docstring). Full scope in `SCOPE_TCGA_DE_PIPELINE.md`.

VERIFIED (not assumed): a TCGA somatic-recurrence feature (the rejected Option B) would largely DUPLICATE
COSMIC. `cosmic_tcga_overlap_probe.py` intersected a real open-access TCGA-BRCA masked MAF (downloaded via
the GDC API) against the COSMIC index → **95.7% of TCGA-MAF variants already in COSMIC (45/47)**. This is
a one-aliquot spot-check (strong direction, not a population estimate). Confirms COSMIC ingests TCGA
(consistent with the CMC README's DNDS "TCGA whole-exome data in COSMIC" column).

## 4. SMOKE RUN — 97 FEATURES, GREEN
`smoke_all_models.py` taught the three new connectors (installer `Install_smoke_connectors.ps1`:
`--cosmic-path`/`--kegg-path` forwarding + audit EXPECT_RUN17 for genomiclm/cosmic/kegg). Ran on
`clinvar_smoke3000` + seq twin + all three connectors:
- `run_phase2_eval exited 0`, **Features: 97**, all 13 models + ENSEMBLE_STACKER present, clean log,
  GNN non-degenerate → **RESULT: GREEN**.
- Annotation coverage: 16c NT 1002/1003, 16d COSMIC 122, 16e KEGG 726.
- Feature importance: **genomiclm_delta_norm #1 (328), kegg_pathway_count #2 (219)**,
  kegg_disease_pathway_flag #7, cosmic_recurrence #9.
- Per-split population audit (`audit_smoke_feature_population.py --run17`): all three new families PASS
  every split (genomiclm_delta_norm ok/ok/ok, cosmic_recurrence ok/ok/ok, kegg_pathway_count +
  kegg_disease_pathway_flag ok/ok/ok). The audit's overall FAIL was correct but off-yardstick: it was
  run in `--run17` full-flag mode, which asserts gnomAD/dbNSFP/1000G/hetero-GNN — sources this
  connectors-only smoke deliberately did not pass (all reported 0 in the annotation log). Not a bug;
  the full run is the correct test.

[HONEST FLAGS from the smoke, for the full run]
- genomiclm #1 by wide margin → leakage audit is priority (not a pass).
- MCC=0.0000 on stacker + several models → small-cohort threshold artifact (223 test rows, imbalance);
  resolve via calibration on the full run.
- xgboost/lightgbm AUROC 0.5000 on dev → isotonic-calibration collapse on tiny folds; expected at smoke scale.

## 5. UNIFIED LEAKAGE AUDIT — BUILT, READY FOR THE REAL MATRIX
`scripts/feature_leakage_audit.py` (SHA `1c43c541`): read-only, audits genomiclm_*/cosmic_*/kegg_* (or any
`--features`) on a real split matrix — coverage overall + by class, point-biserial correlation, lone-feature
rank AUROC (the leakage test), hard-stop (exit 3) if any CLINVAR_CLNSIG column leaked, explicit CLEAN/FLAG/
DEAD verdict. Verified in-container on clean/sparse/leaky/dead/label-column cases. PENDING: run on the full
run's `X_train.parquet` + label file (the smoke's X_train has no inline label — features only, so `--x`+`--meta`,
not `--matrix`). This is the gate that promotes the three connectors from "firing" to "trusted."

## 6. FULL-RUN LAUNCHER — WIRED FOR COSMIC + KEGG
Determination (evidence, not assumption): launch is **Path A** — no wrapper/.ps1/.sh invokes
`preflight_run17.py`; the launcher runs its own inline preflight then `run_phase2_eval.py`. The generator
`gen_run17_ablation_launchers.py` copies the baseline verbatim except 5 FinnGen transforms, so editing the
baseline auto-propagates to the ablations on regeneration.
- `launch_run17_baseline.sh` (`d1b99ed5`→`157a6708`, 312→325 lines): added COSMIC_TSV + KEGG_PARQUET path
  vars; both in the REQUIRED-file preflight loop (hard-fail = no silent zero); a KEGG column probe; and
  `--cosmic-path`/`--kegg-path` in ARGS. `--seq-windows` already present (NT activates automatically).
  Verified: `bash -n` clean; the real generator regenerates r12only + r13only, both pass `bash -n`, both
  inherit all cosmic/kegg wiring with correct per-ablation OUTDIRs; generator's 8 anchors untouched.
- Installer `Install_run17_cosmic_kegg_wiring.ps1` rev2 (SHA `17f3e3d1`): SHA-gates baseline, backs up all
  three launchers, places baseline, POST-SHA verify, regenerates ablations, inherit-check, auto-restore-all
  on fail. rev2 dropped the git-bash `bash -n` dependency (rev1 broke on Windows→POSIX path translation;
  guard auto-restored each time). Applied successfully on box: baseline `157a6708`, ablations regenerated
  + inherited wiring.

## 7. DATA STAGING TO DRIVE — DONE
Both files the full run needs (that weren't already on the VM) are now on Drive under
`genvarcla:genomic-variant-classifier/data/external/`:
- `cosmic/CancerMutationCensus_AllData_v104_GRCh37.tsv.gz` (~301 MiB; matches the launcher's COSMIC_TSV
  name EXACTLY — verified against the local file, 315,219,627 bytes). NOTE: an older
  `CancerMutationCensus_AllData_Tsv_v104_GRCh37.tar` bundle is also on Drive; harmless (launcher names the
  .tsv.gz explicitly), optionally delete to avoid confusion.
- `kegg_gene_pathways.parquet` (73.6 KiB).
VM-side pull (exact, verified against Drive listing):
    cd /workspace/genomic-variant-classifier
    rclone copy genvarcla:.../data/external/cosmic/CancerMutationCensus_AllData_v104_GRCh37.tsv.gz data/external/cosmic/ -P
    rclone copy genvarcla:.../data/external/kegg_gene_pathways.parquet data/external/ -P

## 8. METRICS PANEL — CONFIRMED UNBUILT (the flagged next build)
Grep-verified: `run_phase2_eval.py` contains NONE of the expanded-panel machinery (bootstrap/net_benefit/
ece/calibration_slope/decision_curve/recall_at/delong/mcnemar — the one grep hit was a false-positive
substring in a --gtex-genes help string). The pipeline still emits the baseline 5 metrics
(auroc/auprc/f1/mcc/brier) + feature_importance.csv.
The 10-panel tiered validation spec (`Metric_panel_evaluation_stack.txt`, AUPRC primary, every metric with
bootstrap 95% CI) remains SPECIFIED but UNBUILT — correctly deferred: it is a post-hoc standalone
`evaluation/` package (one module per panel) that reads saved artifacts (oof_predictions/meta_test/y_test),
so it must be built on a CORRECTED RUN's artifacts, not the current stale ones. Tiering: Tier A (computable
from saved artifacts now), Tier B (needs multi-class/ranking reframe), Tier C (needs independent external
data — cannot honestly claim yet). THIS IS THE NEXT REAL BUILD, gated on the run.

## 9. HYGIENE / KNOWN-STALE (inert for launch; flagged, not silently fixed)
- `preflight_run17.py` `EXPECTED_SCHEMA_COLS = 87` + `schema_baseline.json`: stale (real matrix is 97). Inert
  at launch (Path A never calls preflight_run17). Fix only if the `--check`/`--emit` dev path is wanted:
  regenerate schema_baseline.json from the real matrix + bump the constant.
- Generated ablation launchers still carry a "91-feature contract" COMMENT (cosmetic; no code enforces it).
- `*.prekegg.bak` / `*.precosmic.bak` / `*.pregenomiclm*.bak` / `*.precosmickegg.bak` backups on disk — clear
  after the full run confirms everything.
- `data\raw\cache\_probe_genomiclm.parquet` — probe artifact, deletable.

## 10. IMMEDIATE NEXT STEPS (in order)
1. Provision the Vast.ai GPU box; sync the repo at the commit including today's changes.
2. VM-side rclone pull of the two staged files (§7). Launcher inline preflight hard-fails if absent.
3. `bash scripts/launch_run17_baseline.sh` → monitor (`Run17_Monitor.ps1`) → postflight (`Run17_Postflight.ps1`).
4. Post-run: `feature_leakage_audit.py --x <run>\splits\X_train.parquet --meta <run>\<label>` on the 97-feature
   matrix (promotes genomiclm/cosmic/kegg to "trusted"; genomiclm #1 makes this priority).
5. BUILD the metrics `evaluation/` package (§8) on the corrected run's artifacts — the substantial next build.
6. FUSION ARCHITECTURE doc (`docs/FUSION_ARCHITECTURE.md`) — still HELD pending two decisions: disease-label
   taxonomy (MONDO organ-systems vs OMIM terms) and attribution target (per-prediction vs global).

## 11. ARTIFACT SHAs (this session, current on disk)
- kegg.py `d3495ca0` | build_kegg_parquet.py `d99efdb8`
- real_data_prep.py `e5127b52` | variant_ensemble.py `262c04cb` | run_phase2_eval.py `8f450d81`
- launch_run17_baseline.sh `157a6708` | Install_run17_cosmic_kegg_wiring.ps1 rev2 `17f3e3d1`
- feature_leakage_audit.py `1c43c541` | cosmic_tcga_overlap_probe.py `05e1ae1b`
- smoke_all_models.py `04a14ae0` | audit_smoke_feature_population.py `651d45f1`
