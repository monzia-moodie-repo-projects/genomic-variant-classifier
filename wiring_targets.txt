
=== A. real_data_prep.py : AnnotationConfig class (fields incl dbsnp_path, protein_cache_dir, min_protein_coord_coverage) ===
  (AnnotationConfig starts at line 240; showing 60 lines)
 240| class AnnotationConfig:
 241|     """
 242|     Paths and flags controlling which score connectors run during DataPrepPipeline.
 243| 
 244|     All paths default to None -> connector runs in stub mode (returns default
 245|     scores, logs a WARNING, pipeline continues).  Set paths to activate real
 246|     annotation.
 247| 
 248|     Sequence when run:
 249|       1.  DbNSFPConnector(dbnsfp_path)     -- 6 scores for missense SNVs
 250|       2.  PhyloPConnector(phylop_path)     -- phylop_score for all positions
 251|       3.  CADDConnector()                  -- cadd_phred via REST (if annotate_cadd=True)
 252|       4.  SpliceAIConnector(spliceai_path) -- splice_ai_score
 253|       5.  AlphaMissense                   -- alphamissense_score for missense variants
 254|       6.  GTEx                            -- expression and eQTL features
 255|       7.  VEP                             -- codon_position
 256|       8.  OMIM                            -- omim_n_diseases, omim_is_autosomal_dominant
 257|       9.  ClinGen                         -- clingen_validity_score
 258|       10. dbSNP                           -- dbsnp_af
 259|       11. EVE                             -- eve_score
 260|       12. HGMD                            -- hgmd_is_disease_mutation, hgmd_n_reports
 261|       13. RNASpliceIsoformPipeline        -- RNA splice-context features (Phase 6.1)
 262|       14. ProteinStructurePipeline        -- protein structure features (Phase 6.2)
 263|       15. LOVDConnector(lovd_path)        -- lovd_variant_class (ordinal 0-4)
 264|       16. ESM2Connector                   -- esm2_delta_norm for missense variants (Phase 3C)
 265|       17. GnomADConstraintConnector       -- pli_score, loeuf, syn_z, mis_z (Phase 3C)
 266| 
 267|     annotate_cadd is False by default because the CADD REST API requires
 268|     1.5 s/variant. Enable only for small batches or when the pre-computed
 269|     file is available.
 270|     """
 271| 
 272|     dbnsfp_path: Optional[Path] = None
 273|     phylop_path: Optional[Path] = None
 274|     spliceai_path: Optional[Path] = None
 275|     alphamissense_path: Optional[Path] = None
 276|     annotate_cadd: bool = False
 277|     gtex_genes: list[str] = field(default_factory=list)
 278|     gtex_tissues: list[str] = field(default_factory=list)
 279|     gtex_path: Optional[Path] = None  # RNA expression: bulk GTEx median-TPM parquet (build_gtex_parquet.py)
 280|     vep_path: Optional[Path] = None
 281|     omim_path: Optional[Path] = None
 282|     omim_genemap2_path: Optional[Path] = None  # OMIM genemap2.txt -> real omim_is_autosomal_dominant (else hard-0)
 283|     clingen_path: Optional[Path] = None
 284|     dbsnp_path: Optional[Path] = None
 285|     eve_path: Optional[Path] = None
 286|     eve_entry_map_path: Optional[Path] = None  # UniProt index parquet (entry_name col) for EVE entry-name -> HGNC
 287|     hgmd_path: Optional[Path] = None
 288|     kg_path: Optional[Path] = None  # 1000 Genomes Phase 3 AF parquet
 289|     finngen_path: Optional[Path] = None  # FinnGen R10 annotated variants TSV
 290|     finngen_r13_path: Optional[Path] = None  # FinnGen R13 annotated variants (dual-release experiment)
 291|     lovd_path: Optional[Path] = None  # LOVD all-variants parquet
 292|     rna_pipeline: bool = True  # Phase 6.1: RNA splice-context features
 293|     protein_cache_dir: Optional[Path] = None  # Phase 6.2: AlphaFold/UniProt cache dir
 294|     esm2_model_name: str = "esm2_t6_8M_UR50D"  # Phase 3C: ESM-2 model
 295|     esm2_cache_path: Optional[Path] = None  # Phase 3C: SQLite cache
 296|     esm2_uniprot_index_path: Optional[Path] = None  # Phase 3C: local UniProt seq index (no run-time REST)
 297|     esm2_device: Optional[str] = None  # Phase 3C: None/'auto' -> cuda if available, else cpu
 298|     gnomad_constraint_path: Optional[Path] = None  # Phase 3C: gnomAD constraint TSV
 299|     reactome_path: Optional[Path] = None  # Phase D: Reactome gene pathway-count parquet

=== B. real_data_prep.py : step 14 protein pipeline construction (ProteinStructurePipeline / _annotate_scores) ===

LineNumber Line
---------- ----
       262       14. ProteinStructurePipeline        -- protein structure features (Phase 6.2)
       293     protein_cache_dir: Optional[Path] = None  # Phase 6.2: AlphaFold/UniProt cache dir
       999         # 14. Protein structure pipeline (Phase 6.2)
      1000         from genomic_variant_classifier.pipelines.protein_pipeline import ProteinStructurePipeline
      1002         protein = ProteinStructurePipeline(cache_dir=ac.protein_cache_dir)
      1358         feats["alphafold_plddt"] = (
      1359             df.get("alphafold_plddt", pd.Series([50.0] * len(df), index=df.index))


  --- context around ProteinStructurePipeline construction (first hit +/- 25 lines) ---
 254|       6.  GTEx                            -- expression and eQTL features
 255|       7.  VEP                             -- codon_position
 256|       8.  OMIM                            -- omim_n_diseases, omim_is_autosomal_dominant
 257|       9.  ClinGen                         -- clingen_validity_score
 258|       10. dbSNP                           -- dbsnp_af
 259|       11. EVE                             -- eve_score
 260|       12. HGMD                            -- hgmd_is_disease_mutation, hgmd_n_reports
 261|       13. RNASpliceIsoformPipeline        -- RNA splice-context features (Phase 6.1)
 262|       14. ProteinStructurePipeline        -- protein structure features (Phase 6.2)
 263|       15. LOVDConnector(lovd_path)        -- lovd_variant_class (ordinal 0-4)
 264|       16. ESM2Connector                   -- esm2_delta_norm for missense variants (Phase 3C)
 265|       17. GnomADConstraintConnector       -- pli_score, loeuf, syn_z, mis_z (Phase 3C)
 266| 
 267|     annotate_cadd is False by default because the CADD REST API requires
 268|     1.5 s/variant. Enable only for small batches or when the pre-computed
 269|     file is available.
 270|     """
 271| 
 272|     dbnsfp_path: Optional[Path] = None
 273|     phylop_path: Optional[Path] = None
 274|     spliceai_path: Optional[Path] = None
 275|     alphamissense_path: Optional[Path] = None
 276|     annotate_cadd: bool = False
 277|     gtex_genes: list[str] = field(default_factory=list)
 278|     gtex_tissues: list[str] = field(default_factory=list)
 279|     gtex_path: Optional[Path] = None  # RNA expression: bulk GTEx median-TPM parquet (build_gtex_parquet.py)
 280|     vep_path: Optional[Path] = None
 281|     omim_path: Optional[Path] = None
 282|     omim_genemap2_path: Optional[Path] = None  # OMIM genemap2.txt -> real omim_is_autosomal_dominant (else hard-0)
 283|     clingen_path: Optional[Path] = None
 284|     dbsnp_path: Optional[Path] = None
 285|     eve_path: Optional[Path] = None
 286|     eve_entry_map_path: Optional[Path] = None  # UniProt index parquet (entry_name col) for EVE entry-name -> HGNC
 287|     hgmd_path: Optional[Path] = None
 288|     kg_path: Optional[Path] = None  # 1000 Genomes Phase 3 AF parquet
 289|     finngen_path: Optional[Path] = None  # FinnGen R10 annotated variants TSV
 290|     finngen_r13_path: Optional[Path] = None  # FinnGen R13 annotated variants (dual-release experiment)
 291|     lovd_path: Optional[Path] = None  # LOVD all-variants parquet
 292|     rna_pipeline: bool = True  # Phase 6.1: RNA splice-context features
 293|     protein_cache_dir: Optional[Path] = None  # Phase 6.2: AlphaFold/UniProt cache dir

=== C. run_phase2_eval.py : AnnotationConfig(...) construction + argparse args (dbsnp_path anchor) ===

LineNumber Line
---------- ----
        58         "--esm2-uniprot-index",
        60         help="Local UniProt sequence index parquet (gene_symbol,uniprot_id,sequence) "
        62         "scripts/build_uniprot_index.py. Omit or pass '' to use live UniProt REST.",
        65     p.add_argument("--gtex-path", default=None,
       173         "--dbsnp-path",
       189         help="UniProt index parquet (entry_name column) resolving EVE per-protein "
       190         "filenames (UniProt entry names, e.g. 1433G_HUMAN) to HGNC symbols (YWHAG). "
       192         "(eve_score stuck at 0.5). Independent of --esm2-uniprot-index; the launch "
       193         "script points both at the same UniProt index.",
       316         # ESM-2 UniProt sequence index (Phase 3C): required for the no-network
       318         # to live UniProt REST is what stalled the Run-15 smoke (instance
       321         if args.esm2_uniprot_index:
       322             _esm2_index = Path(args.esm2_uniprot_index)
       325                     "ESM-2 UniProt index not found: %s -- build it with "
       326                     "scripts/build_uniprot_index.py, or pass --esm2-uniprot-index '' "
       332         ann = AnnotationConfig(
       334             esm2_uniprot_index_path=_esm2_index,
       350             # Run 17 annotation wiring (see --omim-path/--phylop-path/--dbsnp-path/--eve-path)
       354             dbsnp_path=Path(args.dbsnp_path) if args.dbsnp_path else None,


  --- context around AnnotationConfig( construction (+/- 30 lines) ---
 330|                 return 2
 331| 
 332|         ann = AnnotationConfig(
 333|             spliceai_path=Path(args.spliceai) if args.spliceai else None,
 334|             esm2_uniprot_index_path=_esm2_index,
 335|             alphamissense_path=Path(args.alphamissense) if args.alphamissense else None,
 336|             gtex_genes=args.gtex_genes or [],
 337|             gtex_path=Path(args.gtex_path) if args.gtex_path else None,
 338|             kg_path=Path(args.kg) if args.kg else None,
 339|             gnomad_constraint_path=(
 340|                 Path(args.gnomad_constraint) if args.gnomad_constraint else None
 341|             ),
 342|             # Run 10 wiring fix (3 connectors silent-zero in Run 9)
 343|             lovd_path=Path(args.lovd_path) if args.lovd_path else None,
 344|             dbnsfp_path=Path(args.dbnsfp_path) if args.dbnsfp_path else None,
 345|             reactome_path=Path(args.reactome_path) if args.reactome_path else None,
 346|             clingen_path=Path(args.clingen_path) if args.clingen_path else None,
 347|             rnaseq_path=Path(args.rnaseq_path) if args.rnaseq_path else None,
 348|             finngen_path=Path(args.finngen_path) if args.finngen_path else None,
 349|             finngen_r13_path=Path(args.finngen_r13_path) if args.finngen_r13_path else None,
 350|             # Run 17 annotation wiring (see --omim-path/--phylop-path/--dbsnp-path/--eve-path)
 351|             omim_path=Path(args.omim_path) if args.omim_path else None,
 352|             omim_genemap2_path=Path(args.omim_genemap2_path) if args.omim_genemap2_path else None,
 353|             phylop_path=Path(args.phylop_path) if args.phylop_path else None,
 354|             dbsnp_path=Path(args.dbsnp_path) if args.dbsnp_path else None,
 355|             eve_path=Path(args.eve_path) if args.eve_path else None,
 356|             eve_entry_map_path=Path(args.eve_entry_map) if args.eve_entry_map else None,
 357|         )
 358|         prep = DataPrepPipeline(
 359|             config=DataPrepConfig(
 360|                 min_review_tier=args.min_review_tier,
 361|                 output_dir=outdir / "splits",
 362|             ),
 363|             annotation_config=ann,
 364|         )
 365|         X_train, X_val, X_test, y_train, y_val, y_test, meta_val, meta = prep.run(
 366|             clinvar_path=str(clinvar),
 367|             gnomad_path=args.gnomad,
 368|         )
 369|         logger.info(
 370|             "Data prep: train=%d val=%d test=%d features=%d",
 371|             len(X_train),
 372|             len(X_val),
 373|             len(X_test),
 374|             X_train.shape[1],

  --- context around the --dbsnp-path argparse arg (+/- 10 lines) ---
 169|         help="PhyloP conservation source (data/external/phylop/...). When omitted, "
 170|         "phylop_score defaults to 0.0 (silent stub).",
 171|     )
 172|     p.add_argument(
 173|         "--dbsnp-path",
 174|         default=None,
 175|         help="dbSNP allele-frequency parquet (data/external/dbsnp/...). When "
 176|         "omitted, dbsnp_af defaults to 0.0 (silent stub).",
 177|     )
 178|     p.add_argument(
 179|         "--eve-path",
 180|         default=None,
 181|         help="EVE scores: directory of per-protein CSVs (data/external/eve/) or a "
 182|         "merged parquet. Matched by gene_symbol + one-letter aa_change derived from "
 183|         "the HGVSp-parsed protein_change. When omitted, eve_score defaults to 0.5 "
 184|         "(silent stub). Covers missense substitutions only.",
 185|     )
 186|     p.add_argument(
 187|         "--eve-entry-map",
 188|         default=None,

=== D. launch_run17_baseline.sh : ARGS block + dbSNP abort-gate (mirror target) ===

LineNumber Line
---------- ----
         6 #   - rnaseq_{mean_log_tpm,detection_rate,log2_cv,log2fc,de_neglog10p}  via --rnaseq-path
        11 #   - --rnaseq-path is REQUIRED + wired. The preflight_run17 --emit-kg template OMITS it, so a
        41         echo "==> run_phase2_eval.py exit $rc -- ABORT @ $(date -u +'%F %T') UTC" | tee -a "$LOG"
        95     echo "==> ABORT (exit 2): missing required inputs" | tee -a "$LOG"; exit 2
       103     echo "==> ABORT (exit 3): import failed" | tee -a "$LOG"; exit 3
       113     echo "==> ABORT (exit 3): imodelsx KAN patch failed (KAN would drop from the ensemble)" | tee -a "$LOG"; exit 3
       143     echo "==> ABORT (exit 4): pybigtools import failed -- PhyloP would silent-zero. Install pybigtools>=0.3.0 on the VM." | tee -a "$LOG"; exit 4
       156     print('ABORT: kg parquet missing per-superpop AF cols', miss); sys.exit(4)
       162     print('ABORT: rnaseq parquet missing cols', miss_rs); sys.exit(5)
       169 ARGS="--clinvar $DATA/processed/clinvar_grch38_clean.parquet"
       170 ARGS="$ARGS --seq-windows $DATA/processed/clinvar_grch38_clean_seq.parquet"
       171 ARGS="$ARGS --gnomad $DATA/processed/gnomad_v4_exomes.parquet"
       172 ARGS="$ARGS --spliceai $DATA/external/spliceai/spliceai_index.parquet"
       173 ARGS="$ARGS --alphamissense $DATA/external/alphamissense/AlphaMissense_hg38.tsv.gz"
       174 ARGS="$ARGS --gnomad-constraint $DATA/external/gnomad/gnomad.v4.1.constraint_metrics.tsv"
       175 ARGS="$ARGS --dbnsfp-path $DATA/external/dbnsfp/dbnsfp_clinvar_index.parquet"
       176 ARGS="$ARGS --gtex-path $GTEX_PARQUET"
       177 ARGS="$ARGS --reactome-path $REACTOME_PARQUET"
       178 ARGS="$ARGS --rnaseq-path $RNASEQ_PARQUET"
       179 ARGS="$ARGS --kg $KG_PARQUET"
       181 if [ -f "$FINNGEN_FILE" ]; then ARGS="$ARGS --finngen-path $FINNGEN_FILE"; echo "==> FinnGen wired: $FINNGEN_FILE" | tee -a "$LOG"; else echo "==> ABORT:…
       183 if [ -f "$FINNGEN_R13_FILE" ]; then ARGS="$ARGS --finngen-r13-path $FINNGEN_R13_FILE"; echo "==> FinnGen R13 wired: $FINNGEN_R13_FILE" | tee -a "$LOG"; e…
       186 #     signal). Plus omim/phylop/dbsnp/clingen, whose CLI flags exist but the launch
       192 # dir and ABORT on 0 CSVs -- the old `ls -A` check passed on the CSV-less bundle
       197     ARGS="$ARGS --eve-path $EVE_DIR"; echo "==> EVE wired: $EVE_DIR ($_EVE_CSVN CSVs)" | tee -a "$LOG"
       199     echo "==> ABORT: EVE variant_files missing or no CSVs: $EVE_DIR ($_EVE_CSVN found; expected ~3211). Stage variant_files to the VM." | tee -a "$LOG"; …
       204     ARGS="$ARGS --esm2-uniprot-index $UNIPROT_INDEX"; echo "==> ESM-2 UniProt index wired: $UNIPROT_INDEX" | tee -a "$LOG"
       205     ARGS="$ARGS --eve-entry-map $UNIPROT_INDEX"; echo "==> EVE entry-name map wired: $UNIPROT_INDEX (resolves 1433G_HUMAN -> YWHAG)" | tee -a "$LOG"
       207     echo "==> ABORT: UniProt index missing: $UNIPROT_INDEX" | tee -a "$LOG"; exit 8
       213     ARGS="$ARGS --omim-path $OMIM_FILE"; echo "==> OMIM wired: $OMIM_FILE" | tee -a "$LOG"
       215     echo "==> ABORT: OMIM file missing under $DATA/external/omim/" | tee -a "$LOG"; exit 8
       219 # Without --omim-genemap2-path, all three OMIM columns silent-zero across the cohort.
       222     ARGS="$ARGS --omim-genemap2-path $OMIM_GENEMAP2_FILE"; echo "==> OMIM genemap2 wired: $OMIM_GENEMAP2_FILE" | tee -a "$LOG"
       224     echo "==> ABORT: OMIM genemap2.txt missing under $DATA/external/omim/ (omim_n_diseases/omim_n_diseases_molecular/omim_is_autosomal_dominant would sil…
       229     ARGS="$ARGS --phylop-path $PHYLOP_FILE"; echo "==> PhyloP wired: $PHYLOP_FILE" | tee -a "$LOG"
       231     echo "==> ABORT: PhyloP file missing under $DATA/external/phylop/" | tee -a "$LOG"; exit 8
       233 # dbSNP: DbSNPConnector(parquet_path=...) wants a parquet.
       234 DBSNP_FILE="$(ls "$DATA"/external/dbsnp/*.parquet 2>/dev/null | head -n1 || true)"
       235 if [ -n "$DBSNP_FILE" ] && [ -f "$DBSNP_FILE" ]; then
       236     ARGS="$ARGS --dbsnp-path $DBSNP_FILE"; echo "==> dbSNP wired: $DBSNP_FILE" | tee -a "$LOG"
       238     echo "==> ABORT: dbSNP parquet missing under $DATA/external/dbsnp/" | tee -a "$LOG"; exit 8
       243     ARGS="$ARGS --clingen-path $CLINGEN_FILE"; echo "==> ClinGen wired: $CLINGEN_FILE" | tee -a "$LOG"
       245     echo "==> ABORT: ClinGen CSV missing under $DATA/external/clingen/" | tee -a "$LOG"; exit 8
       249 ARGS="$ARGS --string-db auto"
       250 ARGS="$ARGS --hetero-gnn --kg-edges reactome:$REACTOME_GMT"
       251 ARGS="$ARGS --min-review-tier 3 --n-folds 5"
       252 ARGS="$ARGS --skip-svm"
       256     ARGS="$ARGS --lovd-path $LOVD_PARQUET"
       261 ARGS="$ARGS --unseen-gene-holdout"
       262 ARGS="$ARGS --output $OUTDIR"
       267 echo "==> ARGS: $ARGS" | tee -a "$LOG"
       280 python scripts/run_phase2_eval.py $ARGS 2>&1 | tee -a "$LOG"


  --- context around dbsnp block (+/- 12 lines) ---
 180| FINNGEN_FILE="$DATA/external/finngen/finnge_R12_annotated_variants_v1.gz"  # registry typo 'finnge'
 181| if [ -f "$FINNGEN_FILE" ]; then ARGS="$ARGS --finngen-path $FINNGEN_FILE"; echo "==> FinnGen wired: $FINNGEN_FILE" | tee -a "$LOG"; else echo "==> ABORT: FinnGen file missing: $FINNGEN_FILE" | tee -a "$LOG"; exit 7; fi
 182| FINNGEN_R13_FILE="$DATA/external/finngen/finngen_R13_annotated_variants_v0.gz"  # R13 dual-release (correct spelling, _v0)
 183| if [ -f "$FINNGEN_R13_FILE" ]; then ARGS="$ARGS --finngen-r13-path $FINNGEN_R13_FILE"; echo "==> FinnGen R13 wired: $FINNGEN_R13_FILE" | tee -a "$LOG"; else echo "==> ABORT: FinnGen R13 file missing: $FINNGEN_R13_FILE" | tee -a "$LOG"; exit 7; fi
 184| 
 185| # --- Run 17 EVE/ESM-2 wiring (HGVSp parser delivered -> EVE/ESM-2 now carry REAL
 186| #     signal). Plus omim/phylop/dbsnp/clingen, whose CLI flags exist but the launch
 187| #     script never passed (silent-zero). Hard-fail if a configured source is missing
 188| #     on the VM; each echoes the exact file picked (a wrong pick is LOUD, not silent).
 189| # EVE: directory of per-protein score CSVs (gene_symbol + HGVSp-derived aa_change).
 190| # The 3,211 score CSVs live in EVE_all_data/variant_files (NOT the bundle root,
 191| # which has 0 top-level CSVs). EVE's glob is non-recursive, so point at the leaf
 192| # dir and ABORT on 0 CSVs -- the old `ls -A` check passed on the CSV-less bundle
 193| # root and would have silently scored every variant 0.5.
 194| EVE_DIR="$DATA/external/eve/EVE_all_data/variant_files"
 195| _EVE_CSVN=$(ls "$EVE_DIR"/*.csv 2>/dev/null | wc -l)
 196| if [ -d "$EVE_DIR" ] && [ "$_EVE_CSVN" -gt 0 ]; then
 197|     ARGS="$ARGS --eve-path $EVE_DIR"; echo "==> EVE wired: $EVE_DIR ($_EVE_CSVN CSVs)" | tee -a "$LOG"
 198| else
 199|     echo "==> ABORT: EVE variant_files missing or no CSVs: $EVE_DIR ($_EVE_CSVN found; expected ~3211). Stage variant_files to the VM." | tee -a "$LOG"; exit 8
 200| fi
 201| # ESM-2 UniProt sequence index (offline; else slow live REST per gene).
 202| UNIPROT_INDEX="$DATA/external/uniprot/uniprot_human_reviewed.parquet"
 203| if [ -f "$UNIPROT_INDEX" ]; then
 204|     ARGS="$ARGS --esm2-uniprot-index $UNIPROT_INDEX"; echo "==> ESM-2 UniProt index wired: $UNIPROT_INDEX" | tee -a "$LOG"
 205|     ARGS="$ARGS --eve-entry-map $UNIPROT_INDEX"; echo "==> EVE entry-name map wired: $UNIPROT_INDEX (resolves 1433G_HUMAN -> YWHAG)" | tee -a "$LOG"
 206| else
 207|     echo "==> ABORT: UniProt index missing: $UNIPROT_INDEX" | tee -a "$LOG"; exit 8
 208| fi
 209| # OMIM: prefer a mim2gene file (OMIMConnector(mim2gene_path=...)); else first file.

=== E. variant_ensemble.py : EXPECTED_TABULAR_FEATURE_COUNT, the 4 AF features in TABULAR_FEATURES, PHASE_2_FEATURES comment ===

LineNumber Line
---------- ----
       157 EXPECTED_TABULAR_FEATURE_COUNT = 91
       241     "alphafold_plddt",
       242     "solvent_accessibility",
       243     "secondary_structure_context",
       244     "dist_to_active_site",
       276 PHASE_2_FEATURES: list[str] = []  # All Phase 2 features now active; Phase 3 adds GWAS
       578     feats["alphafold_plddt"] = (
       579         df.get("alphafold_plddt", pd.Series([50.0] * len(df), index=df.index))
       584     feats["solvent_accessibility"] = (
       585         df.get("solvent_accessibility", pd.Series([0.5] * len(df), index=df.index))
       590     feats["secondary_structure_context"] = (
       591         df.get("secondary_structure_context", pd.Series([0] * len(df), index=df.index))
       596     feats["dist_to_active_site"] = (
       597         df.get("dist_to_active_site", pd.Series([100.0] * len(df), index=df.index))


  --- context around PHASE_2_FEATURES (+/- 8 lines) ---
 272|     "rnaseq_log2fc",
 273|     "rnaseq_de_neglog10p",
 274| ]
 275| 
 276| PHASE_2_FEATURES: list[str] = []  # All Phase 2 features now active; Phase 3 adds GWAS
 277| 
 278| PHASE_4_FEATURES: list[str] = [
 279|     "esm2_delta_norm",
 280|     "uncertainty_epistemic",
 281|     "uncertainty_aleatoric",
 282|     "population_1kg_af",
 283| ]
 284| 
 285| SEQUENCE_FEATURES = ["fasta_seq"]
 286| 
 287| 

=== DONE -- upload wiring_targets.txt ===
